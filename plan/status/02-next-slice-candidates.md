# Status 2 — Next Slice Candidates

This file lists focused, reviewable implementation slices that map to the current phase ladder. It intentionally omits completed scaffold work and old progress-note history. The checked-in reference snapshot coverage wording has been aligned to the current corpus rows and no longer repeats the stale 2406 Mars hold-out claim; the 1800-01-03 major-body boundary sample now reaches Sun through Pluto, the checked-in reference snapshot corpus also now carries the 2451914.0 bridge day and now exposes a direct `reference-snapshot-bridge-day-summary` CLI path, and the regenerated packaged-artifact fixture has been refreshed against that corpus change. The lunar-source posture is a little easier to inspect now that `lunar-theory-catalog-summary` and `lunar-theory-catalog-validation-summary` are mirrored in both CLIs alongside the existing lunar theory summaries, and the new lunar-theory-limitations summary now makes the compact Meeus-style baseline limits visible in both the validation and release report paths. The lunar-theory request-policy and frame-treatment summaries now also have short aliases in both CLIs, keeping the compact lunar policy surface aligned with the other summary families. The contributor-facing time-observer policy notes were also refreshed to point at the dedicated request-surface and request-semantics summary commands, and the release reproducibility guide now names the full `request-policy-summary` / `request-policy` / `request-semantics-summary` / `request-semantics` alias set beside `request-surface-summary`. The README and release-reproducibility guide now also call out the compatibility-caveats and native-sidereal policy summaries so the known-gap and sidereal-posture entry points stay one hop away from the quick-audit paths, and the release-reproducibility guide now also names the release-house-system-canonical-names and release-ayanamsa-canonical-names bundle summaries alongside release-profile-identifiers for release-bundle archaeology, while also listing the matching release-house-system-canonical-names-summary and release-ayanamsa-canonical-names-summary commands beside the release-bundle archaeology entries. This cycle also surfaced `lunar-theory-limitations-summary` in those maintainer-facing docs so the compact lunar-baseline limitation note stays discoverable beside the existing lunar theory catalog commands. The compatibility-caveats summary now also names the latitude-sensitive house-system set and the descriptor-only ayanamsa label bucket explicitly, so the constrained entries remain visible from the quick-gap audit path. The README quick-start list now avoids the duplicated `lunar-theory-limitations-summary` mention, and the release reproducibility guide now spells out the `comparison-corpus-guard-summary`, `compatibility-caveats`, `native-sidereal-policy`, `lunar-theory-limitations`, `interpolation-posture`, and `packaged-lookup-epoch-policy` aliases explicitly. The selected-asteroid source-window archaeology route now also has a short alias in both CLIs, and the reference major-body bridge sample at JD 2451915.0 now also has a direct CLI path in both front ends. The new short `major-body-bridge-summary` alias now mirrors that bridge evidence path in both CLIs. The production-generation boundary-window summary now also has a short alias in both CLIs for quicker generation-coverage archaeology. The compatibility-profile summary now also has a short `profile-summary` alias in both CLIs, and the shared CLI regression now pins it beside the canonical command. The backend-matrix summary regression now also pins both bridge evidence lines inline, keeping the reference and selected-asteroid bridge slices locked in the overview tests. The release-specific house-system and ayanamsa canonical-name summaries now also have direct CLI paths in both front ends, keeping the partitioned catalog labels one hop away from the broader compatibility profile. The release bundle now also archives `release-house-system-canonical-names-summary.txt` and `release-ayanamsa-canonical-names-summary.txt` so the release-facing archaeology surface stays complete. The production-generation boundary, overall, and source-summary routes now also reject extra arguments in both CLIs, closing the remaining fail-closed gap on the production-generation overview slice. The release-bundle parser parity work also now accepts `--output` as a synonym for `--out` in the CLI front end, while preserving the duplicate-argument guard on both aliases.
The mean-obliquity frame round-trip summary now also has a short `mean-obliquity-frame-round-trip` alias in both CLIs so the frame-evidence slice is easier to reach from the quick-audit path. The shared mean-obliquity sample corpus now also rejects missing equatorial or wraparound coverage before the report layer renders the summary, so the precision edge cases stay explicit in validation. The low-level frame normalization tests now also cover exact full-turn right-ascension wraparound alongside the existing polar and negative-angle cases, keeping the coordinate edge coverage aligned with the report-facing frame evidence. The reference snapshot source-window regression now also pins representative Sun/Jupiter/Pluto/asteroid spans, keeping the 1749/1800/2500 and 2634167 coverage envelopes structurally covered. The reference snapshot source-window validation now also rejects sample-body order drift, and the independent hold-out source-window validation now rejects sample-body and window order drift too, keeping the validation slice aligned with the reference source-window posture.
The time-scale, Delta T, observer, apparentness, and frame policy summary routes now also reject stray arguments in both CLIs, so the request-semantics surface stays fail-closed alongside the rest of the compact inspection commands. The direct backend metadata preflight now also reports unsupported request-shape policy before malformed observer coordinates, so apparentness and frame failures stay visible even when an observer is invalid. The native sidereal policy posture now also has a dedicated `native-sidereal-policy-summary` / `native-sidereal-policy` inspection path in both CLIs. The signed `with_tt_from_ut1_signed` and `with_tt_from_utc_signed` helper paths now also have direct body-observer and house-observer preservation coverage, keeping the conversion tests aligned with the rest of the request-shape regressions. The `request-policy-summary` route now also has explicit extra-argument regression coverage in both CLIs, keeping the primary request-posture entry point fail-closed too, and the request-surface inventory now also names the request-policy / request-semantics aliases explicitly beside the chart offset aliases. The request-semantics help label now also points at request-semantics-summary, keeping the alias chain readable in both CLIs. The benchmark commands now also reject duplicate `--rounds` inputs in both CLIs, so the maintainer-facing performance entry points match the same fail-closed posture. The workspace-audit and native-dependency-audit summary routes now also reject stray arguments with exact alias-specific diagnostics in both CLIs, closing the remaining audit-summary gap in the fail-closed command surface. The packaged-artifact production-profile, target-threshold, target-threshold scope-envelope, generation-manifest, generation-policy, and regeneration summary routes now also reject stray arguments in both CLIs, keeping the artifact-profile inspection surface fail-closed beside the rest of the release-facing summary routes. The packaged-artifact production-profile summary now also has a short `packaged-artifact-production-profile` alias in both CLIs for quicker archaeology. The comparison-snapshot and reference-snapshot summary routes now also have short aliases in both CLIs for quicker release-facing overview inspection. The production, comparison, reference, and holdout body-class coverage summaries now also have short aliases in both CLIs for quicker archaeology. The reference snapshot source-window archaeology route now also has a short alias in both CLIs, making the main reference coverage slice easier to reach from the quick-audit path. The README quick-start command overview now names the `comparison-corpus-guard-summary`, `compatibility-caveats`, `native-sidereal-policy`, and `lunar-theory-limitations` aliases explicitly, and the release reproducibility guide now spells out the `comparison-corpus-guard-summary`, `compatibility-caveats`, `native-sidereal-policy`, `lunar-theory-limitations`, `interpolation-posture`, and `packaged-lookup-epoch-policy` aliases directly. The backend-matrix summary now also surfaces the selected-asteroid bridge and reference major-body bridge evidence inline, so the 2451915.0 bridge sample is visible from the same overview path.

This cycle also refreshed the contributor-facing command overview so the comparison-corpus release guard and reference/hold-out overlap summaries are easier to discover from the README quick-start path, the new 1749-12-31 major-body boundary summary is surfaced there as well, the new early major-body boundary summary is surfaced there as well, the new 1800-01-03 major-body boundary summary is surfaced there as well, the README now calls out the dedicated time-scale and Delta T policy summaries beside the request-policy overview, the request-surface inventory now names the chart TT/TDB offset aliases alongside the explicit UTC/UT1 flags, the short `request-surface` alias is now mirrored in both CLIs for quicker archaeology, and the request-surface alias now reports alias-specific stray-argument diagnostics in both CLIs so the compact inventory fails closed like the other summary commands, and the remaining top-level compatibility-profile, catalog-inventory, backend-matrix, api-stability, release-notes, release-checklist, release-summary, and artifact summary/report entry points in `pleiades-cli` now follow the same dispatcher so stray arguments fail closed consistently, the chart help text now also repeats the house-observer separation so observer-bearing chart requests cannot be mistaken for topocentric body-position mode, and the chart-façade conversion regression now also verifies that caller-supplied time-scale conversions preserve body-observer metadata and house-observer separation; the UT1 and UTC convenience helpers now also have dedicated regression coverage on the unsigned `with_tt_from_ut1`, `with_tt_from_utc`, `with_tdb_from_ut1`, and `with_tdb_from_utc` paths so that shape stays intact there too, the TDB-origin `with_tt_from_tdb` / `with_tt_from_tdb_signed` helpers now have matching metadata-preservation coverage as well, the UTC→TDB signed helper now has a dedicated regression covering the same metadata-preservation path, the house-code alias summary now also has a short `house-code-alias-summary` alias in both front ends for quicker archaeology, the selected-asteroid terminal boundary summary now also has a short `selected-asteroid-terminal-boundary-summary` alias in both front ends for quicker archaeology, and the new selected-asteroid bridge summary at JD 2451915.0 now also has a direct CLI path in both front ends, the now-populated 2451915.5 boundary day now also has a dedicated `boundary-day-summary` archaeology path, the short `boundary-day-summary` alias is mirrored in both CLIs, the canonical `reference-snapshot-sparse-boundary-summary` route now also rejects extra arguments with exact diagnostics in both CLIs, and the boundary-day report stays visible in structured form, the dense-boundary archaeology route now also rejects extra arguments with alias-specific diagnostics in both CLIs, the high-curvature epoch coverage summary now also has a direct CLI path in both front ends, the packaged-artifact generation residual-bodies summary now also has a short `packaged-artifact-generation-residual-summary` alias in both CLIs for quicker archaeology, the new compatibility-caveats summary now also has a direct CLI path in both front ends so known-gap posture stays visible apart from the broader compatibility-profile summary, and the validation help regression now pins the packaged lookup epoch policy command alongside the interpolation posture summary so those report surfaces stay discoverable from both CLIs, and the short `packaged-lookup-epoch-policy` alias now mirrors that command in both CLIs as well. The release-profile identifiers summary now also has a short alias in both CLIs and the CLI wrapper now rejects stray arguments for that route as well. The packaged frame parity and packaged frame treatment summaries now also have direct CLI paths in both CLIs for artifact-parity archaeology. The direct comparison-snapshot and reference-snapshot source-summary commands in `pleiades-cli` now also reject stray arguments, matching the fail-closed behavior already used by the validation renderer. The backend-matrix summary now also surfaces the boundary-epoch coverage, sparse-boundary, and dense-boundary lines so the 2451915.5 sparse day and 2451916.5 dense day are both visible from the backend overview too. The selected-asteroid bridge, dense-boundary, and terminal-boundary archaeology commands now also reject stray arguments through the shared validation path in both front ends, keeping those newly surfaced report entry points fail-closed as well. The comparison-corpus guard summary now also reports its own alias name in the extra-argument error text in both CLIs, keeping the release-grade guard surface exact. The comparison-corpus summary now also has a short `comparison-corpus` alias in both CLIs, so the corpus overview matches the existing comparison-envelope pattern. The comparison-corpus, benchmark-corpus, house-validation, and ayanamsa-metadata-coverage summaries in `pleiades-cli` now also forward extra-argument checks instead of silently discarding trailing arguments, closing another batch of release-facing summary routes. The README quick-start overview now also names the validation-side comparison, overlap, and native-dependency audit summaries explicitly so the release-archaeology entry points stay one hop away from the same overview. The release bundle now also archives `request-policy-summary.txt`, `time-scale-policy-summary.txt`, `delta-t-policy-summary.txt`, `native-sidereal-policy-summary.txt`, `lunar-theory-limitations-summary.txt`, `request-surface-summary.txt`, `compatibility-caveats-summary.txt`, `catalog-inventory-summary.txt`, and `custom-definition-ayanamsa-labels-summary.txt`, keeping the compact request-semantics, time-semantics, sidereal-posture, lunar-baseline limitation note, request-surface inventory, known-gap, catalog-total, and custom-definition ayanamsa label posture available beside the existing release-facing summary files. The staged-bundle smoke tests now also pin the catalog-inventory, custom-definition-ayanamsa labels, compatibility-caveats, release-profile-identifiers summary, request-policy, native-sidereal-policy, and native-dependency audit files so the release archive inventory stays synchronized with the reproducibility guide; the bundle manifest regression now also asserts the compatibility-caveats, custom-definition-ayanamsa labels, native-sidereal-policy, and native-dependency audit artifacts explicitly. The release-bundle verification output now also mirrors the request-policy, request-surface, compatibility-caveats, and native-dependency audit paths so the verification archaeology stays aligned with the staged bundle inventory. The packaged-artifact regeneration helper now also accepts `--output` alongside `FILE` and `--out FILE` in both CLIs, keeping the packaged-artifact write path aligned with the release-bundle parser parity work.

The README quick-start section now also surfaces the catalog-inventory summary / alias so the compatibility catalog totals stay one hop away from the same overview. The packaged-artifact target-threshold scope-envelope report path now also validates each rendered scope line through the shared scope-summary helper, keeping the body-class breakdown fail-closed in the Phase 2 posture. That inventory now also spells out the explicit TT/TDB alias set in the compact report text so the CLI surface stays readable without decoding the chart help block. The compact validation and release summary tests now also pin the request-surface inventory line so the explicit chart alias set stays covered alongside the request-policy lines. The compact validation and release summary regression coverage now also anchors the packaged lookup epoch policy line so the packaged-artifact lookup posture stays visible in the quick-overview surfaces, and the compatibility catalog inventory now also has a direct `catalog-inventory-summary` path in both CLIs while the inventory line itself now surfaces house formula-family coverage alongside alias counts and the explicit custom-definition ayanamsa label bucket; the new `catalog-inventory` alias now mirrors that summary in both CLIs for quicker archaeology. The production-generation coverage summary now also has a direct `production-generation-summary` path in both CLIs, keeping the overall production-generation posture inspectable alongside the existing boundary/source slices, and the new short `production-generation` alias now mirrors that summary in both CLIs for quicker archaeology. The packaged-artifact boundary envelope now also has a direct `artifact-boundary-envelope-summary` path in both CLIs, the reference boundary-epoch coverage summary now has a shorter `boundary-epoch-coverage-summary` alias in both CLIs for quick sparse-day archaeology, and the shared summary-line rendering is regression-tested in both front ends for quick release-artifact archaeology. The release bundle now also stages and verifies the comparison-corpus release-guard summary alongside the comparison-envelope summary so the release bundle surface keeps the Pluto exclusion guard explicit. The ayanamsa reference-offset sample set is now centralized in `pleiades-ayanamsa` and reused by validation/reporting, reducing drift risk for the release-facing catalog audit. The sample set now also includes Galactic Center (Mula/Wilhelm) and Babylonian (Britton) as release-specific anchors, and the validation/report surfaces now assert those anchors directly. The sample set now also broadens the release-facing zero-point examples with True Revati, True Mula, Suryasiddhanta (Revati/Citra), Suryasiddhanta (Mean Sun), Aryabhata (Mean Sun), Babylonian (Huber), and Galactic Equator (True/Mula), and now also adds Galactic Equator and Babylonian (Kugler 1) as additional release-specific anchors, with the summary regressions pinning those rows directly. The custom-definition ayanamsa label bucket now also has a direct `custom-definition-ayanamsa-labels-summary` inspection path in both CLIs so the profile-only label posture stays one hop away from the catalog inventory summary. The source-documentation-health alias now also mirrors the source-documentation-health summary in both CLIs while the README and release-reproducibility docs now surface that shortcut too. The source-documentation-health alias now also rejects extra arguments with exact diagnostics in both CLIs. The remaining top-level `backend-matrix`, `compatibility-profile`, `api-stability`, `release-checklist`, and `release-summary` routes in `pleiades-cli` now also have explicit extra-argument rejection coverage, keeping the dispatcher hardening aligned with the phase-4 fail-closed posture.

The selected-asteroid terminal boundary summary at JD 2500000.0 now also has a direct CLI path in both front ends, so the 2500-01-01 reference edge is visible beside the existing J2000 and 2001 boundary slices. The reference major-body 2500-01-01 boundary summary now also has direct CLI paths in both front ends, keeping the packaged-range upper edge visible alongside the existing 1800 and 2001 boundary slices. The new Mars/Jupiter boundary evidence at JD 2451918.5 now also has a dedicated summary and direct CLI path in both front ends, keeping the reference boundary runout visible alongside the earlier 2001 boundary slices. The 2451915.5 boundary day now also has a dedicated summary/CLI path in both front ends, so the release-facing boundary day remains easy to inspect. The early and 1800 major-body boundary summaries now also expose short aliases in both CLIs for quicker archaeology, and the remaining Mars/Jupiter, Mars outer-boundary, major-body boundary, and major-body boundary-window summaries now have matching short aliases as well.

Observer-bearing geocentric requests now fail with a dedicated `UnsupportedObserver` kind while malformed observer coordinates still use `InvalidObserver`, aligning the direct-backend policy surface with the spec wording and the report surfaces with the same failure-mode distinction. The observer-policy documentation has now been aligned to that same split so the maintainer-facing guidance no longer implies `InvalidObserver` for geocentric-only topocentric rejection.

Recently completed: Pluto explicit downgrade and corpus cleanup, plus the production-generation boundary overlay corpus and report entry that now seed the full independent hold-out validation snapshot for validation and future artifact-generation work. The compatibility-caveats summary and alias now also reject stray arguments with alias-specific diagnostics in both CLIs, so the known-gap inspection path stays fail-closed alongside the other compact release-facing summaries. The Pluto fallback summary now also has a short `pluto-fallback` alias in both CLIs for quicker archaeology. The selected-asteroid source evidence summary now also has a shorter `selected-asteroid-source-summary` alias in both CLIs for quicker archaeology. The reference asteroid source windows summary now also has a shorter `reference-asteroid-source-summary` alias in both CLIs for quicker archaeology. The production-generation source-window summary now also has regression coverage for body-order drift and derived-summary drift, keeping the merged source-window evidence fail-closed as it continues to support artifact-generation work. Artifact-profile header drift coverage now also checks packaged-artifact generator profile-id and label mismatches, and the generation manifest propagates those checks through its validation path; the packaged-artifact profile summary now also rejects endian-policy drift so the byte-order posture fails closed too. This cycle also added a dedicated Mars outer-boundary evidence summary for the 2600000/2634167 reference samples, surfaced it in both validation and CLI reporting, and pinned it in the validation/release summary regressions. The release bundle now also stages and verifies the comparison-envelope summary alongside the validation-report summary, keeping the release artifact surface aligned with the comparison-envelope posture, and the release-summary surface now also carries the explicit comparison-corpus release-grade guard line beside that evidence; the staged bundle now also carries `comparison-corpus-summary.txt` and `benchmark-corpus-summary.txt` for corpus-level archaeology. The new short `comparison-envelope` alias now mirrors that summary in both CLIs. The staged release bundle now also archives `release-profile-identifiers-summary.txt` alongside `release-profile-identifiers.txt`, and now also stages `release-house-system-canonical-names-summary.txt` and `release-ayanamsa-canonical-names-summary.txt`, keeping the release-profile and release-catalog audit surfaces visible in bundle archaeology. Release-grade comparison and tolerance reports now use a Pluto-excluded corpus, while the full snapshot corpus remains available for provenance and archaeology; the comparison-corpus summary and report surfaces now also render an explicit release-grade guard line for that policy. This cycle also added a dedicated `comparison-corpus-release-guard-summary` command in both CLIs so that guard line can be inspected directly, and the new shorter `comparison-corpus-guard-summary` alias now mirrors it in both CLIs as well. The house-validation and ayanamsa catalog summaries now also carry explicit implementation-posture wording so the phase-3 catalog evidence is easier to audit at a glance. The house-validation corpus now also includes an elevated northern high-latitude mountain stress chart (2,000 m observer elevation) so the topocentric house correction is visible in the report corpus. The compatibility-profile display path now also validates before rendering, so direct profile output fails closed on invalid catalog metadata and unknown house-formula families. The checked-in reference and hold-out CSV corpora now also have a direct regression test anchoring their documented 212-row / 34-row shapes and the 34 shared body-epoch pairs, and the reference snapshot body-class coverage summary now has a drift-rejection regression test anchoring its row-count validation; the reference major-body boundary and high-curvature summaries now also have drift-rejection tests that cover body-list and nested-window mutations. The interpolation-quality corpus now tracks the current 122-sample, 10-body, 15-epoch evidence set, its report surface now includes an explicit provenance line derived from the checked-in reference snapshot, and the interpolation posture itself is now surfaced as a typed release-facing summary so the runtime-validation/transparency-only decision stays explicit; a dedicated `interpolation-posture-summary` command now exposes that posture directly as well, the release-summary surface now also repeats that interpolation posture line for one-screen archaeology, the interpolation-quality evidence slice now also has a direct `interpolation-quality-summary` command for inspection, the interpolation-quality kind coverage slice now also has a direct `interpolation-quality-kind-coverage-summary` command in both CLIs, and the lunar apparent comparison evidence now also has a direct `lunar-apparent-comparison-summary` CLI path, the lunar source-window summary now also has a direct `lunar-source-window-summary` CLI path, and the lunar theory specification/capability/source summaries now also have direct CLI paths in both CLIs, with the lunar request-policy and frame-treatment summaries now also having direct CLI paths in both CLIs, and the lunar reference and lunar equatorial error-envelope evidence now also having dedicated `lunar-reference-error-envelope-summary` and `lunar-equatorial-reference-error-envelope-summary` CLI paths. The reference and hold-out provenance summaries now also fail closed on exact canonical source, coverage, frame/column, and reference-epoch drift so the TDB anchor and release-facing wording stay explicit in archaeology, and the checked-in CSV manifest header blocks now also fail closed on duplicate or reordered comment drift before the report layer renders them. This cycle also wired `pleiades-cli` through the independent-holdout batch and equatorial parity summaries so those report surfaces are now available from both CLIs, added a direct `independent-holdout-summary` path for the overall hold-out posture, and the top-level CLI now also mirrors the benchmark report command for release-facing archaeology. The VSOP87 source audit and generated binary audit summaries now also have direct CLI paths in both CLIs, extending the provenance layer beyond source documentation. The same front-end now also exposes the comparison-snapshot batch parity, reference-snapshot batch parity, reference-snapshot equatorial parity, workspace-audit summary, and `comparison-corpus-summary` / `benchmark-corpus-summary` commands directly, and the new `comparison-envelope-summary` command now surfaces the aggregate comparison envelope and percentile tail directly in both CLIs. The workspace-audit summary now also has a `native-dependency-audit` / `native-dependency-audit-summary` alias in both CLIs so the pure-Rust audit wording matches the release-hardening checklist. The selected-asteroid reference slice now spans J2000, 2001-01-01, 2132-08-31, and 2500-01-01 for Ceres, Pallas, Juno, Vesta, and asteroid:433-Eros, and the JPL snapshot report now also surfaces a broader 20-sample source-backed asteroid window set for provenance alongside the exact J2000 slice. The packaged-artifact regeneration provenance now also has a direct `packaged-artifact-regeneration-summary` CLI path in both CLIs, keeping the regeneration posture visible beside the other artifact-profile summaries. The reference snapshot coverage line now also names the 2451914.5 and 2451915.5 selected-asteroid boundary coverage inside the 2001-01-01..2001-01-07 window, and the reference snapshot provenance text now distinguishes the 2001-01-07 major-body boundary sample from the 2001-01-02..2001-01-05 span. The selected-asteroid reporting now also includes a dedicated body-window summary so the per-asteroid epoch spans are visible without decoding the fixture, plus a 2451914.5 boundary-day summary that now spans the 2451914.5..2451915.5 window and makes both high-curvature days explicit, a terminal 2500-01-01 selected-asteroid boundary summary for the target-range edge, and both the selected-asteroid evidence and window summaries now validate and fail closed on drift; a new reference-snapshot boundary-epoch coverage summary now also makes the sparse 2451915.5 boundary day visible inside the broader 2451913.5..2451917.5 window, and the epoch line now labels that day explicitly so future reference expansion work can target the remaining density gap directly; its release line is now exact-string pinned in the JPL and validation regression suites. The boundary-day evidence also has a direct `selected-asteroid-boundary-summary` CLI path in both CLIs, the selected-asteroid dense 2451916.5 boundary summary now also has a direct CLI path in both CLIs, the selected-asteroid source-evidence and source-window summaries now have matching direct CLI paths too, and the selected-asteroid batch-parity summary now also has a direct CLI path for mixed-frame archaeology in both CLIs, and the reference snapshot now also has a dense 2451916.5 boundary-day summary/CLI path to complement the sparse 2451915.5 archaeology slice. The selected-asteroid evidence and window summaries now also pin the sample-count and epoch-count fields with drift-rejection tests so the broader source-backed slice cannot silently shrink or shift, and the selected-asteroid evidence summary now has an explicit custom-body drift regression that keeps asteroid:433-Eros pinned in the release-facing body list, and the selected-asteroid and reference-asteroid source-window summaries now also reject custom-body drift. The reference asteroid evidence/equatorial-evidence summaries now also have direct CLI paths in both CLIs, and the new reference-asteroid-source-window summary now isolates the asteroid-only source windows from the broader reference snapshot corpus; the JPL batch error taxonomy summary now also has a direct `jpl-batch-error-taxonomy-summary` CLI path in both CLIs, and the combined JPL evidence summary now also has a direct `jpl-snapshot-evidence-summary` CLI path in both CLIs, with its production-generation and source-window sub-lines now pinned by regression coverage; the combined evidence summary now also carries the boundary-epoch coverage and sparse-boundary summaries for quicker archaeology. The production-generation, comparison snapshot, reference snapshot, and independent hold-out body-class coverage summaries now also have direct CLI paths in both CLIs for release-facing archaeology, and the comparison snapshot batch-parity summary, reference snapshot batch-parity summary, reference snapshot equatorial-parity summary, and independent hold-out batch/equatorial parity summaries now also have direct CLI paths in both CLIs. The exact J2000 selected-asteroid batch-parity slice is now surfaced too, so report consumers can inspect the mixed-frame request corpus in the backend-matrix-summary, release-notes-summary, and release-summary views without opening the detailed backend report, and the batch-parity slice now also has a direct `selected-asteroid-batch-parity-summary` CLI path. The backend matrix now also surfaces the exact J2000 reference snapshot evidence summary so the canonical J2000 slice is visible from the same release-facing overview. The reference snapshot source summary now also includes the body/epoch coverage windows that define the checked-in snapshot, so the release-facing evidence surfaces both provenance and window coverage directly. The checked-in reference snapshot source-window summary now also has a typed, validated body-window breakdown for archaeology and report parity, and its regression coverage now pins the current 194-sample / 15-body / 18-epoch source-window line plus the current boundary-window spans; the comparison snapshot source-window summary now does the same for the 131-row comparison corpus, and the new comparison body-class coverage summary now mirrors that validation slice in release-facing reports and now also surfaces in the validation-report and release-summary renderers; the independent hold-out snapshot now also has typed, validated source-window and body-class coverage breakdowns for the validation slice; the release summary and validation report now surface that overlap line alongside the hold-out source-window evidence so the shared validation slice stays visible in the main release-facing reports. The checked-in reference snapshot body-class coverage summary now also surfaces typed major-body and selected-asteroid window breakdowns in release-facing reports, the packaged-artifact fixture has been regenerated from the updated reference snapshot and now matches the current regeneration check output again, and the validation CLI now also mirrors the packaged-artifact regeneration check/write workflow, and packaged-data regression coverage now checks the earliest and latest source-snapshot epoch for every bundled artifact body so the artifact boundary evidence spans the full packaged body set. Packaged-data boundary tests now also reject requests outside the bundled time range with `OutOfRangeInstant`, adding a concrete out-of-range regression check to the compressed-artifact boundary envelope. The reference major-body boundary summary now explicitly labels the 2001-01-08 Mars/Jupiter boundary sample, so the current boundary-day evidence remains easy to spot in the release-facing archaeology. The production-generation boundary overlay now also has a standalone request corpus, inventory summary, per-body window summary, provenance summary, and report surface for reuse in validation and future artifact-generation work, plus a direct `production-generation-boundary-summary` CLI path in both CLIs so the overlay can be inspected without opening the full validation report, the production-generation boundary request corpus now also has a direct CLI path in both CLIs, and the merged production-generation corpus now also has a release-facing source-window summary so combined reference+boundary spans are visible per body without decoding the fixture; the merged production-generation body-class coverage summary now also surfaces the major-body versus selected-asteroid split in the same report family. The combined JPL evidence summary now surfaces the boundary-overlay provenance line, and the production-generation boundary window summary now validates its derived slice and report drift now fails closed as well; the lunar and major-body high-curvature reference summaries now also validate their summary lines and report wrappers fail closed on drift, including the high-curvature window report surface and its new direct CLI inspection paths in both CLIs; the short `high-curvature-summary` and `high-curvature-window-summary` aliases now mirror those report blocks in both CLIs; the packaged artifact regeneration provenance now carries an explicit profile identifier, and residual-body coverage now cross-checks against the bundled body list before release-facing validation proceeds. The packaged-data crate now also exposes deterministic generator-parameter and manifest outputs alongside the production-profile skeleton, and the validation artifact summary now surfaces the generation manifest directly next to the skeleton so the current regeneration posture is visible in the release-facing artifact report; the release bundle now also stages and verifies the packaged-artifact generation manifest alongside the artifact summary, and the new `artifact-profile-coverage-summary`, `packaged-artifact-output-support-summary`, and `packaged-artifact-production-profile-summary` direct CLI paths now expose the packaged-artifact profile coverage posture, output-support posture, and production-profile skeleton on demand in both CLIs, and the packaged-artifact generation policy/residual-body posture is now also directly inspectable from both CLIs. This cycle also tightened packaged-artifact claim-drift coverage with explicit regression checks for generator-parameter body coverage drift and manifest regeneration drift, and exposed the packaged-artifact target-threshold posture and scope envelopes as direct CLI inspection paths in both CLIs. The artifact inspection summary and artifact report now also surface a compact body-class mix for the bundled prototype so the current packaged composition is visible without decoding the body list. The packaged-artifact generator parameter and manifest regression coverage now also pins time-range and request-policy drift, keeping the production-manifest alignment explicit at the release boundary. The lunar source-window summary now also includes the reference-only apparent Moon comparison windows alongside the published 1992-04-12 Moon example and the checked-in 2451911.5/2451912.5 high-curvature samples, and the JPL source-window summary now exposes per-body coverage windows for the checked-in reference snapshot; the new major-body high-curvature reference slice now extends that coverage through the 2451914.5 boundary day across all comparison bodies, and the current high-curvature evidence now also reaches the 2451916.5 major-body boundary day, so the broader corpus evidence is visible without decoding the fixture, and the new major-body high-curvature body-window summary now exposes the per-body spans directly in report surfaces. The house-validation corpus summary now also surfaces the baseline formula-family coverage and the documented latitude-sensitive constraint notes for Placidus, Koch, and Topocentric directly in its release-facing summary, and the domain crate now also exposes the same built-in family list through a shared helper used by the catalog validation summary. The new `house-formula-families-summary` command now also exposes that baseline formula-family set directly in both CLIs for quick interoperability archaeology, and the house-code alias table now also has a direct `house-code-aliases-summary` CLI path in both CLIs for quick interoperability archaeology. The ayanamsa catalog validation summary now also names custom-definition-only labels directly in its sidereal-metadata coverage line so release-facing catalog evidence shows which labels are intentionally metadata-light, and the compatibility profile now cross-checks those custom-definition ayanamsa labels against the metadata-coverage summary while also keeping the profile-only custom-definition labels (`True Balarama`, `Aphoric`, `Takra`) distinct from the catalog's intentionally unresolved names; the compatibility-profile report now also lists the documented custom-definition ayanamsa labels explicitly in the coverage section. The metadata-coverage validator itself now also fails closed on exact custom-definition bucket drift and non-empty sidereal-metadata gap buckets so the release-facing posture cannot silently change shape. Representative ayanamsa reference-offset examples now also render in the compatibility-profile and release-summary reports, and now have a direct `ayanamsa-reference-offsets-summary` CLI path in both CLIs; the same summary now broadens those examples to include Raman, Krishnamurti, Fagan/Bradley, True Chitra, Lahiri (VP285), Krishnamurti (VP291), Usha Shashi, and Aryabhata (522 CE), and the baseline/release golden tests now pin Lahiri and Galactic Equator (Fiorenza) against their published epoch/offset metadata. The ayanamsa sidereal metadata coverage summary now also has direct CLI inspection paths in both CLIs, keeping the metadata-coverage posture visible beside the reference-offset examples, and it now renders `missing-sidereal-metadata=none` explicitly so the current no-gap posture stays visible in release-facing reports. The compatibility-profile summary now also renders that sidereal-metadata posture inline so the same audit is visible from the profile report surface. The built-in house catalog now fails closed if a descriptor ever falls back to an unknown formula family, keeping new house-system variants from silently entering the release profile without audit. Request-semantic summaries, CLI help, and policy docs now also spell out the deferred apparent-place, topocentric, and native-sidereal posture explicitly, and the release-facing request-semantics prose is now centralized behind a shared formatter in `pleiades-validate`. The new `request-policy` short alias now mirrors the same request-posture output in both CLIs. The validation and CLI surfaces now also expose a dedicated `request-policy-summary` / `request-semantics-summary` command so the compact request posture is directly inspectable from the same report entrypoints; the `request-semantics-summary` alias now also reports its own extra-argument rejection name so direct alias diagnostics stay exact. This cycle also added a batch-indexed regression that keeps mixed apparent-plus-topocentric requests failing closed on apparentness before observer validation, and now also adds a sidereal-plus-topocentric batch regression so sidereal rejection still wins before observer validation when both unsupported modes appear in the same request. The backend batch validator now also preserves apparentness before sidereal rejection when apparent and sidereal are combined in the same unsupported slice, and the same precedence is now pinned when apparent, sidereal, and topocentric flags all appear together in a batch request. A new regression also pins apparentness ahead of invalid-observer rejection when both appear together, so malformed coordinates do not hide the unsupported apparent-mode signal. The request-surface inventory now also has a dedicated `request-surface-summary` command in both CLIs. A dedicated `release-profile-identifiers-summary` command now exposes the release-profile pair directly as well, and the CLI regression now exact-matches that block against the current release-profile identifiers. A dedicated `time-scale-policy-summary` command now exposes the shared time-scale posture directly as well. A dedicated `delta-t-policy-summary` command now exposes the deferred no-built-in-Delta-T posture directly as well. The comparison-tolerance-policy summary now also has a direct CLI path in both CLIs, and the comparison-tolerance and comparison-envelope aliases now preserve their own extra-argument validation names when routed through the CLI wrapper. The new `pluto-fallback-summary` command now also gives the Pluto approximate fallback posture a dedicated direct CLI path for quick inspection. The compact validation-report summary now also echoes the comparison tolerance policy line next to the fail-closed audit status so threshold posture stays visible without opening the full report. That formatter now validates the shared request-policy summary before rendering the observer, apparentness, and request-policy lines so drifted policy prose fails closed in the report layer. The observer and apparentness policy summaries themselves are now typed and validated before the report layer renders them, and the observer/apparentness policy summaries now also have dedicated CLI inspection paths in both CLIs. Chart-snapshot diagnostics now also reuse the shared backend time-scale, frame, and apparentness summaries so rendered policy text stays aligned with that same contract, and the core frame-transform regression coverage now includes a near-polar mean-obliquity round-trip case so the precision expectations stay visible at the edge of the ecliptic/equatorial conversion. The shared request-policy summary validation now also rejects blank, whitespace-padded, and multiline drift so the compact report wording stays fail-closed, and the JPL provenance summary wrappers now reject embedded line breaks as well. The direct request validator now also checks both mean and apparent value-mode capabilities, so future apparent-only or mean-only backends will fail closed with specific unsupported-feature errors instead of silently accepting unsupported coordinates. A new regression now also keeps apparentness ahead of observer-topocentric rejection when both unsupported modes appear in the same request, so failure precedence stays explicit. The shared frame-policy summary now also fails closed when it drifts from the current canonical frame-policy posture, and the comparison/regression tolerance audits now use body-class-specific release thresholds for the active planetary corpus; the core frame-transform regression coverage now also includes a negative-right-ascension equivalence sample for equatorial-to-ecliptic normalization alongside the near-polar round-trip case, and the shared coordinate round-trip coverage now also exercises a reverse-transform wraparound sample with a negative-declination equatorial seed. The ayanamsa reference-offset report surface now also validates its representative zero-point examples and fails closed on missing or duplicated labels. The chart CLI now also accepts explicit UTC/UT1 TT-offset aliases plus explicit UTC/UT1 TDB-offset aliases so the common convenience conversion step is spelled out without changing the direct backend policy surface, and the policy docs now mention those aliases alongside the existing help text. The validation and release report renderers now also route the Delta T policy line through the same shared validated formatter, so the no-built-in-Delta-T posture fails closed consistently across report surfaces. Compatibility-profile summaries now also surface house formula families alongside the existing house-code and ayanamsa breadth evidence. The dedicated frame-policy-summary command now exposes the canonical request-frame posture as a standalone summary in both CLIs, the reference/hold-out overlap summary now also has a direct CLI path in both CLIs, and the house-validation corpus plus ayanamsa catalog validation summaries now also have direct CLI inspection paths in both CLIs, and the packaged lookup epoch policy now also has a direct CLI path in both CLIs. The comparison-snapshot-summary and reference-snapshot-summary commands now also provide direct CLI inspection of the overall snapshot coverage posture in both CLIs. The reference and hold-out manifest report helpers now fail closed on exact title, source, coverage, and column drift before rendering. A new shared CLI help parity regression now also compares the mirrored command sets between `pleiades-cli` and `pleiades-validate`, excluding the chart-only entry, so the direct summary surfaces stay aligned across both front ends.
- the compact validation-report summary now also surfaces a dedicated reference snapshot section (summary, source windows, source provenance, and body-class coverage) so the reference coverage posture stays visible without opening the full report;


The lunar source-window summary slice is now complete in both the validation renderer and `pleiades-cli`. The reference lunar boundary evidence slice now also has direct CLI paths in both CLIs. The reference major-body boundary evidence at JD 2451917.5 is now exposed through its own summary and direct CLI paths in both CLIs as well, and the new major-body boundary-window summary now keeps the per-body spans visible beside that evidence in the validation and release summaries. The compact validation-report summary now also carries the boundary evidence line. The release-grade comparison corpus intentionally keeps the 2451913.5 boundary day out of the audit slice so the current 112-row / 14-epoch comparison window remains the clean tolerance baseline until the comparison thresholds are revisited. The 2451915.5 boundary day now also has a dedicated boundary-day summary, so the now-populated day stays easy to inspect while expansion work moves on to other reference-coverage slices. The pre-bridge 2451914.5 boundary day now also has its own dedicated summary and alias in both CLIs, so the earlier boundary-day archaeology is available directly instead of only inline in the epoch coverage block. The boundary-epoch coverage summary now also calls out the 2451914.5 pre-bridge boundary day explicitly, making the earlier 2001 slice easier to spot from the same archaeology pass. The mean-obliquity validation corpus also gained north- and south-polar wraparound samples this cycle so the report-facing frame evidence stays aligned with the core regression tests, and the mean-obliquity frame round-trip summary now also has direct CLI inspection paths in both CLIs. The core sidereal transform path now also has a wraparound regression sample that keeps longitude normalization explicit when a custom offset crosses 360°. The shared angle-normalization tests now also pin exact ±180° boundaries and multi-turn longitude wraparound in `pleiades-types`.

## 2. Production reference corpus expansion

**Phase:** 1 — Accuracy Closure and Request Semantics

**Goal:** provide enough trusted source/reference material for validation and artifact generation.

**Work items:**

- Expand JPL/reference rows for all release-claimed major bodies and selected asteroids. The production-generation boundary overlay now includes the full independent hold-out snapshot, the merged reference-plus-boundary corpus now totals 245 rows across 15 bodies and 19 epochs, and the interpolation-quality corpus now includes the current 122-sample, 10-body, 15-epoch evidence set; the overlay also now has a standalone request corpus, summary, body-class coverage view, and provenance line for generation tooling, and the combined JPL evidence summary now includes the overlay provenance as well. The selected-asteroid source window summary now also surfaces the broader 75-sample asteroid provenance slice, the reference-snapshot reports now expose the broader Moon source-window summary with the apparent comparison windows, and the source-window reporting now covers both the checked-in reference snapshot and the merged production-generation corpus, and the production-generation, comparison snapshot, and reference snapshot source-window summaries now also have direct CLI paths in both CLIs; the production-generation boundary source summary and the independent hold-out source summary now also have direct CLI paths in both CLIs; the production-generation boundary window summary now also has a direct CLI path in both CLIs; the merged production-generation corpus now also has a combined source-provenance summary with a direct CLI path in both CLIs; the checked-in reference source-window summary is now typed and validated, it now derives its body order directly from the checked-in snapshot entries, the new reference/hold-out overlap summary shows that the current validation slice still shares 34 body-epoch pairs with the reference corpus, so any future expansion must draw from fresh public-source rows rather than borrowing the hold-out fixture, the checked-in reference corpus now also carries the expanded selected-asteroid boundary slice, the added 1800-01-03 major-body slice, and the 2451913.5 through 2451917.5 boundary window, bringing the snapshot to 245 rows across 15 bodies and 19 epochs with 75 asteroid rows, while the comparison and interpolation-quality corpora keep their release-grade windows by excluding that reference-only epoch, the comparison snapshot/report surfaces now remain aligned with the current 132-row / 16-epoch comparison corpus, and the independent hold-out source-window and body-class summaries are now available to mirror the comparison/reference archaeology; the comparison-snapshot-source-summary and reference-snapshot-source-summary commands now also expose the snapshot source metadata directly in both CLIs, the interpolation posture summary now also has a short `interpolation-posture` alias in both CLIs for quick inspection, and the lunar apparent comparison evidence now also has a direct `lunar-apparent-comparison-summary` CLI path. The comparison snapshot manifest summary now also carries the explicit J2000 coverage line in the checked-in CSV header and validates the expected title/source/coverage/column metadata before rendering, keeping provenance wording aligned with the comparison snapshot source summary. The JPL snapshot regression suite now also anchors the 1749-12-31 and 1800-01-03 boundary rows for exact lookup parity.
- Preserve deterministic manifests, provenance summaries, checksums, and pure-Rust parsing/loading.
- Decide whether interpolation is a runtime feature, generation-only aid, or transparency-only evidence.

## 3. Advanced request semantics decision

**Phase:** 1 — Accuracy Closure and Request Semantics

**Goal:** make time/observer/apparentness behavior release-ready.

**Work items:**

- Decide first-release posture for built-in Delta T and UTC/UT1 convenience conversion. The current backend/report surface now exposes the deferred no-built-in-Delta-T posture as a separate typed summary, but the decision itself is still to keep or implement a real model.
- Decide first-release posture for apparent-place corrections.
- Decide first-release posture for topocentric body positions.
- Update metadata, errors, docs, CLI flags, and tests so unsupported modes continue to fail closed. The explicit UTC/UT1/TDB alias spelling is already wired through the CLI and documented in the policy notes and README; the remaining task is broader reference-corpus expansion.

## 4. Production artifact profile and generator skeleton

**Phase:** 2 — Production Compressed Artifacts

**Goal:** prepare the artifact-generation path without overstating prototype accuracy.

**Work items:**

- Define production artifact profile identifiers, body sets, stored/derived/unsupported outputs, and target thresholds. The packaged-data crate now exposes a dedicated production-profile skeleton summary, a structured target-threshold scaffold that captures the current measured fit envelope and body-class scope envelopes, and deterministic generator-parameter/manifest outputs that capture the current prototype posture; the target-threshold scaffold now also carries the release-profile identifier, the compact validation and release summaries now surface the target-threshold posture and generation manifest, the target-threshold and target-threshold scope-envelope summaries now also have direct CLI inspection paths in both CLIs, the production-profile skeleton now also rejects request-policy drift directly, and the remaining work is to finalize the release thresholds and align the manifest with production fit validation.
- Keep the prototype artifact labeled as prototype until Phase 2 fit validation passes.
- Add tests for profile/header validation and claim drift. Production-profile label drift, artifact_version drift, artifact_profile drift, body_coverage drift, request-policy drift, and target-threshold drift are now covered; generation-manifest label drift is covered too, and the manifest now also has source, artifact_version, and artifact_profile drift coverage on its regeneration/parameter paths; extend the remaining header-field drift checks as Phase 2 hardening continues.

## 5. Artifact fit improvement loop

**Phase:** 2 — Production Compressed Artifacts

**Goal:** reduce packaged-data error to release-grade thresholds.

**Work items:**

- Tune segment lengths, polynomial order, quantization, and residual corrections by body class.
- Add interior samples, segment-boundary checks, and any additional high-curvature body windows needed beyond the current lunar and major-body evidence slices.
- Compare decoded outputs against Phase 1 generation sources.
- Publish measured errors and benchmarks in artifact summaries.

## 6. House-system evidence audit

**Phase:** 3 — Compatibility Evidence and Catalog Completion

**Goal:** ensure every release-advertised house system has formula and failure-mode evidence.

**Work items:**

- Add or verify golden scenarios across hemispheres, latitudes, and polar/high-latitude constraints. The release-facing house-validation corpus now also surfaces the baseline formula-family coverage and the documented latitude-sensitive constraint notes for Placidus, Koch, and Topocentric directly in its summary, and it now includes mirrored southern polar, southern high-latitude mountain, northern high-latitude stress, and western hemisphere reference charts so both hemispheres are represented in the high-latitude evidence slice and longitude coverage is no longer prime-meridian-only; its validation now also fails closed on case-insensitive duplicate scenario labels so scenario-heading drift is caught before report rendering.
- Check cusp ordering, angle derivation, normalization, and house placement. The compatibility-profile display and shared validation summary now also render the latitude-sensitive house-system set from the same formatter, keeping the release-facing constraint list in sync with the house evidence audit; compatibility-profile validation now also fails closed on malformed built-in house and ayanamsa descriptor-local metadata before the broader coverage checks run.
- Update compatibility caveats and profile verification for descriptor-only or constrained entries.

## 7. Ayanamsa evidence audit

**Phase:** 3 — Compatibility Evidence and Catalog Completion

**Goal:** ensure every release-advertised ayanamsa has provenance, sidereal metadata, aliases, and tests appropriate to its claim.

**Work items:**

- Expand golden/reference offsets for baseline and representative release-specific entries; Lahiri (ICRC), Lahiri (1940), Valens Moon, True Pushya, Udayagiri, True Citra, True Sheoran, Hipparchus, Djwhal Khul, DeLuce, Yukteshwar, PVR Pushya-paksha, Galactic Equator (IAU 1958), Galactic Equator (True), Galactic Equator (Mula), Aryabhata (499 CE), Sassanian, JN Bhasin, Babylonian (Kugler 2), Babylonian (Kugler 3), Babylonian (Eta Piscium), Babylonian (Aldebaran), Babylonian (Huber), and the galactic-center / Dhruva anchors are now in the sample set, and more release-specific anchors can be added if the catalog audit needs them.
- Classify custom-definition-only entries explicitly.
- Verify alias uniqueness and compatibility-profile labels.
- Keep sidereal conversion deterministic and domain-layer owned.

## 8. Release gate hardening

**Phase:** 4 — Release Hardening and Publication

**Goal:** turn release rehearsal commands into final publication gates.

**Work items:**

- Wire final validation thresholds, artifact checksums, compatibility verification, bundle verification, and pure-Rust audit into CI/release workflow.
- Harden release-bundle argument parsing so duplicate `--out` / `--rounds` inputs fail closed in both CLIs.
- Regenerate release summaries from current code and production artifacts.
- Review README/docs/rustdoc for units, frames, time scales, failure modes, and known limitations.

## Selection guidance

Prioritize slices 1-3 for the active frontier. Slices 4-5 should wait for trusted source inputs before making production accuracy claims. Slices 6-8 are safe parallel work when they do not depend on unresolved backend accuracy decisions.
