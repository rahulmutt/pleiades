# Status 2 — Next Slice Candidates

This file lists focused implementation slices for the current phase ladder. Completed report surfaces and historical cleanup tasks are intentionally omitted.

## Phase 1 — Artifact accuracy and packaged-data production

### Completed diagnostic slice

- The channel-major fit-outlier report now keeps segment-span and family sample-count context, tie-breaks prefer the shorter failing family when two candidates have the same delta, and distance-channel entries are rendered first in the body/channel summaries; the report lattice is denser than the calibration lattice.
- Completed: the long-span subdivision path now also consults one-sixth/five-sixth probes on very long dense-body spans before falling back to the one-third/two-thirds split bias, giving the source-window search a denser curvature check on the most asymmetric intervals; when the sixth-point probes are unavailable, the split search still falls through to the one-third/two-thirds ladder instead of reverting immediately to midpoint splitting, and the dense split selection now keeps that sixth/third fallback available even when quarter-point samples are missing. The longest dense-body spans now also consult one-fifth/four-fifth probes after the coarser ladders stay balanced, so the source-window search has one more dense fallback before midpoint splitting.
- Completed: the packaged-artifact body-class span-cap summary command now renders the compact entries without duplicating the summary prefix.
- Completed: `ArtifactProfile` now exposes a direct `motion_output_support` accessor, so approximated motion classification can be reused without re-deriving the speed policy at each call site.
- Completed: the backend frame-policy posture now has a validated report helper, and the core chart façade renders its frame-policy line through that helper instead of the frame-treatment wording.
- The packaged-artifact generator now tries six-point Chebyshev-Lobatto fits before falling back to the previous cubic/quadratic ladder, and sensitive bodies now also get a denser candidate lattice before that legacy fallback path; the dense candidate lattice now includes 10-point, 12-point, 14-point, 16-point, 18-point, and 20-point Chebyshev-Lobatto options for luminaries, Pluto, selected asteroids, and custom bodies, and the generator now also tries 8-point and 10-point Chebyshev-Lobatto baseline candidates for every body before that body-specific dense ladder, with inner and outer planets now also using 12-point and 14-point medium candidate baselines before fallback, and the best dense candidate now wins before fallback.
- Completed: fit-candidate scoring now prefers the smaller sample count on equal error, keeping identical-fit candidates compression-lean.
- Completed: the fit-candidate scorer now also breaks equal-error, equal-sample-count ties by preferring the simpler segment, so compression-lean candidates stay preferred when fit quality and sample count match.
- The generator now applies measured-fit subdivision on short spans, chooses the better candidate-versus-fallback reconstruction when the span is tiny, compares span-limited candidates against their fallback reconstruction before accepting them, regenerates the checked-in fixture, can attempt residual-correction channels across the bundled body set when they improve the measured fit, and now explores residual-channel combinations rather than only greedy single-channel additions and now also explores all remaining channel-order permutations when composing those channels while still caching artifact-derived fit samples so CLI/report rendering stays tractable; the residual search now also prefers the smaller residual footprint when equal-error candidates tie, and equal-error candidate-versus-fallback decisions now prefer the simpler segment, and the candidate-vs-fallback path is now accuracy-first so a worse fit no longer wins purely because it is simpler, the residual correction tables now use channel-specific sample fractions so angular channels can stay dense while distance channels remain sparse, the luminary and custom-body residual lattices now use denser body-specific cadences instead of the prior flat sample grid, and the validation lattice now gets body-specific density for luminaries, lunar points, selected asteroids, Pluto, and custom bodies. The residual-channel lattice helper now explicitly centralizes the dense-body angular-channel rule and the regression coverage now includes selected asteroids and custom bodies. The split heuristic now also reserves quarter-biased dense-body windows for very long asymmetric spans, and the fallback reconstruction path now tries a dense quarter-point control-point lattice before the exact-third fallback on irregular spans.
- Completed: the moon residual-search regression now also covers the equal-error, equal-residual-count coefficient-footprint tie-break, so the smaller residual polynomial set remains preferred when candidate errors match, and the coefficient-count branch now has a direct regression.
- The validation, benchmark, and packaged-artifact smoke-report paths now cache expensive report objects and use reduced timing subsets so bundle verification stays tractable under the test harness; the shared house-validation corpus construction now also caches its default and release reports so repeated chart-benchmark and validation-report assembly avoid rebuilding that corpus.
- Completed: the validation-report interpolation-quality subsection now appends the JPL body-class error-envelope evidence line, so the release-facing backend diagnostics keep the empirical envelopes visible alongside the existing quality, coverage, posture, and hold-out rows.
- Completed: the reference snapshot exact J2000 evidence summary now renders through the direct release-facing helper and now stages and verifies in the release bundle, keeping the validation crate doc-test path green while preserving the same report text.
- Completed: the reference snapshot summary now also stages and verifies in the release bundle with a live-renderer cross-check, so the corpus overview line fails closed alongside the other reference-provenance summaries.
- Completed: the comparison body-class error-envelope summary now has a compact CLI surface with alias coverage, the release summary now surfaces that empirical envelope count directly so the report surfaces keep the body-class error evidence visible outside the validation report, and the release bundle now stages and verifies that same error-envelope summary with a live-renderer cross-check.
- Completed: the VSOP87 source-documentation and source-documentation-health summaries now also route through validated helpers in the validation layer and CLI aliases, so the source-catalog evidence line fails closed on catalog drift before reuse.
- Completed: the comparison snapshot source-window summary now routes through a validated helper, so the comparison-snapshot-source-window CLI/report surfaces fail closed the same way the source-summary surfaces do.
- Completed: the reference snapshot batch parity, mixed TT/TDB batch parity, and independent hold-out batch parity summary surfaces now route through validated helpers too, keeping the other corpus-parity lines aligned with the existing comparison-snapshot fail-closed path.
- Completed: the reference snapshot source-window summary now routes through a validated helper and has an explicit release-facing validated accessor, so the checked-in reference source-window surface fails closed alongside the rest of the reference-corpus provenance lines.
- Completed: the workspace audit report now caches its filesystem scan results so repeated release-smoke and release-gate checks reuse the same expensive audit object within a process.
- Completed: the packaged-artifact report and summary renderers now cache their rendered text in-process, trimming repeated CLI/report recomputation on the compact artifact report surfaces, and the release-summary, release-checklist, release-checklist-summary, release-profile-identifiers summary, catalog-inventory summary, and release-smoke renderers now cache their rendered text in-process too.
- Completed: the release bundle verifier now also cross-checks the release notes summary against the live renderer, so release-notes-summary drift fails closed alongside the other release-facing summaries.
- Completed: the packaged-artifact body cadence summary now has a validated structured wrapper and fail-closed report rendering, and the release-bundle verifier now also cross-checks it against the live renderer, so the cadence posture can fail closed the same way the other packaged-artifact report surfaces already do.
- Completed: the packaged-artifact fit margin summary now has a validated structured wrapper, so the release-facing error-statistics posture can reuse the same fail-closed summary pattern as the other packaged-artifact reports.
- Completed: the packaged-artifact fit envelope now uses the same body-specific dense validation cadence as the outlier summaries for luminaries, selected asteroids, Pluto, and custom bodies, and the cadence-alignment regression keeps that dense-body contract explicit.
- Completed: the packaged-artifact dense-body cadence classification now flows through a shared helper, keeping the fit-candidate, residual, and validation sampling rules aligned instead of duplicating the dense-body list across the artifact generator, and the validation fraction lattice now reuses that same cadence helper too; the residual-channel lattice helper now explicitly centralizes the dense-body angular-channel rule and the regression coverage now includes selected asteroids and custom bodies.
- Completed: the long-span subdivision path now uses a curvature-aware split bias on dense bodies, so off-center windows can be chosen when the quarter-point probes show asymmetric motion instead of always splitting at the geometric midpoint; very long dense-body spans can now escalate to quarter-biased splits when the asymmetry is strong enough, and the split selection now also consults one-third/two-thirds probes on long dense spans when the quarter-point curvature stays balanced.
- Completed: the packaged-artifact regeneration helper now caches the deterministic rebuilt artifact in-process, so repeated CLI regression invocations only pay the full rebuild cost once per process, and the regeneration command now reuses cached encoded bytes for repeated sidecar writes. The CLI regeneration path now also uses the regenerated bytes helper directly, so repeated sidecar writes stay on the live rebuild cache instead of the committed fixture bytes; the CLI regression suite now also invokes the sidecar-writing path twice and confirms the emitted artifact and sidecars stay stable across repeated writes.
- Completed: the packaged-artifact regeneration write path now reuses the committed artifact bytes in the fixture-writing command while `--check` still exercises regeneration, so the release-gate smoke tests stay tractable without changing the checked-in bytes.
- Completed: the normalized-intermediate summary now carries a deterministic checksum, routes through a validated wrapper in release-facing validation surfaces, and is cross-checked against current rendering during release-bundle verification so the regeneration provenance fails closed if the rendered payload drifts.
- Completed: the packaged artifact now exposes a bundled-body cadence summary surface so future body-specific window tuning stays explicit without perturbing the current validation lattice.
- The packaged-artifact target-threshold posture is now represented by a typed release-state enum, the current posture is recorded as production-ready, the target-threshold validation now fails closed when any advertised scope exceeds the calibrated fit thresholds, the typed state now also has a direct `Display` surface and direct production-ready validation helper, the state now also exposes a direct validated summary helper for future release-facing surfaces, and the parameter-validation regression now checks that a Draft posture is rejected against that baseline; the compact validation-report summary now also surfaces that state line, and the CLI now exposes a dedicated target-threshold-state summary command. Completed: the packaged-artifact target-threshold state summary now also has a semantic-drift release-bundle regression, so the release-facing state line fails closed even after checksum refreshes.
- Completed: the release-bundle verifier now also cross-checks the packaged-artifact target-threshold summary against the current render helper, and now also cross-checks the packaged-artifact access summary and the packaged-artifact source-fit and hold-out sync summary against the live renderer, so stale threshold, access, source-fit, and generation-policy prose fail closed alongside the other release-facing summaries, and the packaged-artifact generation manifest summary now cross-checks against the live renderer as well.
- Completed: the packaged-artifact generation manifest checksum summary now routes through a validated helper and the release bundle verifier cross-checks it against the live renderer, so the manifest-checksum posture fails closed alongside the generation-manifest summary.
- Completed: the packaged-artifact target-threshold scope envelopes summary now routes through a validated helper in the CLI, validation-report, and release-summary surfaces, so the scope-envelope posture fails closed alongside the target-threshold and source-fit blocks.
- Completed: the packaged-artifact body-class span-cap summary now exposes a direct validated helper, and the CLI/report surfaces now reuse it instead of calling the validation step twice.
- Completed: the packaged-artifact source-fit and hold-out sync summary now exposes a validated CLI surface and also appears in artifact, validation, and release-bundle surfaces that couple the calibrated fit thresholds with the current target-threshold posture, so the source-fit checks can be reviewed alongside the hold-out-aligned release policy; the sync posture now also includes the explicit phase-2 corpus alignment evidence in the same line, the validation crate now also has a direct drift regression for the helper, and the release-facing report surfaces now route it through a dedicated validated helper. The packaged-artifact phase-2 corpus alignment summary now also has a live-renderer cross-check in release-bundle verification, so checksum refreshes cannot hide semantic drift in the standalone phase-2 posture.
- Completed: the phase-2 corpus alignment validation now reports field-specific nested corpus drift instead of collapsing every nested validation failure into the generic top-level payload label, so release-gate diagnostics stay actionable when the corpus posture diverges.
- Completed: the source-fit and hold-out sync regression now explicitly pins the shared phase-2 corpus alignment payload against the stand-alone phase-2 summary and the current production-ready target-threshold posture, so the release-facing sync checks fail closed if either alignment path drifts.
- Completed: the packaged-artifact output-support summary now renders the explicit empty unlisted bucket so capability drift stays visible in release-facing output-support reports, and artifact-profile validation rejects any unlisted output classification so that posture stays fail-closed rather than only being rendered.
- Completed: the packaged-artifact fit sample classes summary now stages and verifies alongside the other packaged-artifact release files, with the manifest and directory checks now tracking the new summary line explicitly, and the release bundle verifier now also cross-checks that summary against the live renderer so stale fit-sample prose fails closed.
- Completed: the packaged-artifact profile summary now has validated wrappers for the full capability line and explicit output-support entry rendering, keeping the release-facing capability surface fail-closed.
- Completed: the validation-report packaged-artifact profile block now routes output support, speed policy, access, and generation policy through dedicated validated wrappers, keeping that release-facing artifact posture aligned with the same fail-closed helper pattern as storage and production-profile summaries.
- The regenerated packaged-artifact fixture is now resynced to the current code path, restoring the checksum sidecar and repeat-write regressions to green, and the release bundle directory/manifest listings now include the release-house-validation summary with checksum verification and a live-renderer cross-check.
- Completed: the packaged-artifact generator now uses higher-order reconstruction from fit samples and a shared four-point control-point reconstruction across longitude, latitude, and distance channels when the higher-order fit does not quantize cleanly, and the checked-in fixture remains byte-stable.
- The compatibility-profile and release-summary renderers now re-validate the target-house and target-ayanamsa scope prose before emitting release-facing text, so the catalog posture fails closed if those scope sections drift.
- Completed: the packaged-artifact generation policy wording now matches the quadratic-window strategy, keeping the release-facing generation-policy prose aligned with the actual adjacent same-body quadratic window label.
- Completed: the packaged-artifact source/provenance prose now reuses the same generation-tail wording across the artifact source summary and the generation-policy note, reducing drift in release-facing narrative.
- Completed: the production-generation source summary now also validates its rendered provenance fragments, so the release-facing columns and redistribution-posture wording fail closed if the prose drifts. The release bundle verifier now also cross-checks the production-generation manifest summary and checksum summary against the live renderer, so those phase-2 manifest files fail closed on semantic drift.
- Completed: the packaged-artifact generation residual bodies summary now routes through a validated helper in the CLI and release/validation report surfaces, and the release bundle now stages and verifies it with a live-renderer cross-check, so residual-body drift fails closed instead of echoing stale text.

### 1. Improve fitting/reconstruction strategy

- Evaluate denser source windows, body-specific cadence, Chebyshev segments, residual tables, or any remaining channel-specific reconstruction gaps.
- The current generator now compares span-limited polynomial candidates against their fallback reconstruction before accepting them and can already attach residual-correction channels across the bundled body set when that improves the measured fit; further improvement can still come from denser source windows or residual tables.
- Keep artifact size and decode benchmarks current, but do not trade correctness away for size.

### 2. Keep the finalized threshold policy aligned with Phase 2 corpus evidence

- Completed: the packaged-artifact target-threshold summary now carries explicit phase-2 reference, comparison, and independent hold-out corpus alignment evidence and fails closed on phase2 alignment drift.
- Completed: the combined release-grade body-claims and Pluto-fallback posture validation now lives in `pleiades-backend` and is reused by the validation/report surfaces, so the Pluto boundary check is centralized with the other public summary helpers.
- Completed: the comparison snapshot source summary now stages and verifies in the release bundle, and now carries redistribution posture directly in the provenance line, keeping the comparison corpus provenance aligned with the reference and hold-out source summaries.
- Completed: direct and batch request-policy validation now pins invalid-observer precedence ahead of unsupported-body coverage, so mixed request-shape failures keep surfacing the malformed observer before body-coverage rejection.

## Phase 2 — Reference/source corpus productionization

### 1. Source ingestion decision

- Completed: the production source posture is now documented as a hybrid fixture corpus, with checked-in reference and hold-out fixtures plus a separate generation-input path; the reference snapshot source summary now exposes deterministic source checksums alongside the CSV column schema metadata and redistribution posture, the independent hold-out source summary now exposes deterministic source checksums alongside the CSV column schema metadata and redistribution posture, the checked-in snapshot manifests now also record a redistribution posture comment, the production-generation source summary now includes explicit per-fixture checksum revision markers plus CSV column schema metadata and the generation command and now validates the merged provenance block before rendering, and the production-generation source-window summary now stages and verifies alongside it in release bundles, the independent-holdout source-window summary now does the same, and the source-revision metadata is now modeled as a structured checksum pair instead of a raw string, the boundary overlay source summary now carries an explicit hold-out checksum marker, the selected-asteroid source request corpus now exposes frame-specific and mixed-frame validation shapes, the selected-asteroid source request corpus summary now fail-closes on frame, epoch, body-order, and apparentness drift, the selected-asteroid source request corpus summary now also has a release-facing CLI surface and now stages and verifies in the release bundle, and the new equatorial selected-asteroid request-corpus report helper now exposes the frame-specific slice as a dedicated release-facing alias, and the new semantic-drift regression now rejects checksum-refresh tampering for that staged request corpus, the selected-asteroid source window summary now also stages and verifies in the release bundle, and the request corpus now also has a release-facing equatorial alias through the new report helper, the reference-asteroid source window summary now also exposes a validated report helper, and the validation front-end routes the reference-asteroid commands through the validated helper instead of the plain formatter; the release bundle now also stages and verifies that summary, so the reference-asteroid provenance slice has a dedicated bundle artifact alongside the request-slice evidence, and the missing-entry regression now covers the staged file. The reference snapshot mixed TT/TDB batch parity summary now has a release-facing CLI surface, the production-generation boundary source summary now stages and verifies in the release bundle, and the comparison snapshot source summary now does the same for the comparison corpus while also failing closed on source, coverage, columns, and checksum drift; the production-generation source-summary regression now also locks the current source-window ordering against the report helper so the merged source block stays in sync with the source-window placement. The release-bundle verifier now also cross-checks the embedded production-generation source-window payload against the standalone source-window summary and the live renderer, so the merged source block fails closed if those release-facing files drift apart. The independent-holdout source-window summary now uses the same live-renderer cross-check in release-bundle verification, so the hold-out source-window posture fails closed alongside production-generation source-window drift. The release-bundle verifier now also cross-checks the reference snapshot bridge day summary against the live renderer, so the bridge-day posture fails closed too. The reference snapshot bridge-day summary now also has a validated wrapper for release-bundle serialization, so the provenance line can fail closed before the bundle is written.
- Completed: the release bundle verifier now also cross-checks the staged production-generation boundary source and request-corpus summaries against the live renderers, so phase-2 provenance drift fails closed even when the manifest checksums still line up.
- Completed: the packaged-artifact phase-2 corpus alignment posture now carries source/provenance lines for the reference, comparison, and hold-out corpora alongside the body-class coverage evidence, the standalone validated CLI summary surface stays in sync with that richer corpus posture, it now also carries the selected-asteroid source evidence and window summaries in the same phase-2 posture, and it now also carries the production-generation boundary source evidence, production-generation source provenance, and body-class coverage so the threshold posture explicitly tracks the separate generation-input corpus; the typed phase-2 corpus alignment accessor is public for direct reuse, and the regenerate-packaged-artifact regression now batches its sidecar writes so the slow fixture rewrite remains tractable under the test harness. The artifact summary and validation-report summary now also surface the packaged-artifact phase-2 corpus alignment line explicitly beside the target-threshold posture. The phase-2 corpus alignment summary now also uses its own dedicated validation error type, and the validation/report surfaces now route that block through a dedicated validated wrapper so the live renderer path follows the same fail-closed pattern as the target-threshold and source-fit sync summaries. The comparison body-class tolerance posture now also has a compact standalone summary surface with alias coverage for release-facing report reuse, and the comparison body-class tolerance summary renderer now validates each class row before formatting so malformed tolerance rows fail closed instead of rendering partial drift text.
- Completed: the broader source-corpus expansion contract is now spelled out in the release-reproducibility guide and phase-2 checklists, keeping provenance, license/redistribution posture, frame, time scale, columns/channels, source revision, generation command, checksums, and evidence-class boundaries explicit for any future corpus addition.

### 2. Coverage expansion

- Completed: the JPL interpolation-quality report now also includes body-class error envelopes for the luminary, major-planet, selected-asteroid, and custom-body slices, keeping empirical envelope evidence explicit in the validation path, and the interpolation-quality sample request corpus now also has a release-facing summary surface for the exact held-out request slice.
- Completed: the comparison body-class error envelopes now also have a standalone compact CLI summary, the release summary now surfaces the envelope count directly alongside the comparison tolerance posture, and the new comparison-body-class-error-envelope summary now stages and verifies in the release bundle so the empirical envelope evidence also fails closed at the bundle boundary.
- Completed: the house-code alias inventory now also has a shared validated report helper in `pleiades-core`, and the backend-matrix report path now uses the current-profile helper directly so the alias surface fails closed without duplicating the profile plumbing in each report layer.
- Expand body/epoch/channel coverage only where it supports artifact fitting or release claims.
- Preserve evidence classes: reference, hold-out, boundary overlay, fixture exactness, and provenance-only.
- Keep selected asteroid support bounded to validated bodies and epochs.

## Phase 3 — Body-model completion and claim boundaries

- Resolve Pluto as source-backed, artifact-backed, approximate, constrained, or excluded.
- Completed: the packaged-artifact boundary lookup regression now treats Pluto with a body-aware terminal-boundary tolerance, matching the explicitly approximate posture while exact-body boundary checks stay strict.
- Decide whether fuller ELP-style lunar coefficients are required for first production release.
- Keep lunar node/apogee/perigee claims aligned with implemented formulas and evidence.
- Completed: the JPL reference snapshot already contains source evidence for Ceres/Pallas/Juno/Vesta, and the release body claims summary now explicitly lists the selected asteroid validation bodies (Ceres, Pallas, Juno, Vesta, asteroid:433-Eros, asteroid:99942-Apophis) while keeping them validation-only rather than release-grade claims; the backend-matrix and reference-asteroid selected-asteroid coverage line now routes through a validated summary helper.
- Completed: backend-layer validated summary-line helpers now centralize the Pluto fallback and release-grade body-claims posture checks, so the claim-boundary surfaces can share one validation path instead of duplicating policy parsing.
- Completed: the lunar-theory source-family summary is now exposed as a release-facing CLI and validation surface and is documented in the release-reproducibility guide, keeping the selected-family label visible alongside the existing lunar source and catalog surfaces. The lunar-theory source-selection summary is now also exposed as a release-facing CLI surface and now stages in the release bundle, so the typed source-selection record has a direct inspection path in the same lunar provenance family, and the release-bundle verifier now cross-checks it through a validated wrapper.

## Phase 4 — Advanced request modes and policy

- Decide whether built-in UTC/Delta-T convenience belongs in the first production release.
- Implement apparent-place or topocentric body support only with capability metadata, validation, and docs.
- Keep native sidereal backend output unsupported unless a backend explicitly implements it.
- Add precedence tests for invalid/unsupported request combinations when behavior changes.
- Completed: the frame-policy summary surface now validates before rendering in the CLI and validation report paths.
- Completed: request-policy validation now preserves invalid-observer precedence ahead of unsupported-body coverage in both direct and batch metadata checks, and the request-policy/request-semantics report titles now share a guarded validation helper so the paired release-facing blocks fail closed if the title wording drifts.
- Completed: direct request-policy validation now also preserves unsupported-apparentness precedence ahead of invalid-observer validation, keeping malformed observers from displacing the advertised apparentness failure in the direct backend preflight path.
- Completed: the chart façade's request preflight now preserves invalid-observer precedence before unsupported apparentness in both single-request and batch validation, so malformed observer coordinates still fail closed before backend capability checks in the façade-level path.
- Completed: the chart/help text request-policy block now validates the shared request-policy summaries before rendering, so the user-facing CLI help mirrors the release-facing policy validation posture instead of echoing unchecked wording.
- Completed: the chart help block now keeps the request-policy and request-semantics compact aliases on the same shared line instead of performing a dead string rewrite, which keeps the doc-facing policy wording and the CLI help wording aligned.
- Completed: the chart CLI help clause now validates the shared request-surface inventory before rendering the explicit UTC/UT1 and TT/TDB alias wording, so the help path fails closed if the request-surface posture drifts.
- Completed: the backend frame-policy posture now has a validated report helper, and the core chart façade renders its frame-policy line through that helper instead of echoing the raw summary line.
- Completed: the zodiac policy summary now also routes through a validated release helper in validation and release-facing report surfaces, so the shared zodiac-posture wording fails closed when report prose drifts.

## Phase 5 — Compatibility catalog evidence

- Add formula/reference evidence for any house system promoted to fully implemented status.
- Add provenance/reference evidence for any ayanamsa promoted beyond descriptor/custom-only status.
- Completed: representative golden/reference tests now pin the latitude-sensitive topocentric house snapshot and baseline ayanamsa epoch offsets.
- Completed: the release-specific house-system and ayanamsa canonical-name summary commands now route through explicit validated helpers before formatting, keeping the public release-facing aliases fail-closed when the catalog posture drifts.
- Completed: the core façade now also exports matching release-specific house-system and ayanamsa canonical-name report helpers, and the validation surfaces reuse those public wrappers so the release-facing catalog summaries share one public path.
- Completed: the UTC convenience and native sidereal policy summaries now also route through validated release helpers in backend, core, CLI, and validation-report surfaces, so the advanced request-policy wording no longer depends on ad hoc validation formatting.
- Completed: the remaining time-scale, Delta T, observer, apparentness, and request-policy summary surfaces now also route through validated release helpers in backend, core, and CLI surfaces, so the shared request-policy wording follows the same fail-closed path as the UTC/native-sidereal helpers.
- Completed: the ayanamsa provenance summary now validates before rendering in release-facing report surfaces, so representative provenance examples fail closed if the sample notes drift.
- Completed: the compatibility-profile summary now fails closed if it stops describing the baseline/release split explicitly, and the related house-formula-family, latitude-sensitive, house-code-alias, custom-definition, catalog inventory, release-profile identifier, compatibility-caveat, release-notes, and release-summary surfaces continue to use validated wrappers; the individual house and ayanamsa descriptor summary surfaces still validate before rendering, the ayanamsa catalog validation summary still has a validated wrapper in release-facing report surfaces, and the compatibility-profile verification summary still exposes the validated compact summary-line helper.
- Completed: the compatibility-profile and release-summary renderers now reuse core target-scope summary helpers for the target-house and target-ayanamsa sections, so the long-term catalog scopes share the same validation path instead of duplicating the join logic in the validate crate.

## Phase 6 — Release gate hardening

- Make release-gate commands fail on stale generated files, artifact threshold violations, profile drift, unsupported-mode claim drift, or native-dependency regressions.
- Stage and verify all release-bundle artifacts from a clean checkout.
- Completed: release bundles now stage and verify the release-house-validation summary alongside the other release catalog evidence files, and now also stage and verify the packaged-artifact phase-2 corpus alignment summary in the bundle manifest and directory checks; the bundle manifest and verification path now also record cargo-version provenance alongside source revision, workspace status, and rustc version, the house-validation summary now also exposes a validated report helper for release-facing reuse, and the bundle generator now validates the release-grade body-claims and Pluto-fallback summaries before serializing them, the comparison-corpus release-grade guard summary now validates before rendering in comparison/report surfaces, the comparison tolerance policy, scope coverage, body-class tolerance posture, envelope, and Pluto fallback renderers now fail closed if comparison report construction fails, the comparison body-class tolerance posture summary now runs through a validated helper before rendering, the backend matrix summary now validates the compatibility profile before rendering, the release bundle verifier now semantically cross-checks the catalog inventory and custom-definition ayanamsa labels summaries against the current compatibility profile, and the release bundle verifier now also cross-checks the request-policy, request-surface, and API stability summaries against their live renderers.
- Completed: the release bundle verifier now also cross-checks the request-policy, request-semantics, and request-surface summaries against the live render helpers, so stale bundle prose fails closed even when the manifest checksums still line up.
- Completed: the release bundle verifier now also cross-checks the packaged-artifact lookup-epoch policy summary against the live renderer, so lookup-epoch posture drift fails closed too. It now also cross-checks the validation report's packaged-artifact fit margins line, so the report summary fails closed on the companion margin prose as well.
- Completed: the release bundle verifier now also cross-checks the native sidereal policy summary against the live renderer, so stale sidereal-policy prose fails closed alongside the rest of the request-policy block.
- Completed: the release bundle verifier now also cross-checks the compatibility profile summary and release summary against the live renderer, so the overview files fail closed on semantic drift even after checksum refreshes.
- Completed: the release bundle verifier now also cross-checks the compatibility-caveats summary against the live renderer, so the caveat inventory fails closed even after checksum refreshes.
- Completed: the release-house-validation summary now also routes through a shared helper so the CLI command and bundle writer use one validated release-facing path, and the release-bundle verifier now also cross-checks that summary against the live renderer.
- Completed: the backend matrix and validation-report summary surfaces now route house-code aliases through a validated profile-backed helper, keeping the release-facing summary surfaces aligned with the compatibility-profile validation gate.
- Completed: the release-bundle verifier now cross-checks the Pluto fallback summary against the release-grade body claims posture before accepting a staged bundle, so the Pluto claim boundary fails closed even when the individual summary checksums still match; it now also cross-checks the shared phase-2 corpus alignment payload between the source-fit/hold-out sync summary and the stand-alone phase-2 corpus alignment summary. The release bundle now also stages and verifies the lunar-theory catalog validation summary alongside the lunar-theory limitations summary.
- Completed: the release notes and release summary surfaces now route the compatibility profile release note through a validated helper before rendering, so those release-facing entries fail closed on profile summary drift.
- Completed: the release notes summary now also re-validates the target-house and target-ayanamsa scope prose before rendering, so the release-facing notes block fails closed if the compatibility scope wording drifts.
- Completed: the release bundle verifier now also cross-checks the release-profile identifiers summary against the live renderer, so the staged profile-identifiers payload fails closed if the compact release-profile prose drifts.
- Completed: the release summary now also surfaces the comparison body-class tolerance posture line, keeping the empirical backend-path comparison envelope visible in the quick release overview.
- Completed: the JPL snapshot evidence classification and source-posture report lines now route through validated summary helpers, so the release-facing evidence banner fails closed if the posture text drifts.
- Completed: the validation report now renders archived regression findings through validated summary helpers so malformed notes fail closed instead of leaking into release-facing output.
- Completed: the release-bundle verifier now cross-checks the backend matrix's selected-asteroid source evidence and source-window lines against the live renderer, so that selected-asteroid provenance fails closed even after checksum refreshes.
- Completed: the packaged-artifact body-class span cap summary now validates before rendering in CLI and validation-report paths, so the release-facing body-layout evidence can fail closed if the generated posture drifts.
- Completed: the packaged-artifact fit sample classes summary now validates its boundary continuity component before rendering, so the combined fit sample report no longer skips validation on the boundary half of the line.
- Completed: the packaged-artifact storage/reconstruction and production-profile summary surfaces now route through explicit validated wrappers before rendering, so the release-facing artifact posture lines fail closed if the underlying summary validation drifts.
- Completed: the release bundle verifier now also cross-checks the staged packaged-artifact production-profile summary against the live renderer, so stale production-profile prose fails closed.
- Completed: the validation-report and packaged-data backend provenance surfaces now route request-policy and frame-treatment through validated helpers, so those release-facing metadata lines fail closed instead of echoing raw summary text.
- Completed: the comparison tolerance scope coverage summary command now publishes the per-scope body-class tolerance rows behind the aggregate comparison tolerance policy line, and it now fails closed when any per-scope coverage row drifts, so the release-facing tolerance posture can be inspected independently from the combined policy summary without tolerating malformed row data.
- Completed: comparison snapshot summary validation now explicitly rejects missing rows, missing bodies, and invalid epoch-range drift before the release-facing summary text is rendered, keeping the comparison snapshot coverage posture fail-closed under corpus shape drift.
- Completed: the verify-release-bundle display now includes the packaged-artifact phase-2 corpus alignment summary in staged-bundle output, matching the manifest entry and checksum checks.
- Completed: the release bundle verifier now also cross-checks the lunar-theory source-family summary against the live renderer, so the family-posture line fails closed even when the staged checksum is refreshed.
- Completed: the release bundle verifier now also cross-checks the lunar-theory limitations summary against the live renderer, so the limitations posture fails closed even when checksum refreshes would otherwise mask drift.
- Completed: the release bundle verifier now also cross-checks the lunar-theory catalog validation summary against the live renderer, and the new regression rejects checksum-updated tampering of that summary, so catalog-validation drift fails closed alongside the lunar source-family posture.
- Completed: benchmark provenance now records cargo version alongside source revision, workspace status, and rustc version so the release bundle's benchmark report carries the additional tool-version evidence needed for reproducibility.
- Completed: the benchmark corpus summary now has a validated wrapper and the release bundle verifier now cross-checks it against the live renderer, so benchmark corpus drift fails closed in release bundles; the release-bundle tamper regression now also updates its checksum sidecar and still rejects semantic benchmark-corpus-summary.txt drift.
- Completed: the reference-asteroid source window summary now also has a semantic-drift release-bundle regression, so the reference-asteroid provenance slice fails closed even after the checksum sidecar is refreshed.
- Completed: the packaged-artifact generation manifest checksum summary now stages and verifies in release bundles, and the lunar-theory source-selection summary now routes through a validated wrapper in the release-bundle verifier.
- Completed: the packaged-artifact regeneration summary now stages and verifies in release bundles with a live-renderer cross-check, so regeneration prose now fails closed alongside the other packaged-artifact release files.
- Keep README/docs aligned with the published release compatibility profile and known gaps.
