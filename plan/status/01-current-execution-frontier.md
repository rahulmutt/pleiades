# Status 1 — Current Execution Frontier

## Frontier

The active frontier is still formally **Phase 1 — Reference Accuracy and Request Semantics**, but the current reference-breadth slice is saturated; the next reviewable code slice is Phase 2 production compressed-artifact manifest drift-proofing.

The repository is past bootstrap, MVP API work, catalog scaffolding, report-surface expansion, and release-bundle rehearsal. The remaining work now centers on turning the packaged-data prototype into a reproducible production artifact without widening claims prematurely.

## Evidence reviewed

Current implementation status shows:

- all mandatory first-party crates exist and respect the `pleiades-*` naming rule;
- the backend trait, metadata, batch APIs, composite/routing helpers, chart façade, compatibility profiles, and validation/report commands are in place;
- first-party backend request policy is explicit: mean geometric, tropical, geocentric TT/TDB requests are supported; unsupported time scales, apparent-place requests, topocentric body-position requests, and native sidereal backend requests fail with structured errors; the validation suite now also pins unsupported-time-scale precedence ahead of malformed observer input in direct metadata checks and batch checks;
- chart APIs preserve the distinction between house observers and body observers and provide caller-supplied UTC/UT1/TT/TDB offset helpers, but built-in Delta T/UTC conversion policy remains a release decision; the validation-report summary now mirrors that UTC-convenience posture explicitly, the backend and core façades now re-export the typed UTC-convenience, Delta T, and native sidereal policy summaries plus their current policy constructors for consumers that want the same wording without a backend dependency, the backend and core façades also expose matching `request_semantics_summary_for_report()` aliases for the combined request-policy wording, the validation and CLI surfaces now render `request-semantics-summary` / `request-semantics` with a distinct `Request semantics summary` title while keeping the same policy body, the request-surface inventory now surfaces Delta T as a separate report entrypoint, the validation/CLI surfaces now also provide a standalone release-body-claims summary for the release-grade major-body posture, the release bundle now also stages a distinct request-semantics summary sidecar rendered from its own entrypoint alongside the request-policy summary, and the shared CLI request-policy help block now prints the UTC convenience line alongside the time-scale, Delta T, and request-semantics lines; frame precision is explicitly bounded by the shared mean-obliquity frame round-trip envelope;
- `pleiades-vsop87` is source-backed for Sun through Neptune via generated VSOP87B tables; Pluto remains an explicitly approximate mean-elements fallback excluded from release-grade major-body claims, and the validation/release reports now surface a dedicated release-grade body-claims summary alongside that Pluto posture;
- `pleiades-elp` is a compact Meeus-style lunar baseline with validation evidence for supported lunar channels, and its limitations summary now publishes the measured reference and equatorial channel envelopes directly; the first release keeps this compact posture rather than shipping a full ELP coefficient implementation;
- `pleiades-jpl` is a checked-in JPL Horizons snapshot/hold-out fixture backend with provenance and selected asteroid evidence, plus a dedicated 2500000 selected-asteroid source summary with CLI/validation aliases, including dedicated 1500-01-01, 1600-01-11, 1750-01-01, 1800-01-03, 1900-01-01, and 2200-01-01 selected-body boundary report surfaces, plus 2524593, 2415020, and 2634167 JD-labeled aliases in the CLI/validation front ends, with the 2415020 label now also promoted through the top-level reference snapshot and release-notes summaries, 1749 major-body boundary coverage and the early major-body boundary coverage slice, and the early boundary slice now also has a 2378498 exact-JD alias, a 2451912.5 major-body boundary report surface plus 2451913.5/2451915.5/2451917.5/2451918.5/2451919.5 major-body boundary report surfaces and a 2451916.0 interior reference surface with a direct CLI/report alias, a direct CLI/report surface for the 2451913.5 boundary slice, a 2451918.5 Mars/Jupiter boundary report surface now promoted into the top-level reference snapshot summary and now also exposed via an epoch-specific 2451918 major-body boundary alias in the public backend API with direct backend drift regression coverage and explicit 2451918 wording in release-facing reports, a 2451919 major-body boundary summary is now also surfaced in the validation-report and release-summary layers, a 2451916.5 dense boundary report surface now promoted into the top-level reference snapshot summary alongside the generic dense boundary day, sparse boundary summary, and pre-bridge boundary summary, and now also exposed via an epoch-specific 2451916 major-body dense-boundary alias plus a generic 2451916 major-body boundary alias for discoverability; the top-level reference snapshot summary now also surfaces that generic boundary alias, and the reference snapshot summary regression now also explicitly anchors the 2451916 major-body boundary slice, a 2500-01-01 selected-body boundary slice for Mars, Mercury, Moon, Sun, and Venus with direct CLI parity in `pleiades-cli`, a new 1750-01-01 interior boundary slice for Sun through Neptune, now also surfaced through a first-class 1750 major-body interior report surface, a 2360234.5 interior comparison slice now exposed through a first-class report surface, a 2451910.5 major-body boundary report surface now surfaced through a first-class report surface, with the validation-report and release-notes layers now also surfacing the 2451910 major-body boundary slice explicitly, an exact-J2000 evidence summary now promoted into the top-level reference snapshot summary, a first-class high-curvature hold-out summary for the 2451915.25/2451915.75 Sun/Moon/Mercury/Venus window now also surfaced through the combined JPL evidence report, a 2451920.5 interior reference slice now regression-tested through the combined reference snapshot summary and now also surfaced through the validation-report, release-summary, and release-notes layers, a 2400000.0 major-body boundary summary now surfaced through a first-class report surface, a new 2451545.0 major-body boundary summary for the J2000 major-body reference slice with direct CLI/report aliases, a generic major-body boundary summary now also surfaced through the top-level reference snapshot summary, an added 2453000.5 major-body boundary summary promoted into the top-level reference snapshot summary, a 2500000.0 major-body boundary summary now surfaced through the top-level reference snapshot summary and CLI/report aliases, a 2600000.0 major-body boundary summary for the Mars outer-boundary anchor with CLI/report/help aliases, a 2500 major-body boundary summary now surfaced through the top-level reference snapshot summary, and a Mars outer-boundary summary now promoted into the top-level reference snapshot summary, an epoch-specific 2451914 major-body pre-bridge alias, an epoch-specific 2451914 bridge-day alias, an epoch-specific 2451914 major-body bridge-day alias, and the top-level reference snapshot summary now also includes the generic bridge-day summary plus the 2451914 pre-bridge and bridge-day report surfaces through the explicit 2451914 helper aliases and also surfaces the distinct 2451914 major-body bridge-day alias, an epoch-specific 2451915 major-body bridge alias, and newly surfaced 2451917 major-body boundary and bridge summaries, and the CLI parity layer now mirrors the 2451912, 2451913, 2451914, 2451917, 2451918 boundary aliases plus the 2451914 pre-bridge, 2451914 bridge-day, and generic bridge-day aliases, including the 2451914-bridge-day-summary alias, the 2451915 bridge alias, the 2451917 bridge alias, and the 2451916 dense-boundary alias, while the JPL backend API now also exposes epoch-specific aliases for the 2360233 and 2378499 major-body boundary report surfaces, plus the 2451914 bridge-day, 2451914 pre-bridge, 2451914 bridge, 2451914 major-body bridge-day, and 2451915 bridge report surfaces with explicit 2451915 wording in release-facing reports, and the release-notes and release-summary layers now also mirror the 2451914 major-body bridge summary explicitly, and a reference snapshot summary that now surfaces the 1500-01-01, 1600-01-11, 1749, early major-body boundary, Mars/Jupiter boundary, 1750-01-01, 1750 major-body interior comparison, 1800-01-03, 1900-01-01, 2451912.5, 2451913.5, 2451916.0, 2451916.5, 2451917.0, 2451917.5, 2451918.5, 2451919.5, 2451920.5, 2400000.0, 2453000.5, 2500 major-body boundary, Mars outer-boundary, boundary-epoch-coverage, reference source, and reference source-window slices alongside the earlier boundary summaries, plus an updated comparison-corpus guard that now reflects the current 2451913.5 boundary-day coverage; the boundary-window and boundary-epoch-coverage aggregate report surfaces now have direct regression coverage, and the boundary-epoch-coverage slice now widens to JD 2451912.5..JD 2451919.5, not a broad production reader/corpus. The combined JPL evidence report now also makes the checked-in fixture backend / separate generation-input posture explicit, now also surfaces the generic bridge-day summary, the 2451914 major-body bridge-day summary, and the 2451914 major-body bridge summary, so it is not yet a broad production reader/corpus. The reference-snapshot summary regression now also explicitly anchors the 2451912, 2451913, 2451914, 2451914 bridge, 2451915 bridge, 2451916 dense, and 2451917 bridge slices, and the CLI/validation summary checks now also pin the 2451918 and 2451919 major-body boundary surfaces. The validation-report surfaces now also surface the 2451917 boundary and bridge summaries explicitly, and the validation-report and release-summary surfaces now also surface the 2451918 and 2451919 boundary slices explicitly, while the release-notes summary also surfaces the 2451917 boundary and bridge summaries explicitly, and the CLI/validation parity slice covering 2451912, 2451913, 2451914, 2451917, 2451918, 2451914 pre-bridge/bridge, 2451915 bridge, and 2451916 dense aliases is now complete and regression-tested. The CLI runtime now also exercises the 1500 selected-body, 1750 major-body interior, 1900 selected-body, 2360233 major-body boundary, 2360234 major-body interior, 2378499 major-body boundary, and 2451911 major-body boundary aliases directly, so the help/runtime alias slice is covered end to end. The top-level reference snapshot summary now also includes the lunar boundary evidence summary, the major-body high-curvature evidence/window/epoch-coverage summaries, the 2451911.5 major-body boundary slice, and the major-body boundary-window aggregate alongside the release-facing boundary slices; it is not yet a broad production JPL reader/corpus.
- the CLI/validation help inventory now stays synchronized with the advertised reference-summary surface set, including the manifest, reference snapshot, body-class coverage, exact-J2000, boundary-day, sparse/pre-bridge/dense, Mars/Jupiter, Mars outer, 2360233/2378499 major-body boundary, and 2524593 selected-body entries, and the 2451911 boundary plus the 2451914 bridge-day, 2451915 bridge, 2451916 boundary, 2451918 boundary, and 2451919 boundary aliases now have explicit CLI help coverage too; the production-generation boundary request corpus, source-window, and boundary-source entrypoints are also kept in CLI/validation parity, and direct aliases now mirror those entrypoints;
- the major-body high-curvature summary/window/epoch-coverage surfaces now also expose snapshot-prefixed and major-body-prefixed aliases in the CLI and validation front ends for discoverability;
- `pleiades-data` ships a deterministic draft artifact with codec/profile/checksum/regeneration support, and the checked-in fixture tracks the current reference-snapshot slice; the production-profile and generator-parameter summaries now explicitly carry speed-policy metadata alongside the existing lookup-epoch policy, the packaged generation surface now exposes both the generation and regenerate entrypoints for the fixture workflow, and they also make the segment strategy explicit in the production-profile/generator/manifest summaries, while the packaged-data surfaces continue to expose a standalone speed-policy summary in CLI/validation reporting, explicit storage/reconstruction summaries that distinguish derived ecliptic/equatorial coordinates from unsupported apparent, topocentric, sidereal, and motion outputs, a packaged-artifact target-threshold scope envelopes summary sidecar alongside the target-threshold summary, and a packaged-artifact generation policy summary sidecar alongside the generation manifest in the release bundle; the bundle-release help text now also names the packaged-artifact generation manifest summary sidecar so the staged bundle contents and CLI documentation stay aligned. Regeneration provenance now also surfaces the production-generation source summary alongside codec quantization-scale metadata and the production-generation source summary now also publishes cadence details and the checked-in reference-snapshot summary equality check; the artifact is not yet a production 1500-2500 CE data product and current fit posture is not release-grade; hold-out rows remain separately classified from reference and production-generation evidence, and the combined snapshot regression now pins that separation explicitly, and the reference snapshot summary regression now also asserts that the hold-out block stays out of the reference summary;
- the combined JPL evidence report now also surfaces the 2451916.0 interior and 2451916.5 dense-boundary reference slices alongside the existing 2451910 through 2451915 boundary coverage, keeping the evidence report aligned with the validated reference frontier; the hold-out high-curvature slice remains outside the reference snapshot summary and is only surfaced through the hold-out evidence block;
- house and ayanamsa catalogs are broad, but not every release-advertised entry has independent formula/provenance/reference evidence sufficient for full interoperability claims; the catalog inventory now carries an explicit claim-audit clause for baseline guarantees, release additions, custom-definition territory, and known gaps, the core façade now re-exports shared compatibility-summary helpers for house formula families, latitude-sensitive house systems, custom-definition ayanamsa labels, and catalog inventory, the release summary now includes representative ayanamsa provenance excerpts for curated built-in entries, and the curated ayanamsa provenance sample set now also has a dedicated `ayanamsa-provenance-summary` / `ayanamsa-provenance` report surface;
- the top-level reference snapshot summary now also includes the selected-asteroid boundary, bridge, dense, terminal, and source evidence/window slices alongside the lunar boundary evidence summary and major-body boundary-window aggregate; the selected-asteroid source evidence/window commands now also have reference-snapshot-prefixed CLI/validation aliases for naming parity, and the selected-asteroid 2003-12-27 source slice now has a first-class epoch-specific summary as well; the release notes summary now also surfaces the 2451916 dense boundary slice explicitly, and now carries direct 2451916 interior/boundary lines in addition to the aggregated reference snapshot coverage; the release notes summary and release summary now also surface the 2451916 interior and boundary slices explicitly, and the release notes summary and validation report now also surface the terminal slice explicitly;
- release-bundle generation and verification exist, and validation/report surfaces now classify evidence as release-tolerance, hold-out, fixture exactness, or provenance-only; the comparison audit surface now also mirrors body-class tolerance posture for the release-grade corpus, the boundary-epoch coverage summary now spans JD 2451912.5..JD 2451919.5 across 12 epochs, and the staged bundle now carries the comparison-body-class-tolerance summary alongside the envelope/guard outputs plus the release-grade body-claims, Pluto-fallback, and reference snapshot source summaries; final release gates still need to be rerun over production accuracy evidence, production artifacts, and truthful compatibility profiles.

- the top-level reference snapshot summary now also surfaces the reference-asteroid evidence, equatorial-evidence, and source-window summaries alongside the existing selected-asteroid and major-body evidence blocks, and now also carries the reference equatorial and batch parity summaries, so the current reference breadth is a little more complete without changing the fixture/backend posture.
- the generic reference bridge-day summary, generic major-body bridge summary, and 2451914 major-body bridge-day alias are now also promoted through the top-level reference snapshot and release notes summaries, closing that aggregate reporting slice.

## Why this frontier comes first

Phase 2 production artifacts require trusted generation inputs and target thresholds. Phase 4 release claims require the same evidence. Therefore maintainers should close source-backed accuracy and request-policy gaps before claiming production packaged-data coverage or broad compatibility.

## Immediate blockers

1. **Reference corpus breadth** — The production-generation input path is now explicitly documented as the checked-in CSV fixture pair, now widened with a 1750-01-01 interior boundary slice and a 2200-01-01 interior selected-body slice; continue expanding or replacing fixture evidence with production-suitable public source/reference data for validation and artifact fitting.
2. **Pluto release posture** — Pluto is now visibly approximate and excluded from release-grade major-body claims; the posture is also surfaced through a shared typed summary in `pleiades-backend`/`pleiades-core` and the validation report, and a source-backed path remains optional if future release claims need it.
3. **Lunar release posture** — The first release keeps the compact lunar baseline; fuller ELP-style coefficient support remains follow-on work, and the compact path now publishes measured reference and equatorial channel envelopes in the limitations summary.
4. **Advanced request semantics** — These request modes are now explicitly deferred in the first-party backend posture; keep the report wording, metadata, and rustdoc aligned while the next implementation slice shifts back to reference breadth. The request-semantics help-text wording has been aligned with the alias name, and the request-policy/request-semantics renderers now share the same formatter, while the concise `bridge-summary` alias is now available for the generic major-body bridge report, so any remaining cleanup should stay limited to report phrasing. Frame precision remains stated via the shared mean-obliquity frame round-trip envelope.

## Recommended next slice

The reference corpus breadth is already covered by the existing 1500-2500 CE boundary and interior slices, including the 2451911 and 2451916 release-facing report surfaces and their release-note mirrors. The next reviewable step should move to Phase 2 artifact-manifest drift-proofing rather than adding another boundary row.

Next, implement another small, reviewable slice:

- harden the packaged-artifact manifest and checksum sidecars so production-profile, lookup-epoch, and generation-policy metadata stay in lockstep;
- keep the prototype fixture path separate from production artifact generation;
- add or update validation/report tests only where they tighten drift detection without claiming production fit accuracy.

## Parallel safe work

The following can proceed without blocking Phase 1:

- house/ayanamsa formula, alias, latitude, and custom-definition audits;
- custom-definition ayanamsa example labels are now centralized in `pleiades-ayanamsa` and surfaced through the compatibility profile;
- documentation cleanup for already-explicit request policy and known gaps;
- artifact-profile metadata hardening that does not claim production fit accuracy;
- release-bundle smoke-test maintenance that keeps existing rehearsal tooling accurate.

## Constraints

- Preserve pure Rust and first-party crate layering.
- Do not make domain crates depend on concrete backends.
- Do not silently satisfy unsupported apparent/topocentric/native-sidereal requests.
- Do not publish broader accuracy, artifact, or compatibility claims until validation evidence supports them.
