# Status 1 — Current Execution Frontier

## Frontier

The active frontier is **Phase 1 — Production Ephemeris Accuracy**.

The repository has a complete architectural foundation and broad planning/reporting scaffolding. The next material risk is correctness: the backends that drive chart outputs, validation comparisons, and future compressed artifacts must move from preliminary/sample behavior to source-backed astronomical implementations with measured accuracy.

## Why this is first

Several downstream requirements depend on trusted ephemeris outputs:

- compressed artifacts must be generated from validated source data;
- release compatibility profiles must not imply unsupported accuracy;
- chart APIs need deterministic body positions across supported bodies and time ranges;
- validation reports need real error envelopes rather than placeholder comparisons.
- Latest progress (2026-04-30): `pleiades-core::HouseCodeAliasInventorySummary` now exposes a validated summary-line helper, and the compatibility-profile alias report path reuses it so the short-code inventory line can fail closed through the typed inventory record instead of only through the profile wrapper.
- Latest progress (2026-05-01): `pleiades-backend::TimeScalePolicySummary` now also validates against the current canonical time-scale posture, so the compact report line now fails closed if the TT/TDB + caller-supplied-conversion wording drifts away from the current backend contract.
- Latest progress (2026-05-01): `pleiades-core::AspectSummary` now also exposes a validated summary helper and the chart snapshot formatter preflights the major-aspect count before rendering, which keeps the compact aspect line on the same fail-closed path as the other chart summary surfaces instead of trusting the derived match list blindly.
- Latest progress (2026-05-01): `pleiades-validate::RequestSurfaceSummary` now also exposes a validated summary-line helper, and the compact request-surface report path reuses it so the policy inventory stays fail-closed at the typed helper boundary instead of only through the report wrapper.
- Latest progress (2026-05-01): the lunar equatorial reference evidence now also includes the reference-only published 1992-04-12 apparent geocentric Moon comparison datum, broadening the checked-in equatorial validation slice to 3 samples across 1 body while keeping the current validation envelope within limits.
- Latest progress (2026-05-01): the independent JPL hold-out corpus now also includes a Pluto 2451545.0 sample plus additional Jupiter rows at 2451545.0 and 2500000.0, and now also carries a Moon 2634167.0 sample from the reference snapshot, which broadens the checked-in derivative slice to 31 rows across 10 bodies, preserves the mixed-scale parity split at 16 TT and 15 TDB requests, and exercises the quadratic interpolation tier in the release-facing hold-out summaries.
- Latest progress (2026-05-01): `pleiades-backend::BackendMetadata` now also exposes a validated summary-line helper, and the validation backend-matrix report now reuses it so backend inventory lines fail closed before rendering instead of trusting the raw summary string alone.
- Latest progress (2026-05-01): `pleiades-jpl::JplSnapshotRequestPolicy` and `JplSnapshotBatchErrorTaxonomySummary` now also expose validated summary-line helpers, and the release-facing report paths reuse them so the request-policy and batch-taxonomy provenance lines fail closed at the typed summary boundary instead of validating and rendering in separate steps.
- Latest progress (2026-05-01): `pleiades-core::ReleaseProfileIdentifiers` now also exposes a validated summary-line helper, and the release-profile formatter in `pleiades-validate` reuses it so the compact profile-identifier line fails closed at the typed helper boundary instead of validating and rendering separately.
- Latest progress (2026-05-01): the CLI validation-report-summary smoke coverage now also exact-pins the release-profile identifiers line, which widens the release-profile status hardening beyond the one-screen release summary surface.
- Latest progress (2026-05-01): `pleiades-jpl::ReferenceAsteroidEvidenceSummary` and `ReferenceAsteroidEquatorialEvidenceSummary` now also expose validated summary-line helpers, and the release-facing asteroid evidence formatters reuse them so the exact J2000 asteroid provenance stays fail-closed at the typed summary boundary instead of only at the surrounding evidence validation step.
- Latest progress (2026-05-01): `pleiades-vsop87::Vsop87SourceBodyClassEvidenceSummary` now also exposes a validated summary-line helper, and the source-body-class report wrapper reuses it so the luminary/major-planet evidence lines fail closed at the typed class-summary boundary instead of only through the surrounding list formatter.
- Latest progress (2026-05-01): `pleiades-vsop87::Vsop87SourceSpecification` now also exposes a validated summary-line helper, and the single-spec formatter reuses it so drifted source metadata fails closed before the compact source-specification line renders.
- Latest progress (2026-05-01): `pleiades-compression::ArtifactResidualBodyCoverageSummary` now also exposes a validated plain summary helper, and the packaged residual-body report path reuses it before appending the bundled-body count so the Moon-only residual-correction inventory stays fail closed at the typed coverage boundary.
- Latest progress (2026-05-01): the compact artifact inspection summary now also routes through a validated inspection-report helper, which keeps the bundled-body, residual-body, and benchmark provenance line fail-closed at the typed summary boundary instead of formatting the compact line directly from the raw report.
- Latest progress (2026-05-01): the time-observer policy doc now also names `pleiades-core::ChartSnapshot` as a first-class computed-chart entry point, and the `pleiades-core` crate docs now point at the same policy so the house-observer versus body-observer split stays visible on both the request and summary surfaces.
- Latest progress (2026-04-30): the JPL reference, comparison, and independent hold-out snapshot corpora now also expose plain ecliptic/equatorial `*_requests()` aliases alongside the existing request-corpus helpers, which keeps the checked-in batch-shape naming consistent with the other phase-1 corpus helpers without changing the underlying rows.
- Latest progress (2026-04-30): the aggregate and body comparison summaries now expose validated summary-line helpers, and the comparison-envelope report path reuses them so the compact comparison lines fail closed at the typed summary boundary before the report formatter renders them.
- Latest progress (2026-05-01): the VSOP87 source-audit and generated-binary audit summaries now also expose validated summary-line helpers, and the release-facing reproducibility report paths reuse them directly so the compact VSOP87 provenance lines fail closed at the typed summary boundary instead of validating and rendering separately.
- Latest progress (2026-04-30): `ComparisonEnvelopeSummary` now also exposes a validated summary-line helper and the compact comparison-envelope formatter reuses it directly, which keeps the median-envelope report path fail-closed at the helper boundary instead of validating and rendering in separate steps.
- Latest progress (2026-04-30): `pleiades-compression::ArtifactProfileCoverageSummary` now validates canonical bundled-body ordering as part of its fail-closed summary path, and `pleiades-data::PackagedArtifactProfileSummary` now preserves the checked-in empty/duplicate/body-set diagnostics while still rejecting drifted packaged coverage before the report wrappers reuse it. That keeps the packaged-artifact body inventory deterministic as it flows through the release-facing summary path.
- Latest progress (2026-04-30): `pleiades-backend::EphemerisResult` now exposes a typed validation helper plus a checked summary-line helper, and `pleiades-core::BodyPlacement` / `ChartSnapshot` now validate backend result rows, house-number consistency, and house-snapshot observer requirements before rendering fail-closed snapshot summaries. That keeps malformed placement data out of the chart-report path instead of only relying on the backend's raw result metadata.
- Latest progress (2026-04-30): `pleiades-vsop87::Vsop87SourceBodyEvidenceSummary` now also exposes a validated summary-line helper, and the release-facing source-body evidence formatter reuses it so the compact source-backed provenance line now fails closed at the summary boundary instead of only inside the report wrapper.
- Latest progress (2026-04-30): the JPL snapshot manifest summaries now also route through a shared validated summary-line helper in `pleiades-jpl`, which keeps the comparison/reference/hold-out provenance lines fail-closed without repeating the validate-then-render sequence in each report wrapper.
- Latest progress (2026-05-01): the independent hold-out JPL batch and equatorial parity summaries now also expose validated summary-line helpers, and the release-facing report wrappers reuse them directly so the hold-out provenance lines fail closed at the typed summary boundary instead of validating and rendering in separate steps.
- Latest progress (2026-05-01): the JPL independent hold-out summary now also exposes the same validated summary-line helper, so the release-facing hold-out formatter can fail closed through the typed summary record instead of only validating the surrounding wrapper.
- Latest progress (2026-05-01): the reference snapshot coverage, reference equatorial parity, reference batch parity, and independent hold-out coverage summaries now also expose validated summary-line helpers, and the JPL report wrappers reuse those typed helpers directly so the compact snapshot provenance lines fail closed at the summary boundary instead of validating and rendering separately.
- Latest progress (2026-05-01): `pleiades-elp::LunarTheoryCatalogSummary` and `LunarTheoryCapabilitySummary` now also expose validated summary-line helpers, and the lunar catalog/capability report wrappers reuse them so the compact lunar provenance lines fail closed at the typed summary boundary instead of validating and rendering separately.
- Latest progress (2026-05-01): `pleiades-elp::LunarTheorySourceSummary` now also exposes a validated summary-line helper, and the lunar source-selection report wrapper reuses it directly so the compact provenance line fails closed through the typed summary boundary instead of only through a validate-then-render split.
- Latest progress (2026-04-30): `pleiades-core` now exposes `ChartRequest::observer_summary()` and `ChartSnapshot::observer_summary()` alongside the existing observer policy enum, which keeps the geocentric-vs-house-only posture and rendered observer location co-located as reusable chart-report data instead of only as ad hoc summary text.
- Latest progress (2026-04-30): chart requests and snapshots now keep the house observer separate from an optional topocentric body observer, and the chart façade forwards that body observer into body-position backend requests so the request-shape split stays explicit in both summaries and dispatch. The chart regression suite now also covers the combined house-observer/body-observer case directly, which keeps the geocentric house channel and the optional topocentric body channel from being conflated when both are present in the same chart request.
- Latest progress (2026-04-30): `pleiades-jpl` now also exposes plain `*_equatorial_batch_parity_request_corpus()` compatibility aliases for the reference, comparison, and independent hold-out batch-parity helpers, which keeps the equatorial request-corpus naming aligned with the rest of the JPL request-shape helpers without changing the underlying rows.
- Latest progress (2026-04-30): the packaged frame-treatment line is now exact-pinned across validation, release-summary, and artifact-summary smoke coverage, which keeps the checked-in compressed-artifact ecliptic-versus-derived posture fail-closed instead of only substring-matched in the release-facing report surfaces.
- Latest progress (2026-04-30): `pleiades-validate` now exposes a typed `ReleaseChecklistSummary` helper and the release checklist renderers reuse it for the compact summary line, which keeps the release-gate counts and release-profile identifiers fail-closed at the report boundary instead of only as ad hoc text.
- Latest progress (2026-04-30): the shared request-policy wording now explicitly names that house/body-observer split too, so the compact validation and release policy lines now mirror the chart-layer separation instead of only implying it.
- Latest progress (2026-04-30): the shared `ObserverSummary` render path now also exercises the separate body-observer label in both the doctested example and the regression suite, which keeps the house-observer versus topocentric-body split explicit in the compact API docs as well as the validated summary output.
- Latest progress (2026-04-30): `pleiades-types::ObserverLocation` now has a typed validation helper and `pleiades-houses` reuses it for house-request preflight, which keeps invalid latitude, longitude, and elevation inputs fail-closed at the shared observer-location boundary instead of only inside the house crate's local helper.
- Latest progress (2026-04-30): `pleiades-types::ObserverLocation` now also exposes a checked `validated_summary_line()` helper, which keeps the shared observer text on the same fail-closed path as the latitude/longitude/elevation validator when report code wants a compact one-line rendering.
- Latest progress (2026-04-30): `pleiades-core::ChartRequest::validate_observer_location()` now preflights observer coordinates even when no house system is requested, and `validate_house_observer_policy()` reuses that helper before house-specific checks, which keeps malformed observer data from slipping through the geocentric chart path or the house-only path just because the observer is only being rendered.
- Latest progress (2026-04-30): `pleiades-core::ObserverSummary` now has explicit validation plus a checked summary-rendering helper, which keeps the geocentric-vs-house-only posture and rendered observer location ready for fail-closed report callers instead of only as an unchecked display record.
- Latest progress (2026-04-30): `pleiades-backend::validate_observer_policy()` now validates any supplied `ObserverLocation` before it considers the topocentric support flag, which keeps malformed latitude and longitude inputs fail-closed even if a future backend advertises topocentric observer support.
- Latest progress (2026-04-30): direct backend request preflight now also validates observer coordinates before the routing-family deferral branch runs, which keeps malformed observer locations fail-closed even when a routing backend advertises topocentric support for the selected provider.
- Latest progress (2026-04-30): that same observer-policy helper now has a regression that proves invalid observer locations are rejected even when topocentric support is available, which keeps the fail-closed location check covered on the supported-observer path instead of only on the unsupported one.
- Latest progress (2026-04-30): `ObserverSummary::validate()` now fails closed on house-only summaries without a location and on invalid rendered locations, which gives the observer summary a typed integrity check before future report surfaces reuse it.
- Latest progress (2026-04-30): `ObserverSummary` now also exposes a validated body-location label helper, which keeps the topocentric observer rendering on the same fail-closed path as the house-observer location label instead of only validating the whole summary line.
- Latest progress (2026-04-30): `pleiades-core::CompatibilityProfile` now exposes validated catalog-inventory and house-code-alias summary helpers, and the validation/release report paths now use them for the compact compatibility-profile, release-notes, and release-summary surfaces so those catalog lines fail closed before rendering.
- Latest progress (2026-04-30): `pleiades-core::ChartRequest` and `ChartSnapshot` now also expose validated observer-summary and validated-summary-line helpers, which keeps chart report callers on the same fail-closed observer-location path without reconstructing the observer policy text ad hoc.
- Latest progress (2026-04-30): `pleiades-backend::BackendProvenance` now also exposes a validated summary-line helper, and the comparison/backend-matrix renderers reuse it so backend provenance text now fails closed at the report boundary instead of relying only on a prior validation pass.
- Latest progress (2026-04-30): `pleiades-vsop87` now exposes a validated frame-treatment report helper too, so the J2000 ecliptic/equinox posture line is rendered through the backend crate's fail-closed path instead of only as an unchecked string literal.
- Latest progress (2026-04-30): `pleiades-backend::FrameTreatmentSummary` now exposes a validated summary-line helper, and the JPL, VSOP87, ELP, and packaged-data frame-treatment report paths reuse it so the shared mean-obliquity wording fails closed before the compact report surfaces render.
- Latest progress (2026-04-30): `pleiades-data::PackagedFrameTreatmentSummary` now exposes the same validated summary-line helper, and the packaged-artifact frame-treatment report path now reuses it too so the stored-vs-derived posture stays fail-closed on the compressed-artifact backend as well.
- Latest progress (2026-04-30): `pleiades-vsop87` now also exposes a reusable supported-body canonical batch-matrix request corpus plus compatibility alias, which keeps the four-slice J2000/J1900 ecliptic/equatorial matrix reusable for downstream validation and reproducibility tooling without reconstructing the combined batch shape at each call site.
- Latest progress (2026-04-30): the CLI release-notes-summary surface now also exact-pins the JPL request-policy and batch-error-taxonomy lines, so the compact maintainer-facing notes view keeps the reference-backend policy and failure taxonomy visible alongside the packaged-artifact and compatibility-profile summaries instead of only in the fuller validation reports.
- Latest progress (2026-04-30): the release-notes-summary surface now also surfaces the VSOP87 source-documentation health line, so the compact maintainer-facing notes view now carries the source-backed planetary provenance alongside the JPL policy lines and the compatibility/package status rows.
- Latest progress (2026-04-30): the CLI release-notes and release-summary smoke coverage now derive the packaged-artifact access wording from the shared packaged-artifact helper, so the feature-gated access posture stays aligned across default and `packaged-artifact-path` builds instead of hardcoding the disabled-only wording in the test harness.
- Latest progress (2026-04-30): the release-notes-summary and release-summary smoke coverage now also exact-pins the `Workspace audit summary: workspace-audit-summary` line, and release-summary now exact-pins `Release checklist summary: release-checklist-summary` too, which keeps two more compact release-profile status lines fail-closed instead of only substring-matched.
- Latest progress (2026-04-30): release-bundle metadata validation now requires canonical one-line source revision, workspace status, and rustc version fields before a staged bundle is accepted, so the release archive provenance stays fail-closed instead of allowing padded or multiline metadata through the bundle surface. The same bundle validation now also rejects placeholder provenance values like `unknown`, which keeps the staged release metadata from archiving a fallback toolchain or revision marker as if it were a real release fingerprint.
- Latest progress (2026-04-30): the packaged-artifact profile summary validation now also checks the byte-order policy against the checked-in artifact header, which keeps the compact byte-order line fail-closed instead of only displaying the current little-endian posture.
- Latest progress (2026-04-30): the compact comparison tail envelope is now a public typed summary helper in `pleiades-validate`, and the release-summary smoke coverage exact-pins the full `Comparison tail envelope: ...` line through that helper, which keeps the 95th-percentile error tail aligned with the report formatter instead of only checking it through substring matches.
- Latest progress (2026-04-30): the median comparison envelope is now also public and reusable from `pleiades-validate`, which keeps the compact median wording available to downstream tooling as a typed helper instead of only through the combined comparison summary.
- Latest progress (2026-04-30): the comparison-report smoke coverage now exact-pins the report header, corpus name, corpus description, epoch-label, span, and backend lines, and the request-surface summary test now exact-pins the one-line primary request surface inventory too, which keeps both compact report surfaces fail-closed instead of only checking them with substring or prefix matches.
- Latest progress (2026-04-30): the benchmark provenance block now travels through a validated typed `WorkspaceProvenance` helper, and the benchmark-report smoke coverage now exact-pins the full multi-line provenance block instead of only checking the heading, which keeps the workspace/revision/rustc status line fail-closed in the compact release-facing benchmark output.
- Latest progress (2026-05-01): `pleiades-validate::BenchmarkReport` and `ChartBenchmarkReport` now also expose validated summary-line helpers, so downstream benchmark callers can fail closed at the typed report boundary before reusing the compact backend and chart benchmark lines.
- Latest progress (2026-04-30): the VSOP87 batch-parity validation helpers now also re-check the exact/interpolated/approximate/unknown quality mix against fresh backend requests, which keeps the canonical and supported-body batch summaries fail-closed if only the quality distribution drifts while the total request count still matches.
- Latest progress (2026-04-30): `pleiades-vsop87` now also exposes a plain `supported_body_canonical_request_corpus()` compatibility alias for the combined J2000/J1900 matrix slice, which keeps the reusable supported-body batch matrix discoverable through the same short request-corpus naming pattern as the other release-facing corpora.
- Latest progress (2026-04-30): the VSOP87 source-backed body and body-class evidence summaries now also compare their explicit within-limit and outside-limit counts against the current canonical evidence before report formatting, which keeps the compact source-backed envelope from accepting a stale count split even if the body order and peak provenance still look coherent.
- Latest progress (2026-04-30): the release-summary and validation-summary smoke coverage now exact-pins the time-scale, observer, apparentness, and request-policy lines, which keeps the shared time/observer posture fail-closed in the compact release-facing report surfaces instead of only checking for those headings by substring.
- Latest progress (2026-04-30): the shared time-scale policy wording now also travels through a typed `TimeScalePolicySummary` helper, and the validation/release report paths reuse that typed record directly so the TT/TDB + caller-supplied-conversion posture stays co-located with the backend contract instead of only as a raw string. The matching request-policy summary now also exposes a validated one-line helper, and the report formatters reuse it so the compact policy lines fail closed before rendering instead of trusting a cached string.
- Latest progress (2026-04-30): the VSOP87 canonical J2000 ecliptic and equatorial summaries now also re-derive their full field sets from the current body-evidence slices during validation, which keeps drifted peak bodies, source metadata, and summary statistics from surviving as internally coherent but stale release-facing evidence.
- Latest progress (2026-04-30): the supported-body J2000 and J1900 request surfaces now also expose plain `supported_body_j2000_request_corpus()` and `supported_body_j1900_request_corpus()` compatibility aliases for the ecliptic batch slices, which keeps the astrology-facing Sun-through-Pluto corpora discoverable through the same plain request-corpus naming pattern as the other release-facing helpers.
- Latest progress (2026-04-30): the release-summary smoke coverage now exact-pins the `Release summary` header and the `Backend matrix summary: backend-matrix-summary` line, which keeps two more user-facing status headers fail-closed instead of relying only on substring matches in the compact release view.
- Latest progress (2026-04-30): the validation-report-summary and release-summary smoke coverage now also exact-pin the packaged lookup epoch policy line, which keeps the TT-grid retag posture on the same fail-closed path as the other compact packaged-data status rows instead of leaving it as a substring-only check.
- Latest progress (2026-04-30): the `pleiades-types::TimeScaleConversion` docs now include explicit UT1→TT and signed TDB→TT examples, and the unit tests now preflight the signed TDB→TT validation path too, which keeps the caller-supplied conversion policy visible as both documented API behavior and executable regression coverage.
- Latest progress (2026-04-30): `pleiades-types::TimeScaleConversion` now also exposes a validated summary-line helper, which keeps the caller-supplied conversion policy on the same fail-closed summary path as the other typed time-observer records.
- Latest progress (2026-04-30): the release-notes-summary and release-summary smoke coverage now also exact-pins the packaged mixed TT/TDB batch-parity line, which keeps that release-facing packaged-data status row fail-closed instead of only checking it with a prefix match in the compact summary views.
- Latest progress (2026-04-30): the release-summary smoke coverage now also exact-pins the JPL request-policy and batch-error-taxonomy lines in both the CLI and validation paths, which keeps the compact release overview fail-closed on those backend policy and taxonomy summaries instead of only checking them with substring matches.
- Latest progress (2026-04-30): the validation-report-summary and release-summary smoke coverage now exact-pins the JPL request-policy and batch-error-taxonomy lines as exact report rows too, which keeps those release-facing backend-policy summaries on the same fail-closed path as the other compact report lines instead of only matching them by line predicate.
- Latest progress (2026-04-30): the validation-report-summary, release-checklist-summary, and release-summary smoke coverage now exact-pins the shared mean-obliquity frame round-trip line, which keeps the ecliptic↔equatorial round-trip evidence fail-closed instead of only asserting the shorter prefix.
- Latest progress (2026-04-30): the shared mean-obliquity frame round-trip summary now also reports mean and 95th-percentile deltas in addition to the maxima, and the summary validates against the canonical sample set before release-facing formatting, which broadens the frame-conversion envelope while keeping the compact report line fail-closed.
- Latest progress (2026-04-30): the release-checklist-summary and release-summary smoke coverage now exact-pins the `Validation report summary: validation-report-summary / validation-summary / report-summary` line, which keeps the report-summary heading fail-closed in the compact release-facing checklist and overview paths instead of relying on substring matches.
- Latest progress (2026-04-30): the validation-report-summary, release-notes-summary, release-summary, and CLI report-summary smoke coverage now exact-pins the comparison snapshot coverage line, which keeps the JPL comparison inventory fail-closed across the compact release-facing summary surfaces instead of only checking it with a substring match.
- Latest progress (2026-04-30): `pleiades-compression::CompressedArtifact::validate()` now also rejects non-canonical bundled body ordering before encode/decode, which keeps deterministic artifact layout fail-closed alongside the existing duplicate-body and segment-metadata checks.
- Latest progress (2026-04-30): the CLI chart help text now explicitly spells out that observers are used for house calculations only while geocentric body positions remain the default, which keeps the time/observer separation visible at the command-line surface alongside the existing explicit time-scale flags.
- Latest progress (2026-04-30): `pleiades-vsop87::Vsop87SourceSpecification::validate()` now also fails closed on drifted canonical provenance fields such as the documented frame, units, coordinate family, variant, transform note, and date range, which keeps the source-documentation health line anchored to the current public source catalog instead of only checking for non-blank metadata.
- Latest progress (2026-04-30): the same VSOP87 source-specification validation now also exact-matches reduction and truncation-policy metadata against the current catalog-derived source spec, which tightens the release-facing source-documentation drift guard so the canonical wording cannot degrade into a merely substring-matched description. The regression suite now also pins explicit transform-note and date-range drift cases, so the source-provenance guard now exercises more of the canonical field set directly rather than only the existing frame, reduction, and truncation-policy examples. The same drift guard now also has explicit regression coverage for canonical variant, coordinate-family, and units drift, which keeps the remaining source-metadata fields fail-closed too.
- Latest progress (2026-04-30): the same VSOP87 source-specification validation now also rejects unknown public source files and body/file drift, which keeps the body-to-source catalog pairing fail-closed before reproducibility tooling reaches the release-facing source report.
- Latest progress (2026-04-30): the JPL independent hold-out corpus now also exposes mixed TT/TDB compatibility aliases alongside the existing batch-parity helpers, which keeps the hold-out request shapes discoverable through the same `*_mixed_time_scale_*` naming pattern already used by the other public request corpora.
- Latest progress (2026-04-30): the JPL reference and independent hold-out corpora now also expose ecliptic request-corpus aliases, which keeps those checked-in batch slices discoverable through the same plain `*_ecliptic_request_corpus()` naming pattern as the other release-facing helpers.
- Latest progress (2026-04-30): the JPL reference, comparison, and independent hold-out equatorial parity corpora now also expose `*_equatorial_batch_parity_requests()` compatibility aliases, which keeps the batch-parity naming aligned with the other reusable request-corpus helpers for downstream tooling.
- Latest progress (2026-04-30): the JPL reference, comparison, and independent hold-out corpora now also expose explicit `*_mixed_tt_tdb_*` compatibility aliases for the same mixed-scale batch shapes, which keeps the request-surface discoverability aligned with the VSOP87 naming pattern without changing the underlying request rows.
- Latest progress (2026-04-30): those same JPL mixed-scale corpora now also expose plain `*_request_corpus` aliases for the batch-parity helpers, which keeps the mixed TT/TDB validation slices easier to discover from downstream tooling without changing the request shapes.
- Latest progress (2026-04-30): the JPL reference snapshot now also exposes an equatorial-parity request-corpus alias pair, which keeps the checked-in mean-obliquity batch shape reusable alongside the existing frame-parameterized helper instead of rebuilding the same request list ad hoc in downstream validation tooling.
- Latest progress (2026-04-30): the JPL comparison snapshot now also exposes an equatorial-parity request-corpus alias pair, which keeps the comparison corpus discoverable under the same frame-qualified naming pattern as the reference and hold-out slices.
- Latest progress (2026-04-30): the JPL reference, comparison, and hold-out equatorial batch shapes now also expose plain `*_equatorial_request_corpus()` aliases, which keeps the frame-specific batch slices discoverable through the same shorter helper naming pattern without changing the underlying request rows.
- Latest progress (2026-04-30): the independent JPL hold-out corpus now also includes a J2000 Mars anchor, which expands the checked-in derivative fixture to 17 rows across 5 bodies and 8 epochs while keeping the release-facing provenance and parity summaries aligned with the broader interpolation-evidence slice.
- Latest progress (2026-04-29): the compact request-surface inventory is now a public `pleiades-validate::RequestSurfaceSummary` helper, which keeps the primary entry-point matrix reusable by downstream tooling instead of only embedded in the validation report formatter.
- Latest progress (2026-04-29): `pleiades-validate` now exposes the expected comparison-tolerance catalog as a reusable backend-family/body-class helper, and the validation/report paths reuse that catalog directly so the stored thresholds stay centralized instead of being rebuilt only inside the report formatter.
- Latest progress (2026-04-29): `pleiades-validate::ComparisonReport` now also exposes the typed comparison-audit summary publicly, which keeps the compact compare-backends-audit body/status counts reusable for downstream tooling instead of only inside the report formatter.
- Latest progress (2026-04-29): that same `ComparisonAuditSummary` record now also exposes a public `validate()` helper, which keeps the fail-closed body-count check reusable for downstream tooling before the compact audit line renders.
- Latest progress (2026-04-29): `ComparisonAuditSummary` now also exposes a public `summary_line()` helper and a public API regression test, which keeps the compact compare-backends-audit wording reusable from downstream tooling instead of only through the private report formatter.
- Latest progress (2026-04-30): `ComparisonAuditSummary` now also exposes a validated summary-line helper, and the comparison-audit report path now reuses it so the compact compare-backends-audit line fails closed at the typed summary boundary instead of re-validating and rendering in separate steps.
- Latest progress (2026-04-30): `pleiades-types::Motion` now exposes an explicit validation helper and non-finite longitudinal speeds no longer classify as stationary, while `pleiades-core::BodyPlacement::motion_direction()` now fails closed on malformed motion samples before motion summaries count them, which keeps the speed-classification path aligned with the chart-layer unknown-motion fallback. `pleiades-core::MotionSummary` now also exposes a placement-count validation helper, and the chart snapshot formatter preflights the derived motion inventory before rendering the compact summary line, which keeps the motion audit on the same fail-closed path as the other report summaries. The same motion sample type now also has a compact typed summary/display helper for diagnostics, which keeps the underlying speed tuple ready for future release-facing motion provenance text without rebuilding the fields ad hoc.
- Latest progress (2026-04-30): the validation, release-summary, release-notes, and backend-matrix smoke coverage now exact-pins the full `Primary request surfaces:` line instead of only checking its prefix, which keeps the request-surface inventory fail-closed in the compact release-facing report paths.
- Latest progress (2026-04-30): the VSOP87 canonical J2000, source-backed J2000, J1900, and mixed TT/TDB corpora now also expose plain `*_request_corpus` aliases alongside the existing batch-parity names, which keeps the source-backed batch slices easier to discover from downstream validation and reproducibility tooling without changing the canonical request shapes.
- Latest progress (2026-04-30): the supported-body J2000 and J1900 batch corpora now also expose plain `*_request_corpus` aliases for the equatorial and ecliptic request shapes, which keeps the same discoverability pattern available for the Sun-through-Pluto helper slices too.
- Latest progress (2026-04-30): `pleiades-vsop87` now also exposes a backend-owned supported-body canonical batch matrix summary that rolls the J2000/J1900 ecliptic and equatorial batch slices into one validated release-facing helper, which keeps the all-supported-planets canonical-epoch coverage available as a single audit record instead of four separate batch rows.
- Latest progress (2026-04-30): that same supported-body canonical batch surface now also has a `supported_body_canonical_batch_matrix_request_corpus()` compatibility alias, which keeps the matrix-oriented request-corpus name discoverable alongside the existing parity helper without changing the underlying Sun-through-Pluto request shape.
- Latest progress (2026-04-30): `pleiades-vsop87` now also exposes a `canonical_epoch_request_corpus()` compatibility alias for the canonical J2000 batch slice, which keeps the legacy request-corpus name available alongside the newer canonical batch helper for downstream validation and reproducibility tooling.
- Latest progress (2026-04-30): the canonical J1900 batch-parity slice now also exposes a `canonical_j1900_batch_parity_request_corpus()` compatibility alias, which keeps the J1900 request-corpus naming aligned with the other batch helper families for downstream validation and reproducibility tooling.
- Latest progress (2026-04-30): the canonical mixed TT/TDB batch slice now also exposes explicit `canonical_mixed_tt_tdb_*` request-corpus aliases alongside the existing `canonical_mixed_time_scale_*` names, which keeps the time-scale-specific batch wording discoverable for downstream validation and reproducibility tooling without changing the request shape.
- Latest progress (2026-04-30): the canonical mixed TT/TDB batch slice now also exposes a direct `canonical_mixed_tt_tdb_batch_parity_requests()` compatibility alias, which keeps the mixed-scale corpus discoverable through the same `*_requests` naming pattern already used by the other VSOP87 batch corpora.
- Latest progress (2026-04-29): `pleiades-vsop87` now also exposes the canonical J1900 supported-body batch corpus as a public helper alongside the existing J2000 request slice, which keeps the release-facing equatorial parity evidence reusable for downstream validation and reproducibility tooling without reconstructing the body order from the sample metadata. The canonical mixed TT/TDB batch-parity corpus now also has a request-corpus alias, which keeps the alternating ecliptic slice reusable through the same naming pattern as the other validated corpora.
- Latest progress (2026-04-29): the canonical J2000 batch-parity summary now also has explicit `sample_bodies` and `frame` drift regressions, which keeps the source-backed canonical epoch aligned with the same fail-closed guardrails already exercised on the J1900 summary path.
- Latest progress (2026-04-29): the VSOP87 canonical J2000 request corpus now has an explicit `canonical_j2000_batch_parity_requests()` alias, which keeps the source-backed J2000 batch slice discoverable under the same naming pattern as the summary helper while preserving the older compatibility alias for existing callers.
- Latest progress (2026-04-29): the VSOP87 source-backed J1900 ecliptic batch corpus now also exposes matching `source_backed_body_j1900_ecliptic_batch_parity_requests()` / `..._request_corpus()` aliases, which keeps the supported-body ecliptic slice reusable under the same body-order-oriented naming pattern as the other validated VSOP87 batch helpers.
- Latest progress (2026-04-29): the source-backed J1900 equatorial batch corpus now also has matching `source_backed_body_j1900_equatorial_batch_parity_requests()` / `..._request_corpus()` aliases, which keeps the canonical J1900 equatorial slice discoverable under the same body-order-oriented naming pattern as the other source-backed VSOP87 request helpers.
- Latest progress (2026-04-29): the VSOP87 canonical J1900 batch-parity summary now uses an explicit J1900 TDB reference epoch instead of inferring it from the request corpus, and the summary test suite now pins the reference-epoch drift case directly, which keeps the canonical epoch anchor explicit even if the request constructor changes later.
- Latest progress (2026-04-29): direct backend batch preflight now has an explicit mixed TT/TDB plus topocentric-supported regression, which keeps the request-shape contract visible when a backend actually advertises both time scales and topocentric calculations instead of only through the rejection-path tests. The shared time/observer policy note now also states that supported topocentric batch slices may pass through unchanged, so the policy prose stays aligned with that acceptance case.
- Latest progress (2026-04-29): `pleiades-core::ChartEngine::validate_chart_requests()` now also has a routing-backend regression that accepts mixed TT/TDB requests together with a sidereal, observer-bearing topocentric house request, which keeps the facade-level batch preflight aligned with the routing deferral rule instead of reimposing the combined metadata gates too early.
- Latest progress (2026-04-29): the checked time-scale retagging helpers now have doctested examples at the type, chart-façade, and direct-backend layers, and the shared time/observer policy note now names those checked convenience paths explicitly, which keeps the release-grade preflight story easier to find from the public API docs.
- Latest progress (2026-04-29): the foundation- and chart-level observer/time-scale preflight helpers now also have direct rustdoc examples, which keeps the explicit TT/TDB and house-observer contract easier to discover from the public API docs alongside the policy note.
- Latest progress (2026-04-29): the CLI chart parser now rejects repeated identical time-scale and apparentness tags too, so duplicate `--tt` / `--tdb` / `--utc` / `--ut1` and `--mean` / `--apparent` inputs now fail as explicit duplicate policy choices instead of being silently tolerated.
- Latest progress (2026-04-29): the comparison-tolerance limit rows now also use a typed `ComparisonToleranceEntry::summary_line()`/`Display` helper with validation preflight, which keeps the compact scope-limit wording anchored to the backend-owned tolerance entry record instead of rebuilding it ad hoc in the report formatter.
- Latest progress (2026-04-29): the compact release-notes summary now also surfaces the comparison-tolerance policy line, so the release-facing notes view keeps the current backend-family scope table visible alongside the snapshot and compatibility summaries instead of only in the detailed validation and release-summary paths.
- Latest progress (2026-04-29): the compact release-summary and validation smoke coverage now also pin the exact compatibility catalog inventory line, which keeps the house-system, house-code, ayanamsa, custom-definition, and known-gap counts fail-closed in the most compact release-facing summary views.
- Latest progress (2026-04-29): `pleiades-houses::HouseSystemCodeAlias` now also exposes a typed `validate()` helper that checks normalization and round-trip behavior for one short alias entry, and the table-level alias validator reuses it before duplicate-label checks, which keeps the Swiss-Ephemeris short-code inventory aligned with the descriptor-local validation pattern already used by the built-in house and ayanamsa catalogs.
- Latest progress (2026-04-29): `pleiades-jpl` now exposes a public comparison-snapshot request-corpus helper and `pleiades-validate` reuses it for the default comparison corpus, which keeps the checked-in comparison batch shape reusable without reconstructing the TT-tagged request rows in the validation crate. The comparison corpus now also has a mixed-frame batch-parity helper and release-facing summary line, so downstream tooling can reuse the alternating ecliptic/equatorial validation slice instead of rebuilding that parity pattern from the snapshot metadata. The comparison corpus now also exposes a mixed TT/TDB batch-parity request helper, which keeps the same validation rows reusable by downstream tooling without rebuilding the time-scale alternation in each caller.
- Latest progress (2026-04-29): the independent hold-out batch-parity path now also reuses a public mixed-scale request-corpus helper, which keeps the alternating TT/TDB hold-out slice reusable without rebuilding the request list inline in the summary path. The same hold-out parity summary now also fails closed if the mixed-scale slice ever collapses to a single time scale, which keeps the transparency corpus explicit about the TT/TDB mix instead of only the aggregate row count.
- Latest progress (2026-04-29): the release-notes-summary smoke coverage now also pins the packaged-artifact storage/access/generation lines, which keeps the conservative packaged-data posture visible in the compact release notes view alongside the existing profile, output-support, and regeneration summaries. Latest progress (2026-04-30): the compact residual-body report line now uses the body-count annotated wording in validation, release-notes, and release-summary output, so the Moon-only posture stays explicit in the user-facing reports too.
- Latest progress (2026-04-29): that same release-notes-summary smoke coverage now also pins the packaged request policy, lookup epoch policy, and batch parity lines, which keeps the compact release notes view aligned with the release-summary packaged-data posture instead of leaving those request-shape details visible only in the fuller release summary.
- Latest progress (2026-04-29): the release-notes-summary smoke coverage now also pins the primary request-surface inventory line, which keeps the compact maintainer-facing notes view aligned with the typed entry-point matrix instead of omitting that cross-layer contract from the release notes artifact.
- Latest progress (2026-04-30): the release-notes-summary smoke coverage now also pins the target house and ayanamsa scope lines plus the release-profile identifier pair, which keeps the compact maintainer-facing notes view anchored to the long-term compatibility catalog and release provenance alongside the existing request-surface and packaged-data posture lines.
- Latest progress (2026-04-29): the compact release-summary smoke coverage now also pins the lunar source-selection provenance line, so the current Meeus-style baseline stays explicitly anchored in the user-facing release overview alongside the lunar theory catalog and capability summaries.
- Latest progress (2026-04-29): the CLI release-summary smoke coverage now also pins the lunar high-curvature continuity evidence and artifact inspection lines, which keeps two more compact release-status rows anchored in the one-screen overview instead of only in the deeper validation renderers.
- Latest progress (2026-04-29): `LunarTheoryCatalogEntry` now also exposes a compact typed summary/display helper, and the lunar catalog-selection regression now pins the selected/unselected entry wording too, which gives the one-entry source-selection baseline a reusable entry-level audit line for future lunar variants.
- Latest progress (2026-04-29): `LunarTheorySpecification::validate()` now also covers the lunar baseline's model name plus truncation, unit, date-range, frame, and validation-window fields, and the catalog validation path reuses that check before the existing alias/body round-trip audits, so drift in the descriptive lunar provenance now fails closed alongside the selection and coverage guards.
- Latest progress (2026-04-27): the lunar reference, equatorial reference, apparent-comparison, and high-curvature evidence summaries now render their epoch ranges with explicit time-scale-aware `TimeRange` formatting, and the corresponding regression checks now pin the visible `TT` labels in the report text, so the release-facing lunar provenance lines are a little more explicit about the sampled epoch scale.
- Latest progress (2026-04-29): the backend matrix and release summary now also surface the lunar high-curvature continuity evidence line alongside the existing lunar reference and apparent-comparison summaries, which keeps the nearby-motion regression visible in the compact release-facing inventory instead of only in the lower-level validation surfaces.
- Latest progress (2026-04-29): the lunar high-curvature continuity and equatorial continuity slices now also expose public request-corpus helpers plus compatibility aliases, and now also carry explicit batch-parity alias pairs, so downstream tooling can reuse the fixed Moon regression windows without reconstructing the ecliptic or equatorial request shapes inline.
- Latest progress (2026-04-30): the lunar reference and equatorial reference batch corpora now also expose plain `request_corpus` compatibility aliases, which keeps the canonical Moon reference slices discoverable through the same naming pattern used by the VSOP87 and JPL corpora without changing the underlying batch shape.
- Latest progress (2026-04-27): the shared comparison-audit summary now validates its derived body-count totals before validation, release, and backend-matrix formatting, so the compact `compare-backends-audit` line now fails closed if the audit counts ever drift out of coherence.
- Latest progress (2026-04-28): the comparison-envelope helper records now also validate their median and percentile fields directly before the release-facing percentile line reuses them, which keeps the compact error-envelope posture on the same fail-closed path as the body-count audit summary instead of relying only on the enclosing sample checks.
- Latest progress (2026-04-28): the comparison-envelope validation path now also re-derives and compares its stored median and 95th-percentile envelopes against the sampled comparison slice before release-facing formatting, so stale compact error-envelope metrics now fail closed instead of only proving their internal finiteness.
- Latest progress (2026-04-28): the comparison-envelope formatter now also fails closed if a sample slice mixes present and absent distance deltas, which keeps partially populated distance channels from being averaged into the release-facing error envelope.
- Latest progress (2026-04-29): the validation and release summaries now also include a mean-obliquity frame round-trip envelope over representative ecliptic samples, which gives the shared ecliptic↔equatorial rotation an explicit release-facing error check instead of only the frame-policy wording.
- Latest progress (2026-04-29): that mean-obliquity frame round-trip evidence is now also owned by a typed validation helper with a fail-closed report wrapper and `summary_line()`/`Display` rendering, and the helper is now publicly reusable for downstream tooling, which keeps the compact frame-round-trip wording centralized instead of rebuilding it as ad hoc report text.
- Latest progress (2026-04-30): the canonical mean-obliquity frame round-trip sample corpus is now public too, which lets downstream validation and reproducibility tooling reuse the exact TT-tagged sample instants directly instead of reconstructing the five-point evidence slice from the summary helper.
- Latest progress (2026-04-28): the VSOP87 source-backed body-class evidence summary now also validates the provenance kind/file pair for each class peak against the current body catalog, so the compact luminary and major-planet envelopes now fail closed if the max-axis source metadata ever drifts away from the body that drives it.
- Latest progress (2026-04-28): the VSOP87 canonical ecliptic and equatorial evidence summaries now also validate their internal mean/median/percentile/RMS ordering before release-facing formatting, so the compact planetary error envelopes fail closed if a future field swap ever makes the derived shape incoherent even when all values remain finite.
- Latest progress (2026-04-28): the VSOP87 body-source catalog entries now also validate their provenance and source-kind pairing before the canonical evidence builders reuse them, so a drifted per-body profile now fails closed in the source-backed evidence path instead of only surfacing later in the release report.
- Latest progress (2026-04-28): VSOP87 source-manifest length mismatches now also report the expected and actual body/source pairs inline, which makes a truncated or expanded manifest easier to diagnose from the reproducibility boundary instead of only by count.
- Latest progress (2026-04-27): the packaged-artifact decode benchmark now validates its label, source, round, sample-count, and encoded-size metadata before it is returned to the artifact report path, so the compact packaged-artifact benchmark line now fails closed if its provenance or sizing fields ever drift.
- Latest progress (2026-04-29): that packaged-artifact decode benchmark now also exposes a typed compact summary helper, and the artifact inspection report reuses it so the release-facing decode-cost provenance stays centralized instead of being rebuilt inline. The compact artifact inspection summary now also carries the encoded-byte count directly, which keeps the artifact size visible in the release-facing provenance line instead of only in the detailed benchmark block.
- Latest progress (2026-04-29): the packaged-artifact regeneration check now compares the regenerated bytes directly against the checked-in fixture bytes via a public helper, which keeps the maintainer-facing reproducibility check aligned with the committed binary artifact instead of depending on a decode/re-encode round-trip.
- Latest progress (2026-04-29): the Swiss-Ephemeris house-code alias table exact compact rendering is now pinned in `pleiades-houses` against the full current mapping list, so the release-facing alias table stays order-stable as the compatibility report surfaces it.
- Latest progress (2026-04-28): the backend and chart benchmark constructors now also reject zero-round or zero-sample placeholder runs after preflighting backend metadata, so the benchmark-report path now fails closed instead of formatting an empty workload.
- Latest progress (2026-04-28): the backend and chart benchmark reports now also expose compact typed summary lines, and the detailed report output reuses those summary helpers so the benchmark posture now has a stable one-line inventory in addition to the longer methodology breakdown.
- Latest progress (2026-04-29): the validation report now preflights its aggregate corpus, benchmark, chart, and regression archives before rendering, and the full report/summary paths fail closed if a drifted benchmark corpus or archive would otherwise have produced stale release text.
- Latest progress (2026-04-28): the VSOP87 canonical batch evidence now also includes a mixed TT/TDB parity slice, and the validation/release/backend-matrix summaries now surface it alongside the J2000 and J1900 batch evidence so the planetary time-scale policy has a compact release-facing regression too. The mixed-slice validator is intentionally exact-count driven, so the canonical alternating TT/TDB split is already enforced by the existing request totals rather than a separate degenerate-mix guard.
- Latest progress (2026-04-28): the supported-body J2000 ecliptic batch parity summary now also surfaces the full Sun-through-Pluto supported set in validation, release, and backend-matrix output, which keeps the canonical supported-body batch regression visible alongside the source-backed J2000/J1900 slices.
- Latest progress (2026-04-29): the supported-body J2000 equatorial batch parity summary now also surfaces the full supported planetary set, which keeps the mean-obliquity batch/single parity cross-check visible beside the existing ecliptic canonical-epoch batch regression.
- Latest progress (2026-04-29): the supported-body J1900 equatorial batch parity summary now also surfaces the full supported planetary set, which completes the supported-body canonical-epoch parity matrix in the mean-obliquity frame alongside the existing J2000 equatorial and J1900 ecliptic slices.
- Latest progress (2026-04-29): the VSOP87 supported-body J1900 ecliptic batch-parity slice now also surfaces alongside the existing J2000 and mixed-scale batch checks in validation, release, and backend-matrix output, which keeps the chart-facing canonical epoch coverage visible in both the equatorial and ecliptic batch regressions.
- Latest progress (2026-04-29): the VSOP87 canonical J1900 equatorial batch-parity slice now also has an explicit frame-qualified request-corpus alias, which keeps the equatorial canonical batch shape discoverable under the same naming pattern as the J2000 helper without changing the existing compatibility alias.
- Latest progress (2026-04-29): the VSOP87 supported-body J1900 ecliptic batch-parity slice now also exposes a matching frame-qualified request-corpus alias, which keeps the ecliptic canonical batch shape discoverable through the same helper naming pattern as the J2000 supported-body corpus.
- Latest progress (2026-04-29): the VSOP87 canonical J2000 and supported-body equatorial batch-parity slices now also expose matching `*_request_corpus` aliases, which keeps the source-backed canonical and supported-body batch shapes aligned with the same discoverability pattern as the existing ecliptic helper aliases for downstream tooling.
- Latest progress (2026-04-28): that supported-body batch parity summary now also has a report-wrapper drift regression for the frame field, so the compact VSOP87 batch-parity evidence now fails closed if the release-facing ecliptic batch line ever drifts out of sync with the validated supported-body slice.
- Latest progress (2026-04-28): `pleiades-core::ChartEngine` now also exposes a batch chart-request preflight helper that validates a slice of `ChartRequest` values against backend metadata once and prefixes the first failing request's 1-based index in the returned error message, which gives chart-corpus callers the same fail-closed batch ergonomics as the direct backend request helper.
- Latest progress (2026-04-29): that batch preflight helper now also has a worked rustdoc example covering a mixed TT/TDB Sun-only batch, which keeps the public batch-validation workflow documented alongside the existing single-request chart examples.
- Latest progress (2026-04-29): the chart-facade batch preflight example now also shows the indexed house-observer failure alongside the mixed TT/TDB acceptance case, which keeps the façade-level batch contract visible in the public API docs instead of only in the test surface.
- Latest progress (2026-04-29): chart request and chart snapshot summaries now render the stored observer location separately from the geocentric/house-only policy label, which keeps the house-observer coordinates explicit in diagnostics without implying a topocentric body-position request.
- Latest progress (2026-04-28): the compatibility-profile verification and release-summary paths now reuse the shared descriptor-name summary helper for release-specific house-system and ayanamsa canonical names, which keeps the compact catalog wording on the same typed helper used by validation and clears the stale dead-code warning in `pleiades-validate`. Latest progress (2026-04-30): the core compatibility profile now also owns validated release-specific house-system and ayanamsa canonical-name summary helpers, and the verification/release-note renderers reuse them directly so the compact catalog names fail closed at the profile boundary instead of only in report-local formatting code.
- Latest progress (2026-04-29): the Swiss-Ephemeris house-code alias table now also has a typed `summary_line()`/`Display` mapping helper, and the compatibility-profile verification summary renders and validates the compact `P -> Placidus`-style alias table directly so the short-code source table stays visible in the release-facing catalog audit.
- Latest progress (2026-04-29): the compact compatibility-profile summary, release-notes summary, and release-summary views now also surface the Swiss-Ephemeris house-code alias mapping line from `CompatibilityProfile::house_code_aliases_summary_line()`, so the short code-table inventory is visible in the compact release-facing audit paths instead of only in the verification summary.
- Latest progress (2026-04-29): the release-notes-summary and release-summary smoke coverage now also pin the exact `House code aliases: P -> Placidus, ...` line, which keeps the compact Swiss-Ephemeris code-table wording fail-closed in the release-facing summary tests instead of only exercising the separate count-based assertions.
- Latest progress (2026-04-29): `pleiades-core::CompatibilityProfile` now also exposes a typed house-code alias inventory summary helper with reusable count, rendering, and validation access, which keeps the short Swiss-Ephemeris code table centralized on the core profile record instead of being reconstructed separately at each report call site.
- Latest progress (2026-04-29): the compact compatibility-profile, release-notes summary, and release-summary views now also surface the target house and ayanamsa scope lines, so the long-term Swiss-Ephemeris-class catalog scope stays visible in the compact release-facing audit paths too.
- Latest progress (2026-04-29): those same target-scope summary lines now render without repeating the leading label prefix, and the release-facing smoke coverage pins the exact joined scope lines directly, which keeps the compact catalog scope easier to read while still fail-closing on drift.
- Latest progress (2026-04-29): the packaged-data backend now also exposes a standalone lookup-epoch policy summary helper and the validation/release report surfaces now render it explicitly, which keeps the TT-grid retag posture visible apart from the broader packaged request-policy line while the time-observer policy and stored-vs-derived artifact docs now point at the same helper.
- Latest progress (2026-04-29): the shared frame-treatment summary validation now also rejects embedded line breaks in addition to blank and padded text, so backend-owned provenance notes stay fail-closed as compact one-line release text instead of silently stretching across multiple lines.
- Latest progress (2026-04-29): the release-notes and release-notes-summary smoke coverage now also pins that same house-code alias inventory line directly, which keeps the short code-table audit visible in both the full and compact release-notes views instead of only in the compatibility-profile and backend-matrix reports.
- Latest progress (2026-04-29): those compatibility-profile summary and release-note surfaces now also validate the current compatibility profile before rendering, so a drifted release-facing catalog record now fails closed instead of formatting a stale profile snapshot.
- Latest progress (2026-04-29): that same compatibility-profile path now also fails closed if the Swiss-Ephemeris house-code alias table ever drifts into a duplicate label or a broken round-trip, so the compact code-alias inventory is now guarded as well as displayed.
- Latest progress (2026-04-29): `pleiades-houses` now exposes the short-code check as a reusable typed validation helper, and the compatibility-profile verifier reuses it before the compact alias inventory renders, which keeps the Swiss-Ephemeris code table validated at the source of truth instead of only in the report layer.
- Latest progress (2026-04-29): the backend-matrix summary now also surfaces the Swiss-Ephemeris house-code alias inventory line, so the short code-table audit is visible in the backend inventory view as well as the compatibility-profile and release-summary views.
- Latest progress (2026-04-29): the ayanamsa metadata-coverage posture now also travels through a typed `summary_line()` helper, and the core, validation, and compatibility-profile report paths reuse it so the reference-epoch/offset line remains anchored to the catalog metadata instead of being reconstructed separately in each formatter. `pleiades-ayanamsa::metadata_coverage()` now also has a doctested example, which makes that compact catalog-health line easier to discover from the public API docs too.
- Latest progress (2026-04-29): `AyanamsaMetadataCoverage` now also validates its derived counts and label buckets before rendering, which keeps the compact sidereal-metadata posture fail-closed if the coverage record drifts away from the catalog-derived partition.
- Latest progress (2026-04-29): the body-class tolerance summary now also has a typed validation helper and fail-closed report wrapper, so the coarse comparison-class audit rejects drifted counts or stale distance metrics before it reaches the compact release-facing envelope.
- Latest progress (2026-04-28): the chart batch preflight now also has a mixed TT/TDB acceptance regression for backends that advertise both scales, so the façade-level batch contract stays aligned with the explicit per-request time-scale policy instead of implying a batch-wide normalization step.
- Latest progress (2026-04-28): that batch chart preflight now also has explicit regressions for unsupported time-scale and apparentness failures, so the chart-corpus validation path now exercises the same policy split as the single-request façade instead of only the observer-missing branch.
- Latest progress (2026-04-28): the direct backend batch preflight now also has explicit apparentness and topocentric-observer regressions with indexed failure text, which keeps the lower-level `validate_requests_against_metadata()` path aligned with the chart-corpus batch guardrails instead of only the sidereal branch.
- Latest progress (2026-04-28): the chart-facade batch preflight now also has an explicit topocentric-house regression, so the façade-level `validate_chart_requests()` path now fails closed with the same indexed observer message before any chart assembly begins. The same chart batch preflight now also has a house-aware mixed TT/TDB acceptance regression, which keeps observer-bearing chart requests aligned with the per-request time-scale contract instead of implying a batch-wide retagging step.
- Latest progress (2026-04-28): the backend and chart benchmark report validators now also reject zero estimated corpus heap footprints, so the memory-footprint evidence lines fail closed instead of suggesting an empty benchmark corpus.
- Latest progress (2026-04-28): the packaged-data API docs now also show runnable examples for loading the checked-in artifact from encoded bytes and from a temporary artifact file, which makes the backend-selection and artifact-loading path a little easier to follow from the public crate docs while the release-facing packaging work continues.
- Latest progress (2026-04-28): the compact compare-backends-audit summary now also rejects empty comparison-body sets before it renders, which keeps the release-facing audit gate fail-closed if a future report ever drifts into an empty body inventory instead of a validated comparison corpus.
- Latest progress (2026-04-29): the VSOP87 crate docs now spell out the current source-documentation fields and include a doctested catalog example, which makes the current variant/frame/units/truncation/date-range posture easier to audit from the public API docs.
- Latest progress (2026-04-29): the VSOP87 source-documentation report now routes through a testable formatter helper and has a drift regression for stale catalog counts, which keeps the compact provenance line fail-closed when the cached summary stops matching the current source catalog.
- Latest progress (2026-04-29): the compatibility-profile display and compatibility-profile command now also pin the long-term Swiss-Ephemeris-class target house and ayanamsa scope lines, so the end-state catalog goal stays visible in the release-facing profile surface instead of living only in the validation helper.
- Latest progress (2026-04-28): artifact profile validation now also requires derived coordinate outputs to be backed by Longitude, Latitude, and DistanceAu stored channels, which keeps the packaged-artifact stored-vs-derived contract aligned with the actual lookup path before future generated artifacts advertise reconstructed coordinates.
- Latest progress (2026-04-28): the lunar reference and equatorial evidence summaries now also validate against the checked-in reference slices before their release-facing wrappers render, so stale lunar-reference summaries fail closed instead of drifting into a cached one-liner.
- Latest progress (2026-04-29): the individual lunar reference, equatorial reference, and apparent-comparison evidence rows now also validate their own TT epochs, finite coordinates, and canonical notes before the summary constructors reuse them, which keeps drifted lunar sample metadata from slipping past the release-facing evidence summaries even if the aggregate counts still look coherent. The same row-level lunar validators now also pin the supported body sets explicitly, so a future Moon/node/apogee/perigee body drift now fails closed before the compact report lines render.
- Latest progress (2026-04-29): the lunar equatorial reference slice now also has a dedicated batch-parity corpus plus batch-parity report line, which keeps the mean-obliquity lunar companion evidence visible through the batch path alongside the existing mixed TT/TDB lunar reference parity slice. The lunar reference and equatorial batch corpora now also expose explicit `*_batch_parity_request_corpus` compatibility aliases, which keeps the two canonical lunar batch shapes discoverable through the same corpus naming pattern used by the VSOP87 and JPL batch helpers.
- Latest progress (2026-04-28): the lunar reference and equatorial error envelopes now also validate their computed metric fields and report wrappers before release-facing formatting, so stale lunar-envelope summaries fail closed instead of emitting partial or drifted error text.
- Latest progress (2026-04-28): polynomial channel validation now also rejects non-finite coefficients before compression encoding or segment preflight runs, so artifact generators fail closed if a stored or residual coefficient drifts to NaN or infinity instead of quietly carrying an invalid polynomial channel.
- Latest progress (2026-04-28): `pleiades-data::PackagedArtifactStorageSummary` now also has a typed validation helper plus a fail-closed report wrapper, which keeps the packaged storage/reconstruction posture line on the same canonical-string guardrail as the packaged frame-treatment summary while the current packaged backend remains fixture-backed.
- Latest progress (2026-04-28): the packaged-artifact profile views now also surface a typed output-support line for the built-in coordinate and motion outputs, which makes the stored/derived/unsupported breakdown a little more explicit in the validation and release summary paths without changing the bundled artifact format. The packaged-data wrapper now formats that release-facing line from a dedicated prefix-free helper instead of trimming the profile summary string, and the core compression lookups now also require the artifact profile to advertise the requested derived output before serving `lookup_ecliptic()` or `lookup_equatorial()`, so unadvertised derived-output access now fails closed instead of silently reconstructing a coordinate family that the profile did not claim.
- Latest progress (2026-04-28): the release-summary smoke coverage now also pins the packaged-data request-policy, frame-treatment, and artifact-boundary-envelope lines, so the compact release-facing packaged-artifact posture stays anchored in the one-screen summary as well as the artifact report.
- Latest progress (2026-04-28): the release-notes-summary and release-summary views now also surface a compact house-formula-family inventory line, so the current Equal / Whole Sign / Quadrant / Equatorial-projection / Great-circle / Solar-arc / Sector implementation posture is visible in the release-facing compact summaries alongside the latitude-sensitive house list.
- Latest progress (2026-04-28): that house-formula-family inventory now derives from a shared `pleiades-core::CompatibilityProfile::house_formula_family_names()` helper, which keeps the validation and release report formatting on one canonical family list instead of rebuilding the same deduplicated labels separately in each formatter.
- Latest progress (2026-04-28): the compact workspace-audit summary now also validates its aggregated rule counts and clean/dirty posture before rendering, so the pure-Rust release gate now fails closed if the summarized native-hook data ever drifts from the detailed audit report.
- Latest progress (2026-04-28): the compatibility-profile guard now also rejects duplicate canonical names or aliases across the published house-system and ayanamsa inventories, while still allowing the intentional case-only Vehlow spelling variant, so the release-facing catalog summary now fails closed if the visible descriptor inventory drifts out of sync.
- Latest progress (2026-04-28): the house and ayanamsa catalog validation summaries now also spell out the latitude-sensitive house-system labels and the custom-definition-only ayanamsa labels directly, which makes the remaining compatibility-catalog known-gap inventory easier to scan from the compact release-facing profile path without widening the backend API.
- Latest progress (2026-04-28): that same alias-collision coverage now also exercises cross-entry canonical/alias overlaps for both house-system and ayanamsa catalogs, so the duplicate-label guard stays pinned on the inter-entry path as well as the existing within-entry collision checks.
- Latest progress (2026-04-28): the compatibility-profile smoke coverage now also pins the full Vehlow alias cluster, including the visible `Vehlow equal`/`Vehlow Equal` pair, so the release-facing house-system audit keeps that intentional case-only spelling variant anchored in both the validation and CLI surfaces.
- Latest progress (2026-04-28): the compatibility-profile verification smoke coverage now also pins the exact `Ayanamsa reference metadata verified:` count line and the custom-definition label count line in both the CLI and validation paths, which makes the release-facing profile summary a little less prone to silent count drift in the compact verification output.
- Latest progress (2026-04-28): the validation and CLI compatibility-profile smoke coverage now also pins the lower-case `Vehlow equal` spelling directly, which keeps the case-only alias pair exercised on both user-facing surfaces instead of only in the backend catalog tests.
- Latest progress (2026-04-28): the compatibility-profile validation now also treats the current lowercase galactic-reference spellings as intentional case-only variants too — including `Galactic center`, `Galactic equator`, and the corresponding `Galactic center Cochrane` / `Galactic equator true` aliases — so the built-in alias guard stays aligned with the current catalog reality instead of only the Vehlow family.
- Latest progress (2026-04-27): the time-observer policy doc now includes a layer-by-layer matrix for `pleiades-types::Instant`, `pleiades-core::ChartRequest`, `pleiades-backend::EphemerisRequest`, and `pleiades-houses::HouseRequest`, which makes the caller-supplied conversion boundary versus library-retagging surface easier to audit in one place.
- Latest progress (2026-04-28): that same policy note now also includes a short primary-entry-point matrix for the main chart/backend/house/CLI surfaces, and the backend crate docs now point at the same note so the explicit time-scale and observer contract stays easy to find from the public API docs.
- Latest progress (2026-04-28): validation, release, and backend-matrix summaries now also surface a compact `Primary request surfaces:` line that mirrors the matrix, so the entry-point split stays visible in report output as well as the policy note.
- Latest progress (2026-04-28): that primary-request-surface record now also implements `Display`, and the compact report wrapper reuses the typed summary directly instead of formatting the report string from the cached fields by hand, which keeps the entry-point matrix co-located with the report-facing validation helper.
- Latest progress (2026-04-29): the CLI chart parser now also rejects repeated time-scale offset flags and alias collisions up front, so duplicate TT, TDB-TT, or TT-TDB conversion inputs fail explicitly instead of relying on last-write-wins parsing when callers exercise the explicit time-scale policy from the command line. The same CLI guardrail now also has regression coverage for alias collisions regardless of which TDB-TT alias appears first, which keeps the explicit conversion-policy parsing order-independent as well as fail-closed.
- Latest progress (2026-04-29): `TimeScaleConversion::summary_line()` now also has a signed-offset regression in `pleiades-types`, which keeps the compact release-facing time-scale policy wording anchored for explicit negative TT/TDB-style policies as well as the positive caller-supplied examples.
- Latest progress (2026-04-28): the comparison snapshot source summary report now also preflights the parsed manifest before formatting, so malformed comparison provenance headers now fail closed instead of reaching the release-facing source line.
- Latest progress (2026-04-28): the JPL source-summary validation helpers now also reject surrounding whitespace in cached source, coverage, frame-treatment, and column labels, which keeps the comparison/reference/hold-out provenance records on the same fail-closed normalization path as the parsed manifests themselves.
- Latest progress (2026-04-28): the JPL reference snapshot coverage summary now also revalidates its derived epoch and asteroid-row counts against the checked-in fixture, and the equatorial parity path now reuses the same derived asteroid-row check before it formats release-facing text, so drifted coverage metadata now fails closed with a field-specific error instead of a generic summary mismatch.
- Latest progress (2026-04-28): the chart-facade request-validation path now also has explicit regressions for mean-only backend rejection of apparent requests and for house requests that omit the required observer, so the façade now fails closed before backend dispatch on both sides of the current time/observer policy split.
- Latest progress (2026-04-28): the comparison-tolerance policy summary now also rejects bodies that appear in more than one scope row before it renders, which keeps the compact tolerance catalog partition fail-closed if future backend-class coverage ever drifts into overlapping body sets.
- Latest progress (2026-04-28): the time-observer policy doc now spells out the current request-scale contract as a layer-by-layer matrix, so the typed `Instant`/`ChartRequest`/`EphemerisRequest` retagging helpers and the TT/TDB-only backend posture stay visible in one place instead of being inferred from the individual API docs.
- Latest progress (2026-04-28): `pleiades-backend::validate_requests_against_metadata()` now has a doctested batch-preflight example and the policy note now names the batch helper explicitly too, so direct backend callers can see the single-request and batch-request validation surface together instead of only through the lower-level helper docs.
- Latest progress (2026-04-28): the backend module docs and time-observer policy note now also spell out the batch preflight surface in the direct-backend contract, which keeps the explicit time-scale and observer guidance aligned for both single-request and slice-based callers. The doctested batch example now also shows the indexed observer-failure path alongside the batch-support preflight, and now includes a mixed TT/TDB acceptance slice for callers that explicitly advertise both scales, so the direct-backend contract is a little easier to audit from the public API docs.
- Latest progress (2026-04-28): the batch request-policy preflight now also preserves tropical-only sidereal failures with the same batch index prefix, so slice-shaped direct-backend callers still see the explicit tropical-only guardrail instead of a normalized batch wrapper error.
- Latest progress (2026-04-28): the VSOP87 and ELP backends now also have explicit batch-path regressions for unsupported UTC time-scale requests, which keeps the TT/TDB-only posture pinned on the slice-shaped request path instead of only in single-request checks.
- Latest progress (2026-04-28): routing backends now defer request-shape and batch gating to the selected provider while still preflighting body coverage, which keeps composite provider swaps from being blocked by a coarse aggregate capability summary before the routed backend has a chance to accept the request.
- Latest progress (2026-04-29): the routed batch metadata preflight now also has a mixed TT/TDB observer/apparentness regression, so `validate_requests_against_metadata()` on a routing provider stays aligned with the selected-provider deferral already exercised by the single-request path while the batch slice preserves the request-specific time-scale labels.
- Latest progress (2026-04-29): the routing backend batch path now also preserves mixed TT/TDB requests when the first provider rejects a topocentric observer and the request falls back to a later provider, which keeps the routed batch positions path aligned with the same request-shape policy now exercised by the direct backend and chart-facade batch preflights.
- Latest progress (2026-04-28): the routing batch-path regressions now also preserve mixed TT/TDB request scales while falling back from a retryable primary Sun provider to secondary Sun and Moon providers, which keeps the composite batch path aligned with the direct-backend mixed-scale contract instead of only the single-request fallback path.
- Latest progress (2026-04-28): `pleiades-validate::BodyToleranceSummary::validate()` now rejects zero-sample summaries and non-finite tolerance metrics before the compact body-tolerance report formatter reuses them, so the release-facing per-body tolerance rows now fail closed instead of carrying an invalid comparison envelope.
- Latest progress (2026-04-28): the release-facing API stability posture now validates its profile identifier, summary, and release-facing section lists before the compact API-stability and release-summary renderers reuse it, so whitespace drift or an empty surface section now fails closed instead of formatting stale posture text.
- Latest progress (2026-04-28): body tolerance summaries in `pleiades-validate` now also reject zero-sample and non-finite metric drift before the compact comparison/tolerance sections render, so the expected-tolerance status rows fail closed if a derived body-level envelope ever loses its measured samples or picks up NaN/∞ values.
- Latest progress (2026-04-28): the shared release-profile identifier pair now also fails closed if the compatibility and API-stability identifiers ever collapse to the same value, which keeps the release-bundle identity pair distinct in addition to matching the current profile accessors.
- Latest progress (2026-04-28): the shared release-profile identifier validator now also rejects blank or whitespace-padded identifier literals before the release-facing summaries reuse them, which keeps the release-bundle provenance pair fail-closed if either identifier ever drifts into an empty or padded form.
- Latest progress (2026-04-28): the compact compatibility-profile, backend-matrix, API-stability, validation, release-notes, release-checklist, and release-summary renderers now route the release-profile identifier pair through a validated helper before formatting, so those release-facing summaries fail closed if the paired identifiers ever drift or collapse to the same value.
- Latest progress (2026-04-28): the VSOP87 canonical J2000 evidence summaries now also fail closed when any of their compact error-envelope metrics go non-finite or negative, which keeps the release-facing planetary envelope aligned with the derived evidence instead of only with the body/source provenance.
- Latest progress (2026-04-28): the VSOP87 per-body source profile records now also expose a typed summary/display helper, and the validation backend-matrix report now reuses it directly, so the Sun-through-Neptune provenance lines stay owned by the VSOP87 crate instead of being hand-formatted only in the reporting layer.
- Latest progress (2026-04-27): `pleiades-houses::HouseRequest` now also exposes a `validate()` preflight for observer latitude, obliquity overrides, and topocentric elevation, and `calculate_houses()` reuses that shared request check so the house-observer contract stays centralized before the quadrant formulas run.
- Latest progress (2026-04-28): that house-observer preflight now also rejects non-finite observer longitudes, and the chart façade reuses the house-layer validation before house derivation, so malformed observer locations fail fast with the same structured observer error path instead of reaching the quadrant formulas.
- Latest progress (2026-04-27): `pleiades-types` now exposes typed validation helpers for custom body, house-system, and ayanamsa definitions, including whitespace, duplicate, separator, finite-value, and paired-field checks, so user-defined catalog entries fail closed before they can leak into release-facing display text.
- Latest progress (2026-04-28): the chart façade now also checks custom house-system and ayanamsa labels against the built-in compatibility catalogs before request dispatch, so user-defined names that collide with shipped labels fail closed instead of drifting into ambiguous profile text.
- Latest progress (2026-04-28): `pleiades-vsop87` now exposes a public source-manifest helper for the generated-binary body/source pairings, and the regeneration helper threads that manifest through its write/check diagnostics, which makes a missing or mismatched checked-in blob easier to audit without changing the pure-Rust reproducibility path. The library regression suite now also pins that source-manifest ordering against the source-specification catalog and checked-in blobs directly, so the reproducibility path now fails closed if the manifest ever drifts from the release-order body/file pairing. The same manifest now also has a compact typed summary/display helper, and the regeneration CLI prints that release-order body/source summary before write/check work begins so maintainers can audit the checked-in pairing at a glance.
- Latest progress (2026-04-28): that same regeneration path now also validates the manifest against the current source catalog before write/check modes run, so body/file drift now fails closed at the CLI boundary instead of only after the regenerated bytes are compared.
- Latest progress (2026-04-28): the workspace now passes `cargo clippy --workspace --all-targets --all-features -- -D warnings` and `RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps --all-features` after fixing broken intra-doc links in `pleiades-backend` and `pleiades-core`, so the release-doc gate is green again alongside the existing test and lint coverage.
- Latest progress (2026-04-27): those typed custom-definition validators now also run in the chart and backend request preflights, so malformed custom bodies, house systems, and ayanamsas are rejected before request dispatch instead of only being discoverable in downstream catalog/report surfaces.
- Latest progress (2026-04-28): the shared compatibility-profile validation now also rejects custom-definition labels that resolve to built-in house systems or built-in ayanamsas, while still preserving the intentional Babylonian house-family homographs, so the release-facing custom-definition bucket now fails closed at the profile layer as well as in the validation commands.
- Latest progress (2026-04-27): `pleiades-types::TimeRange::validate()` now centralizes finite-bound, matching-scale, and ordering checks for reusable nominal ranges, `pleiades-backend::BackendMetadata::validate()` now reuses it for backend nominal-range metadata, and the validation comparison-tolerance summary now reuses the same shared range check while also rejecting duplicate coordinate-frame entries before formatting, so the shared time-range guardrail lives at the foundation layer instead of only inside backend-specific code.
- Latest progress (2026-04-27): custom house-system definitions now also reject aliases that case-insensitively collide with each other or with the canonical name, which keeps user-defined house labels from silently drifting into ambiguous lookup territory before they reach the compatibility profile.
- Latest progress (2026-04-30): the independent hold-out corpus now also includes Uranus and Neptune samples at 2451545 and 2500000, broadening the public-input derivative slice to 17 rows across 5 bodies while keeping the same 8-epoch span and mixed-scale batch posture intact.
- Latest progress (2026-04-30): the independent hold-out batch-parity helper and summary tests now reflect that 24-row corpus directly, with the mixed-scale request slice balanced at 12 TT and 12 TDB requests so the release-facing parity posture stays aligned with the expanded fixture.
- Latest progress (2026-04-27): the comparison-tolerance policy summary now also validates its scope rows, per-row body counts, per-row tolerance limits, and aggregate coverage totals before the compact report formatter renders it, so the release-facing tolerance policy line now fails closed if the derived policy inventory ever drifts.
- Latest progress (2026-04-28): the compact validation comparison corpus summary and expected-tolerance rows now also validate their derived epoch ordering, boundary labels, and margin/status coherence before the report formatter reuses them, so the release-facing corpus/tolerance lines now fail closed if a future summary drifts away from the measured comparison data.
- Latest progress (2026-04-27): that same comparison-tolerance policy summary now also validates the comparison-window ordering plus duplicate coordinate-frame coverage before it is rendered, so the compact tolerance policy line now fails closed if the evidence window drifts or if the candidate frame list becomes incoherent.
- Latest progress (2026-04-27): the shared backend frame-treatment summaries now also validate their compact posture text before the validation and release report wrappers render them, and the packaged-data frame-treatment summary now carries the same blank-line guard, so the VSOP87, JPL, lunar, and packaged frame-treatment lines now fail closed instead of emitting stale or whitespace-only provenance text.
- Latest progress (2026-04-28): those same backend-owned and packaged-data frame-treatment summaries now also reject surrounding whitespace, so the release-facing posture lines fail closed if a future literal gets padded or otherwise drifts off the canonical one-line form.
- Latest progress (2026-04-28): compatibility-profile verification now also compares the canonical house-system and ayanamsa descriptor names across the total catalog and the baseline/release partitions, so descriptor-count parity can no longer hide a partition that quietly drops or renames one of the published built-ins.
- Latest progress (2026-04-28): that same compatibility-profile audit now also fails closed on cross-catalog case-insensitive label collisions between the built-in house-system and ayanamsa inventories, which keeps the published compatibility profile disjoint even if a future release batch accidentally reuses the same visible label across categories.
- Latest progress (2026-04-28): that same compatibility-profile audit now also fails closed on cross-entry case-insensitive label collisions in the built-in house and ayanamsa catalogs, and the current release profile trims a couple of redundant aliases so the stricter validation remains green while the existing same-entry alias spellings still resolve through the catalog helpers.
- Latest progress (2026-04-28): the CLI and validation release-profile smoke coverage now also pins the `Whole-sign` and `Equal Midheaven house system` spellings in the compatibility-profile and release-notes surfaces, so a couple of still-visible house-alias forms remain explicitly exercised on the user-facing audit path.
- Latest progress (2026-04-28): that same release-profile smoke pass now also pins the `Galactic Center (Gil Brand)` / `Gil Brand` alias pair, keeping another currently visible galactic-reference spelling batch anchored in the CLI and validation audit paths.
- Latest progress (2026-04-28): the CLI compatibility-profile smoke coverage now also pins the `Galactic Equator mid-Mula` / `Mula galactic equator` alias cluster, which keeps the remaining visible Galactic Equator (Mula) wording anchored in the release-facing audit path alongside the other galactic-reference spellings.
- Latest progress (2026-04-28): the CLI release-summary smoke coverage now also pins the house-formula-family inventory line directly, so the compact one-screen release overview keeps the Equal / Equatorial projection / Great-circle / Quadrant / Sector / Solar arc / Whole Sign posture anchored alongside the house-system inventory.
- Latest progress (2026-04-28): that same release-summary smoke coverage now also pins the release-profile identifier line and the API-stability summary line, which keeps the compact one-screen release overview anchored to the current release bundle ids and posture headline instead of only the catalog counts.
- Latest progress (2026-04-28): the release-notes-summary and release-summary smoke coverage now also pin the `Workspace audit summary: workspace-audit-summary` line, so the compact release-facing audit path keeps the pure-Rust workspace gate visible alongside the other bundled status summaries.
- Latest progress (2026-04-27): `pleiades-cli` now routes custom body and custom ayanamsa parsing through the shared typed validation helpers, so padded or otherwise malformed user-defined catalog entries fail closed at the CLI boundary before they can reach chart assembly or release-facing output.
- Latest progress (2026-04-27): `pleiades-backend::EphemerisResult` now exposes a compact typed `summary_line()`/`Display` helper that includes the available ecliptic, equatorial, motion, and quality channels, so backend result diagnostics can reuse one stable record instead of reconstructing the same provenance shape ad hoc.
- Latest progress (2026-04-27): the VSOP87 regeneration helper now also has a `--check` mode that regenerates the checked-in binary blobs in memory and compares them against the committed artifacts without writing to disk, which makes the reproducibility workflow a little easier to script alongside the existing rewrite path.
- Latest progress (2026-04-28): the VSOP87 generated-blob audit records now validate each body/source-file pairing directly before the release-facing summary renders, so the reproducibility line now fails closed if a blob ever points at the wrong public input file even when the aggregate counts still look coherent.
- Latest progress (2026-04-28): the generated-blob audit report path now also preflights the checked-in blob lookup itself before building its compact summary, which means a missing checked-in binary now produces an unavailable reproducibility line instead of reaching the report formatter through a panic-prone lookup path.
- Latest progress (2026-04-28): the canonical J2000 and J1900 VSOP87 batch-parity report wrappers now surface the validation error in their unavailable text and have regression coverage for drifted summaries, so the release-facing batch evidence now fails closed with a field-specific message instead of a generic unavailable placeholder.
- Latest progress (2026-04-28): the VSOP87 source-specification validator now also trims duplicate file labels before reporting them, and the regression suite now covers a whitespace-padded duplicate source file plus a blank `date_range` field, which keeps the release-facing provenance checks a little more explicit about canonicalized file labels and the catalog's date-scope metadata.
- Latest progress (2026-04-27): the JPL reference and independent hold-out provenance summaries now also validate their own blank-field labels before formatting release-facing source lines, so the compact snapshot provenance now fails closed if future fallback text ever drifts to an empty source, coverage, or column label.
- Latest progress (2026-04-28): the JPL interpolation-quality summary now also checks its derived evidence record during validation, and the independent hold-out summary now mirrors that fail-closed drift guard too, so both transparency envelopes now reject stale summary fields before the release-facing wrappers can reuse them.
- Latest progress (2026-04-27): the typed `TimeScaleConversion` summary now renders explicit `source=...; target=...; offset_seconds=...` field labels, and the related docs plus `pleiades-types` / `pleiades-core` regression checks were updated to match that structured conversion wording, so the caller-supplied Delta T contract is a little easier to scan in diagnostics.
- Latest progress (2026-04-27): the house lookup docs now include doctested wraparound and exact-boundary examples for the shared cusp helper plus a chart-facing `house_for_body` example, so the release-facing API documentation for house workflows is a little less abstract.
- Latest progress (2026-04-27): `pleiades-core::ChartEngine::chart()` now batches body-position dispatch through the backend `positions()` path after metadata validation, so batch-aware backends can service whole charts efficiently without changing the chart request shape or the geocentric-vs-house observer split.
- Latest progress (2026-04-27): the compatibility-profile partition audit now also has a regression that rejects case-normalized alias overlaps between the baseline and release slices, so release-catalog drift now fails closed even when a conflicting label only differs by casing in a visible alias.
- Latest progress (2026-04-27): the compatibility-profile verification summary now also has a regression that reverses the latitude-sensitive house-system list and confirms the stale summary fails closed, so the order-sensitive cached release profile line stays covered alongside the broader compatibility-profile summary fields.
- Latest progress (2026-04-27): `pleiades-vsop87::Vsop87SourceSpecification` now exposes a reusable `validate()` helper and the release-facing source-specification report preflights it, so blank source metadata now fails closed before the compact source-specification catalog is rendered.
- Latest progress (2026-04-30): that same VSOP87 source-specification validator now also checks the canonical variant, coordinate-family, frame, units, transform-note, and date-range fields against the catalog values, which keeps the compact provenance line fail-closed if the documented source metadata drifts away from the generated-table catalog.
- Latest progress (2026-04-28): the canonical VSOP87 source-backed evidence summaries now also preflight their peak source-kind and source-file provenance against the current catalog before the release-facing report wrappers render them, so a stale peak provenance label now fails closed instead of silently reusing an old source mapping.
- Latest progress (2026-04-27): the VSOP87 source-specification catalog validation now also rejects duplicate public source-file labels before report formatting, so the reproducibility line fails closed if two catalog entries ever point at the same public input file.
- Latest progress (2026-04-27): the validation report's comparison envelope now also preflights the aggregate summary plus the sampled deltas before rendering, so empty or non-finite comparison slices fail closed instead of formatting a stale envelope line.
- Latest progress (2026-04-27): backend capability metadata now fails closed when it advertises neither geocentric nor topocentric positions, or neither mean nor apparent values, and `BackendMetadata::validate()` now reuses that guard so malformed capability combinations cannot reach release-facing inventory reports.
- Latest progress (2026-04-27): body-specific comparison summaries now validate their finite, zero-sample, and per-body extrema invariants before the validation report renders them, so a malformed per-body envelope now fails closed instead of emitting stale body-level delta text.
- Latest progress (2026-04-28): the JPL interpolation-quality, reference snapshot, and independent hold-out summaries now also validate against the derived checked-in fixture snapshots, and the reference batch parity summary now fails closed on derived coverage drift too, so the release-facing transparency evidence now rejects cached-metric, row-count, and coverage-order drift before the compact report wrappers reuse stale snapshot state.
- Latest progress (2026-04-29): the JPL interpolation-quality distinct-body coverage summary now also validates against the derived checked-in sample set before the compact report wrapper reuses it, so stale cubic/quadratic/linear body counts now fail closed alongside the rest of the hold-out transparency evidence.
- Latest progress (2026-04-28): the parsed JPL snapshot manifests now also reject surrounding whitespace in title, source, and coverage provenance fields, which keeps the checked-in fixture metadata on the same fail-closed normalization path as the existing blank-field and duplicate-column checks.
- Latest progress (2026-04-28): the shared backend batch adapter now also has a regression that preserves mixed TT/TDB request scales through the default `positions()` path, so the batch contract now explicitly guards against silently normalizing a mixed dynamical-time slice into a batch-wide scale.
- Latest progress (2026-04-28): the compact `Primary request surfaces:` report line now validates its typed labels before formatting and has a drift regression for blank request-surface entries, so the release-facing entry-point matrix fails closed if one of the currently documented chart/backend/house/CLI surfaces ever regresses. The validation, release-summary, and backend-matrix smoke tests now also pin the full five-entry surface string explicitly, which keeps the compact request-surface posture anchored to the current typed labels instead of only the leading entry.
- Latest progress (2026-04-28): the canonical VSOP87 evidence summary now also re-derives its out-of-limit count from the current body-evidence slice before the compact report formatter reuses it, so the release-facing envelope now fails closed if that count ever drifts out of sync with the measured sample evidence.
- Latest progress (2026-04-28): the VSOP87 supported-body batch regression now also covers the full planetary set at the canonical J2000 epoch in equatorial TDB mode, which keeps the J2000 source-backed batch/single parity visible alongside the existing J1900 supported-body coverage.
- Latest progress (2026-04-28): that same supported-body batch slice now also exercises the canonical J2000 planetary set in the astrology-facing ecliptic frame, so the source-backed batch path is now pinned in both the equatorial release-report frame and the geocentric ecliptic frame used by the chart-facing API.

## Current repo state summary

Completed foundations:

- per-body comparison summaries, body-class error envelopes, and expected tolerance status sections in validation reports, including class-level median offsets alongside the existing maxima, means, and RMS figures, so measured backend deltas are visible by body and grouped by class against explicit interim evidence thresholds; the per-body comparison summary record now also has a backend-owned `summary_line()`/`Display` helper that the validation formatter reuses directly instead of rebuilding the same compact wording inline;
- Latest progress (2026-04-27): `pleiades-types` now exposes a typed `TimeScaleConversion` policy record plus generic instant/chart helpers, so caller-supplied Delta T and TDB offset rules can travel as one explicit conversion contract instead of only as per-source helper arguments.
- Latest progress (2026-04-27): `pleiades-core` now re-exports that same `TimeScaleConversion` policy from the façade, so applications can stay on the main crate while still constructing and validating the explicit time-scale conversion contract described in the policy docs.
- Latest progress (2026-04-27): that same `TimeScaleConversion` policy now also has a preflight `validate(instant)` helper, so callers can check the source-scale match and finite-offset rule before they mutate a chart instant or hand the policy off to the façade.
- Latest progress (2026-04-27): `pleiades-core::ChartRequest` now also exposes a matching `validate_time_scale_conversion()` preflight, so the façade can check the same explicit source/target/offset contract against the request instant before applying a chart-level time-scale retagging helper.
- Latest progress (2026-04-27): `pleiades-types::Instant::validate_time_scale_conversion()` now mirrors the same source/target/offset preflight at the foundation layer, so callers can validate the typed conversion policy directly from the instant before they mutate it or hand it up to the façade.
- Latest progress (2026-04-27): the time-scale policy docs and backend-request docs now also call out `pleiades-backend::EphemerisRequest::validate_time_scale_conversion()` alongside the façade and foundation helpers, so the explicit caller-supplied conversion contract is documented across the three request layers instead of only at the instant level.
- Latest progress (2026-04-27): `pleiades-types::Instant::with_time_scale_offset_checked()` and `pleiades-core::ChartRequest::with_instant_time_scale_offset_checked()` now provide the same checked raw-offset convenience at both layers, so the explicit caller-supplied conversion contract can reject non-finite offsets before the request instant is retagged.
- Latest progress (2026-04-27): `pleiades-core::ChartRequest` now also exposes a dedicated house-observer preflight helper, and `ChartEngine::chart()` uses it before backend orchestration so house requests fail fast with the same structured missing-observer error instead of relying on the later house-calculation branch.
- Latest progress (2026-04-27): `pleiades-backend::validate_request_against_metadata()` now provides a direct-request preflight that combines the shared time-scale, frame, apparentness, zodiac, observer, and body-coverage checks, so direct backend callers can reject unsupported shapes before dispatch without reconstructing the common guardrails.
- Latest progress (2026-04-27): `pleiades-backend::EphemerisRequest` now also exposes caller-supplied time-scale conversion helpers plus the checked offset convenience at the backend-request layer, so direct backend callers can preflight and retag explicit Delta T/TDB policies without reconstructing the request shape by hand.
- Latest progress (2026-04-27): `pleiades-backend::EphemerisRequest` now also exposes a typed `validate_against_metadata()` convenience, and the new `validate_requests_against_metadata()` batch helper rejects unsupported batch-capability metadata before failing fast on the first invalid request in the slice, which keeps the direct backend preflight path ergonomic for batch-oriented callers too. The batch helper now also prefixes the failing request's 1-based index in its error text, so callers can correlate a structured unsupported-shape failure with the exact slice position that triggered it.
- Latest progress (2026-04-27): the VSOP87 and JPL request-policy summary helpers now also validate their structured backend posture before formatting release-facing report lines, so the compact policy text now fails closed if a future snapshot summary drifts away from the current ecliptic/equatorial TT/TDB mean-only posture.
- Latest progress (2026-04-27): the JPL reference snapshot summary now also rejects cached body-order drift against the checked-in reference body sequence before report formatting, so the release-facing coverage line fails closed if the ordered body list ever diverges even when the row and epoch counts still look sane.
- Latest progress (2026-04-27): the JPL reference and independent hold-out equatorial-parity summaries now validate their derived coverage counts and epoch ranges before release-facing formatting, so the frame-transform parity lines fail closed instead of rendering stale batch metadata.
- Latest progress (2026-04-27): the compatibility-profile smoke coverage now also pins the full set of intentionally unresolved Babylonian custom-definition homographs, including both `Babylonian (House)` and `Babylonian (Sissy)`, so the release-facing custom-definition carve-out stays explicit alongside the broader ayanamsa label audit.
- Latest progress (2026-04-27): `ChartEngine::validated_metadata()` now also has a worked doctest example and a regression that rejects duplicate backend body coverage before release-facing inventory checks run, which keeps the metadata preflight behavior and its failure mode visible in the public chart façade docs as well as the core test suite.
- Latest progress (2026-04-27): `ChartEngine::validate_chart_request()` now mirrors the façade-level chart-shape preflight as a convenience wrapper, so callers can validate chart requests without separately reconstructing the backend metadata lookup.
- Latest progress (2026-04-27): `ChartEngine::chart()` now validates backend metadata before it reuses the inventory for request preflight or assembly, so malformed backend metadata now fails closed at the façade boundary instead of being reused in a supposedly validated chart run.
- Latest progress (2026-04-27): the compact comparison-envelope formatter now also reuses a typed median helper before appending it to the aggregate summary, which keeps the release-facing median wording centralized alongside the existing aggregate and 95th-percentile helpers instead of rebuilding the same string inline.
- Latest progress (2026-04-27): the compact VSOP87 source-documentation summary report now passes through the catalog-health validator before formatting, so stale source/file/body provenance now fails closed at the compact report boundary instead of rendering a silently drifted summary line.
- Latest progress (2026-04-27): `BackendMetadataValidationError` now exposes a typed compact `summary_line()` helper, and the backend metadata validation regression reuses that record directly before checking the `Display` output, which keeps the backend-metadata failure wording on the same summary-helper pattern used elsewhere while the current frontier remains clippy-clean.
- Latest progress (2026-04-27): that same backend metadata regression now also exercises out-of-order and mixed-scale nominal-range failures, so the new compact validation summary stays covered across the remaining range-specific branches as well.
- Latest progress (2026-04-27): the validation, release, and backend-matrix summaries now also share a typed comparison-audit summary helper and surface it explicitly in their compact output, which keeps the `compare-backends-audit` body/count/status line centralized instead of rebuilding the counts separately in each formatter.
- Latest progress (2026-04-27): the canonical VSOP87 source-backed evidence and equatorial companion summaries now also validate their sample-body, peak-body, and source-file invariants before release-facing formatting, so malformed compact VSOP87 evidence now fails closed instead of rendering a stale source-backed line.
- Latest progress (2026-04-27): the VSOP87 source-backed body-class evidence summaries now also validate duplicate sample bodies, duplicate outlier bodies, and blank peak source-file provenance before the release wrapper formats them, which keeps the coarse luminary/major-planet envelope from rendering stale provenance if the derived class rows ever drift.
- Latest progress (2026-04-27): compatibility-profile verification now also checks the freeform release-note, validation-reference-point, and compatibility-caveat sections for blank, whitespace-padded, or duplicate entries in addition to the existing catalog and alias drift checks, which keeps the release-profile prose a little less likely to diverge silently from the documented profile.
- Latest progress (2026-04-27): that same verification slice now also validates the long-term target house and ayanamsa scope notes plus their cross-section uniqueness, so the profile’s explicit end-state catalog framing stays under the same drift guard as the release prose.
- Latest progress (2026-04-27): the compact compatibility-profile canonical-name summaries now preflight through a typed descriptor-name validator before formatting, so the release-specific house-system and ayanamsa name lists fail closed if a malformed summary ever slips in.
- Latest progress (2026-04-27): the compatibility-profile smoke coverage now also pins the currently visible `Galactic Equator (True)` and `Galactic Center (Mula/Wilhelm)` spellings in the CLI and validation release-facing tests, which keeps the broader reference-frame aliases exercised while future catalog batches fill in any remaining gaps.
- Latest progress (2026-04-27): the CLI compatibility-profile and release-notes smoke coverage now also pins the lowercase `True galactic equator` / `Galactic equator true` aliases, so the user-facing release-profile audit now exercises the canonical `Galactic Equator (True)` entry and its documented alternate spellings together.
- Latest progress (2026-04-27): the compatibility-profile summary and verification smoke tests now also pin the full release-specific house-system and ayanamsa canonical-name breakdown lines, so the compact release-facing catalog audit now exercises the exact visible counts and ordered canonical lists instead of only the opening entries.
Latest progress (2026-04-27): `pleiades-core::CompatibilityProfile` now also exposes a typed `validate()` helper for the profile text sections and baseline/release partitions, and the validation summary preflights that helper before rendering the release-facing compatibility-profile checks, so malformed profile metadata now fails closed at the core boundary instead of only in the report formatter.
- Latest progress (2026-04-27): the compact compatibility-profile summary now also renders the custom-definition label names alongside the count, and the release-notes summary now surfaces the same inventory, which keeps the custom-definition bucket visible in the one-screen catalog audit instead of only as an aggregate total.
- Latest progress (2026-04-28): the compatibility-profile verification summary now also carries the explicit custom-definition label names and validates them against the current profile, so the verification-line audit stays aligned with the curated custom-definition bucket instead of only counting it. The built-in house and ayanamsa catalog validators now also reject blank or whitespace-padded descriptor notes, so the release-facing catalog audit now fails closed if the implementation-status prose behind a shipped descriptor drifts out of shape.
- Latest progress (2026-04-27): the same compact release-profile smoke coverage now also pins the exact 25-house/59-ayanamsa total counts in the summary and release-overview paths, which keeps the short catalog audit aligned with the rendered baseline-versus-release split instead of only checking the section headers.
- Latest progress (2026-04-27): the same release-facing smoke coverage now also pins `Babylonian (True Geoc)`, `Pullen SR (Sinusoidal Ratio) table of houses`, `Pullen SD (Sinusoidal Delta)`, and `Galactic Center (Rgilbrand)` in the CLI profile and release-notes paths, extending the currently visible catalog audit a little further without changing the release profile shape.
- Latest progress (2026-04-27): the CLI compatibility-profile smoke coverage now also pins `Galactic Equator (IAU 1958)` and `Dhruva Galactic Center (Middle Mula)`, keeping a couple more release-profile ayanamsa spellings anchored in the compact catalog audit without widening the public profile shape.
- Latest progress (2026-04-27): the compatibility-profile and release-notes smoke coverage now also pins the plain `Equal MC` / `Equal Midheaven` spellings plus the Babylonian `1` / `2` / `3` shorthand aliases, which keeps a few more still-visible release-profile forms exercised in both the CLI and maintainer-facing release text without changing the published catalog shape.
- Latest progress (2026-04-27): notable regression findings now also have a typed `summary_line()`/`Display` helper, and the validation/artifact regression sections reuse that record directly instead of rebuilding the body, delta, and note wording inline, keeping the compact release-facing regression text aligned with the rest of the typed summary pattern.
- Latest progress (2026-04-26): the aggregate comparison summary now also has a backend-owned `summary_line()`/`Display` helper, and the compact validation/release comparison envelope now reuses that typed aggregate line before appending the median values, keeping the maxima/means/RMS wording centralized alongside the existing per-body summary helper. The matching 95th-percentile tail line is now also backed by a typed `ComparisonPercentileEnvelope` helper, and the validation/release report paths reuse that record directly instead of rebuilding the tail wording inline.
Latest progress (2026-04-26): backend provenance now has a typed `BackendProvenance::summary_line()`/`Display` helper too, and the validation comparison/backend-matrix renderers reuse that typed provenance record directly so source-material summary text stays owned by the backend metadata type instead of being rebuilt from the raw string field in each report path.
- Latest progress (2026-04-27): `BackendProvenance` now also exposes a reusable `validate()` helper, and `BackendMetadata::validate()` reuses it before the shared metadata checks map provenance drift back into the existing backend-metadata error types, so blank summaries, padded source labels, and duplicate provenance entries fail closed before release-facing metadata is rendered.
- Latest progress (2026-04-26): the JPL interpolation-quality, distinct-body coverage, and independent hold-out summaries now also expose typed `summary_line()`/`Display` helpers, and the validation/release report paths reuse those displayable records directly, keeping the JPL transparency evidence aligned with the other backend-owned release summaries.
- Latest progress (2026-04-27): the JPL reference snapshot batch-parity evidence is now surfaced in the release summary alongside the existing equatorial-parity line, and the new smoke coverage pins that mixed-frame corpus through the combined JPL evidence helper so the checked-in reference snapshot stays visible in release-facing output. The reference snapshot coverage summary validation now also rejects duplicate body labels in the cached body-order slice, and the equatorial-parity release wrapper now preflights that shared reference summary too, which keeps the release-facing JPL parity line from silently reusing a stale body list if the static snapshot metadata ever drifts.
- Latest progress (2026-04-27): the JPL reference and independent hold-out batch-parity summaries now validate their derived request and quality counts before report formatting, and the report wrappers now fail closed if those summary records drift.
- Latest progress (2026-04-27): `pleiades-elp::LunarTheorySpecification` now validates its duplicated source-selection and request-policy fields before the release-facing lunar summary is rendered, and the validation/release report paths now reuse a validated lunar-theory summary helper so stale lunar provenance text fails closed instead of formatting a silently drifted compact line.
- Latest progress (2026-04-27): the independent JPL hold-out corpus now also exposes a mixed-scale batch-parity summary through the validation and release report paths, so the externalized Horizons check shows request-order and single-query parity alongside the existing hold-out envelope and equatorial cross-check.
- Latest progress (2026-04-27): the validation, release-notes, and release-summary views now also render the checked-in JPL reference snapshot source provenance line, keeping the reference corpus provenance visible alongside the coverage and comparison summaries instead of only surfacing the coverage count.
- Latest progress (2026-04-27): the compact validation and release-facing JPL summaries now also render the checked-in reference snapshot manifest line plus the independent hold-out source/manifest lines directly, which keeps the fixture provenance visible without needing to rely on the combined JPL evidence block alone. The reference, comparison, and independent hold-out coverage summaries now also preflight their body-count and epoch-range invariants before formatting, so malformed snapshot metadata now surfaces as unavailable instead of stale coverage text. While wiring that through, the packaged-data batch-parity helper also had to acknowledge the backend's non-exhaustive `QualityAnnotation` enum with a wildcard fallback so future result annotations remain classified as unknown instead of tripping an exhaustive match.
- Latest progress (2026-04-27): the release-notes-summary and release-summary smoke tests now also pin the exact time-scale, observer, apparentness, frame, and zodiac policy lines, and the release-notes-summary path now also pins the API stability summary line plus the JPL source-provenance lines, which broadens the compact release-facing status coverage for currently visible text without changing the underlying release profile shape.
- managed Rust workspace and mandatory crate layout;
- typed domain vocabulary in `pleiades-types`;
- backend contract, metadata, errors, batch fallback, and composite routing in `pleiades-backend`, with the lower-level request docs now also showing the default tropical/ecliptic/mean-geometric request shape explicitly, the shared request-policy errors now using stable display labels for supported time scales, frames, and zodiac modes, and the backend matrix now surfacing an explicit family-posture line for algorithmic, data-backed, and routing families;
- Latest progress (2026-04-27): the shared backend batch adapter now also has an explicit observer-bearing regression, so batch-mode topocentric requests fail with the same structured `InvalidObserver` error as single-request calls.
- Latest progress (2026-04-27): `pleiades-backend::BackendMetadata` now also exposes a shared validation helper that rejects blank identifiers, duplicate provenance sources, duplicate coverage entries, and inverted nominal ranges, and `pleiades-core::ChartEngine` now surfaces the same check through `validated_metadata()` so consumers and release tooling can preflight backend inventory metadata without changing the existing `metadata()` accessor.
- Latest progress (2026-04-27): the validation backend-matrix report now also preflights each implemented backend's metadata through that shared checker before formatting, so malformed backend inventory data now fails closed at the report boundary instead of rendering a stale capability matrix.
- Latest progress (2026-04-27): `pleiades-backend::BackendMetadata` now also exposes a typed compact summary helper plus `Display`, and the detailed backend-matrix path reuses that record directly before the field-by-field breakdown so the backend identity, provenance, body/frame coverage, and capability posture stay co-located in one backend-owned summary string.
- house and ayanamsa catalogs with descriptors and aliases;
- Latest progress (2026-04-27): `pleiades-houses` now exposes a backend-owned house catalog validation summary plus a reusable catalog validator, and the compatibility-profile verifier now preflights the built-in house catalog through that typed helper before running the release-profile cross-catalog alias checks.
- Latest progress (2026-04-27): `pleiades-types::HouseSystem` now has stable `Display` labels that mirror the catalog names, and `pleiades-houses` reuses those typed labels in house-request and catalog-validation reporting instead of `Debug` spellings, which keeps the house-system vocabulary aligned across the shared domain and release-facing error text.
- Latest progress (2026-04-27): `pleiades-ayanamsa` now exposes a backend-owned ayanamsa catalog validation summary plus a reusable validator, and the compatibility-profile, validation-report, and release-summary compact paths render that typed health line while `verify-compatibility-profile` preflights the built-in ayanamsa catalog before the alias checks.
- chart façade, sign/house/aspect summaries, sidereal conversion, motion accessors, and profile exports in `pleiades-core`; the detailed placement rows now also flow through a typed `BodyPlacement::summary_line()`/`Display` helper so chart snapshot tables reuse the same row formatting instead of rebuilding it inline;
- Latest progress (2026-04-27): `ChartSnapshot::summary_line()` now only reports `observer=house-only` when the observer actually feeds house calculations, so observer-bearing snapshots without house computations stay geocentric in the compact chart policy text and better match the actual request usage.
- Latest progress (2026-04-27): `pleiades-houses::HouseRequest` now also has a typed `summary_line()`/`Display` helper, and the house-validation detailed report reuses that request record directly so the explicit instant, observer, house-system, and obliquity policy is visible at the lower-level house layer too.
- Latest progress (2026-04-27): the shared `pleiades-core::MotionSummary` chart record now also has a typed `summary_line()`/`Display` helper, and the chart snapshot renderer reuses it directly instead of rebuilding the motion-count wording inline, keeping the compact motion audit on the same typed pattern as the other report summaries. Latest progress (2026-04-30): that motion summary helper now also validates its total placement count before the chart snapshot renderer reuses it, which keeps the compact motion summary on the same fail-closed path as the rest of the report helpers. Latest progress (2026-04-29): that motion summary helper now also has a public doctested example plus a direct regression for its stable count ordering, which keeps the speed-summary API discoverable from the crate docs and anchored by a typed summary-line test. Latest progress (2026-04-29): `pleiades-core::AspectSummary` now also has the same doctested summary-line pattern plus a crate-root re-export, which keeps the major-aspect summary discoverable from the public API docs alongside the motion summary helper. Latest progress (2026-04-30): `pleiades-core::AspectDefinition` now also exposes a public validation helper plus typed summary-line/Display support for custom aspect tables, which keeps the configurable aspect API on the same explicit fail-closed path as the other typed chart helpers.
- Latest progress (2026-04-27): `pleiades-core::ChartRequest` now also exposes a compact `summary_line()` helper for request-shape diagnostics, and the shared time/observer policy doc now points at that typed summary so the current TT/TDB, observer, apparentness, and house-system vocabulary stays easy to reference in both code and docs.
- Latest progress (2026-04-28): the CLI help summary for `pleiades-cli chart` now also advertises both `--tdb-from-tt-offset-seconds` and `--tt-from-tdb-offset-seconds` alongside the other caller-supplied time-scale flags, so the explicit TDB/TT retagging options are discoverable from the top-level help text as well as the chart subcommand synopsis. The CLI regression suite now also pins that top-level help line, so the discoverability claim stays exercised in tests instead of living only in the string literal.
- Latest progress (2026-04-27): `pleiades-core::ChartSnapshot` now also exposes a compact `summary_line()` helper for computed-chart diagnostics, which keeps the backend ID, placement count, and house-observer policy aligned with the request-shape vocabulary without forcing callers through the verbose report text.
- Latest progress (2026-04-27): `pleiades-core::ObserverPolicy` now also has a typed `summary_line()` helper plus a small doctested example, so the compact observer-policy wording stays co-located with the chart request/snapshot helpers instead of only living as a raw display label.
- Latest progress (2026-04-27): the remaining signed TT/TDB helper docs now also include doctested examples for `tt_from_tdb_signed`, `tdb_from_ut1_signed`, and `tdb_from_utc_signed`, which keeps the caller-supplied time-scale policy documented at the lowest shared instant layer and exercises the signed conversion aliases in doctests too.
- Latest progress (2026-04-27): `TimeScaleConversionError` now also exposes a typed `summary_line()` helper, and the display implementation reuses that record directly so caller-supplied time-scale conversion failures render through the same compact summary pattern as the rest of the typed diagnostics. `EphemerisError` now also exposes a compact `summary_line()` helper, and the CLI/validation error renderers reuse that typed record directly instead of reconstructing the `kind: message` string locally, which keeps structured backend failures aligned with the other typed summary records.
- Latest progress (2026-04-27): `pleiades-backend::RequestPolicySummary` now also has a doctested example showing the compact shared policy helper in use, which keeps the time-scale, observer, apparentness, and frame summary documented alongside the existing policy prose and report-facing helper.
- Latest progress (2026-04-27): `ChartRequest` now also implements `Display` with the same compact request-shape text, which keeps the façade's typed request summary aligned with the rest of the summary-record pattern used throughout the release-facing reports. The same summary path now also preserves custom house-system names, aliases, and notes instead of collapsing them to an opaque `Custom` label.
- Latest progress (2026-04-27): chart request time-scale conversion helpers are now regression-covered as a request-shape preservation slice too, so signed UTC/TDB retagging keeps the observer, body list, zodiac mode, apparentness, and custom house-system details intact while only changing the instant scale that the summary line reports.
- Latest progress (2026-04-27): the same request-shape preservation coverage now also exercises the signed UT1-to-TDB helper path, so explicit UT1 staging keeps the same observer, body, zodiac, apparentness, and custom house details intact while only retagging the instant to TDB.
- Latest progress (2026-04-27): the lower-level `pleiades-types::Instant` and `ObserverLocation` records, plus `pleiades-backend::EphemerisRequest`, now also expose compact summary-line/display helpers, so the request shape and observer details stay typed all the way down to the backend contract instead of only at the chart façade.
- Latest progress (2026-04-27): `pleiades-core::ChartRequest::validate_against_metadata()` now preflights chart-level body coverage, zodiac routing, frame support, apparentness, and the house-observer contract against backend metadata before dispatch, so unsupported chart shapes can fail before the façade asks any backend for positions.
- Latest progress (2026-04-27): chart placements now expose the backend motion sample plus longitude/latitude/distance speed accessors, so consumers can reach the raw motion values without drilling into the backend result record. The shared `pleiades-types::Motion` sample now mirrors that ergonomic surface too, so callers can read the raw speed components through typed accessors instead of touching the struct fields directly.
- Latest progress (2026-04-27): topocentric house calculations now reject non-finite observer elevations with a distinct `InvalidElevation` house error, and the chart façade maps that to `InvalidObserver` before any backend body query runs, keeping the observer/topocentric failure path explicit without implying backend support.
- Latest progress (2026-04-27): the shared geocentric-only observer-policy error now also names the rejected observer coordinates, which makes the `InvalidObserver` diagnostic more actionable while preserving the same structured error kind for chart and backend callers.
- Latest progress (2026-04-27): the Gauquelin house-formula regression now also verifies the 36-sector spacing in the descending wraparound order in addition to the existing anchor points, so the special 36-cusp shape is exercised as a derived-domain regression instead of only as a sector-count check.
- preliminary `pleiades-vsop87`, `pleiades-elp`, and `pleiades-data` crates, including finite-difference mean-motion estimates in the VSOP87 path and compact ELP Moon/lunar-point path with a structured lunar source-selection helper and explicit source-alias metadata for future source-backed variants, plus a compact lunar catalog summary that now reports the selected alias count and supported/unsupported body counts, generated binary coefficient-table slices for the vendored IMCCE/CELMECH VSOP87B source files across the Sun-through-Neptune paths, per-body VSOP87 source profiles that distinguish generated-binary, vendored full-file source-backed, and fallback mean-element paths in validation reports, explicit geocentric-only rejection of direct topocentric requests in the VSOP87/ELP placeholder backends, explicit VSOP87 frame-treatment notes for the J2000 ecliptic/equinox source frame and mean-obliquity equatorial transform, plus a small JPL Horizons derivative-fixture backend with exact lookup, linear interpolation, and a denser hold-out corpus for interpolation transparency;
- Latest progress (2026-04-27): the last stale VSOP87 motion-helper wording now also describes the finite-difference mean-geocentric rates directly, so the source comments match the production-neutral backend posture instead of referring to the earlier preliminary model.
- Latest progress (2026-04-27): the JPL, VSOP87, and lunar frame-treatment notes now also travel through typed backend-owned summary records, and validation/release formatting reuses those records directly instead of rebuilding the same frame-treatment wording from raw strings.
- Latest progress (2026-04-27): the VSOP87, JPL snapshot, and lunar request-policy records now also implement `Display`, and the release-facing request-policy helpers reuse those typed renderers directly instead of rebuilding the same compact wording from the raw fields, which keeps the backend posture lines aligned with the summary-record pattern used elsewhere.
- Latest progress (2026-04-27): the VSOP87 source-specification records now also have typed summary helpers and reusable catalog-format helpers, so the documented variant, frame, unit, and date-range fields can travel through typed reporting code instead of being rebuilt ad hoc.
- Latest progress (2026-04-29): `pleiades-vsop87` now also exposes the canonical J2000 request corpus as a public helper, which lets downstream validation and reproducibility tooling reuse the exact source-backed body order without reconstructing it from the sample metadata. The supported-body J2000 equatorial, supported-body J1900 equatorial, and J1900 ecliptic batch-parity slices now have matching public request-corpus helpers too, so the downstream batch-parity tooling can reuse the exact supported-body request shapes without rebuilding them from the summary metadata. The supported-body J2000 ecliptic slice now also exposes the same reusable corpus helper, plus an explicit `supported_body_j2000_ecliptic_batch_parity_requests()` compatibility alias, which keeps the Sun-through-Pluto ecliptic batch shape available to downstream tooling without reconstructing it from the summary metadata. The broader reusable batch-request corpus helper for arbitrary body iterators, instant tags, and coordinate frames is still in place too, and the canonical J2000/J1900 batch-path regressions continue to reuse it directly so validation tools can share the same request-shape construction instead of rebuilding it at each call site. The J1900 equatorial helper now also has a direct crate-level regression that checks the release-facing summary line and the supported-body order against the backend metadata. Latest progress (2026-04-29): the source-backed J2000 batch slice now also has a dedicated `source_backed_body_j2000_batch_parity_requests()` / `..._request_corpus()` alias pair, which keeps the exact Sun-through-Neptune batch shape available under a body-order-oriented name for downstream validation and reproducibility tooling. Latest progress (2026-04-30): the source-backed J2000 ecliptic request corpus now also has matching `source_backed_body_j2000_ecliptic_batch_parity_requests()` / `..._request_corpus()` aliases, which keeps the source-backed geocentric-ecliptic slice aligned with the same naming pattern as the frame-qualified supported-body helpers. Latest progress (2026-04-30): the source-backed J2000 and J1900 frame-specific VSOP87 corpora now also expose plain `request_corpus` aliases, which keeps the reusable source-backed ecliptic/equatorial batch slices discoverable under the same naming pattern as the supported-body helpers.
- Latest progress (2026-04-27): the compact and detailed lunar validation/release reports now also surface a backend-owned mixed TT/TDB lunar batch-parity summary, so the current time-scale coverage for the lunar reference slice is visible alongside the other lunar evidence instead of living only in tests.
- Latest progress (2026-04-28): that same lunar mixed TT/TDB batch-parity summary now also validates its derived request counts and parity flags before the release-facing report line is reused, so the compact lunar batch-parity evidence now fails closed on count or parity drift instead of echoing stale mixed-scale metadata.
- Latest progress (2026-04-29): the canonical mixed-scale lunar request corpus now also lives in a reusable `lunar_reference_batch_requests()` helper, which keeps the batch-parity slice shared between validation and future tooling without duplicating the request construction.
- Latest progress (2026-04-29): `pleiades-vsop87::canonical_mixed_time_scale_batch_parity_requests()` is now public and regression-covered, which keeps the canonical mixed TT/TDB batch-parity slice reusable by future tooling instead of only by the summary builder.
- Latest progress (2026-04-27): validation and release summary tests now pin the VSOP87 source-documentation health line exactly, so the source-file order and source-backed body-order checks stay exercised in the release-facing smoke suite. The health line now also surfaces the explicit source-backed partition order, which keeps the generated-binary/vendored/truncated grouping visible in the same release-facing smoke text.
- Latest progress (2026-04-27): the VSOP87 source-documentation health validator now also checks list/count coherence for the source-backed and fallback partitions, so partition-order or profile-count drift now fails closed even if the summary booleans are accidentally left optimistic.
- Latest progress (2026-04-27): the shared `pleiades-types::Instant::mean_obliquity` helper now centralizes the mean-obliquity approximation used by the JPL, VSOP87, ELP, and packaged-data equatorial transforms, so the frame-conversion policy lives in the foundation crate instead of being duplicated across backend implementations. The shared ecliptic/equatorial rotation also now has a multi-quadrant round-trip regression in `pleiades-types`, which keeps the inverse transform covered at the typed foundation layer instead of only through backend-specific fixtures.
- compression model, sample artifact lookup, explicit body/segment random-access helpers, a typed output-support query helper for artifact profiles, motion-policy consistency validation for artifact profiles, an explicit little-endian byte-order field in the compressed artifact header that validation/release reports now surface, and a packaged-data output-support summary that now fails closed if the checked-in equatorial or motion posture drifts;
- Latest progress (2026-04-27): the compression artifact, body, and profile types now expose public `validate()` helpers that mirror the codec's encode/decode checks, giving generators and release tooling a fail-fast validation path before writing deterministic binary payloads.
- CLI, validation reports, backend matrix, release notes/checklists, bundle generation, and bundle verification;
- Latest progress (2026-04-28): the release-profile identifiers record now validates against the current compatibility and API-stability posture before release bundle generation, so stale profile-id drift fails closed alongside the existing bundle checksum checks. The compact release-profile identifiers summary is now also explicitly versioned (`v1`), which keeps the release-facing identifiers artifact self-describing as the bundle-check path continues to harden. Latest progress (2026-04-30): the release bundle manifest and verification smoke coverage now also exact-pin the release-profile identifiers checksum entry, which keeps the bundle's profile-id sidecar visible alongside the existing identifiers and workspace-audit status lines.
- validation benchmark reports now include single-request latency, batch throughput, an estimated corpus heap footprint for the representative corpus, and a packaged-artifact decode benchmark, so the benchmark command and report summaries better match the validation/testing spec;
- Latest progress (2026-04-26): the release-facing benchmark reports now render elapsed durations with explicit second units instead of `Debug`-style `Duration` output, keeping the validation, artifact, and chart timing lines unit-stable across the compact and detailed views;
- comparison reports and comparison-audit output now carry the comparison corpus summary, including epoch labels and julian-day span, so the validation-facing tolerance window is visible alongside the body deltas instead of only the corpus name;
- Latest progress (2026-04-26): the per-body tolerance status record now also renders as a typed `summary_line()`/`Display` helper, and the validation formatter reuses that record directly instead of rebuilding the compact expected-tolerance wording inline.
- Latest progress (2026-04-27): the body-class comparison envelope now follows the same typed-summary pattern, so the coarse class report path reuses the backend-owned summary helper instead of reconstructing the release-facing wording inline.
- Latest progress (2026-04-28): the VSOP87 source-backed body evidence summary now also rejects empty sample sets and duplicate body labels before the release-facing formatter reuses it, which keeps the compact source-evidence line fail-closed if the canonical body order or coverage bookkeeping ever drifts.
- Latest progress (2026-04-28): the source-backed body evidence summaries now also verify that any outlier bodies belong to the sampled body set, which keeps the compact source-evidence outlier note aligned with the canonical sample list instead of only with the aggregate counts.
- Latest progress (2026-04-27): the VSOP87 source-backed body evidence and body-class envelopes now also validate their derived counts before the report wrappers render them, which keeps the release-facing source-evidence summaries on the same fail-fast pattern as the existing source-documentation and generated-audit helpers. Latest progress (2026-04-28): the source-backed body-class evidence summaries now also validate their finite, non-negative, and metric-order invariants before the release-facing formatter reuses them, so the coarse luminary/major-planet envelopes now fail closed on the same error-shape checks as the canonical per-body evidence. Latest progress (2026-04-30): the source-backed body and body-class evidence summaries now also validate their canonical sample order, so the compact provenance lines fail closed if a future report ever reorders the canonical Sun-through-Neptune or class-specific sample lists.
- tests and doctests for current behavior.
- Latest progress (2026-04-27): the VSOP87 crate-level docs now describe the explicit topocentric-observer and apparent-request rejection posture that the runtime already enforces, keeping the public crate narrative aligned with the current geocentric-only request policy.
- Latest progress (2026-04-27): the Pluto VSOP87 source-profile provenance now explicitly calls the body a mean-element fallback special case, which keeps the non-VSOP87 outlier note visible in the backend matrix and source-profile summaries.
- Latest progress (2026-04-27): the VSOP87 source-documentation partition order is now available through a public helper too, so regeneration tooling can reuse the backend-owned generated-binary/vendored/truncated partitioning without reconstructing it locally.
- Latest progress (2026-04-26): the house-validation corpus summary is now pinned through the typed `HouseValidationReport::summary_line()` helper and an exact release-summary regression, so the representative baseline scenarios, sample count, and latitude-sensitive system list stay visible as a stable release-facing string instead of only a scenario count.
- Latest progress (2026-04-28): the house-validation corpus summary now also preflights a typed validation helper before the compact validation and release summary paths reuse it, so a drifted scenario label or sample shape now fails closed with an unavailable house-validation line instead of rendering stale release-facing house evidence.

Latest progress (2026-04-26): the VSOP87, ELP, and JPL backend summary helpers now reuse the shared typed `Display` labels for frames, time scales, zodiac modes, apparentness, and instants instead of maintaining parallel enum-to-string match tables, which keeps the compact release-facing request-policy text aligned with the public type vocabulary.

Latest progress (2026-04-26): the release checklist summary now also surfaces the shared zodiac policy line, keeping the compact maintainer-facing release workflow aligned with the other report summaries and the current tropical-only request posture.
Latest progress (2026-04-26): custom sidereal labels now round-trip through stable `Display` output as well, so `ZodiacMode::Sidereal` renders user-defined ayanamsas with their explicit names instead of a debug dump while the built-in release labels continue to use the shared typed formatting path.
Latest progress (2026-04-26): the workspace serde feature now enables `std` alongside `derive`, which keeps the serde-gated String and Vec payloads in the shared types crate compatible with all-features linting instead of failing on the optional serialization path.
Latest progress (2026-04-26): comparison-tolerance scope coverage rows now also have backend-owned `summary_line()`/`Display` helpers, and the validation/release tolerance-policy renderers reuse those typed rows directly instead of rebuilding the per-scope wording inline, which keeps the compact release-facing tolerance catalog a little more consistently typed.
Latest progress (2026-04-26): the core compressed-artifact profile record now also implements `Display` and a `summary_line()` helper, so downstream validation and release helpers can reuse the typed codec metadata directly instead of reconstructing the stored/derived/unsupported capability wording locally.
Latest progress (2026-04-26): the API stability profile now also exposes a compact `summary_line()` helper, and the release/validation summary views reuse it for the short API stability lines so the compact posture wording stays typed instead of being rebuilt from ad hoc literals.
Latest progress (2026-04-26): the VSOP87 source-documentation health summary now checks the source-backed body partition order explicitly in addition to the source-file and catalog counts, and the compact release-facing health line now labels that partition as a canonical order so reproducibility drift is a little easier to spot at a glance.
Latest progress (2026-04-27): the same source-documentation health check now also uses a typed issue enum with stable display labels, so the reproducibility and release-facing health path no longer depends on raw string issue literals.
Latest progress (2026-04-26): backend quality annotations now have stable display labels in `pleiades-backend`, and chart snapshots render that typed label directly instead of Debug formatting, keeping the chart body-quality column aligned with the shared contract.
Latest progress (2026-04-26): the JPL reference snapshot equatorial parity line now renders with a single backend prefix instead of a duplicated report label, keeping the compact provenance text cleaner while preserving the same coverage evidence.
Latest progress (2026-04-26): the JPL reference, comparison, equatorial-parity, and independent hold-out coverage summaries now all have backend-owned `summary_line()`/`Display` helpers, so the validation and release reporting surfaces can reuse the typed coverage records directly instead of rebuilding those provenance strings separately. The comparison snapshot now also surfaces its checked-in J2000 source and manifest metadata through the validation, release, and backend-matrix reports, so the comparison corpus provenance now matches the reference and hold-out fixtures a little more closely.
Latest progress (2026-04-26): the JPL reference and independent hold-out source-material summaries are now backend-owned typed records too, so the checked-in Horizons provenance and hold-out coverage notes can be reused as structured data instead of only formatted report strings.
Latest progress (2026-04-26): the artifact summary now reuses the shared packaged-artifact profile-with-body-coverage helper, so the compact artifact report names the bundled body set explicitly instead of only reporting the aggregate count. The packaged body coverage itself is now also exposed as a structured backend-owned summary record in `pleiades-data`, which keeps the packaged-body list reusable by regeneration and release-facing reporting code instead of only as a formatted string. Latest progress (2026-04-27): that packaged-body coverage summary now also validates against the bundled body set before report formatting, so the compact packaged-data provenance line now fails closed if the body list or count ever drifts out of sync with the checked-in artifact.
Latest progress (2026-04-26): the packaged-data request-policy summary helper now derives its cached release-facing string from the structured summary record instead of a duplicated hardcoded literal, keeping the artifact policy wording centralized on the typed backend data.
Latest progress (2026-04-27): that same packaged-data request-policy summary record now also implements `Display`, and the packaged backend metadata plus validation/artifact report paths reuse the typed record directly instead of rebuilding the request-policy wording from the helper output.
Latest progress (2026-04-27): the packaged-data backend now also publishes a mixed ecliptic/equatorial batch-parity summary and matching batch regression, which keeps the packaged frame reconstruction path visible alongside the existing mixed TT/TDB transparency evidence in validation and release summaries. The same packaged mixed-frame and mixed TT/TDB batch-parity summaries now also validate their derived count totals before the release-facing wrappers render them, so the packaged transparency lines fail closed if the frame or time-scale split ever drifts out of coherence. Latest progress (2026-04-28): those same packaged batch-parity summaries now also validate preserved request order and single-query parity, so the compact release-facing batch evidence now fails closed if the parity regression stops matching the reference request sequence or the single-request path.
Latest progress (2026-04-26): the chart CLI now rejects time-scale offset flags that do not match the tagged instant, so caller-supplied TT/TT-TDB retagging helpers stay explicit instead of being silently ignored on UTC/UT1/TDB requests. TDB-tagged chart requests now also reject a direct `--tdb-offset-seconds` retagging flag, and the CLI regression suite covers that explicit failure path alongside the existing TT/UTC/UT1 mismatch checks.
Latest progress (2026-04-26): the lunar reference and lunar equatorial error-envelope summaries now also surface median and 95th-percentile deltas alongside the existing mean/max fields, so the compact lunar evidence mirrors the richer VSOP87/JPL reporting shape more closely without changing the public request model.
Latest progress (2026-04-27): the lunar reference and lunar equatorial error-envelope records now also expose backend-owned `summary_line()`/`Display` helpers, which keeps the release-facing lunar envelope wording co-located with the typed summary records instead of only in the formatter functions.
Latest progress (2026-04-26): the compatibility-profile and release-summary smoke tests now also pin the Wang, Aries houses, Fagan/Bradley, and Usha Shashi release-facing spellings, keeping a few more still-visible equal-house and ayanamsa labels anchored in the command-line audit path while the broader catalog breadth audit continues.
Latest progress (2026-04-26): the compression codec and packaged-data fixture were bumped to a version-4 format that supports optional residual-correction channels in segment payloads, so the current packaged artifact can stay deterministic while future lunar fits gain a home for high-curvature correction terms.
Latest progress (2026-04-28): `pleiades-compression::Segment` and `BodyArtifact` now expose compact typed summary lines for residual-channel-bearing segment spans and body coverage, so future artifact-generation and report code can inspect the residual-correction layout without unpacking the vectors ad hoc. Latest progress (2026-04-29): `CompressedArtifact` now also exposes an artifact-level residual-coverage summary plus segment/body counts, which gives the residual-correction posture a typed aggregate view ahead of any residual-fit packaging work, and the artifact inspection report now renders the residual-bearing segment and body counts directly so the release-facing artifact summary stays aligned with that new coverage view. Latest progress (2026-04-29): that same artifact inspection report now also validates its derived body, segment, residual-body, and coverage bounds before rendering, so the compact release-facing artifact view fails closed if the decoded inspection record drifts away from the packaged artifact it describes. Latest progress (2026-04-29): the compact release notes and release summary views now also surface the artifact inspection line itself, which keeps the residual-bearing segment and body counts visible in the one-screen audit path alongside the boundary envelope. Latest progress (2026-04-29): the release-summary and validation smoke coverage now also pin the residual-bearing body line directly, so the one-screen and report-level artifact audits keep the Moon-only residual posture visible instead of relying only on the shared inspection helper text. Latest progress (2026-04-29): the artifact inspection report now also validates the residual-bearing body list against the per-body residual segment counts, so the compact artifact summary fails closed if that list ever omits a bundled residual body. Latest progress (2026-04-29): the same artifact inspection validation now also checks the total residual-bearing segment count against the inspected body-level totals, which keeps the compact artifact summary aligned with the decoded residual coverage instead of only the body list.
Latest progress (2026-04-26): the JPL backend evidence summary now also surfaces the full reference snapshot equatorial parity line alongside the existing ecliptic coverage and asteroid equatorial evidence, so the full checked-in frame-transform regression is now visible in the compact release-facing backend report instead of living only in the batch-path test.
Latest progress (2026-04-26): the independent JPL hold-out corpus now also publishes an equatorial parity summary and batch-path regression, so the Mars/Jupiter/Saturn transparency rows are exercised through the same mean-obliquity transform path as the reference snapshot and now surface in the compact validation, backend-matrix, and release summary views too.
Latest progress (2026-04-26): the independent JPL hold-out batch path now also preserves mixed TT/TDB request scales, so the externalized Horizons check exercises the same per-request dynamical-time labeling contract as the reference snapshot batch path.
Latest progress (2026-04-26): the backend matrix and release summary views now also render the JPL reference snapshot equatorial-parity line explicitly, which keeps the mean-obliquity batch regression visible in the compact inventory and release-facing summary paths instead of only inside the combined source-backed evidence block.
Latest progress (2026-04-26): the validation artifact summary and release summary now also render the packaged-artifact regeneration provenance line, so the compact release-facing artifact reports keep the maintainer-facing regeneration source visible alongside the profile and request-policy lines. Latest progress (2026-04-27): the packaged-artifact regeneration summary now also has a typed validation helper and a report-facing wrapper that checks the bundled body list against the checked-in reference snapshot before formatting the provenance line, keeping the regeneration metadata aligned with the public-input coverage at the report boundary. Latest progress (2026-04-27): that same validation now also requires the bundled body list to match the checked-in packaged body set exactly, so any order or membership drift in the packaged artifact coverage now fails before the regeneration provenance line is rendered. Latest progress (2026-04-27): the packaged artifact regeneration summary now also validates its label, source, artifact version, and checksum against the checked-in packaged artifact metadata before rendering, so the maintainer-facing provenance line now fails closed if the regenerated header drifts from the committed bytes. Latest progress (2026-04-29): the packaged artifact regeneration summary now also surfaces the residual-correction body set explicitly, which keeps the Moon-only residual posture visible in the compact regeneration provenance instead of only inside the generation-policy note.
Latest progress (2026-04-26): the release-facing validation, API-stability, and release summaries now render the paired compatibility/api-stability release identifiers through a shared formatter, so the authoritative profile ids stay co-located in the compact outputs instead of being repeated as separate string literals.
Latest progress (2026-04-26): the workspace audit summary now groups native-build-hook violations by rule before listing the individual findings, so the pure-Rust release gate is a little easier to scan when the workspace audit fails.
Latest progress (2026-04-27): the compact workspace-audit summary now also reuses a typed summary record with the workspace root, manifest count, lockfile path, violation count, and grouped rule counts, which keeps the compact release-facing audit output aligned with the detailed workspace-audit report instead of rebuilding that one-line synopsis in the CLI formatter. The workspace audit now also flags direct `-sys` dependency names and renamed `package = "...-sys"` declarations in manifest dependency tables, which closes a slightly broader native-dependency gap before the lockfile scan runs.
Latest progress (2026-04-26): the VSOP87 regeneration path now also exposes an explicit checked-in generated-blob lookup helper, so the source-file-to-binary mapping used by the reproducibility tests is discoverable without a hardcoded match block.
Latest progress (2026-04-26): the VSOP87 crate now also exposes a dedicated canonical source-backed body-order helper for the Sun-through-Neptune profile sequence, so reproducibility tooling can reuse the release-facing order without re-deriving it from the profile list.
Latest progress (2026-04-26): the backend-owned time-scale policy wording is now centralized in `pleiades-backend` and reused by validation/release summaries, so the compact report surfaces keep the TT/TDB and caller-supplied conversion posture aligned without a duplicate local literal. The same policy posture is now also exposed as a typed `RequestPolicySummary`, which keeps the time-scale, observer, apparentness, and frame lines together for report rendering instead of scattering the strings across each formatter. The same typed summary now also has a compact one-line renderer, and the release-facing validation/release summaries now reuse that typed record for their consolidated request-policy line.
Latest progress (2026-04-27): the request-policy summary is now also validated before the validation/release report renderers format it, so stale policy wording now fails closed at the report boundary instead of leaking into release-facing output. The validator now compares against the current backend-owned request-policy posture rather than only screening for blank or whitespace-padded fields, which makes the release-facing guardrail a little stricter when the compact text drifts semantically.
Latest progress (2026-04-28): the request-policy-summary regression now also covers observer, apparentness, and frame drift in addition to the existing time-scale check, which keeps all four shared policy fields under the same fail-closed coverage instead of validating only the first one.
Latest progress (2026-04-27): the report-facing request-policy helper is now named explicitly in `pleiades-backend` and reused by the validation report, backend-matrix, and release-summary paths, which makes the shared time-scale, observer, apparentness, and frame summary a little easier to identify as a report contract instead of just a raw summary record.
Latest progress (2026-04-26): the packaged-artifact regeneration summary is now self-contained with the bundled body coverage inline, so the maintainer-facing regeneration provenance no longer needs a separate CLI line just to name the packaged body set. Latest progress (2026-04-27): the `regenerate-packaged-artifact` CLI now accepts either a positional output path or `--out FILE`, and the help text and README examples document the regeneration path explicitly so maintainers can invoke the fixture rebuild without guessing the argument shape.
Latest progress (2026-04-26): the VSOP87 source-backed body evidence summary now also prints the canonical source-backed body order, so the compact provenance line now mirrors the Sun-through-Neptune ordering instead of only listing aggregate counts. The formatter now labels that sequence explicitly as the source-backed body order in release-facing output, which keeps the reproducibility cue a little clearer in the compact report text. The VSOP87 source-documentation summary now also names the exact public source file set (`VSOP87B.ear` through `VSOP87B.nep`), which makes the compact reproducibility line a little more concrete without changing the runtime path. The supported source-file catalog is now also exposed as a public helper for regeneration tooling, so downstream reproducibility checks can reuse the backend-owned source-file order without hardcoding the vendored list.
Latest progress (2026-04-26): the VSOP87 and ELP backend-matrix status notes now use production-neutral wording for the current Pluto and lunar baselines, and the remaining stale pending phrasing has been removed from the VSOP87 source comments, ELP source-material summary, lunar-theory policy doc, and the corresponding crate metadata strings so the compact release-facing provenance text stays aligned with the current implementation posture.
Latest progress (2026-04-26): `Vsop87BodySourceKind` now has a stable `Display` implementation and the VSOP87 canonical evidence formatter renders the typed source-kind labels directly, which keeps the release-facing provenance text aligned with the public enum instead of a parallel string helper.
Latest progress (2026-04-26): the VSOP87 canonical J2000 evidence now also has a backend-owned equatorial companion summary, so the current source-backed planetary slice surfaces a mean-obliquity frame cross-check alongside the long-standing ecliptic envelope in validation, backend-matrix, and release summaries.
Latest progress (2026-04-26): the VSOP87 canonical J2000 and equatorial evidence summaries now also implement typed `summary_line()`/`Display` rendering, and the backend report helpers now reuse those typed records directly, which keeps the release-facing formatter ownership aligned with the other backend-owned VSOP87 summaries.
Latest progress (2026-04-26): the validation backend-matrix VSOP87 rows now also render source-kind and accuracy through the typed `Display` labels instead of enum debug spellings, so the release-facing source-profile lines stay aligned with the public enum contracts instead of ad hoc formatting.
Latest progress (2026-04-26): the VSOP87 canonical J2000 evidence now also carries a concise interim-outlier note in the release-facing validation, backend-matrix, and release summaries, so any future envelope regression can name the bodies involved without forcing maintainers to infer that from the aggregate count alone.
Latest progress (2026-04-28): that interim-outlier note is now backed by a typed canonical-outlier summary with fail-closed validation in `pleiades-vsop87`, so the compact release-facing note now uses the same drift guardrail as the rest of the source-backed VSOP87 envelopes.

Latest progress (2026-04-26): the VSOP87 generated-binary audit is now also surfaced as a backend-owned summary and reused by the validation and release summaries, so the checked-in `.bin` artifacts now carry an explicit reproducibility line alongside the source-audit and canonical evidence reports. The generated-binary summary now also carries the canonical body and source-file order directly, which makes the checked-in blob provenance a little easier to audit without reconstructing it in the reporting layer.
Latest progress (2026-04-26): the VSOP87 source-documentation, health, and body-evidence summaries now also expose typed `summary_line()`/`Display` rendering, so the release-facing report helpers can reuse backend-owned records directly instead of rebuilding those strings in validation.
Latest progress (2026-04-26): the VSOP87 source audit summary now also names the source-backed body order and public source-file set explicitly, so the compact reproducibility line can audit the exact public inputs instead of only surfacing aggregate counts.
Latest progress (2026-04-27): the VSOP87 source-audit and generated-binary audit summaries now validate their derived fields before the release-facing report wrappers render them, so provenance drift now surfaces as an unavailable report line instead of quietly formatting a stale summary record.
Latest progress (2026-04-28): the VSOP87 per-source audit records now also expose typed summary/display and self-validation helpers against the vendored source text, and the aggregate source-audit summary now preflights those records before report formatting, so a drifted single-file audit now fails closed even if the overall totals still look coherent.
Latest progress (2026-04-26): the lunar reference evidence and lunar theory date-range note now explicitly call out the J2000 mean apogee anchor alongside the existing mean-perigee and node references, so the compact lunar baseline reports show the full current lunar-point anchor set without changing the public API.
Latest progress (2026-04-26): chart snapshots now render an explicit time-scale policy line and observer-policy line, making the caller-supplied time-scale contract and the house-only observer separation visible in the default chart output instead of only in backend docs. The chart snapshot regression suite now also covers the no-observer branch, so the geocentric-body-position policy line stays exercised even when no house observer is supplied. House calculations also now classify non-finite obliquity overrides with a dedicated invalid-obliquity error kind instead of treating them as generic numerical failures, which keeps the house-semantics boundary a little clearer for chart callers. Latest progress (2026-04-30): the house-formula regression slice now also checks that Regiomontanus, Campanus, and Koch collapse to the documented sidereal-phase spacing on an equatorial, zero-obliquity chart, which keeps the equatorial-projection and prime-vertical limit behavior explicit alongside the existing quadrant and special-shape regressions. Latest progress (2026-04-27): the `ChartRequest::summary_line()` request summary now also stays geocentric when an observer is supplied without a house system, so the compact request policy mirrors the actual house-observer usage instead of implying a house-only request that will never feed house calculations. Latest progress (2026-04-27): `pleiades-core::ChartRequest` and `ChartSnapshot` now also expose typed `ObserverPolicy` helpers, so the geocentric-versus-house-only distinction is available as a reusable API instead of only as compact report text.
Latest progress (2026-04-26): chart and CLI chart-render outputs now use the shared `TimeScale` display labels for the rendered instant line and unsupported time-scale errors, so the release-facing chart output stays aligned with the typed time-scale labels instead of the enum debug spelling.
Latest progress (2026-04-26): the compact backend-matrix summary now also carries the shared time-scale, observer, apparentness, and request-policy line alongside the existing frame/zodiac posture, keeping the release-facing backend inventory aligned with the same request-shape contract that validation and release summaries already expose.
Latest progress (2026-04-26): the compact lunar source-selection, catalog, and capability summaries now also carry the typed source-family enum directly, so the current one-entry lunar baseline exposes its structured family key in addition to the human-readable label without requiring string parsing.
Latest progress (2026-04-26): the compact compatibility profile, release notes summary, and release summary now explicitly surface the latitude-sensitive house-system subset, so the current release-facing catalog view names the systems with polar failure constraints instead of leaving that information only in the detailed profile display.
Latest progress (2026-04-26): the compatibility-profile regression coverage now also pins a few additional house-system spellings that still appear in the release catalog — `Polich Page`, `Poli-Equatorial`, `Equal Quadrant`, `Meridian table of houses`, and `Whole-sign` — so the release-facing alias audit keeps the resolver coverage and compact profile output aligned as the catalog continues to expand.
Latest progress (2026-04-27): compatibility-profile verification now rejects case-insensitive duplicate labels across house systems, ayanamsas, and custom definitions, while still allowing same-entry alias spellings such as the current `Vehlow Equal`/`Vehlow equal` pair to coexist, so future catalog additions cannot silently drift into ambiguous cross-entry alias collisions even when spellings differ only by ASCII case. The same verification path now also checks that the freeform release-note, validation-reference-point, and compatibility-caveat prose sections stay pairwise disjoint, which keeps duplicated narrative from drifting between the release-facing text blocks. Latest progress (2026-04-27): the prose-section verifier now also rejects case-insensitive duplicate entries within a section and across sections, so release-note drift cannot hide behind capitalization changes alone when the compatibility profile is revalidated. Latest progress (2026-04-27): the compatibility-profile verification summary now also renders explicit release-note and validation-reference-point counts alongside the existing custom-definition and caveat totals, so the compact release-facing profile line now shows the full prose-section inventory instead of only the caveat count. The same summary path now also centralizes the release-specific house-system and ayanamsa canonical-name list wording behind a typed descriptor summary helper, which keeps the repeated count/list formatting aligned between the verification and release-summary renderers. Latest progress (2026-04-27): the compatibility-profile verifier now also checks that the baseline and release house-system/ayanamsa slices remain disjoint before the broader alias scan runs, which gives the release-profile partitioning an explicit guardrail if either catalog ever starts to overlap across milestones. Latest progress (2026-04-27): the compatibility-profile alias-mismatch diagnostics now render the house-system and ayanamsa target labels with human-readable display names, so the release-facing verification error text stays aligned with the published catalog spellings instead of leaking enum-debug forms.
Latest progress (2026-04-27): the compatibility-profile alias validator now also reports the alias spelling itself when an alias carries surrounding whitespace, and the release-facing regression coverage now pins whitespace-padded house and ayanamsa aliases explicitly so alias-path metadata drift stays visible in the same compact catalog audit.
Latest progress (2026-04-26): the compatibility-profile verification path is now also backed by a structured summary record, which keeps the release-facing verification posture typed and reusable instead of forcing each caller to reconstruct the catalog-audit line locally. The built-in house and ayanamsa descriptor records now also expose compact summary-line/Display helpers, and the compatibility-profile formatter reuses those typed records directly so the release-facing catalog sections stay co-located with the descriptor definitions.
Latest progress (2026-04-26): the compact release summary now also includes the release-specific house-system and ayanamsa canonical-name breakdowns, so the one-screen release view now mirrors the catalog profile's explicit name lists instead of only showing the aggregate counts.
Latest progress (2026-04-27): the compact release summary now also lists the custom-definition label names alongside the existing count, which keeps the nine custom-definition labels visible in the one-screen release view instead of only exposing the aggregate total. Latest progress (2026-04-27): the compact release-notes-summary smoke test now also pins the custom-definition and compatibility-caveat counts explicitly, and the release-summary smoke test now also pins the Ayanamsa total line explicitly, which keeps the compact release-facing profile audits aligned with the rendered status lines instead of only checking that the sections exist.
Latest progress (2026-04-26): the compact compatibility-profile, release-notes summary, and release-summary smoke tests now pin the exact custom-definition label count and compatibility-caveat count, so the release-facing profile summaries stay anchored to the current known-gap totals instead of only asserting that those lines exist.
Latest progress (2026-04-26): the compatibility-profile and release-notes regressions now also pin the remaining Equal-from-MC spellings — `Equal from MC`, `Equal (from MC)`, and `Equal (from MC) table of houses` — so the Midheaven-anchored equal-house forms stay visible in the release-facing catalog audit alongside the already-covered `Equal/MC` variants.
Latest progress (2026-04-26): the compatibility-profile smoke tests now also pin the Horizon/Azimuth family labels (`Horizon house system`, `Horizontal house system`, `Azimuth house system`, `Azimuthal house system`) plus the Carter and Krusinski interoperability spellings, keeping a few more release-profile house-system variants anchored in the CLI and validation renderers.
Latest progress (2026-04-26): the compatibility-profile and release-summary smoke tests now also pin `Albategnius` and `Gauquelin sectors`, keeping the remaining release-specific house-system additions visible in the compact release-facing catalog views as the broader alias audit continues.
Latest progress (2026-04-27): the same smoke tests now also pin the Babylonian (True Topc) and Babylonian (True Obs) ayanamsa spellings, keeping the Babylonian true-topocentric and true-observer release-profile variants explicitly covered in the compact catalog audit.
Latest progress (2026-04-27): compatibility-profile coverage now also pins DeLuce, Zij al-Shah, Sasanian, and Aryabhata (522 CE) release-profile spellings, extending the historical/reference-frame alias audit a little further without changing the published catalog shape.
Latest progress (2026-04-27): the compatibility-profile smoke test now also pins the custom-definition ayanamsa labels `True Balarama`, `Aphoric`, and `Takra`, so the intentional non-standard labels remain explicitly tracked in the custom-definition bucket instead of drifting into the built-in catalogs.
Latest progress (2026-04-26): the compatibility-profile smoke tests now also pin `Skydram/Galactic Alignment` and `Skydram (Mardyks)`, keeping the Galactic Center (Mardyks) alias spellings explicitly covered in the CLI compatibility-profile output as the breadth audit continues.
Latest progress (2026-04-26): the CLI and validation compatibility-profile smoke tests now also pin `Nick Anthony Fiorenza`, `Galactic Center (Cochrane)`, and `P.V.R. Narasimha Rao`, extending the release-facing galactic and equal-house label coverage a little further without changing the published catalog shape.
Latest progress (2026-04-26): the JPL snapshot backend now also publishes a backend-owned equatorial asteroid evidence slice derived from the exact J2000 asteroid rows, and the release-facing JPL evidence summaries now surface that mean-obliquity cross-check alongside the exact ecliptic asteroid evidence so the asteroid frame transform stays visible in report output. Latest progress (2026-04-28): the exact and equatorial asteroid evidence report wrappers now preflight their body order, canonical J2000 epoch, and mean-obliquity transform consistency before formatting, so the compact asteroid provenance line now fails closed if the checked-in slice drifts.
Latest progress (2026-04-26): the packaged-artifact capability profile now uses stable `Display` labels for stored channels, derived outputs, unsupported outputs, and speed policy, so the artifact summary no longer depends on `Debug` formatting for release-facing wording.
Latest progress (2026-04-26): the packaged-data backend now also publishes a backend-owned frame-treatment summary, and the artifact, validation, release, and CLI summary paths now render it alongside the existing packaged request policy so the ecliptic-only packaged-artifact posture stays explicit in release-facing provenance and summary output.
Latest progress (2026-04-26): that packaged frame-treatment summary is now a typed backend-owned record as well, so the packaged-artifact provenance line can reuse the same `summary_line()`/`Display` helper directly instead of relying on a crate-local string literal.
Latest progress (2026-04-26): the packaged-artifact profile summary now also lives as a structured backend-owned record in `pleiades-data`, and validation reuses that record instead of recomputing the packaged profile locally, which keeps the compact artifact summary aligned with the checked-in header metadata. The packaged-artifact profile summary record now also implements `Display`, which makes the typed packaged profile line reusable from downstream report code without reconstructing the same summary shape in each caller.
Latest progress (2026-04-26): the benchmark command and validation benchmark summaries now also surface workspace provenance alongside the timing numbers, so the release-facing benchmark output records source revision, workspace status, and rustc version in addition to the measured latencies and throughput. The benchmark provenance line is now also owned by a typed summary helper in `pleiades-validate`, which keeps the compact report formatting co-located with the provenance record instead of rebuilding the same three-line block ad hoc. The compact validation-report summary now also mirrors that benchmark provenance and summary block, so the shorter validation view now shows the reference/candidate/packaged/chart timings and packaged-artifact decode size without opening the full report.
Latest progress (2026-04-26): the dedicated benchmark report now also spells out the benchmark methodology for the backend and chart workloads, so the rounds, samples-per-round, and workload split are visible in the CLI benchmark output instead of only being implied by the surrounding section labels.
Latest progress (2026-04-27): the release bundle now also archives that dedicated benchmark report as `benchmark-report.txt` with its own checksum, so the staged release bundle carries the benchmark evidence alongside the validation report instead of only the in-memory CLI output.
Latest progress (2026-04-29): the release-bundle help text and reproducibility guide now explicitly name the benchmark report artifact alongside the staged bundle contents, which closes a small documentation drift between the generated bundle and the operator-facing release instructions.
Latest progress (2026-04-29): the public `ReleaseBundle` metadata now also validates the expected artifact paths, required provenance fields, and nonzero validation-round count before the verified bundle object is returned, so the staged release-bundle surface now fails closed even if a caller mutates the in-memory bundle after verification.
Latest progress (2026-04-26): the shared `pleiades-types` time-scale and coordinate-frame enums now have stable `Display` labels, and the validation report helpers now reuse them for compact instant/frame listings instead of duplicating the same string-matching logic.
Latest progress (2026-04-26): mixed TT/TDB batch requests now preserve the per-request scale on the VSOP87, ELP, and JPL paths, so the time-scale contract stays explicit even when a single call mixes dynamical-time labels.
Latest progress (2026-04-26): backend-family and accuracy labels now have stable `Display`-backed renderings in `pleiades-backend`, and the validation backend-matrix formatter now uses those public labels directly so the release-facing metadata lines stay consistent with the shared backend contract.
Latest progress (2026-04-26): backend capability flags now have a shared backend-owned `summary_line()`/`Display` helper in `pleiades-backend`, and the validation backend-matrix capability row now reuses that typed record directly so the compact capability posture stays centralized instead of being rebuilt locally.
Latest progress (2026-04-26): `EphemerisErrorKind` now has stable `Display` labels and both the backend error type and validation error-kind lists render those labels directly, which keeps structured request failures and release-facing expected-error-kind lists aligned without relying on `Debug` formatting.
Latest progress (2026-04-26): the packaged-data backend now also accepts TDB-tagged batch requests and returns the same decoded coordinates as the TT-grid lookup path, so the current time-scale policy is now exercised on the positive path in addition to the existing rejection coverage. The single-request packaged-data path now has the same TDB acceptance regression too, which keeps the positive time-scale policy covered outside batch-only lookups. The packaged-data release-facing report paths now also surface a mixed TT/TDB batch-parity summary, which keeps the positive batch-scale contract visible alongside the request-policy lines instead of only in the unit test. The VSOP87, ELP, and packaged-data batch paths now also reject apparent-place and topocentric requests explicitly, so the shared request-policy gate is exercised through `positions()` as well as single-request lookups for the remaining mean-only first-party backends.
Latest progress (2026-04-26): zodiac-mode validation is now centralized in the backend request-policy helper, so the tropical-only first-party backends reject sidereal requests with a consistent structured `InvalidRequest` error instead of repeating manual checks in each backend crate. Zodiac-mode rendering in chart snapshots now uses the shared typed display label, and the validation backend-matrix time-scale listing now also uses the shared `Display` labels instead of debug formatting so the release-facing request-policy text stays aligned with the typed enums.
Latest progress (2026-04-26): the JPL snapshot and packaged-data frame-treatment helpers now return bare notes while the validation and artifact summaries add the release-facing label once, so the compact provenance text no longer repeats the frame-treatment prefix.

Latest progress (2026-04-25): the JPL snapshot backend now exposes coarse leave-one-out interpolation quality samples derived from the checked-in fixture, and backend matrix release artifacts render those expanded public-input error checks. The JPL fixture itself also now includes an additional 2500000.0 TDB epoch across the comparison-body set, and the checked-in comparison window now also includes a 2400000.0 epoch across the Sun-through-Pluto bodies, which widens the hold-out coverage while keeping the backend geocentric and pure Rust. The JPL backend now prefers cubic interpolation on four-sample windows when the fixture has enough same-body epochs, with quadratic and linear fallbacks for smaller windows, so the reference fixture can serve as a slightly stronger pure-Rust interpolator without widening the public contract. The compact validation report summary also now carries a JPL interpolation-quality envelope, so the current leave-one-out evidence is visible alongside the broader comparison summaries. The JPL interpolation-quality summary now also names the worst-case body for each error metric, which makes the reported envelope easier to audit without changing the evidence corpus. The JPL interpolation-quality samples now also derive from leave-one-out runtime interpolation against held-out exact rows, so the release-facing evidence now measures the backend's actual interpolation path rather than only a linear counterfactual. The same summary now also distinguishes cubic, quadratic, and linear fallback tiers, so the checked-in fixture can show which interpolation path each held-out sample exercised. The validation report and backend matrix summaries now also surface body-class error envelopes alongside the per-body summaries, and the comparison report now adds a body-class tolerance posture that counts within/outside-tolerance bodies by class and names the outliers, so release-facing validation output shows coarse luminary/planet grouping in addition to the per-body and tolerance tables. The compact release summary now also carries a short validation evidence line with comparison sample and tolerance counts, and now names the comparison-audit status explicitly, so the release-facing overview exposes a bit more of the current error envelope without expanding into the full report. The same compact summary now also surfaces the body-named comparison envelope, so the one-screen overview mirrors the detailed comparison report a little more closely while broader threshold refinement remains outstanding. The same release summary now also surfaces the JPL interpolation-quality envelope, so the compact overview keeps the reference backend's leave-one-out evidence visible alongside the comparison totals. The same release summary now also surfaces JPL reference snapshot coverage, exact asteroid evidence, VSOP87 source evidence, and the lunar reference/equatorial error envelopes, so the one-screen release view now carries the missing source-backed evidence tables and frame-conversion envelope alongside the already visible comparison and tolerance data. The compact release overview now also carries the body-class tolerance posture and expected tolerance status so release-facing evidence shows the current outlier classes and body counts without opening the full validation report. The backend-matrix summary now also distinguishes bounded versus open-ended nominal ranges, which keeps the packaged-data versus open-ended backend posture visible in the compact catalog view. The compact release summary now also surfaces the mean longitude, latitude, and distance deltas alongside the maxima, which gives the one-screen error envelope a bit more shape without changing the comparison corpus. The body-class tolerance posture now also prints the candidate backend family and class-level tolerance profile directly, which keeps the coarse class audit aligned with the backend-scoped expectation table. The JPL interpolation-quality summary now also carries the worst-case epoch for each measured envelope, and now reports mean bracket-span and mean error metrics alongside the maxima, so the report can name both the body and the held-out instant that produced the current interpolation peak while also exposing the average leave-one-out envelope. The same summary now also reports RMS longitude, latitude, and distance error so the leave-one-out envelope shows both central tendency and spread without widening the fixture. The same summary now also reports median bracket-span and median error magnitudes, giving the release-facing interpolation envelope a compact central-tendency view alongside the mean and RMS figures.

Latest progress (2026-04-26): the same JPL interpolation-quality summary now also includes 95th-percentile bracket-span and error tails, giving the transparency evidence a compact upper-tail view alongside the existing mean/median/RMS envelope.
Latest progress (2026-04-26): the JPL snapshot backend now also publishes an independent Mars/Jupiter/Saturn hold-out corpus from the same public Horizons source material, and the validation/release evidence now renders that separate hold-out interpolation envelope alongside the existing leave-one-out transparency summary, so the JPL interpolation story now shows both internal and externalized checks without changing the public backend contract.
Latest progress (2026-05-01): the independent JPL hold-out corpus now also carries later-epoch Sun, Mercury, and Venus rows at 2634167.0 TDB, which widens the checked-in derivative fixture to 27 rows while preserving the first-seen body order and the mixed-scale parity checks.
Latest progress (2026-04-26): the JPL interpolation-quality sample corpus now also has a batch-path regression, so the same hold-out epochs that feed the transparency summary are now exercised through `positions()` with preserved order and interpolated-quality results.
Latest progress (2026-04-26): the independent JPL hold-out summary now also labels itself as transparency evidence rather than a production tolerance envelope, which keeps the release-facing wording aligned with the rest of the JPL validation surface.
Latest progress (2026-04-26): that same independent JPL hold-out evidence is now surfaced in the full validation report, compact validation summary, and release summary alongside the leave-one-out interpolation evidence, so the externalized Horizons check is visible in the release-facing report set instead of living only in the backend-owned helper. The same combined evidence line now also surfaces the independent hold-out manifest summary, which keeps the checked-in public-input provenance explicit alongside the source, coverage, and interpolation envelopes.
Latest progress (2026-04-30): the JPL snapshot manifest summaries now also travel through a typed wrapper in `pleiades-jpl`, and the comparison/reference/hold-out report helpers reuse that validated record directly so the compact provenance lines fail closed on label drift as well as manifest drift.
Latest progress (2026-04-26): the JPL snapshot backend now also has a batch-path regression over the full checked-in reference snapshot in equatorial mode, preserving exact order and exact-coordinate parity across every reference row while exercising the mean-obliquity transform on the complete public-input fixture. The same reference snapshot now also has a full ecliptic batch-path regression, so both coordinate frames are now exercised across the entire checked-in corpus instead of only the asteroid subset. The mixed-frame batch regression is now also surfaced as a backend-owned summary and reused by validation/release reporting, so the alternating ecliptic/equatorial parity evidence is visible alongside the equatorial snapshot line.
The VSOP87 backend matrix entry now also prints canonical J2000 source-backed evidence for Sun-through-Neptune at the same public IMCCE reference points used by the regression tests, so the vendored Earth-through-Neptune source-file paths now carry exact evidence from public IMCCE VSOP87B inputs while Pluto remains the remaining mean-element fallback. Compact validation summaries now also surface the same source-backed VSOP87 evidence line, now include a concise source-documentation count for the structured VSOP87 body profiles, and now add a compact body-profile evidence count so the release-facing summaries carry measured-error and provenance snapshots without duplicating the full backend matrix detail. The VSOP87 crate now also exposes the canonical J2000 body-evidence envelope as public structured data, and the validation layer consumes that shared release-facing summary instead of recomputing the same envelope privately. The validation report and backend matrix summaries now also distinguish the generated-binary VSOP87 source-backed body paths from the Pluto mean-element fallback, making the current source state clearer without changing the public backend contract. The backend matrix output itself now pairs each source-backed body profile with its source file, provenance, and measured canonical deltas so the validation reports make the body/profile linkage explicit. The compact ELP backend now also publishes a structured lunar-theory specification with supported bodies, provenance, redistribution posture, and date-range notes, and the validation backend matrix/summary now render that structured selection explicitly so the current Moon/node/apogee/perigee baseline is visible in release-facing metadata while full ELP coefficient selection remains pending. Validation-side compatibility-profile tests now also pin several recent release-profile entries — including the Equal (MC) and Equal (1=Aries) house-table forms, Pullen SR, True Citra Paksha, P.V.R. Narasimha Rao, and B. V. Raman, Raman Ayanamsha, and Raman ayanamsa — so the release-facing catalog text stays anchored to the currently published breadth. The compatibility-profile command now also pins additional release-profile spellings such as Equal table of houses, Whole Sign system, Morinus house system, Galactic Equator (Fiorenza), Valens Moon, and Babylonian (House Obs), extending the release-facing breadth checks without changing the verification model. The same slice now also pins the Placidus, Koch, Whole Sign, and Sunshine table-of-houses spellings so a few more exact source-label variants remain covered as the catalog grows. The compatibility-profile and release-notes regressions now also pin the exact Polich-Page \"topocentric\" table of houses, T Polich/Page (\"topocentric\"), Babylonian Huber, Galactic Equator (True), and Valens Moon ayanamsa spellings surfaced by the current release profile, keeping the source-label appendix text anchored as more catalog breadth lands. The same verification slice now also anchors the Galactic Center (Rgilbrand), Galactic Center (Mardyks), Galactic Center (Mula/Wilhelm), Suryasiddhanta (499 CE), Aryabhata (499 CE), Sassanian, PVR Pushya-paksha, and Galactic Center (Cochrane) reference entries so the release-profile audit stays tied to the current historical/reference-frame coverage. The VSOP87 source documentation is now also structured enough for release reports to show variant, frame, unit, reduction, and date-range notes per source-backed body, which gives the future generated-table work a machine-readable documentation hook rather than just freeform provenance text. The VSOP87 source-backed body catalog is now centralized in one internal table, and the public profile/spec/sample accessors derive from that single source of truth so future generated-table ingestion has one shared catalog to extend. The VSOP87 metadata now also carries an explicit frame-treatment note so the J2000 ecliptic/equinox source frame and the mean-obliquity equatorial transform stay visible in release-facing reports. UTC-tagged chart and instant inputs now also have explicit caller-supplied TT conversion helpers, closing the remaining UTC convenience gap in the current time-scale policy surface without adding built-in leap-second or Delta T modeling. The validation report corpus summaries now also print explicit epoch labels with time-scale tags, so the current release-facing reports surface the time-scale choice alongside each corpus window. `pleiades-types::TimeRange` now also has a typed `summary_line()`/`Display` helper, and the lunar validation/reporting paths now reuse that shared range renderer instead of duplicating the same epoch-window wording locally, which keeps the validation and lunar provenance strings a little more consistently typed. The compact validation report summary and comparison-audit output now also name the regression bodies directly, which makes the current release-facing tolerance evidence easier to scan without changing the underlying comparison corpus. The comparison audit now also mirrors the body-class error envelopes, body-class tolerance posture, and tolerance policy sections so release-failure output carries the same coarse envelope detail as the full validation report. The Expected tolerance status section now also reports signed margins to each class limit alongside the measured maxima, which makes the remaining headroom or overshoot explicit in release-facing output without changing the comparison corpus. The JPL interpolation-quality summary now also lives in `pleiades-jpl`, and validation now reuses the backend-owned summary/formatter instead of recomputing the same envelope privately, so the interpolation evidence follows the same ownership pattern as the lunar reference summary. The backend now also publishes a distinct-body interpolation-kind coverage helper for the cubic/quadratic/linear tiers, which gives the hold-out transparency slice a small extra audit hook without expanding the fixture. The rendered interpolation-quality summary now also calls out that it is transparency evidence rather than a production tolerance envelope, keeping the release-facing wording explicit about the current evidence class. The compact VSOP87 body-evidence summary now also calls out any bodies outside interim limits explicitly (or `none` when all measured bodies are within the interim envelope), which makes the release-facing source-backed audit a little easier to scan at a glance. The same summary is now owned by `pleiades-vsop87` as a structured backend helper, and validation reuses it directly instead of recomputing the source-kind and interim-limit counts in the tooling layer. The VSOP87 source documentation, audit, canonical evidence, and body-evidence report strings are now all backend-owned too, so the release-facing output stays co-located with the source metadata instead of being rebuilt in validation. The source-documentation summary now also carries the fallback-body list as structured data, which keeps the Pluto fallback name available without recomputing it from the body-profile table in the reporting layer.
Latest progress (2026-04-30): the canonical VSOP87 body-evidence rows now expose typed summary/validation helpers and the canonical-evidence builder now preflights them before the aggregate summaries render, which keeps the per-row source provenance, source-file pairing, and interim-limit status fail-closed instead of trusting only the aggregate envelope.

Latest progress (2026-04-26): the VSOP87 source-documentation summary now also enumerates the source-backed body list explicitly, so the compact release-facing provenance line now names the current Sun-through-Neptune source-backed set alongside the fallback Pluto path without requiring the backend matrix to be opened.
Latest progress (2026-04-26): the VSOP87 backend-matrix implementation note now correctly describes the Sun-through-Neptune generated-binary paths as derived from the vendored full-file inputs, keeping the compact release-facing backend status aligned with the runtime path.
Latest progress (2026-04-26): the VSOP87 source-documentation health check now also verifies the documented variant, coordinate-family, frame, units, reduction, transform-note, truncation-policy, and date-range invariants in addition to the catalog counts, so the compact release-facing provenance can flag documentation drift as well as body-count drift. The same health summary now also carries the source-backed and fallback body lists directly, so the release-facing provenance line can name the current partition without reconstructing it in validation. The health formatter now also emits explicit issue labels when the catalog ever drifts, which keeps the failure mode actionable without changing the current consistent-path wording.

Latest test progress (2026-04-25): the JPL snapshot backend now has exact J2000 regression coverage for the named asteroid subset plus the custom 433-Eros entry, so the checked-in reference fixture now exercises Ceres, Pallas, Juno, Vesta, and the source-backed custom asteroid path with explicit golden coordinates. The same asteroid slice is now also covered by a batch-path regression, so `positions()` preserves the checked-in reference order as well as the single-request golden values.

Latest test progress (2026-04-25): the JPL comparison fixture now also includes an added 2600000.0 TDB Mars hold-out epoch on top of the 2400000.0 TDB comparison epoch, expanding the checked-in reference snapshot to 46 rows across 15 bodies and 6 epochs and the leave-one-out interpolation envelope to 21 samples across 10 bodies while keeping the backend geocentric and pure Rust.

Latest test progress (2026-04-25): the lunar backend now also covers its Moon/node/apogee/perigee reference slice through `positions()`, so the current compact lunar theory baseline is exercised by both batch and single-request queries. The same backend now also rejects true apogee and true perigee through the batch API with the same structured `UnsupportedBody` error used for single-request calls, which keeps the deferred lunar channels explicit even when callers submit them through `positions()`. The current source-selection record is now also exposed through a direct `lunar_theory_source_selection()` accessor, which makes the structured lunar provenance fields easier to reuse in backend-owned reporting. The selected Meeus-style family is now also available through a dedicated source-family summary helper, which keeps the current baseline label visible as its own typed provenance line.

Latest progress (2026-04-25): packaged-artifact summaries now surface the conservative artifact profile directly in `artifact-summary`, `validation-report-summary`, and `release-summary`, and now also carry an explicit packaged request-policy note for the current geocentric, ecliptic, TT/TDB, tropical, mean-only lookup surface so the compressed-artifact posture stays honest about its TDB handling without widening the production artifact claim. The packaged-data request policy is now also backed by a structured summary record with an explicit lookup-epoch policy label, and validation/artifact reporting reuses that record so the geocentric-only, TT/TDB, ecliptic, tropical, mean-only posture stays aligned with the metadata. The packaged-data backend metadata now also uses a shared provenance helper that names the full 11-body packaged set explicitly, which removes the last stale Sun/Moon-only wording from the packaged-artifact source summary. The packaged backend now also loads a checked-in binary artifact fixture, and the regression suite compares that fixture against a deterministic regeneration helper from the checked-in JPL snapshot so the packaged-data path is now fixture-backed instead of hand-authored sample segments. The compact artifact summary now also carries the expected-tolerance rows and comparison tolerance audit counts, so the release-facing artifact view exposes the current regression posture without forcing a full validation report.
Latest progress (2026-04-26): the packaged-data backend now advertises equatorial-frame support and reconstructs equatorial coordinates from the stored compressed ecliptic channels at query time, so the artifact profile, runtime request policy, and frame-treatment summary now align on the stored-versus-derived contract instead of implying an ecliptic-only packaged surface. Latest progress (2026-04-30): the packaged frame-treatment report path now also uses a dedicated `packaged_frame_treatment_summary_for_report()` helper, which keeps the compact label once-only and the report wording co-located with the backend-owned summary record. Latest progress (2026-04-30): `pleiades-compression` now also exposes a dedicated packaged-artifact profile alias for the stored-ecliptic / derived-equatorial shape, and `ArtifactHeader::new()` now uses that named helper so the current packaged posture has an explicit identifier in the codec API without changing the artifact layout.
Latest progress (2026-04-27): the packaged-data backend metadata now also routes its storage/reconstruction provenance through a typed backend-owned summary record, so the last packaged-artifact data-source line no longer relies on a crate-local string literal.
Latest progress (2026-04-29): the packaged-data backend now also publishes a typed packaged-artifact access summary that states whether external artifact-path loading is enabled, and the backend provenance list reuses it so the release-facing packaged-data posture makes the feature-gated external-artifact path explicit alongside the checked-in fixture. Latest progress (2026-04-29): the validation, release-notes, and release-summary smoke coverage now also pins that packaged-artifact access posture line directly, which keeps the feature-gated external-artifact flag visible in the compact release-facing audit paths as well as the backend provenance helper. Latest progress (2026-04-30): that packaged-artifact access summary now also exposes a validated summary-line helper, and the report-facing access renderers reuse it so the feature-gated external-artifact posture stays on the same fail-closed path as the other typed summary records instead of re-checking only at the display boundary. Latest progress (2026-04-30): the validation, release-notes, and release-summary smoke coverage now exact-pins the packaged-artifact generation policy and residual-body lines too, which keeps the Moon-only residual-correction posture fail-closed in the compact report surfaces instead of only substring-matched.
Latest progress (2026-04-29): the packaged mixed-frame and mixed TT/TDB batch-parity slices now expose public request-corpus helpers plus compatibility aliases, which keeps the exact packaged batch shapes reusable by downstream validation and reproducibility tooling instead of only by the summary constructors.
Latest progress (2026-04-29): the compressed-artifact output-support model now also distinguishes stored motion from derived motion, so future packaged artifacts can surface a stored-vs-derived speed policy explicitly while the current packaged fixture remains unchanged.

Latest progress (2026-04-25): the `pleiades-elp` Moon path now uses a Meeus-style truncated lunar position series instead of the earlier simplified orbital surrogate, and the backend now validates against the published 1992-04-12 geocentric Moon example in addition to the existing J2000 lunar-point checks. The lunar-theory metadata now also carries an explicit Meeus citation plus a conservative license/provenance note, which makes the current source-selection posture easier to audit while true ELP coefficient selection still remains pending.

Latest progress (2026-04-25): the checked-in JPL reference snapshot now also publishes a compact coverage summary with total rows, distinct bodies, distinct epochs, asteroid-row count, and epoch range, and the validation/release-facing summaries now surface that snapshot coverage alongside the exact asteroid evidence rows. The same reference snapshot summary now also carries the first-seen body order, and the equatorial-parity summary reuses that structured body list so release-facing JPL provenance can name the covered bodies alongside the row and epoch counts.
Latest progress (2026-04-25): the JPL snapshot coverage and exact asteroid evidence summaries now also label their reference epochs with TDB in release-facing output, which keeps the checked-in JPL snapshot aligned with the current time-scale policy while broader corpus expansion remains queued.

Latest progress (2026-04-25): the checked-in JPL asteroid subset now renders exact J2000 evidence rows in validation reports, backend matrices, and release notes, so the named asteroid coverage is visible as coordinates rather than only as body labels. The JPL snapshot coverage and exact asteroid evidence summaries now also live in `pleiades-jpl` as backend-owned report helpers, and validation reuses those strings directly so the JPL coverage text follows the same ownership pattern as the lunar and VSOP87 evidence summaries.

Latest progress (2026-04-25): the compact ELP lunar backend now also exposes a canonical lunar evidence slice for the published 1992-04-12 Moon example plus J2000 lunar-point references, and the validation reports now render that evidence alongside the backend matrix and summary output so the current lunar baseline is easier to audit without changing the public API. The lunar reference summary now also prints the JD 2419914.5..2459278.5 coverage window explicitly, which makes the current 1913-node-to-2021 evidence span easier to scan in release-facing output. The lunar evidence summary and formatter now also live in `pleiades-elp`, so validation reuses the backend-owned summary instead of carrying a duplicate copy of the same counts and date range. The same compact lunar slice now also carries a dedicated equatorial cross-check against the published 1992-04-12 Moon RA/Dec example, and the backend matrix summary surfaces that equatorial evidence explicitly so the shared mean-obliquity transform stays visible in release-facing output. The compact lunar theory date-range note now also names the reference-only 1968-12-24 apparent geocentric Moon comparison datum, keeping the broader validation slice visible in the release-facing provenance line.

Latest progress (2026-04-25): the same lunar backend now also publishes a dedicated high-curvature continuity evidence slice around J2000, and validation/backend-matrix summaries render that regression window explicitly so the nearby-motion check stays visible alongside the canonical Moon, node, and perigee evidence. The backend and release summaries now also render a companion equatorial continuity slice for the same J2000 window, which keeps the mean-obliquity frame check visible alongside the ecliptic motion check. The lunar reference and equatorial error envelopes now also name any outlier bodies explicitly when regression limits are exceeded, so the release-facing lunar audit follows the same body-naming pattern as the VSOP87 and JPL envelopes even when the current canonical slices remain within limits. Latest progress (2026-04-27): the lunar high-curvature continuity evidence slices now also expose backend-owned typed `summary_line()`/`Display` helpers, and the report formatter reuses those typed records directly so the nearby-motion regression wording stays centralized in `pleiades-elp` rather than being rebuilt only as ad hoc report text. Latest progress (2026-04-27): those same high-curvature continuity summaries now also preflight their sample/body/epoch windows before report formatting, so future drift in the compact regression metadata now falls back to an unavailable report line instead of emitting stale continuity evidence. Latest progress (2026-04-27): the underlying J2000 continuity window is now sampled on a denser six-point half-day grid, which gives the nearby-motion regression a little more temporal resolution while keeping the same compact release-facing summary shape. Latest progress (2026-04-29): the high-curvature continuity envelopes now also validate their stored regression-limit flags against the derived step thresholds, so the compact nearby-motion evidence fails closed if the summary bool drifts away from the actual maxima. Latest progress (2026-04-30): the high-curvature ecliptic and equatorial continuity slices now also expose plain `request_corpus` compatibility aliases, which keeps those fixed Moon regression windows discoverable through the same corpus naming pattern as the other release-facing helpers without changing the batch shape.

Latest progress (2026-04-25): the shared `pleiades-types::EclipticCoordinates::to_equatorial` helper now centralizes the mean-obliquity frame rotation used by the VSOP87 and ELP backends, reducing duplicate transform logic while keeping the current ecliptic-to-equatorial behavior explicit and test-covered. The shared type layer now also exposes the inverse mean-obliquity rotation as `EquatorialCoordinates::to_ecliptic`, which makes frame round-trips easier to test without changing the current approximate obliquity policy. Raw backend requests now also default to mean geometric output, which keeps the request model aligned with the current mean-only first-party backends instead of nudging callers toward unsupported apparent-mode defaults.

Latest progress (2026-04-25): the `pleiades-cli` chart command now accepts an explicit `--tdb` instant tag in addition to its default TT-tagged request, so chart reports can surface a TDB-tagged instant directly instead of implying a hidden conversion policy. The same chart path now also accepts `--tt-from-tdb-offset-seconds` for explicitly re-tagging a TDB instant as TT before assembly, which keeps the caller-supplied TDB-to-TT policy surface symmetric with the existing TT/TDB helpers.

Latest progress (2026-04-25): `pleiades-types` and `pleiades-core::ChartRequest` now also expose caller-supplied TT-to-TDB conversion conveniences alongside the existing UTC/UT1 helpers, so TDB-tagged validation or backend requests can stay explicit without introducing a built-in relativistic model. Signed TDB-TT variants now round out the same surface for TT-, UT1-, and UTC-tagged requests, and the same TDB-to-TT path now also has an explicit `*_signed` alias for the signed-offset naming convention used by the other helpers, which keeps the explicit conversion policy usable at epochs where the correction is negative without forcing callers to pre-quantize it into a non-negative duration. Signed UT1-to-TT and UTC-to-TT helpers now also exist in the same typed surface, so historical or externally modeled Delta T policies can stay explicit even when the chosen correction is negative.
Latest progress (2026-04-27): the signed time-scale conversion helpers now also reject non-finite offsets with a structured conversion error, so caller-supplied Delta T or TDB-TT policy values fail explicitly instead of propagating `NaN` or infinite instants through the chart and backend request surface. The chart façade's signed TT/TDB retagging helpers now mirror that same policy too, so the builder API rejects non-finite offsets before they can reach the backend request surface.

Latest progress (2026-04-25): UT1-tagged callers now also have a direct caller-supplied UT1-to-TDB convenience in `pleiades-types` and `pleiades-core::ChartRequest`, so applications that stage a TDB request from UT1 can compose the same explicit TT-UT1 and TDB-TT policy steps without hidden time-scale logic.

Latest progress (2026-04-25): the `pleiades-cli` chart command now exposes explicit `--utc` / `--ut1` instant tags plus caller-supplied `--tt-offset-seconds` and signed `--tdb-offset-seconds` flags, so the command-line chart workflow can exercise the same conversion policy without implying a built-in Delta T or relativistic model.

Latest progress (2026-04-25): the VSOP87 and compact lunar backend paths now accept both TT and TDB dynamical-time requests while still rejecting UT-based requests explicitly, so the current phase-1 time-scale policy is a little less frictionful for validation and chart callers without adding a built-in Delta T or relativistic conversion model. The backend crates now also share centralized request-policy helpers for time-scale, frame, apparentness, and observer validation, which reduces duplicated guardrail code while keeping the same structured error kinds. The shared batch adapter now also has a fail-fast regression that preserves structured error kinds when a request sequence contains an unsupported body, so chart-style batch queries keep the same explicit failure semantics as single-request calls. The same batch path now also preserves mean-only backends' explicit `InvalidRequest` rejection for `Apparentness::Apparent`, so unsupported apparentness still fails with a structured backend error even when callers submit the request through `positions()`.

Latest progress (2026-04-25): the time-observer and lunar-theory policy docs now mirror the TT/TDB acceptance behavior, so the public policy notes no longer imply TT-only backend handling while the caller-supplied conversion model remains explicit. The shared time-scale helpers now also include caller-supplied UTC-to-TDB conveniences in `pleiades-types` and `pleiades-core::ChartRequest`, so civil-time chart inputs can be lifted to TDB through explicit TT-UTC and TDB-TT policy steps without introducing a built-in conversion model. Chart assembly now also has a regression proving house observers are used for house calculations only and are not forwarded into geocentric body-position backend requests, keeping the observer/topocentric separation explicit in the façade layer.

Latest progress (2026-04-25): the validation-report-summary and release-summary views now also render an explicit time-scale policy line summarizing the TT/TDB direct-request posture and caller-supplied UTC/UT1 conversion model, so the release-facing report text mirrors the documented Delta T policy more directly.
Latest progress (2026-04-26): those same validation, release, and backend-matrix summaries now also render an explicit observer policy line, keeping the geocentric-only / houses-only separation visible in the compact report views alongside the time-scale policy note. The backend matrix now also renders a typed family-posture line (`algorithmic`, `data-backed`, `routing`, or `other`) derived from `BackendFamily`, so the compact inventory makes the data-backed-versus-formula-driven distinction explicit instead of implying it from the raw family label. That posture now also has a dedicated `BackendFamilyPosture` enum in `pleiades-backend`, so downstream report code can reuse the typed posture classification instead of only the derived string label.
Latest progress (2026-04-26): the same compact validation, release, and backend-matrix summaries now also render an explicit apparentness policy line, so the current mean-only first-party posture is visible alongside the time-scale and observer notes without opening the full report. Chart snapshots now also render a matching apparentness policy line, which keeps the chart-level summary aligned with the same mean/apparent contract before backend-specific details are inspected. The same compact validation, release, and backend-matrix summaries now also surface an explicit frame policy line, which makes the mean-obliquity ecliptic/equatorial contract visible beside the time-scale and observer notes without needing to open the backend-specific frame-treatment sections. Mixed-frame batch regressions now also cover the JPL, VSOP87, and ELP source-backed backends, so `positions()` is explicitly exercised with alternating ecliptic and equatorial requests instead of only uniform-frame batches. The same comparison-report family now also surfaces a 95th-percentile tail envelope for absolute comparison deltas, which gives the release-facing error audit one more shape metric without changing the underlying corpus. The lunar evidence sections in those summaries now also use shorter section labels, which trims a bit of repeated wording without changing the backend-owned evidence strings themselves.
Latest progress (2026-04-27): the shared validation corpus summaries now also expose a typed one-line summary helper and `Display`, which keeps the benchmark-corpus metadata reusable from release-facing report code instead of only through the detailed multi-line formatter.
Latest progress (2026-04-26): the CLI compatibility-profile smoke tests now also pin `Equal (cusp 1 = Asc)`, `Equal from MC`, `WvA`, and `Gauquelin table of sectors`, keeping the command-line release view aligned with the current source-label appendix and interoperability aliases that the validation-side catalog already exercises.
Latest progress (2026-04-26): the compatibility-profile regression coverage now also pins the True Chitra Paksha and True Chitrapaksha spellings alongside the already-covered True Citra Paksha alias batch, so the release-facing ayanamsa appendix keeps that interoperability trio explicitly covered in both the CLI and validation smoke tests.
Latest progress (2026-04-26): the compatibility-profile smoke tests now also pin True Sheoran, closing the remaining release-profile spelling for that ayanamsa variant in the CLI and validation audit paths.
Latest progress (2026-04-26): the compatibility-profile regression coverage now also pins Aryabhatan Kaliyuga, Krishnamurti-Senthilathiban, the Sri / Shri Yukteshwar transliterations, and the De Luce spelling, so a few more historical ayanamsa label variants stay anchored in the CLI and validation smoke tests.
Latest progress (2026-04-26): the lunar apparent comparison evidence summary now also records the peak epoch for each reported delta axis, so the reference-only apparent Moon comparison can point at the exact sample timing that drives each envelope instead of only showing the max magnitudes. The same apparent-comparison record now also has a backend-owned `summary_line()`/`Display` helper in `pleiades-elp`, and validation now reuses that typed line directly, which keeps the compact release-facing wording aligned with the other backend-owned lunar evidence summaries instead of reconstructing the sentence locally.
Latest progress (2026-04-27): the apparent comparison envelope now also reports mean, median, and 95th-percentile absolute deltas for longitude, latitude, distance, right ascension, and declination, so the release-facing lunar comparison slice now has the same compact spread view as the other lunar evidence envelopes. The same apparent-comparison summary now also validates itself against the checked-in evidence slice before the report wrapper formats it, so any drift in the derived sample count or peak axes now falls back to an unavailable report line instead of emitting stale apparent-comparison prose.
Latest progress (2026-04-26): `pleiades-data` now exposes a public pure-Rust regeneration helper for the packaged artifact, and `pleiades-cli` can write the checked-in packaged fixture back to disk from the reference snapshot with an explicit `regenerate-packaged-artifact` command, giving Phase 2 a concrete maintainer-facing regeneration path instead of only a test-only helper. The regeneration output now also surfaces the packaged body set explicitly alongside the JPL reference snapshot coverage, so the maintainer-facing provenance line names both the output corpus and the public-input lineage.
Latest progress (2026-04-26): the same regeneration provenance now also reports the packaged artifact format version, checksum, and the current linear-segment generation policy, so the compact release-facing regeneration line carries both integrity evidence and the current deterministic fit rule.
Latest progress (2026-04-26): the VSOP87 request-policy and unsupported-body error text now use the production backend label instead of the older MVP phrasing, keeping release-facing request failures aligned with the source-backed backend state. The backend matrix, validation report summary, and release summary also now render a backend-owned VSOP87 request policy line, so the current mean-only tropical geocentric/ecliptic posture is visible alongside the existing frame and source evidence.
Latest progress (2026-04-26): the JPL snapshot backend now also has batch-path regressions for unsupported time scales, observer-bearing requests, and apparent-place requests, so the shared request-policy posture is covered through `positions()` as well as single-request lookups. The checked-in JPL reference and hold-out CSV comments are now parsed into structured manifest metadata too, and the release-facing evidence helper now surfaces the hold-out source line from that metadata so the public-input provenance is less hardcoded. The parsed JPL snapshot manifests are now also exposed through public summary-line helpers, so downstream code can reuse the checked-in reference and hold-out provenance metadata directly instead of treating the fixture headers as report-only strings. The comparison snapshot source material is now also available as a backend-owned typed summary, which keeps the comparison provenance line aligned with the other JPL snapshot records and lets the detailed validation report reuse the same typed line instead of rebuilding the fallback/source/coverage fields locally. Latest progress (2026-04-27): the JPL snapshot manifest parser now preserves blank column slots, so malformed provenance headers can now fail through the structured `BlankColumn` validation path instead of being silently normalized away. The same provenance pass now also rejects duplicate column names after trimming, so the checked-in manifest can no longer silently advertise the same field twice. The same provenance pass now also rejects blank coverage comments and normalizes malformed coverage/source/title labels in compact summary output, tightening the manifest-side release text before it is reused in validation and release summaries. Latest progress (2026-04-27): the JPL snapshot loader now also rejects duplicate body/epoch rows with a structured duplicate-entry error before interpolation can reuse the same sample twice, which keeps the checked-in reference fixtures a little safer to regenerate and audit. The duplicate-entry diagnostic now also carries the first-seen line number, so the malformed-row error points to both the original and repeated sample. The same loader now also treats blank body cells as a distinct structured parse error instead of folding them into the generic unsupported-body path, which keeps malformed snapshot rows a little easier to diagnose during regeneration.
Latest progress (2026-04-27): the JPL manifest report helpers now validate the parsed snapshot manifest before rendering the release-facing summary, so malformed provenance headers can no longer leak a formatted report line when the cached fixture is reused. The matching reference, comparison, and hold-out source-summary report wrappers now apply the same manifest preflight before formatting their provenance lines, which keeps the release-facing source-material summaries from quietly rendering against an invalid checked-in header. The comparison snapshot source summary itself now also validates its source, coverage, and column labels before the report wrapper formats them, so the compact comparison provenance line now fails closed instead of relying on the derived summary string alone. The reference snapshot coverage summary itself now also exposes a typed `validate()` helper with a body-count, epoch-range, and asteroid-row consistency check, so the checked-in coverage record can fail fast if its internal invariants ever drift. The interpolation-quality and independent hold-out summaries now apply the same sort of typed validation before their compact report wrappers render them, which keeps the JPL transparency evidence from formatting a stale derived count if the underlying summary state ever drifts. The interpolation-quality and independent hold-out summary validators now also reject duplicate and blank body labels, and the interpolation-quality path now fails closed on non-finite metric values, which keeps the release-facing JPL transparency evidence a little harder to drift into a stale state. The same JPL transparency summaries now also reject blank peak-body provenance labels when those peak metrics are populated, so the compact maximum-error wording fails closed if the peak body names ever disappear from regenerated evidence.
Latest progress (2026-04-30): the reference, comparison, and hold-out JPL source-material summaries now also expose public `validated_summary_line()` helpers, and the report wrappers reuse those typed helpers directly so the compact provenance lines stay co-located with the source summaries instead of being rebuilt ad hoc in the tooling layer.
Latest progress (2026-04-26): the JPL snapshot evidence summary now consolidates the reference snapshot coverage, exact asteroid evidence, and comparison snapshot coverage into a backend-owned report helper, so the compact release-facing JPL line stays co-located with the snapshot backend instead of being assembled piecemeal in validation. The same JPL slice now also surfaces a compact request-policy line in the validation and release summaries, keeping the current TT/TDB, ecliptic/equatorial, mean-only, and geocentric-only posture explicit alongside the interpolation and source evidence. The comparison snapshot coverage helper now also carries the bodies covered by the comparison corpus in first-seen order, which makes the ordered JPL comparison provenance reusable as typed data instead of only as a formatted line. Latest progress (2026-04-27): the comparison snapshot summary now also validates that body order against the checked-in comparison corpus before release-facing formatting, so the comparison coverage line now fails closed if the ordered body list ever drifts.
Latest progress (2026-04-29): the compact validation and release summaries now also surface a JPL batch error-taxonomy line, so the canonical unsupported-body and out-of-range batch regressions are visible alongside the request-policy posture instead of only in the backend test slice. The backend matrix summary and detailed backend matrix report now mirror that same JPL taxonomy line too, which keeps the batch error posture visible in the inventory views as well.
Latest progress (2026-04-26): the same JPL release-facing evidence helper now also includes a backend-owned source-material line for the checked-in DE441 fixture, so the compact validation and release summaries now call out the JPL vector-table provenance directly alongside the coverage and asteroid rows. The parsed JPL snapshot manifest now also exposes typed provenance fallback helpers for source and coverage labels, which keeps the remaining release-facing defaults centralized instead of repeating the same fallback strings in multiple report functions.
Latest progress (2026-04-26): the JPL snapshot backend now also publishes a backend-owned frame-treatment summary, and validation/release summaries render it alongside the request-policy and interpolation evidence, keeping the checked-in ecliptic-to-equatorial mean-obliquity transform explicit in release-facing output.
Latest progress (2026-04-26): the independent JPL hold-out summary now also reports 95th-percentile longitude, latitude, and distance errors, so the smaller transparency corpus now mirrors the main interpolation-quality envelope with the same p95 shape statistics without widening the checked-in snapshot.
Latest progress (2026-04-26): the CLI compatibility-profile smoke tests now also pin the `Whole Sign system`, `Whole Sign house system`, and `Whole Sign (house 1 = Aries)` spellings, keeping the whole-sign family anchored in the command-line catalog audit alongside the equal-house and topocentric variants.
Latest progress (2026-04-26): the same independent JPL hold-out summary now also names the sampled bodies directly in its release-facing envelope, keeping the compact interpolation report aligned with the hold-out coverage summary while the checked-in Mars/Jupiter/Saturn corpus remains unchanged.
Latest progress (2026-04-26): the independent hold-out coverage summary now also lists the Mars/Jupiter/Saturn body names explicitly in first-seen order, so the compact transparency line now shows the exact sampled bodies alongside the row, epoch, and time-scale window without changing the hold-out corpus.
Latest progress (2026-04-26): the independent JPL hold-out corpus now also has a batch-path parity regression, so the Mars/Jupiter/Saturn transparency rows are exercised through `positions()` as well as the existing single-query interpolation check.
Latest progress (2026-04-26): the JPL interpolation-quality summary now also reports distinct epoch coverage plus the earliest/latest hold-out epoch window, so the transparency evidence shows the sampled span alongside the body/kind breakdown. The full validation report's JPL interpolation section now also renders the backend-owned kind-coverage line alongside the summary and sample rows, so the detailed report mirrors the compact release view more closely.
Latest progress (2026-04-27): the JPL interpolation-quality kind-coverage helper now also lists the distinct sampled bodies in first-seen order, which makes the compact transparency line a little easier to audit without changing the checked-in hold-out corpus or the existing cubic/quadratic/linear counts.
Latest progress (2026-04-29): the JPL interpolation-quality corpus now also exposes a public exact request-corpus helper plus compatibility alias, which keeps the hold-out sample batch shape reusable for downstream validation and reproducibility tooling without reconstructing the request rows inline.
Latest progress (2026-04-26): the backend matrix summary now also mirrors the current comparison-audit status and counts, so the compact backend capability view exposes the active regression gate alongside the backend inventory and source-backed evidence lines.
Latest progress (2026-04-26): the compatibility-profile smoke tests now also pin `True Sheoran`, which keeps one more release-profile ayanamsa spelling explicitly covered in both the core release-note rendering and the CLI compatibility-profile output as the breadth audit continues.
Latest progress (2026-04-26): the CLI compatibility-profile summary now also pins `Galactic Equator (Fiorenza)`, and the full profile/release-notes renderers now pin `True Mula (Chandra Hari)` alongside `Galactic Equator (Fiorenza)`, keeping the compact and full release-facing breadth checks aligned on two more visible ayanamsa labels.
Latest progress (2026-04-27): the CLI compatibility-profile and release-notes smoke coverage now also pins `JN Bhasin`, extending the currently visible ayanamsa canonical-name audit by one more release-profile spelling in both the compact and full user-facing paths.
Latest progress (2026-04-27): the CLI compatibility-profile smoke coverage now also pins `Lahiri (ICRC)`, `Udayagiri`, `Valens Moon`, `Babylonian (House Obs)`, `B. V. Raman`, and `Raman Ayanamsha` in the full and compact compatibility-profile renderers, which keeps a few more still-visible ayanamsa spellings anchored in the release-facing catalog audit while the broader breadth sweep continues.

Latest progress (2026-04-25): validation reporting now includes a release-grade comparison tolerance audit section, and `compare-backends-audit` now fails when the built-in tolerance audit finds regressions, so release tooling has an explicit pass/fail gate in addition to the existing per-body tolerance tables. The tolerance policy used by those reports is now also surfaced as a structured backend-family/body-class catalog, so release-facing summaries can name the exact scope table they are applying. The compact tolerance-policy summary now also carries the current comparison corpus size, so the release-facing catalog line states how many bodies and samples the scope table is being applied to.

Latest progress (2026-04-26): the compact release summary’s comparison tolerance policy line now also includes per-scope body-and-sample coverage counts alongside the backend-family scope table and tolerance limits, so the one-screen release overview makes the policy-to-corpus relationship a little easier to audit without opening the full validation report. The body-class tolerance posture now also reports out-of-tolerance sample counts alongside the body counts, which gives the release-facing coarse error envelope a slightly finer-grained regression signal. The tolerance-policy rendering now also names the bodies covered by each scope and prints `none` for empty scopes, so the policy catalog is easier to scan without losing the scope-to-body mapping. The comparison tolerance policy now also has a structured backend-owned summary record in `pleiades-validate`, which keeps the compact release wording reusable without reconstructing the scope catalog string in validation callers.

Latest progress (2026-04-28): the comparison tolerance policy summary now renders its comparison window through the shared `TimeRange` formatter, so the release-facing tolerance line now includes explicit time-scale tags on the epoch range instead of only bare Julian-day numbers.
Latest progress (2026-04-29): comparison tolerance profile labels now fail closed when they are blank or padded, so the release-facing tolerance catalog keeps its profile names aligned with the rest of the report-label validation rules instead of reusing drifted whitespace.

Latest progress (2026-04-27): the detailed validation report now reuses a typed body-class tolerance summary formatter in `pleiades-validate`, so the class-margin wording is centralized instead of being rebuilt inline for each class row.

Latest progress (2026-04-25): the body-class tolerance posture now names the bodies that drive each class-level max longitude, latitude, and distance delta, so the coarse tolerance audit can point at the exact worst-case body instead of only aggregating counts and magnitudes.

Latest progress (2026-04-25): the body comparison and body-class error-envelope summaries now also name the body that drives each measured max longitude, latitude, and distance delta, which makes the release-facing comparison output easier to audit without changing the measured envelopes or tolerance policy.

Latest progress (2026-04-25): the aggregate comparison summary now mirrors that worst-case body labeling for the overall max longitude, latitude, and distance deltas, and the compact validation/audit reports render the body names alongside the magnitudes so the release-facing top-level comparison envelope now points at the exact sample driving each maximum.
Latest progress (2026-04-25): the aggregate comparison envelope now also carries median longitude, latitude, and distance deltas in the full report, compact validation summary, and release summary, giving the release-facing error envelope one more robust central-tendency statistic alongside the existing max/mean/RMS lines.

Latest progress (2026-04-25): the VSOP87 canonical J2000 source-backed evidence summary now names the body that drives each maximum delta axis, and now also records the source kind and source file behind each axis peak, while the release-facing per-body evidence lines now spell out the interim limit and signed margin for each axis so the source-backed envelope is easier to audit without changing the public comparison corpus. The same canonical VSOP87 evidence path now also derives from one batch query over the full source-backed sample set, and the VSOP87 regression suite now checks that batch requests preserve the canonical sample order, so the validation-facing evidence and the backend batch contract stay aligned. The VSOP87 source-audit summary now also reports the deterministic fingerprint count directly in release-facing output, keeping the reproducibility line explicit alongside the raw source-size and term-count evidence. The VSOP87 body-evidence summary now also surfaces the number of bodies outside the interim envelope explicitly alongside the list, which makes the compact validation audit slightly easier to scan. The canonical J2000 evidence summary now also reports mean absolute deltas and an out-of-limit count, so the compact release-facing envelope now mirrors the richer lunar and JPL summaries a little more closely.

Latest progress (2026-04-27): the lunar reference and equatorial evidence summaries now also expose typed `summary_line()`/`Display` helpers, and the validation/release formatters now reuse those typed records directly instead of rebuilding the same release-facing wording inline.

Latest progress (2026-04-26): the VSOP87 canonical J2000 evidence summary now also reports median and RMS longitude, latitude, and distance deltas alongside the existing mean/max envelope, giving the release-facing source-backed audit a more complete central-tendency and spread view without changing the canonical comparison corpus.
Latest progress (2026-04-26): the VSOP87 canonical evidence summary now also reports 95th-percentile longitude, latitude, and distance deltas, giving the backend-owned source-backed envelope a compact upper-tail metric alongside the existing mean/median/RMS view.
Latest progress (2026-04-26): the same canonical VSOP87 evidence summary now also renders the measured source-backed body list in release-facing output, so the compact envelope now shows the Sun-through-Neptune sample order alongside the max/mean/median/p95 statistics instead of only the aggregate counts.
Latest progress (2026-04-26): the VSOP87 source-documentation summary now also carries explicit generated-binary, vendored full-file, and truncated body lists, so the compact reproducibility line can point at the exact body coverage behind each source-path category instead of only reporting counts. The VSOP87 source-documentation health summary now mirrors those per-category body lists too, and now also surfaces the exact source-file order in the same structured record, which keeps the compact catalog check aligned with the richer source-path breakdown. The validation, backend-matrix, and release summaries now also render a VSOP87 source-documentation health line, which makes the catalog partition check visible alongside the source-path mix. `pleiades-vsop87` now also exposes public source-backed and fallback body-profile helpers, so downstream reproducibility tooling can reuse the backend-owned catalog partition directly instead of repeating the source/fallback filter logic.

Latest progress (2026-04-25): the VSOP87 source documentation summary now also reports generated-binary, vendored full-file, and truncated profile counts alongside the source-specification total, so the release-facing summary can show the current source-path mix without altering the canonical body-evidence contract. The same summary now also names Pluto explicitly as the mean-element fallback body, so the remaining non-VSOP87 special case stays visible in release-facing output. The source-documentation summary now also carries the shared date-range note for the retained VSOP87 slices, so the release-facing line now surfaces the documented epoch scope instead of only the source-path mix. The VSOP87 source-data path now also returns structured parse errors for malformed vendored source rows, and the maintainer regeneration helper now surfaces those errors instead of collapsing them into a silent `None`, which makes the reproducibility hook safer to use when the checked-in source text ever drifts. The regeneration helper now also reports an explicit unknown-source-file error that lists the supported vendored inputs, which keeps the maintainer-facing reproducibility failure mode typed instead of stringly.

Latest progress (2026-04-25): the compact ELP lunar-theory specification now exposes a structured request-policy field — TT/TDB only, tropical only, mean only, ecliptic/equatorial frames, and no topocentric observer mode — which makes the current lunar baseline easier to audit while the eventual source-backed ELP selection remains queued. The request-policy summary is now also exposed as a backend-owned helper and reused by validation reporting, which keeps the structured lunar policy text centralized for future source-selection variants. The compact lunar-theory specification now also carries explicit truncation and output-unit notes, and the validation/release summaries render those notes alongside the provenance fields so the current truncated baseline is easier to inspect without changing the public API. The baseline also exposes an explicit source-family label in metadata and summaries, which keeps the source-selection posture structured without widening the API surface. The lunar-theory selection data is now centralized into a single static specification, and the canonical evidence slice is checked against that supported-body list so the current Meeus-style baseline stays internally consistent as source-backed selection work continues. The lunar metadata now also exposes its validation window as a structured `TimeRange`, which lets the release summaries render the sampled epoch span directly instead of inferring it from prose. The release-facing lunar summary formatter is now centralized in `pleiades-elp`, so validation and reporting reuse the backend-owned provenance text instead of duplicating the same formatting logic in the tooling layer. The new compact lunar capability summary helper now gives validation a structured body/frame/time-scale snapshot without parsing prose, and now also carries the supported and unsupported lunar body lists so the current baseline channels stay visible without opening the full specification. The lunar source-selection, catalog-validation, and capability summary records now also implement `summary_line()`/`Display`, so validation and release callers can render the typed lunar records directly without rebuilding those strings in the reporting layer. The backend-owned lunar reference summary now has a measured error-envelope companion rendered in the detailed report and backend-matrix summary, keeping the source-selection posture easier to reuse in later release reports. The lunar reference slice now also includes the 1992 Moon example, J2000 lunar-point anchors, published 1913-05-27 true-node and mean-node examples, a published 1959-12-07 mean-node example, and a published 2021-03-05 mean-perigee example, broadening the current validation evidence without changing the public request model. The `pleiades-elp::LunarTheorySpecification` record now also carries the selected source family directly, so the current baseline selection can be audited without parsing the summary text. The backend-owned lunar summary is now parameterized through `format_lunar_theory_specification(&LunarTheorySpecification)`, which keeps the release-facing one-liner reusable for future source-selection variants without re-implementing the template in validation. The `LunarTheorySpecification` record now also exposes a typed `summary_line()`/`Display` helper, and the lunar summary path reuses it directly so the specification text can travel as a typed record instead of only through the free function formatter. The lunar source-family enum is now also `Display`-backed, so release text can reuse the same typed label without a parallel stringly helper.

Latest progress (2026-04-25): the lunar reference and equatorial error envelopes now also report mean residuals and explicit out-of-limit sample counts alongside the existing maxima, which makes the release-facing lunar evidence a little easier to scan while the current Meeus-style baseline remains the active lunar source-selection placeholder. The same lunar-selection slice now also centralizes the supported and unsupported body accessors behind the backend's shared specification, so the metadata and request checks stay aligned with one source of truth as the eventual source-backed swap work continues. The lunar backend now also publishes a compact source-selection summary helper, and the validation/release summaries now render that shorter provenance line alongside the full specification so the selected lunar baseline is easier to scan without parsing the longer catalog text. The lunar error-envelope report strings now also spell out the applied regression limits directly, so the phase-1 lunar evidence stays explicit about the current threshold policy in release-facing summaries.

Latest progress (2026-04-27): the lunar apparent comparison evidence now also includes a second reference-only Moon anchor from the 1968-12-24 low-accuracy Meeus-style example, broadening the apparent evidence slice while the release-facing mean/apparent gap note continues to report the largest signed gap across the matched comparison samples. The same slice now also adds a published 2004-04-01 NASA RP 1349 apparent-geocentric Moon table row, with the equatorial row kept as the source datum and the ecliptic row derived through the shared mean-obliquity transform, so the apparent comparison evidence now spans one more published epoch without changing the unsupported-apparent request policy. The 1992 Moon frame-conversion companion now also feeds the lunar equatorial reference evidence slice through the shared mean-obliquity transform, which broadens the frame-conversion envelope beyond the original direct RA/Dec anchor while keeping the same release-facing request policy. The release-facing mean/apparent gap note is therefore a little less single-pointed while apparent requests remain rejected by the backend. The same lunar baseline is now also exposed as a one-entry catalog plus catalog summary helper, which gives the release-facing source-selection posture a stable selection surface for future source-backed variants without changing the public API. The lunar source-selection surface now also has typed lookup helpers by source identifier, model name, family label, and the documented short alias `Meeus-style truncated lunar baseline`, which makes the current one-entry catalog easier to resolve from future source-backed selection code without changing the public API shape. The source-selection path now also has a structured family-key lookup and resolver, which makes the selected source family itself directly addressable without falling back to the string label. This keeps the source-selection surface aligned with the documented plan to expose the selected lunar baseline through explicit typed keys rather than only a generic matcher. The lunar catalog now also exposes a direct `lunar_theory_catalog_entry_for_selection(...)` helper so the selected source record can round-trip back to its catalog entry without recomputing the lookup key in reporting code. The same catalog surface now also has a backend-owned consistency helper that checks the selected-entry round trip plus case-insensitive uniqueness for identifiers, model names, family labels, and documented aliases, which makes the one-entry baseline a little safer to extend when a second lunar-theory source lands. The catalog validation status is now also surfaced through validation, backend-matrix, and release-summary outputs, so the one-entry selection surface now shows its round-trip and alias-uniqueness health alongside the catalog summary itself. The validation helper now also rejects duplicate aliases within the selected entry itself, which keeps the current one-entry baseline explicit about alias collisions before a second lunar source lands. The lunar catalog validator now also rejects duplicate supported bodies, duplicate unsupported bodies, and supported/unsupported body overlap within a single entry, so future source-backed lunar selections now have body-coverage disjointness checked alongside the existing alias and round-trip guardrails.

Latest progress (2026-04-27): the 2004-04-01 NASA RP 1349 apparent Moon sample now also has an explicit regression that reconstructs the published equatorial row through the shared mean-obliquity `EquatorialCoordinates::to_ecliptic` transform, which gives the broader lunar apparent evidence a direct transform check in addition to the summary/count assertions.
Latest progress (2026-04-28): the 1992 apparent-geocentric Moon comparison datum now also has the same direct mean-obliquity transform regression, so the reference-only apparent slice keeps both published comparison anchors pinned instead of only the later NASA RP 1349 row.
Latest progress (2026-04-29): the lunar apparent-comparison slice now also includes a published 2006-09-07 EclipseWise apparent Moon coordinate row, and the compact apparent-evidence summary now reports four reference-only samples across one body while keeping the mean/apparent request policy unchanged.
Latest progress (2026-04-29): the reference-only apparent Moon sample record now also exposes a typed `summary_line()`/`Display` helper, so the individual published rows can travel through the same backend-owned reporting pattern as the aggregate apparent-evidence summary instead of relying on ad hoc note inspection.
Latest progress (2026-04-30): the lunar apparent-comparison slice now also exposes explicit ecliptic and equatorial batch-parity request helpers plus `request_corpus` compatibility aliases, which keeps the four published mean/apparent comparison rows reusable by downstream validation and reproducibility tooling without reconstructing the request shapes inline.
Latest progress (2026-04-29): the lunar catalog summary now also surfaces the selected typed catalog and family keys alongside the source identifier and family label, which keeps the compact release-facing source-selection view aligned with the typed resolver surface instead of leaving those keys visible only in the validation summary.
Latest progress (2026-04-27): the lunar request-policy summary now also validates its supported frames, time scales, zodiac modes, apparentness, and topocentric-observer posture against the current selection before the compact report line is rendered, so the release-facing policy text now fails closed at the lunar backend boundary instead of formatting a potentially stale policy snapshot.

Previous progress (2026-04-24): backend matrix release artifacts now report implementation status separately from body/catalog presence. Each implemented backend has an explicit status label and note (fixture reference, partial source-backed, preliminary algorithm, prototype artifact, or routing façade), and the compact matrix summary counts those statuses so release artifacts do not imply production accuracy merely because a backend advertises body coverage. The compact lunar-theory spec now also surfaces true apogee and true perigee as explicitly unsupported bodies, and the ELP regression tests now assert the structured `UnsupportedBody` failures for those requests.

Recent test progress (2026-04-24): the VSOP87 backend now also exercises its batch API over the full source-backed Sun-through-Neptune sample set, and the canonical sample order is now checked explicitly through a reversed batch request set, so the canonical J2000 evidence is verified both body-by-body and as an ordered batch.

Latest test progress (2026-04-25): the VSOP87 regression suite now also covers the full supported major-planet batch at J2000, including the Pluto fallback path, so batch-path verification now spans the complete supported planetary set rather than only the source-backed subset.
Latest progress (2026-04-27): the VSOP87 batch-path suite now also exercises the full supported planetary set at the J1900 canonical epoch in equatorial TDB mode, preserving batch/single parity and the mean-obliquity frame transform across a second reference instant instead of only at J2000. The canonical J1900 batch-parity helper is now public and reused by validation/release reporting, which keeps that second-epoch coverage visible outside the test module.
Latest test progress (2026-04-26): the packaged-data backend now also preserves mixed TT/TDB batch request scales, so the geocentric packaged-artifact path now exercises the same per-request dynamical-time labeling contract through `positions()` that the source-backed backends already expose.

Latest progress (2026-04-25): validation comparison outputs now also carry the candidate backend family on the tolerance-policy and expected-tolerance sections, so the release-facing tolerance tables are scoped to the backend family as well as the body class and Pluto override. The compact validation report summary now also mirrors the signed expected-tolerance margins, which keeps the release-facing summary aligned with the detailed validation report.

Latest progress (2026-04-25): compatibility-profile verification now also pins the exact Swiss Ephemeris house-table code spellings and the J2000/J1900/B1950 and Suryasiddhanta source-label forms in release-facing tests, so the source-label appendix stays anchored to the current interoperability spellings. The built-in house source-label appendix now also explicitly carries the Vehlow Equal table of houses and Pullen SD (Sinusoidal Delta) table of houses forms, so those table-label variants stay visible in the release audit as well. Custom-definition verification now also rejects accidental built-in ayanamsa collisions while explicitly allowing the current intentional Babylonian house-family homographs, so custom-definition labels cannot drift into either catalog by accident without breaking the current release profile.

Latest progress (2026-04-25): the compatibility-profile regression coverage now also pins the `Equal Midheaven house system`, `Vehlow Equal house system`, `Topocentric house system`, `Whole Sign house system`, `Gal. Center = 0 Sag`, and `Gal. Center = 0 Cap` spellings, keeping the current release-profile house and reference-frame variants anchored as the catalog continues to grow.

Latest progress (2026-04-25): the JPL, ELP, and VSOP87 batch-path regressions now also exercise explicit equatorial-frame requests, so the shared mean-obliquity transform and frame-preserving batch contract are now covered in both the single-request and batch-request paths.

Latest progress (2026-04-25): the VSOP87 and compact ELP batch-path regressions now also cover explicit TDB-tagged requests for their supported body slices, so the current time-scale policy is exercised through `positions()` as well as single-request lookups without adding a built-in Delta T or relativistic conversion model.

Latest progress (2026-04-25): the release bundle now also includes a compact `workspace-audit-summary` artifact and manifest entry, so the pure-Rust/native-dependency audit is surfaced in the same bundle family as the compatibility, backend, validation, and artifact reports.

Latest progress (2026-04-25): the validation comparison envelope now also reports RMS longitude, latitude, and distance deltas in the full report, compact summary, and body-by-body envelope output, so the broader release-grade error envelope includes spread metrics in addition to maxima and means. The body-class error envelopes now also carry RMS longitude, latitude, and distance deltas, and now include median longitude, latitude, and distance offsets in the detailed and compact release views, so the coarse class audit has the same spread and central-tendency metrics as the per-body reports. Latest progress (2026-04-26): the body-class envelopes now also surface 95th-percentile longitude, latitude, and distance tails, adding a compact tail metric to the coarse release audit alongside the existing max/mean/median/RMS rows. Latest progress (2026-04-26): the backend-owned VSOP87 body-class rollups now also report median and 95th-percentile longitude, latitude, and distance deltas directly in `pleiades-vsop87`, so the release-facing class envelopes no longer depend on mean/RMS alone for their central-tendency and tail shape.

Latest progress (2026-04-25): the compact release summary now also surfaces the ELP lunar capability line plus the lunar reference and lunar equatorial evidence summaries alongside the JPL interpolation evidence, so the one-screen release overview keeps the current lunar theory baseline visible next to the other phase-1 accuracy evidence. The compact release and validation summaries now also render the lunar request-policy line explicitly, so the current TT/TDB, frame, apparentness, and observer posture is visible at a glance in release-facing output.

Latest progress (2026-04-25): the validation report and comparison-audit tolerance posture now also carry class-level mean and RMS deltas alongside the existing maxima and margin checks, so the release-facing class audit is a little easier to compare across runs while the broader threshold refinement work stays queued.

Latest progress (2026-04-25): the compare-backends-audit failure report now also renders the same body-class RMS lines in its error-envelope section, so failed audit output mirrors the release-grade comparison report more closely while threshold refinement remains queued.

Latest progress (2026-04-25): validation benchmark reporting now also includes a full-chart assembly slice alongside the backend request-throughput benchmark, using the representative house-validation scenarios so full-chart latency stays visible in the release-facing benchmark output.

Latest progress (2026-04-25): the JPL comparison corpus now also has a backend-owned coverage summary that is surfaced in the validation report, release notes, and compact release summary, so the checked-in comparison window is easier to audit alongside the interpolation-quality evidence.

Latest progress (2026-04-25): the compact validation and release summaries now also spell out the current comparison-tolerance limits per scope alongside the scope names and evidence counts, so the release-facing policy line is a little more actionable without widening the full audit output.

Latest progress (2026-04-26): the comparison-tolerance policy summaries now also carry the comparison window and candidate coordinate-frame coverage alongside the scope catalog, so the release-facing tolerance view ties the scope table to the evidence range more explicitly.
Latest progress (2026-04-26): chart snapshots now also render an explicit ecliptic frame policy line, so the chart-facing report makes the geocentric ecliptic body-position request shape visible alongside the existing time-scale, observer, and apparentness notes.

Latest progress (2026-04-27): the release-summary and validation-summary CLI paths now use a minimal nonzero validation-report sample, and the bundle-release wrapper now does the same, so the compact comparison-tolerance policy and release-bundle outputs render again instead of failing on zero-round benchmark datasets.
Latest progress (2026-04-29): the shared ecliptic and equatorial coordinate types now expose fail-closed validation helpers for finite angles, latitude bounds, and non-negative distances, and the frame-conversion round-trip tests now preflight those helpers before asserting the shared mean-obliquity rotation math.

Active gaps:

- broader release-grade error envelopes and tolerance tables beyond the comparison-audit gate and new class-level mean/median/p95/RMS posture;
- full source-backed lunar theory selection and broader lunar-point validation evidence beyond the current canonical sample slice;
- larger JPL-style reference corpus, interpolation validation, and documented tolerance envelopes beyond the current expanded fixture;
- production Delta T conversion, TDB handling, apparent-place corrections, and broader validated frame-conversion error envelopes beyond the shared transform helper and initial documented policy;
- fuller source-backed evidence tables in validation/release summaries, reference-backed tolerance tables, and broader validation reports.

## Recommended next slice

Continue expanding the `pleiades-vsop87` source-data increment:

1. move the next phase-1 accuracy slice toward the lunar theory source-selection work, now that the source-backed VSOP87 generated-table path covers all current major planets;
2. preserve Pluto as an explicitly documented non-VSOP87 special case until a Pluto-specific source path is selected;
3. the release-grade error-envelope audit follow-up is now available through `compare-backends-audit`; remaining work is broader source-backed envelopes and threshold refinement if the release process needs it.

Small preparatory backend-semantics increments have landed: supported planetary results now include deterministic central-difference mean-motion estimates, the compact ELP Moon/node path now exposes matching finite-difference mean-motion estimates, mean lunar apogee and mean lunar perigee are now explicitly supported as mean-only lunar points with finite-difference longitude speeds, direct observer-bearing requests to the geocentric VSOP87/ELP placeholders now fail explicitly instead of implying topocentric support, unsupported apparent requests are rejected rather than silently returning mean values, the initial time/observer policy is documented, caller-supplied UT1-to-TT/scale-offset helpers now exist in the shared type layer without introducing a built-in Delta T model, and `pleiades-core::ChartRequest` now includes explicit caller-supplied time-scale offset conveniences so chart assembly can adopt a chosen conversion policy before houses or body positions are queried. The TDB-to-TT direction now has a symmetric caller-supplied helper as well, which keeps the explicit conversion policy available to callers that already start from TDB-tagged instants. The Sun through Neptune paths now use public IMCCE VSOP87B source files tested against full-file J2000 golden values, and the Earth, Mercury, Venus, Mars, Jupiter, Saturn, Uranus, and Neptune paths now have generated binary coefficient-table slices derived from vendored source files. The validation backend matrix now surfaces per-body VSOP87 source profiles. The JPL snapshot backend now also accepts equatorial-frame requests and derives equatorial coordinates from the shared ecliptic fixture, keeping the frame semantics explicit while remaining pure Rust. The JPL snapshot and packaged-data backends now also have explicit observer-bearing and apparent-place rejection regressions, so the geocentric-only policy is covered beyond the shared policy helpers. A parallel release-safety increment tightened compatibility-profile verification so descriptor metadata and repeated exact labels are checked before bundle publication, and the same verification path now also confirms custom-definition house labels remain outside the built-in resolvers so the release profile keeps those labels explicitly in custom-definition territory. Release-grade error envelopes remain outstanding; Pluto remains a special case outside VSOP87's major-planet files; true lunar apogee/perigee remain deferred until a source-backed true-point model is selected. The VSOP87 crate now also publishes a deterministic source-audit manifest with byte counts, line counts, parsed term counts, and 64-bit fingerprints for each vendored full-file input, and now includes a maintainer-facing regeneration helper plus binary for rewriting the checked-in generated coefficient blobs from the vendored source text, giving the eventual generated-table path a reproducibility hook without changing runtime behavior. Latest progress (2026-04-30): `pleiades-validate` now exposes the combined comparison-envelope summary as a public typed helper too, which keeps the compact median and 95th-percentile comparison envelope co-located with the aggregate comparison record for downstream release tooling instead of only inside the private report formatter.
Latest progress (2026-04-29): the JPL snapshot backend now also has a batch-path regression that distinguishes unsupported bodies from out-of-range requests, so the reference snapshot's single-request error taxonomy stays visible through the batch API as well. The same batch taxonomy path now also has a typed summary/display helper, and that summary now also validates its canonical field set before the report wrapper formats it, which keeps the compact error-envelope wording co-located with the backend query checks and fail-closed on drift instead of rebuilding the same release-facing string ad hoc in the report layer. Latest progress (2026-04-30): that JPL batch taxonomy summary now also surfaces the supported-body Ceres control sample in the compact release-facing line, which keeps the checked-in Ceres/MeanNode error taxonomy explicit in the validation and release smoke coverage. The same control sample now also has a public request-corpus helper, which keeps the compact batch-taxonomy shape reusable by downstream validation and reproducibility tooling instead of only by the summary constructor. Latest progress (2026-04-30): the comparison snapshot now also exposes a plain `comparison_snapshot_ecliptic_request_corpus()` compatibility alias, which keeps the default comparison slice discoverable through the same request-corpus naming pattern as the other JPL batch helpers. Latest progress (2026-04-30): the comparison snapshot and comparison batch-parity report helpers now also render through validated summary-line helpers, which keeps the compact comparison coverage and batch-parity lines fail closed at the report boundary instead of trusting the raw formatter path.
Latest progress (2026-04-30): the VSOP87 canonical J2000 batch slice now also exposes `canonical_epoch_batch_parity_requests()` and `canonical_epoch_batch_parity_request_corpus()` compatibility aliases, which keeps the older canonical-epoch naming discoverable alongside the explicit J2000 batch helper for downstream validation and reproducibility tooling without changing the request shape.
Latest progress (2026-04-29): the checked-in JPL hold-out corpus now also exposes a public request-corpus helper that preserves the stored row order and requested coordinate frame, which makes the exact held-out batch shapes reusable by downstream validation and reproducibility tooling instead of only inside the crate-local regression tests. The same hold-out corpus now also carries the added Mars 2600000.0 row from the reference snapshot, expanding the transparency slice to 10 rows across 3 bodies and 7 epochs while keeping the provenance comments and report summaries aligned with the checked-in fixture.
Latest progress (2026-04-29): the independent JPL hold-out corpus now also includes a late Mars 2634167.0 sample from the reference snapshot, extending the checked-in interpolation check to 11 rows across the same three bodies and giving the release-facing hold-out envelope one more far-end epoch without changing the request shape.
Latest progress (2026-04-30): the independent JPL hold-out corpus now also exposes an equatorial-parity request-corpus alias, which keeps the frame-specific hold-out batch shape discoverable alongside the existing frame-parameterized helper for downstream validation and reproducibility tooling.
Latest progress (2026-04-30): the JPL interpolation-quality sample rows now travel through a typed summary helper and validation path, which keeps the compact release-facing per-row diagnostics aligned with the backend-owned TDB epoch and interpolation-kind wording instead of rebuilding the sample line inline in the report layer.
Latest progress (2026-04-29): the selected asteroid evidence slice now also exposes public exact-frame and mixed-frame request-corpus helpers plus batch-path regressions, which keeps the Ceres/Pallas/Juno/Vesta/433-Eros batch shape reusable for downstream validation and reproducibility tooling without reconstructing it from the sample metadata.
Latest progress (2026-04-29): that selected-asteroid request surface now also exposes a `reference_asteroid_request_corpus(...)` compatibility alias for the exact-frame helper, which keeps the asteroid batch-shape naming aligned with the other JPL corpus helpers for downstream tooling.
Latest progress (2026-04-29): the reference snapshot now also exposes reusable request-corpus helpers for uniform-frame and alternating mixed-frame batch shapes, which keeps the checked-in batch inputs reusable by downstream validation and reproducibility tooling without reconstructing them from snapshot metadata. The reference snapshot now also exposes a mixed TT/TDB batch-parity request-corpus helper, which keeps the same checked-in rows reusable by downstream time-scale validation tooling without rebuilding the alternation in each caller. Those batch corpus helpers now also have matching `*_request_corpus` compatibility aliases, which keeps the request-shape naming aligned with the VSOP87 corpus helpers for downstream tooling.
Latest progress (2026-04-29): the JPL reference, comparison, and independent hold-out request-surface helpers now also expose compatibility aliases, which keeps the checked-in snapshot batch-shape naming aligned with the existing VSOP87 and lunar request-corpus helpers for downstream tooling.
Latest progress (2026-04-29): the selected asteroid evidence slice now also exposes public exact-frame and mixed-frame request-corpus helpers plus batch-path regressions, which keeps the Ceres/Pallas/Juno/Vesta/433-Eros batch shape reusable for downstream validation and reproducibility tooling without reconstructing it from the sample metadata.
Latest progress (2026-04-29): the lunar reference and lunar equatorial evidence rows now also expose typed summary/display helpers, and the detailed validation report reuses those backend-owned row summaries directly so the sample-level lunar evidence table stays centralized instead of being reconstructed inline in `pleiades-validate`.
Latest progress (2026-04-27): the `ChartRequest` time-scale regression suite now also covers the TDB-to-TT conversion path while preserving the rest of the request shape, and now exercises the generic caller-supplied `TimeScaleConversion` path with custom house metadata as well, which keeps the custom house names, aliases, observer location, bodies, zodiac mode, and apparentness policy explicit across the symmetric conversion helper.
Latest progress (2026-04-29): the chart façade batch-validation docs now also spell out the mixed TT/TDB plus house-observer acceptance case when the selected backend advertises topocentric support, which keeps the documented preflight story aligned with the existing batch-policy tests and the time/observer policy note.
Latest progress (2026-04-28): `pleiades-backend::EphemerisRequest` now mirrors the chart layer's named TT/TDB/UT1/UTC conversion conveniences too, so direct backend callers can stage explicit time-scale policies with the same helper vocabulary instead of dropping to the generic retagging helper for every request-shape conversion. The backend-request regression slice now also exercises both the signed and duration-based helper variants, which keeps the explicit conversion contract covered for direct backend callers that prefer either offset style.
Latest progress (2026-04-28): that same backend request surface now also exposes the non-signed TDB-to-TT helper with the expected `f64` offset signature, so the backend-level time-scale convenience set stays aligned with the underlying `Instant` contract instead of forcing direct callers through the signed alias only.
Latest progress (2026-04-28): the packaged-data mixed-frame and mixed TT/TDB batch-parity summaries now also fail closed on order or single-query parity drift before their compact report wrappers render them, which keeps the packaged-artifact request-shape evidence aligned with the current validation posture instead of silently reusing a stale parity record.
Latest progress (2026-04-27): the remaining `Instant::tt_from_ut1` and `Instant::tt_from_ut1_signed` docs now also include doctested examples, which keeps the caller-supplied Delta T policy visible at the lowest shared instant layer alongside the other time-scale helper examples.

Latest progress (2026-04-25): the comparison-audit rendering path in `pleiades-validate` was cleaned up to remove a needless borrow, keeping the validation report helpers clippy-clean while preserving the current tolerance-audit wording.

Latest progress (2026-04-26): the compact lunar source-selection summary now also carries the current validation window, so the short lunar provenance line stays self-descriptive about the evidence span without forcing readers to open the longer specification block. The lunar theory crate now also exposes a typed `LunarTheoryCatalogKey` plus `resolve_lunar_theory_by_key(...)`, so future source-backed lunar selection work can state lookup intent explicitly instead of relying only on the generic string matcher. The same typed key now also has an explicit alias variant for the documented short label, keeping the alias path aligned with the source-identifier, model-name, and family-label lookups. The lunar catalog validation summary now also reports the selected source and alias count, so the backend-owned validation line stays aligned with the currently selected lunar provenance while the catalog remains a one-entry baseline. The compact lunar summary views now also surface the lunar frame-treatment line explicitly, keeping the mean-obliquity ecliptic/equatorial policy visible without opening the longer specification text. The compact lunar capability summary now also reports whether the catalog validates cleanly, so the one-entry source-selection baseline keeps its catalog-health state visible alongside the body/frame/time-scale counts in release-facing capability output. The lunar catalog lookup surface is now also pinned with case-insensitive regression coverage across identifier, model-name, family-label, and alias keys, and the release-facing lunar catalog validation line now calls out that case-insensitive key matching explicitly, so future source-backed additions can extend the typed resolver path without regressing the current matching rules. The lunar catalog validation summary now also prints the typed selected key alongside the selected source, which keeps the release-facing source-selection provenance aligned with the typed lookup surface. The backend-owned lunar theory specification summary now also includes the typed selected catalog key, which keeps the main lunar provenance line aligned with the same resolver surface used by the smaller source-selection summary. The lunar backend now also owns a validated frame-treatment report helper, and the validation report reuses that backend-owned helper directly so the compact mean-obliquity wording stays co-located with the lunar source metadata instead of being rebuilt in the report layer.
Latest progress (2026-04-28): the lunar source-selection accessor now also has a compact typed summary helper plus `Display`, which gives the current source-family/provenance record a stable reportable line of its own without reopening the longer validation-window summary while the next lunar source-selection slice is planned.
Latest progress (2026-04-30): the lunar catalog now also exposes a convenience `lunar_theory_catalog_entry_for_current_selection()` helper, which keeps the current one-entry selection round-trip available as a single fail-closed lookup instead of reconstructing the current selection call at each use site.
Latest progress (2026-04-29): that same lunar source-selection summary line now also renders the typed selected and family keys alongside the source identifier, family label, and alias list, which keeps the short provenance line aligned with the typed resolver surface instead of only naming the bare selection fields.
Latest progress (2026-04-29): the lunar reference and lunar equatorial batch-parity request corpora now also expose parity-specific wrappers plus compatibility aliases, which keeps the reusable request-shape helpers aligned with the JPL and VSOP87 corpus naming pattern for downstream tooling.
Latest progress (2026-04-28): that same source-selection validation now also round-trips the typed catalog and family keys through the public resolver helpers before rendering, so the compact provenance line now fails closed if either key family ever drifts away from the selected lunar baseline.
Latest progress (2026-04-29): the lunar source-summary regression coverage now also pins drifted `catalog_key`, `source_family_key`, and `source_family_label` fields explicitly, so the typed lunar provenance line now has direct fail-closed tests for both resolver-key branches and the rendered family label instead of only the identifier field.
Latest progress (2026-04-28): the compact lunar source-selection summary now also carries the typed family key alongside the selected catalog key, which makes the release-facing provenance line more explicit about the current resolver surface while the source-backed lunar selection work remains in its current baseline state. The compact lunar catalog-validation summary now also mirrors that family-key visibility, which keeps the short fail-closed audit line aligned with the same resolver surface.
Latest progress (2026-04-28): that same lunar source-selection line now also validates before it is rendered, so drifted source-selection fields now fail closed with an unavailable report line instead of formatting a stale provenance string. The lunar catalog-validation summary now also follows the same fail-closed wrapper path, so the short catalog audit line refuses to render stale selected-count or validation-result fields as well.
- Latest progress (2026-04-27): `pleiades-elp::LunarTheoryCatalogSummary` now also exposes a typed `summary_line()`/`Display` helper, and the validation/reporting path can reuse that backend-owned catalog summary directly instead of reconstructing the compact lunar catalog wording inline, which keeps the one-entry source-selection catalog aligned with the rest of the typed summary pattern. The lunar capability summary now also has a typed validation helper plus a report-facing wrapper, so the compact ELP capability line can fail closed if the current model/source/body/frame/time-scale metadata drifts.
- Latest progress (2026-04-27): `pleiades-elp::LunarTheorySourceSummary` now also validates against the current lunar selection before the release-facing provenance line is rendered, so any future drift in the compact lunar source-selection fields now shows up as an unavailable report line instead of a stale summary.
Latest progress (2026-04-26): the compact artifact summary now also renders the comparison mean, median, 95th-percentile, and RMS envelopes alongside the existing maxima, so the packaged-artifact validation view now mirrors more of the release-grade comparison shape without widening the artifact contract. The same artifact inspection path now also surfaces a packaged-artifact decode benchmark slice, keeping the compression-quality measurement visible directly in the artifact report without requiring a separate command.
Latest progress (2026-04-26): the packaged-artifact profile summary now also carries the bundled body list in a dedicated helper, and the validation/release-facing artifact summaries now render that body coverage alongside the profile line so the compact packaged-data posture stays tied to the actual bundled body set. The compression crate now also exposes a typed `ArtifactOutputSupport` classification plus helper predicates, which keeps derived, unsupported, and unlisted artifact outputs explicit for future reporting code.
Latest progress (2026-04-27): the packaged-artifact profile summary now validates before rendering both the plain and bundled-body variants, so the compact artifact posture line now fails closed instead of formatting stale profile metadata if the packaged header or bundled body list drifts. The same wrapper path now also rejects empty and duplicate bundled-body lists through the shared coverage validator, which keeps malformed packaged-body coverage out of the release-facing artifact posture line as well.
Latest progress (2026-04-27): the artifact-profile coverage summary now also exposes a reusable `validate()` helper, and the packaged-data profile summary now delegates to that check plus its own body-count guard so drift in the bundled body list or capability profile can be caught before the compact packaged-data summaries are rendered.
Latest progress (2026-04-27): the packaged-data request-policy summary now also validates its structured geocentric-only, TT/TDB, ecliptic/equatorial, tropical, mean-only posture before validation and release reports render it, so drift in the compact artifact posture line now fails closed instead of quietly formatting a stale request-policy summary.
Latest progress (2026-04-26): the compatibility-profile verification slice now also pins the Pullen SD/SR sinusoidal house labels, the Polich/Page and T topocentric source spellings, the Krusinski-Pisa-Goelzer `U krusinski-pisa-goelzer` form, the Meridian `X axial rotation system/ Meridian houses` form, the `Zariel` variant, and the `Horizon/Azimuth table of houses` spelling, so the release-facing catalog drift checks keep a few more high-signal interoperability labels anchored as the catalog continues to evolve.
Latest progress (2026-04-26): the compatibility-profile CLI tests now also pin the `Equal (cusp 1 = Asc)` and `Whole Sign (house 1 = Aries)` source-label forms in the rendered release profile, keeping the ascendant-anchored equal-house and whole-sign interoperability wording explicitly covered in the release-facing verification path. The same CLI slice now also pins `Poli-equatorial`, `horizon/azimuth`, `T Polich/Page ("topocentric")`, `Zariel`, and `Equal/MC = 10th`, so a few more high-signal release-profile spellings stay anchored in the compatibility-profile command output. The CLI profile smoke coverage now also pins the plain `Equal Midheaven table of houses`, `Equal/1=0 Aries`, and `Equal (cusp 1 = 0° Aries)` source-label forms, which keeps the remaining equal-house release-line variants explicitly exercised in the user-facing audit path. The same release-facing smoke coverage now also pins the Babylonian/Kugler 2 and Babylonian/Kugler 3 shorthand alias lines, so the remaining Babylonian source-label variants stay exercised in the CLI audit path too.
Latest progress (2026-04-26): the compact compatibility-profile summary and verification output now also label the release-specific house-system and ayanamsa canonical-name lists explicitly, so the release-facing profile audit shows the current release slice names directly instead of only counting them.

Latest progress (2026-04-25): the compact release summary now also includes the body-class error envelopes alongside the existing tolerance posture, so the one-screen release view carries the class-level mean/RMS envelope without needing to open the full validation report.
Latest progress (2026-04-26): the VSOP87 source-backed evidence path now also exposes backend-owned luminary/major-planet envelope rollups, and the validation and release summaries render that new class-level envelope alongside the existing canonical body evidence so the source-backed accuracy posture is a little easier to scan at a glance.
Latest progress (2026-04-27): the compact validation report summary now also surfaces the VSOP87 source-backed body-class envelopes alongside the existing source documentation and body evidence lines, keeping the coarse class rollup visible in the shorter release-facing validation view too.
Latest progress (2026-04-28): the VSOP87 canonical equatorial companion evidence now also renders a backend-owned luminary/major-planet body-class rollup, which keeps the source-backed accuracy posture visible for the equatorial cross-check too instead of only for the canonical ecliptic envelope.
Latest progress (2026-04-27): the VSOP87 source-documentation summary now also exposes a direct validation helper with a typed error path, and the compact report wrapper now preflights both the raw summary and the health summary before formatting the compact provenance line, so drift in the derived source/file/body partition can fail closed even earlier in the release path. The same health gate still rejects duplicate or overlapping source-backed/fallback body lists, which closes a subtle source-documentation drift gap that count-based checks alone would not have caught.
Latest progress (2026-04-28): the source-documentation health report wrapper now returns an unavailable summary instead of asserting when the cached record drifts, which keeps the release-facing provenance line fail-closed even if a caller reaches the wrapper directly with stale data.
Latest progress (2026-04-27): the comparison-envelope formatter now also centralizes the aggregate comparison summary together with its median and 95th-percentile tails in a typed helper record, which keeps the compact comparison wording assembled from one backend-owned envelope instead of stitching the aggregate and tail strings together separately in each report path.
Latest progress (2026-04-27): the VSOP87 canonical J2000 batch parity evidence is now surfaced alongside the J2000 source-backed evidence and the existing J1900 parity slice, so the release-facing planetary batch contract stays visible at both the canonical reference epoch and the later equatorial batch epoch. The canonical J2000/J1900 batch-parity summaries now also validate their derived counts before the compact report wrappers render them, so any future drift in the sample/body or quality-count totals now fails closed as an unavailable release line instead of formatting stale batch evidence. Those batch-parity validators now also pin the expected reference epoch, frame, and body order for the two canonical slices, which makes the compact batch contract a little harder to drift away from the checked-in reference metadata.
Latest progress (2026-04-27): the artifact inspection report now also surfaces an aggregate packaged-artifact boundary envelope across all bundled bodies, and the per-body inspection rows now render through a typed summary helper with mean boundary deltas alongside the existing maxima, so the compact and detailed artifact validation views make segment-edge continuity easier to scan alongside the decode benchmark. The per-body boundary rows now also print the explicit instant time-scale tags through the typed `Instant` formatter, which keeps the artifact continuity window aligned with the rest of the workspace's explicit time-scale policy wording. The aggregated boundary-envelope summary now also validates its finite/count invariants before the report wrappers reuse it, so malformed continuity evidence now fails closed instead of rendering a stale release-facing line.
Latest progress (2026-04-27): the packaged-artifact boundary continuity envelope now also reports RMS longitude, latitude, and distance deltas at both the per-body and aggregate levels, which gives the release-facing artifact audit the same mean/RMS shape as the other comparison envelopes without changing the packaged-artifact contract.
Latest progress (2026-04-27): the compact release summary now also reuses that packaged-artifact boundary envelope helper, which keeps the one-screen release overview aligned with the artifact validation envelope instead of only naming the packaged-artifact summary line.

Latest progress (2026-04-26): house calculations now reject non-finite obliquity overrides explicitly instead of letting NaN or infinite inputs flow into the quadrant formulas, and the remaining house-system docs were retitled to drop the stale MVP wording so the public-facing house module language matches the current production-ready behavior.
Latest progress (2026-04-26): the Sunshine house-system regression now also checks the documented axis anchors directly, so the release-specific Sunshine formula is now covered by an explicit structural test instead of only by a generic availability check.
Latest progress (2026-04-26): `HouseSnapshot::validate()` now checks the derived angles and cusps for finite values, and `calculate_houses()` uses that postcondition check so numerical failures are reported explicitly instead of letting non-finite house outputs escape into callers.
Latest progress (2026-04-27): `HouseSnapshot::validate()` now also verifies the expected cusp cardinality for each supported system, so malformed manual snapshots for Gauquelin or the 12-cusp systems now fail closed before any consumer can treat the body list as structurally valid.
Latest progress (2026-04-28): that cusp-count guard now also has an explicit regression for a malformed 12-cusp standard system snapshot, which keeps the ordinary house-count branch covered alongside the existing Gauquelin sector branch.
Latest progress (2026-04-26): `HouseSnapshot` now also exposes a `house_for_longitude()` convenience wrapper over the shared wraparound helper, and the house-placement regression now covers exact cusp boundaries plus the wraparound lookup path, which keeps the snapshot/query pairing explicit for callers while tightening the house-placement helper contract.
Latest progress (2026-04-26): custom house-system definitions now render their name, aliases, and notes in unsupported-house errors, which makes the release-facing extensibility boundary more explicit without changing the public house API.
Latest progress (2026-04-29): the Swiss-Ephemeris house-code alias helpers in `pleiades-houses` and `pleiades-core` now include doctested examples for the short-code table, which makes the house-code mapping line easier to discover from the public API docs while the compatibility-profile audit continues.
Latest progress (2026-04-27): compatibility-profile verification now also rejects whitespace-padded canonical names, notes metadata, and labels, so the catalog drift gate now catches surrounding whitespace before it can leak into the release profile or custom-definition buckets.
Latest progress (2026-04-28): the descriptor-local validators in `pleiades-houses` and `pleiades-ayanamsa` now also reject blank or padded canonical names and aliases directly, which gives profile ingestion a lower-level normalization guardrail in addition to the existing catalog-level whitespace checks.
Latest progress (2026-04-28): the compatibility-profile verification summary now also validates the derived latitude-sensitive house-system list and house-formula-family inventory through a shared typed name-sequence helper before it renders those release-facing lines, so drift in the compact derived catalog lists fails closed instead of slipping through as a stale summary string.
Latest progress (2026-04-28): those same descriptor-local validators now also reject duplicate aliases within a single entry while still permitting the intentional case-only canonical-name variants already exercised by the built-in Vehlow and Galactic-reference catalogs, so the lower-level profile ingestion guardrail now covers same-entry alias collisions without tightening the documented interoperability spellings.

Latest progress (2026-04-25): the compatibility-profile verification slice now also pins Babylonian (Eta Piscium), the Suryasiddhanta / Surya Siddhanta 499 CE source-label spellings, and several remaining release-profile source names such as Equal/MC = 10th, Equal Midheaven table of houses, Vehlow Equal table of houses, Nick Anthony Fiorenza, Bob Makransky, Treindl Sunshine, True galactic equator, and Galactic equator true in the rendered compatibility profile, keeping a few still-visible release-profile labels anchored as the catalog continues to evolve.
Latest progress (2026-04-27): the CLI compatibility-profile and release-notes smoke coverage now also pins Babylonian (Eta Piscium) and Galactic Equator (Mula), keeping a pair of still-visible release-profile ayanamsa spellings anchored in the user-facing audit path alongside the existing release-profile catalog breadth checks.
Latest progress (2026-04-26): the compact house-validation corpus summary now also names the representative scenario labels directly, which makes the release-facing house evidence a little more traceable without changing the underlying baseline sample set.
Latest progress (2026-04-26): the compact release-summary and validation-summary tests now assert the backend-family/profile wording in the Luminaries tolerance coverage row, so the release-facing comparison corpus checks stay aligned with the current structured tolerance policy text instead of the older abbreviated coverage snippet.

Latest progress (2026-04-30): `pleiades-vsop87::supported_body_canonical_batch_matrix_requests()` now complements the existing matrix corpus alias, which keeps the combined J2000/J1900 ecliptic and equatorial batch slice discoverable through the shorter `requests` spelling as well.

## Constraints

- Keep all implementation pure Rust.
- Do not move astrology-domain logic into source-specific backends.
- Do not expand release claims until validation evidence exists.
- Keep changes small enough to review and test independently.
