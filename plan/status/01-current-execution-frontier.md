# Status 1 — Current Execution Frontier

## Frontier

The active frontier is **Phase 1: Artifact accuracy and packaged-data production**, with **Phase 2: Reference/source corpus productionization** and **Phase 3: Body-model completion and claim boundaries** as dependencies.

Recent progress: the release-bundle verifier's packaged-artifact fit-envelope, fit-margin, and fit-outlier semantic checks now read the staged validation-report-summary.txt payload, matching the summary artifact rather than the full validation report body. The compatibility-caveats release surface now also routes through a shared core formatter plus the cached validate renderer, keeping that catalog surface on the same fail-closed path as the other compatibility summaries, and the benchmark-corpus plus chart-benchmark-corpus summary helpers now cache their rendered text in-process so the validation/report surfaces can reuse those corpus summaries without rebuilding them. The packaged-artifact normalized-intermediate summary now also has a determinism regression that compares repeated renders and checks the embedded checksum against the rendered payload, keeping that checklist item pinned as complete. The core chart snapshot display now routes its frame-policy line through the validated frame-policy helper, keeping the façade's release-facing chart output aligned with the shared request-policy wording. The chart-benchmark corpus now also has a standalone validated CLI surface and alias coverage, so the chart-style benchmark corpus is inspectable outside the benchmark-matrix report. The baseline house-validation summary now also uses a cached no-arg helper, so the common validation and CLI surfaces no longer rebuild the same summary text on repeat renders. The request-semantics help path now also uses a validated alias helper, keeping the compact request-policy and request-semantics wording aligned without cloning raw summary text. The short dense-span candidate-versus-fallback path now also has a dedicated Moon regression, keeping the fit-candidate branch pinned when it is no worse than the fallback on a dense body. The packaged-artifact fit sample-count lattice now also has direct regression coverage for lunar points and custom bodies, so the dense-body cadence helper stays pinned across the remaining dense classes as well, and the new lunar-point regression now explicitly pins the mean and true node/apogee/perigee variants in the fit, validation, and residual lattices. The medium fit-candidate ladder is now also explicitly regression-covered for Venus and Jupiter, keeping the remaining major-planet 12-point and 14-point baseline posture pinned outside the dense-body classes. The packaged-artifact fit margins summary now also has a direct CLI surface with alias coverage, keeping the fit-margin posture inspectable outside the full validation report. The packaged-artifact generation-policy prose now also names the residual-channel combination/permutation search and the smaller-residual-footprint tie-break, and now also names a dense five-point fallback on the longest dense-body spans, keeping the fit-strategy wording aligned with the implemented residual-search and long-span fallback rules. The residual sample lattice now also gives selected asteroids and custom bodies the dense distance-channel fractions, and the residual-distance lattice now also covers inner and outer planets, closing the last channel-specific gap in the residual search path; the residual-sample regression now also pins inner and outer planet longitude/latitude on the sparse lattice while their distance channels stay dense. The validation-report interpolation-quality subsection now appends the JPL body-class error-envelope evidence line, so the release-facing backend diagnostics surface the empirical envelopes alongside the existing quality, coverage, posture, and hold-out rows. The reference/hold-out overlap summary now also routes through a validated helper in the JPL and validation surfaces, keeping that corpus-provenance line fail-closed through the same public path. The VSOP87 source-documentation and source-documentation-health summaries now also route through validated helpers in the validation layer and CLI aliases, so the source-catalog evidence line fails closed on catalog drift before reuse. The checked-in packaged-artifact fixture has now been resynced to the current regenerated bytes, which restores the artifact checksum sidecar assertions and the repeat-write regression to green, and the packaged-artifact fit reconstruction path now shares a control-point fallback helper across longitude, latitude, and distance channels so higher-order fits can degrade cleanly when quantization overflows; the release-bundle tamper coverage now also rejects native-dependency audit summary drift even after the checksum sidecar is refreshed. The release-bundle validation path now also restores the comparison snapshot body-class coverage checksum before manifest comparison, so the staged bundle stays canonical after the new summary is wired in. The interpolation-quality sample request corpus summary now also stages in the release bundle and the bundle verifier now cross-checks it against the live renderer, so the sampled Phase 2 request corpus fails closed on semantic drift. The selected-asteroid source request corpus summary now also stages in the release bundle and the bundle verifier now cross-checks it against the live renderer, so the selected-asteroid request slice now fails closed the same way. The selected-asteroid source window summary now also stages in the release bundle and the bundle verifier now cross-checks it against the live renderer, so the selected-asteroid provenance slice has a dedicated bundle artifact too. The reference snapshot summary now also stages in the release bundle with a live-renderer cross-check, so the corpus overview line fails closed too. The reference snapshot sparse boundary summary now also stages in the bundle with the same live-renderer cross-check, and the release bundle verifier now cross-checks it against the live renderer too. The benchmark corpus summary now also has a validated wrapper and the release bundle verifier now cross-checks it against the live renderer, so the benchmark corpus posture fails closed alongside the other bundle evidence lines; the release-bundle regression now also rejects semantic benchmark-corpus-summary.txt drift even after its checksum sidecar is refreshed. The packaged-artifact phase-2 corpus alignment summary now has its own dedicated validation error type, so the phase-2 posture can fail closed with a more explicit API contract instead of reusing the target-threshold error type, and the validation path now reports field-specific nested corpus drift instead of collapsing every nested validation failure into the generic top-level payload label. The packaged-artifact regeneration write path now reuses the committed artifact bytes while the `--check` path continues to exercise the regeneration validation path, keeping the release-gate smoke tests tractable without changing the checked-in fixture bytes, and the packaged-artifact regeneration path now also groups snapshot entries by body and reconstructs those bodies in parallel, which keeps the regeneration smoke path within the test harness budget without changing the checked-in bytes. The CLI regeneration path now also uses the cached regenerated bytes helper directly so repeated sidecar writes stay aligned with the live rebuild cache; the artifact regeneration helper now also validates the supplied reference-snapshot slice before rebuilding, so the generator boundary explicitly consumes the documented corpus inputs instead of accepting arbitrary slices; the CLI regression suite now also invokes the sidecar-writing path twice and checks the emitted artifact and sidecars stay stable across repeated writes, and the packaged-artifact regeneration check path now also caches its rendered smoke-report text. The packaged-artifact generation manifest checksum summary and the production-generation manifest summary/checksum helpers now also cache their rendered strings in-process, trimming repeated bundle/report recomputation, and the release-summary, release-checklist, release-checklist-summary, release-profile-identifiers summary, catalog-inventory summary, and release-smoke renderers now cache their rendered strings in-process too; the remaining packaged-artifact profile coverage, output-support, speed-policy, generation-policy, and residual-body report wrappers now cache their rendered strings in-process as well. The packaged-artifact generation manifest checksum summary now also has a dedicated validated helper and live-renderer cross-check in release-bundle verification, so the manifest-checksum posture fails closed alongside the generation-manifest summary. The release bundle now also stages and verifies the packaged-artifact generation manifest checksum summary alongside the generation-manifest files, so checksum-posture drift on that summary line fails closed too. The release bundle verifier now also cross-checks the release notes summary against the live renderer, so release-notes-summary drift fails closed alongside the other release-facing summaries. The release notes summary now also re-validates the target-house and target-ayanamsa scope prose before rendering, so the notes block fails closed if the compatibility-scope wording drifts. The packaged-artifact regeneration summary now also stages and verifies in the release bundle with a live-renderer cross-check, so regeneration-prose drift fails closed alongside the rest of the packaged-artifact release set. The comparison snapshot source, source-window, body-class coverage, and batch-parity CLI commands now also route through validated helpers instead of the fallback-string path, so malformed comparison snapshot provenance fails closed at the command boundary. The reference snapshot batch parity, mixed TT/TDB batch parity, and independent hold-out batch parity summary surfaces now also route through validated helpers, keeping the other corpus-parity blocks on the same fail-closed path. The reference snapshot source-window summary now also routes through a validated helper, and it now also stages in the release bundle, so the checked-in reference source-window surface fails closed alongside the rest of the reference-corpus provenance lines. The reference-asteroid source window summary now also stages in the release bundle and the bundle verifier cross-checks it against the live renderer, so the reference-asteroid provenance slice fails closed too; the release-bundle tamper regression now also rejects semantic drift there after the checksum sidecar is refreshed. The chart CLI help clause now validates the shared request-surface inventory before rendering the explicit UTC/UT1 and TT/TDB alias wording, so the user-facing help path fails closed if the request-surface posture drifts. The release-bundle verifier now also cross-checks the packaged-artifact target-threshold summary against the current render helper, so stale threshold prose fails closed alongside the other release-facing summaries, and the release bundle now stages and verifies the packaged-artifact fit sample classes summary inside both the manifest and directory checks. The release-bundle tamper regression now also rejects semantic packaged-artifact target-threshold summary drift after its checksum sidecar is refreshed. The packaged-artifact target-threshold summary path now also routes through a validated wrapper, so the release-facing target-threshold line itself fails closed before rendering, and the target-threshold state now also exposes a direct production-ready validation helper reused by generator-parameter and summary checks; the compact validation-report summary now also surfaces that state line, and the CLI now exposes a dedicated target-threshold-state summary command, and the packaged-artifact target-threshold state summary now also stages and verifies in the release bundle with a live-renderer cross-check. The release bundle verifier now also cross-checks the staged packaged-artifact output-support, access, fit-sample-classes, speed-policy, and generation-policy summaries against the live render helpers, so those artifact posture lines fail closed when the checked-in bundle drifts away from the current profile. The release bundle now also stages and verifies the lunar-theory source-family summary alongside the lunar-theory limitations and catalog-validation summaries, keeps the lunar source-family release surface aligned with the checked-in bundle payload, and it now also cross-checks the lunar-theory limitations summary against the live renderer so limitations prose fails closed even after checksum refreshes, and now also cross-checks the lunar-theory catalog validation summary against the live renderer so checksum refreshes cannot hide catalog-validation drift. The lunar-theory source-selection summary is now also exposed as a release-facing CLI surface and stages with the other lunar provenance files in the release bundle, giving the typed selection record its own direct inspection path alongside the existing source and family provenance lines, and the release-bundle verifier now cross-checks it through a validated wrapper. The packaged-artifact dense-body cadence classification now flows through a shared helper so fit-candidate, residual, and validation sampling stay aligned, the validation fraction lattice now reuses that same helper too, the residual-channel lattice helper now explicitly centralizes the dense-body angular-channel rule and the regression coverage now includes selected asteroids and custom bodies, and the residual-sample regression now explicitly exercises both Ceres and a non-asteroid custom body; the long-span subdivision path now uses a curvature-aware split bias on dense bodies to favor off-center windows when the quarter-point probes show asymmetric motion, with quarter-biased splits now reserved for very long dense-body spans that remain strongly asymmetric, the split selection now also consults one-sixth/five-sixth probes on very long dense spans and one-third/two-thirds probes on long dense spans when the quarter-point curvature stays balanced, while the fallback reconstruction path now also tries a dense quarter-point control-point lattice before the exact-third fallback on irregular spans; the longest dense-body split ladder now also has explicit regression coverage for the four-fifth branch when the coarser probes stay balanced; the split search now keeps the one-third/two-thirds ladder available when the one-sixth/five-sixth pair is unavailable, instead of dropping straight to midpoint splitting, and the recursive splitter now also wires the matching six-sevenths probe for that extreme dense-body rung so the seven-point split can bias either side, and the source-window search now also consults one-fifth/four-fifth probes on the longest dense-body spans when the coarser probes stay balanced. The most extreme dense-body spans now also have an internal one-seventh/six-sevenths rung before that one-fifth ladder, and the newest super-extreme spans now also consult one-ninth/eight-ninths probes before that rung and now also check one-eighth/seven-eighths probes before the one-seventh rung, giving the longest intervals one more dense fallback without widening the published artifact scope. The checked-in packaged-artifact fixture was also resynced to the new bytes after that split-ladder change. The packaged-artifact output-support posture now also distinguishes approximated motion support from deterministically derived outputs, so numerical-difference motion estimates remain explicitly classified. The compression layer now also exposes a direct `ArtifactProfile::motion_output_support` accessor, and the profile-validation path reuses it for that classification. The core façade now also exports matching release-specific house-system and ayanamsa canonical-name report helpers, so the validation surfaces can reuse one public path for those release-facing summaries. The house-code alias inventory now also routes through a shared validated report helper in the core and backend-matrix report paths, keeping that alias surface fail-closed without duplicating the current-profile validation step. The packaged-artifact report and summary renderers now cache their rendered text in-process, trimming repeated compact-report recomputation alongside the already-cached inspection report object. The ayanamsa reference offsets summary now also routes through a validated report helper, so the zero-point evidence line fails closed before the release-facing summary is rendered. The release-bundle verifier now also cross-checks the validation report's packaged-artifact fit envelope, fit margins, and fit outliers lines against the live renderer, so stale fit-envelope or fit-margin prose fails closed even when the bundle checksum is refreshed; it now also cross-checks the packaged-artifact lookup-epoch policy summary against the live renderer, so lookup-epoch posture drift fails closed too. It now also cross-checks the staged packaged-artifact generation manifest summary against the live renderer, so stale generation-manifest prose fails closed even when the bundle checksum is refreshed. It now also cross-checks the staged reference and comparison snapshot source summaries against the live renderers, so source-corpus provenance drift fails closed even when the manifest checksums still line up. It now also cross-checks the staged production-generation boundary source and request-corpus summaries against the live renderers, so checksum refreshes cannot mask phase-2 generation-provenance drift; it now also cross-checks the staged production-generation manifest summary and checksum summary against the live renderer, so those phase-2 manifest files fail closed too, and it now also cross-checks the staged lunar-theory source-family summary against the live renderer so the family-posture line fails closed if the prose drifts, and the backend-matrix selected-asteroid source evidence/window lines now fail closed there too. The release bundle verifier now also cross-checks the independent-holdout source-window summary against the live renderer, so the hold-out source-window posture fails closed too. It now also cross-checks the reference snapshot bridge day summary against the live renderer, so the bridge-day posture fails closed too, and the reference snapshot exact J2000 evidence summary now renders through the direct release-facing helper and now also stages and verifies in the release bundle so the validation crate doc-test path stays green, and the reference snapshot bridge-day summary now also routes through a validated helper before bundle serialization. The release-bundle verifier now also cross-checks the packaged-artifact profile coverage summary against the live renderer, so profile-coverage drift fails closed too. The request-semantics summary now also has a release-bundle semantic cross-check against the live renderer, so the paired request-policy semantics prose fails closed alongside the other request-policy surface checks. The release-checklist manual bundle workflow now also carries the broader source-corpus provenance contract reminder, keeping the release-facing provenance note explicit alongside the reproducibility commands. The release-checklist summary test expectations now derive the manual bundle workflow count from the shared validated helper, keeping the CLI and validation surfaces aligned. The selected-asteroid source request corpus now also has a release-facing equatorial alias through the new equatorial report helper, and Pluto's terminal-boundary regression now uses a body-aware tolerance so the packaged-artifact test suite matches the backend's explicitly approximate Pluto posture without weakening the exact-body checks. The combined release-grade body-claims and Pluto-fallback posture validation is now centralized in `pleiades-backend` and reused by the validation/report surfaces, so the body-boundary check no longer lives only in the validation crate. The super-extreme dense-body split ladder now also checks one-eighth and seven-eighths probes even when ninth-point probes are present but balanced, closing the last open rung-order gap in the long-span fallback path.

The packaged-artifact target-threshold scope envelopes summary now also routes through a validated helper in the CLI, validation-report, and release-summary surfaces, keeping the scope-envelope posture fail closed alongside the target-threshold and source-fit summaries. The aggregate scope-envelope posture now also has a typed wrapper, so the release-facing helper no longer reconstructs the lines manually. The packaged-artifact target-threshold state summary now also has a semantic-drift release-bundle regression, so the release-facing state line fails closed even after checksum refreshes. The release bundle verifier now also cross-checks the packaged-artifact target-threshold scope envelopes summary against the live renderer, and it also cross-checks the release-profile identifiers summary against the live renderer, closing one more drift path in the release-profile manifest payload. The release-bundle missing-entry regressions now also cover the packaged-artifact target-threshold summary, target-threshold state summary, and target-threshold scope envelopes summary, so omission fails closed for that posture too. It now also cross-checks the packaged frame treatment summary against the live renderer, so the packaged frame-treatment posture fails closed too.

Completed work such as workspace bootstrap, broad catalog scaffolding, report aliases, release-bundle rehearsal, artifact manifest completeness, body-class cadence summaries, span-cap summaries, and packaged lookup/decode benchmark surfaces is no longer listed as active implementation work.

## Why this frontier comes first

The specification requires a compressed 1500-2500 CE artifact with measured accuracy, deterministic generation, efficient random access, and explicit stored/derived/unsupported output semantics. The current artifact has the structure and reports needed to diagnose it, and the draft path now passes the current calibrated thresholds while the target-threshold policy is recorded as production-ready; the structured target-threshold state now also has a direct `Display` surface and a regression that rejects Draft states, and the draft-state wording now says production thresholds are not yet release-ready, but release-grade corpus coverage is still not finalized, and the body/channel threshold posture now has a direct regression that validates the advertised scope envelopes against the calibrated thresholds.

Accuracy work should therefore precede any release claim broadening. Phase 2 source/corpus work still needs to broaden the trusted corpus before broader artifact claims move forward.

The release bundle verifier now also cross-checks the request-policy and request-surface summaries against the live render helpers, so stale bundle prose fails closed even when the manifest checksums still line up. It now also cross-checks the native sidereal policy summary against the live renderer, so sidereal-policy prose fails closed alongside the rest of the request-policy block. The reference snapshot mixed TT/TDB batch-parity summary is now exposed as a release-facing CLI surface, so the checked-in batch slice can report its time-scale mix alongside the existing ecliptic batch parity evidence. The packaged-artifact generator now also uses higher-order reconstruction from fit samples and falls back to a shared four-point control-point reconstruction across longitude, latitude, and distance channels when the higher-order fit does not quantize cleanly before quadratic fallback, and its generation-policy wording now matches the quadratic-window strategy, the CLI regeneration regression now expects that quadratic wording, and the checked-in fixture remains byte-stable. The phase-2 corpus alignment posture now also includes the production-generation source provenance and body-class coverage so the threshold summary tracks the separate generation-input corpus, not just the checked-in reference/comparison/hold-out fixtures, and the production-generation source summary now also folds in the merged source-window lattice as release-visible provenance and now models the source-revision checksum pair as structured fields instead of a raw string, and the production-generation source-window summary now stages and verifies alongside it in release bundles, while the release bundle verifier now also cross-checks that staged source-window summary against the live renderer. The release bundle now also cross-checks the full production-generation source summary against the live renderer, so source-summary prose fails closed too. The production-generation summary now also gets a live-renderer cross-check and tamper regression, so the overall coverage line fails closed too. The independent-holdout source-window summary now stages and verifies in the same release-bundle path, so the hold-out source-window posture also fails closed. The release notes / release summary surfaces now route the compatibility profile release note through a validated helper before rendering. The release bundle verifier now also cross-checks the compatibility profile summary and release summary against the live renderer, so stale overview prose fails closed after checksum refreshes as well. The packaged-artifact storage summary now also fails closed in release-bundle verification, so the storage/reconstruction posture stays semantically checked alongside the other artifact summaries. The maintainer-facing docs now also name `utc-convenience-policy-summary` alongside the other request-policy and bundle-reproducibility entry points, keeping the release-smoke guidance aligned with the current bundle payload. UTC convenience, native sidereal, time-scale, Delta T, observer, apparentness, request-policy, and API stability summaries now also have validated release helpers, so the advanced request-policy surfaces no longer rely on ad hoc validation formatting. The packaged-artifact profile output-support summary now also has validated wrappers for the full capability line and explicit support-entry rendering, keeping that release-facing capability surface fail-closed too. The compatibility-caveats summary now also cross-checks in release-bundle verification, so the caveat inventory fails closed even after checksum refreshes. The comparison tolerance policy, scope coverage, body-class tolerance posture, envelope, and Pluto fallback summary renderers now fail closed instead of panicking if comparison report construction fails, and the comparison tolerance scope coverage summary now validates every scope row before rendering, so malformed per-scope coverage no longer leaks through as partial drift text. The comparison body-class tolerance posture summary now runs through a validated helper before rendering. The comparison body-class tolerance summary renderer now also validates each class row before formatting, so malformed tolerance rows fail closed instead of rendering partial drift text.

The packaged-artifact source-fit and hold-out sync summary now has a validated CLI surface and also appears in artifact, validation, and release-bundle surfaces, so the calibrated fit thresholds can be reviewed alongside the current target-threshold posture without leaving the release-facing report set; the sync line now also carries the explicit phase-2 corpus alignment evidence so the threshold posture and corpus posture stay visible together, the source-fit regression now pins that shared phase-2 payload against the stand-alone summary and the production-ready target-threshold posture, the release-bundle verifier now cross-checks that shared phase-2 payload against the stand-alone phase-2 corpus alignment summary before accepting a staged bundle, and it now also cross-checks the target-threshold phase-2 payload against the source-fit and hold-out sync payload so the two release-facing posture lines cannot drift apart, the validation crate now also has a direct drift regression for the source-fit and hold-out sync helper, and the parent validation paths now prefix nested phase-2 field names for clearer drift diagnostics, and the production-generation source summary now also cross-checks its embedded source-window payload against the standalone source-window summary during release-bundle verification. The production-generation source-window summary now also has a matching semantic-drift tamper regression, and its missing-entry bundle regression now keeps the manifest entry itself fail-closed when omitted, so checksum refreshes cannot hide source-window prose drift.

The packaged-artifact source/provenance prose now shares the generation-tail wording between the artifact source summary and the generation-policy note, reducing drift in release-facing narrative while preserving the fail-closed validation path. The validation-report packaged-artifact profile block now also routes output support, speed policy, access, and generation policy through dedicated validated wrappers so that surface follows the same fail-closed pattern as the storage and production-profile summaries, and the packaged-artifact source-fit and hold-out sync summary now routes through a validated helper in the same release-facing report family. The validation crate now also has a direct drift regression for the source-fit and hold-out sync helper, and the release-bundle verifier now also cross-checks the packaged-artifact source-fit and hold-out sync summary against the live renderer, closing one more semantic drift path for the staged bundle. It now also cross-checks the packaged-artifact phase-2 corpus alignment summary against the live renderer, so checksum refreshes cannot mask phase-2 alignment drift, and it now also cross-checks the packaged-artifact target-threshold scope envelopes summary against the live renderer so scope-envelope drift fails closed alongside the target-threshold posture. The production-generation body-class coverage summary now also routes through a validated helper and now also stages and verifies in the release bundle, closing the last phase-2 generation-corpus bundle gap.

## Immediate blockers

1. **Source coverage** — checked-in JPL snapshots and boundary overlays are not yet a broad production corpus for all release bodies and epochs.
2. **Body claim boundaries** — Pluto, fuller lunar theory, lunar points, Ceres/Pallas/Juno/Vesta, and selected custom asteroids need source-backed validation or constrained/excluded release status.
3. **Release fail-closed behavior** — release gates must block stale generated summaries and overbroad compatibility/backend claims.

## Recommended next slice

Align the finalized threshold posture with the Phase 2 corpus and hold-out validation:

- keep the source-fit and hold-out checks synchronized with the recorded production-ready threshold policy;
- keep the artifact labeled draft until the source corpus is aligned;
- continue using the cached fit summaries and measured-fit subdivision path to keep report generation tractable.

## Parallel safe work

- Expand the documented hybrid source corpus with broader coverage and provenance.
- Align the finalized threshold posture with the Phase 2 source corpus.
- Audit body release status for Pluto, lunar channels, and selected asteroids.
- Audit house/ayanamsa entries whose release claims are stronger than their evidence.
- Keep request-policy docs and structured unsupported errors synchronized.
- Harden release-bundle verification without changing feature claims, and keep the lunar-theory source-family and lunar source-window summaries exposed as release-facing CLI surfaces with matching docs.
- The chart help block now reuses the same compact request-policy line for the request-semantics alias instead of applying a dead string rewrite, keeping the CLI help wording aligned with the docs.
- The custom-asteroid cadence classification is now pinned too, so asteroid-catalog custom bodies stay on the selected-asteroid ladder while other custom bodies retain the wider custom-body span cap.
