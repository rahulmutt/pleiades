# Status 1 — Current Execution Frontier

## Frontier

The active frontier is **Phase 1 — Accuracy Closure and Request Semantics**.

The repository is past bootstrap and release-rehearsal scaffolding. The next production blocker is closing the remaining accuracy and request-policy gaps that prevent truthful release-grade ephemeris and packaged-data claims. The compatibility-profile catalog inventory now also surfaces the explicit custom-definition ayanamsa label bucket in the compact inventory summary, so the release-facing overview makes the phase-3 classification split visible without opening the full profile.
The shared time-scale, Delta T, observer, apparentness, and frame policy summary routes now also fail closed on stray arguments in both CLIs, matching the other compact summary commands. The release-grade comparison corpus guard summary and its short alias now also have explicit extra-argument rejection coverage in both CLIs so that release-facing guard surface stays fail-closed too. The CLI dispatcher now also forwards extra-argument rejection through the comparison-corpus, benchmark-corpus, house-validation, and ayanamsa-metadata-coverage summaries so those inspection routes no longer drop trailing arguments silently. The workspace-audit and native-dependency-audit summary routes now also reject stray arguments with exact alias-specific diagnostics in both CLIs, closing the remaining audit-summary gap in the fail-closed command surface. The packaged-artifact production-profile, target-threshold, target-threshold scope-envelope, generation-manifest, generation-policy, and regeneration summary routes now also reject stray arguments in both CLIs, keeping the artifact-profile inspection surface fail-closed too. The production, comparison, reference, and holdout body-class coverage summary routes now also have short aliases in both CLIs for quicker archaeology. The selected-asteroid source-window archaeology route now also has a short alias in both CLIs for quicker provenance inspection. The reference major-body bridge sample at JD 2451915.0 now also has a direct CLI path in both front ends, keeping the integer-day bridge evidence visible beside the existing boundary-day archaeology. The backend-matrix summary now also surfaces that bridge evidence inline for quicker overview archaeology. The new short `major-body-bridge-summary` alias now mirrors that surface in both CLIs. The compatibility-profile summary now also has a short `profile-summary` alias in both CLIs, and the CLI regression now pins that mirror beside the canonical command. The comparison-snapshot and reference-snapshot summary routes now also have short aliases in both CLIs for quicker overview inspection. The production-generation boundary-window summary now also has a short `production-generation-boundary-window` alias in both CLIs for quicker generation-coverage archaeology. The VSOP87 source-audit and generated-binary-audit summaries now also have short aliases in both CLIs for quicker provenance archaeology. The JPL interpolation posture summary now also has a short `interpolation-posture` alias in both CLIs for quicker inspection.
The mean-obliquity frame round-trip summary now also has a short `mean-obliquity-frame-round-trip` alias in both CLIs for quicker frame-evidence archaeology.

## Evidence reviewed

Current summaries show:

- all mandatory crates and release/reporting commands exist;
- the checked-in reference snapshot coverage wording now matches the current rows and no longer advertises a 2406 Mars hold-out that is not present in the corpus;
- VSOP87B source-backed generated binary paths cover the Sun through Neptune;
- Pluto remains an approximate mean-elements fallback in the backend catalog, while release-grade comparison and tolerance reports now exclude Pluto from evidence; the comparison-corpus summary/report surfaces now also render an explicit release-grade guard line for that policy;
- the compact lunar baseline has documented reference evidence for supported lunar channels but is not a full ELP coefficient implementation; the lunar-theory catalog and lunar-theory catalog-validation summaries now also have direct CLI paths in both CLIs so the current one-entry lunar baseline can be inspected alongside the existing theory, capability, source, request-policy, frame-treatment, and new lunar-theory-limitations summaries;
- the JPL backend is a checked-in fixture/snapshot backend with expanded selected asteroid rows (J2000 plus 2451910.5 through 2451918.5, plus a 2451915.0 bridge sample, 2003-12-27, 2132-08-31, and 2500-01-01), a dedicated selected-asteroid body-window summary, a selected-asteroid boundary-day summary spanning JD 2451914.5..2451915.5, a new selected-asteroid bridge summary at JD 2451915.0, a new terminal selected-asteroid boundary summary at JD 2500000.0, a new reference-snapshot boundary-epoch coverage summary that now includes a fully populated 2451915.5 boundary day inside the 2451913.5..2451917.5 window, and a now-truthful boundary-day summary/CLI path for quicker archaeology, plus a new dense 2451916.5 boundary-day summary/CLI path that makes the full-body day adjacent to that day explicit as well, and the selected-asteroid subset now has its own dense 2451916.5 boundary summary/CLI path for the same day; a dedicated Mars/Jupiter boundary summary at JD 2451918.5, and its release line is now exact-string pinned in the JPL and validation regression suites, and expanded interpolation-quality coverage across 122 samples, 10 bodies, and 15 epochs, not a broad production reader/corpus; the release-summary surface now also includes the JPL interpolation posture line for one-screen transparency; the selected-asteroid batch-parity slice now also has a direct `selected-asteroid-batch-parity-summary` CLI path for mixed-frame archaeology in both CLIs, and the reference asteroid evidence/equatorial-evidence summaries now also have direct CLI paths in both CLIs; the JPL batch error taxonomy summary now also has a direct `jpl-batch-error-taxonomy-summary` CLI path in both CLIs, and the combined JPL evidence summary now also has a direct `jpl-snapshot-evidence-summary` CLI path in both CLIs; it now also carries the boundary-epoch coverage and sparse-boundary summaries for quicker archaeology; that combined report is now regression-tested to keep the production-generation snapshot, boundary-source, boundary-window, reference-asteroid source-window, and independent hold-out source-window lines visible; the reference snapshot source summary now also surfaces the broader body/epoch coverage windows that underpin the fixture, and the reference and hold-out provenance summaries now fail closed on exact canonical source, coverage, frame/column, and reference-epoch drift so the TDB anchor and release-facing wording stay explicit, and the reference/comparison/hold-out manifest wrappers now fail closed on expected title, source, coverage, and column drift before rendering; the checked-in CSV manifest header blocks now also fail closed on duplicate or reordered comment drift before the report layer renders them; the checked-in reference corpus now includes the expanded selected-asteroid boundary slice plus the 2451913.5 through 2451917.5 major-body boundary window and the current 19-epoch corpus shape, now totaling 233 rows with 71 asteroid rows, while the 2451917.5 comparison-body boundary day is now fully populated and the comparison and interpolation-quality corpora keep their release-grade windows by excluding that reference-only epoch; the reference snapshot coverage line now also names the 2451914.5 and 2451915.5 selected-asteroid boundary coverage inside the 2451910.5 through 2451918.5 window, and the major-body high-curvature slice now extends through JD 2451916.5 as an additional boundary sample; the comparison snapshot now carries its own typed body-window summary and a matching body-class coverage summary for the 132-row comparison slice, and the validation/release summary surfaces now stay aligned with the current 132-row / 16-epoch comparison corpus; the independent hold-out corpus now also exposes typed source-window and body-class coverage summaries for its validation slice, and the independent hold-out source-window summary now has a direct CLI path in both CLIs; the comparison snapshot batch-parity summary, the reference snapshot batch-parity summary, the reference snapshot equatorial-parity summary, and the independent hold-out batch/equatorial parity summaries now also have direct CLI paths in both CLIs; the new reference/hold-out overlap summary makes the current 34 shared body-epoch pairs explicit so future corpus expansion avoids reusing hold-out rows; the checked-in reference and hold-out CSV corpora now also have a direct regression test anchoring their documented 212-row / 34-row shapes and the 34 shared body-epoch pairs; the release summary and validation report now surface that overlap line alongside the hold-out source-window evidence so the shared validation slice stays visible in the main release-facing reports, and the overlap summary now has a direct CLI path in both CLIs;
- selected asteroid reporting now also surfaces a broader source-backed window set (71 samples across 5 bodies and 15 epochs) alongside the exact J2000 slice for provenance and validation, plus a mixed-frame batch-parity summary for the exact J2000 request slice; those source evidence/window summaries now validate and fail closed on drift, including custom-body drift checks on the source-window summaries, and the selected-asteroid evidence/window/batch-parity slices are now also surfaced in the backend-matrix-summary, release-notes-summary, and release-summary views so report consumers can inspect them without opening the detailed backend report; the backend-matrix-summary also now carries the dense reference boundary day so the 2451916.5 archaeology slice is visible from that same overview; the 2451915.5 boundary-day summary now also carries a typed body list and validates it directly, documenting the now-populated boundary day; packaged-data boundary tests now also reject requests outside the bundled time range with `OutOfRangeInstant`, adding a concrete out-of-range regression check to the compressed-artifact boundary envelope;
- the JPL backend now exposes a production-generation boundary overlay corpus that appends the full independent hold-out validation snapshot to the checked-in reference snapshot for validation and artifact-generation work, and the evidence report now also surfaces the boundary-overlay provenance alongside that broader coverage slice; the overlay now also has a standalone request corpus, inventory summary, per-body window summary, body-class coverage summary, and provenance summary for generation tooling, and the validation report now surfaces the boundary request corpus directly, and the production-generation boundary request corpus now also has a direct CLI path in both CLIs, the production-generation boundary source summary now also has a direct CLI path in both CLIs, and the merged production-generation corpus now also exposes a combined source-provenance summary with a direct CLI path in both CLIs, and the new short `production-generation` alias now mirrors the overall production-generation coverage summary in both CLIs for quicker archaeology; the production-generation boundary window summary now validates its derived slice and the release-facing report path fails closed on drift; the reference snapshot provenance strings now explicitly distinguish the 2001-01-07 major-body boundary sample from the 2001-01-02..2001-01-05 span, and the reference major-body boundary summary now labels the 2001-01-08 Mars/Jupiter boundary sample explicitly; the Mars/Jupiter, Mars outer-boundary, major-body boundary, and major-body boundary-window summaries now also have short CLI aliases in both front ends for quicker archaeology; the lunar and major-body high-curvature reference summaries now also validate their summary lines and report wrappers fail closed on drift; the interpolation-quality corpus now tracks the current 122-sample, 10-body, 15-epoch evidence set;
- the compact validation-report summary now also surfaces a dedicated reference snapshot section (summary, source windows, source provenance, and body-class coverage) so the reference coverage posture stays visible without opening the full report; the JPL snapshot regression suite now also anchors the 1749-12-31 and 1800-01-03 boundary rows for exact lookup parity;
- the packaged artifact is deterministic and validated as a prototype, and the checked-in fixture has been regenerated from the updated reference snapshot, but current fit errors are not release-grade; the artifact summary now also surfaces the deterministic generation manifest alongside the production-profile skeleton and a compact body-class mix so release-facing inspection includes the manifest currently driving regeneration and the current bundled body composition; the compact validation and release summaries now also surface the current packaged-artifact target-threshold posture, body-class scope envelopes, and generation manifest so the prototype production posture is visible from the main release-facing report surfaces, and the target-threshold scaffold now carries the release-profile identifier through that same surface; the packaged-artifact target-threshold and target-threshold scope-envelope summaries now also have direct CLI inspection paths in both CLIs, keeping the still-open production fit posture directly visible beside the existing artifact profile summaries; the packaged-artifact output-support posture now also has a direct `packaged-artifact-output-support-summary` CLI path in both CLIs, keeping the stored/derived/unsupported output matrix easy to inspect alongside the other artifact profile summaries; the packaged-artifact storage/reconstruction summary now also has a direct `packaged-artifact-storage-summary` CLI path in both CLIs, keeping the storage/reconstruction posture visible beside the rest of the artifact profile summaries; the packaged-artifact generation policy and residual-bodies summaries now also have direct CLI paths in both CLIs, keeping the generation skeleton and residual channel posture visible beside the rest of the artifact profile summaries; the merged production-generation corpus now also exposes a release-facing source-window summary so the combined reference+boundary span is visible per body in reports before Phase 2 fit claims harden, and the merged and boundary production-generation body-class coverage summaries now surface the major-body versus selected-asteroid split directly in those same reports; the reference and hold-out manifest report helpers now fail closed on exact title, source, coverage, and column drift before rendering;
- interpolation-quality reporting now includes an explicit provenance line derived from the checked-in reference snapshot, alongside the existing interpolation-quality and hold-out summaries, and the interpolation posture itself is now surfaced as a typed release-facing summary so the runtime-validation/transparency-only decision stays explicit; the same posture now also has a dedicated `interpolation-posture-summary` CLI command for direct inspection, the interpolation-quality evidence slice now also has a direct `interpolation-quality-summary` command for release-facing archaeology, and the lunar apparent comparison evidence now also has a direct `lunar-apparent-comparison-summary` CLI path, the lunar source-window summary now also has a direct `lunar-source-window-summary` CLI path, and the lunar reference and lunar equatorial error-envelope evidence now also has dedicated `lunar-reference-error-envelope-summary` and `lunar-equatorial-reference-error-envelope-summary` CLI paths;
- reference-snapshot reporting now also surfaces the broader lunar source-window summary combining the published 1992-04-12 Moon example with the J2000 high-curvature 2451911.5/2451912.5 window used for lunar-fit closure evidence plus the reference-only apparent Moon comparison windows used to broaden lunar source provenance, and the snapshot source-window summary now breaks the checked-in reference coverage out by body for release-facing archaeology with typed window validation; the new major-body high-curvature reference slice now extends that coverage through the 2451914.5 boundary day across all comparison bodies so the broader corpus evidence is visible without decoding the fixture, and the new major-body high-curvature body-window summary now exposes the per-body spans directly in report surfaces; the high-curvature evidence, window summaries, and epoch coverage summary now also have direct CLI inspection paths in both CLIs, and the short `high-curvature-summary` and `high-curvature-window-summary` aliases now mirror those report blocks in both CLIs; the release-grade comparison corpus still intentionally excludes the 2451913.5 boundary day so the current audit slice remains clean while tolerance thresholds stay anchored to the 132-row / 16-epoch comparison corpus;
- request policy is explicit: mean, tropical, geocentric TT/TDB requests are supported; shared request validation now fails closed on both mean-only and apparent-only value-mode mismatches, using specific unsupported-feature errors for apparentness and sidereal-mode rejections; apparent-place corrections, topocentric body-position requests, native sidereal backend output, and built-in Delta T/UTC convenience conversion are not implemented today; topocentric rejections now use a dedicated `UnsupportedObserver` kind while invalid coordinates keep `InvalidObserver`, and the observer/request policy report strings call out that split explicitly; the chart CLI now also accepts explicit UTC/UT1 TT-offset aliases plus explicit UTC/UT1 TDB-offset aliases for the common conversion step without changing backend policy, and the policy docs plus README now mention those aliases directly; the chart-façade conversion regression now also verifies that caller-supplied time-scale conversions preserve body-observer metadata and the house-observer separation; the chart help text now also repeats the house-observer separation explicitly so callers do not mistake an observer-bearing chart request for a topocentric body-position mode; the shared request-policy and frame-policy summaries now fail closed on blank, whitespace-padded, or multiline drift, and the JPL provenance summary wrappers now reject embedded line breaks; release-facing request-semantics wording is now centralized behind a shared formatter in `pleiades-validate`, and the validation and CLI surfaces now also expose a dedicated `request-policy-summary` / `request-semantics-summary` command so the compact request posture is directly inspectable from the same report entrypoints; the new `request-policy` short alias now mirrors that summary in both CLIs, the `request-semantics` short alias now mirrors it too, the `request-policy-summary` route now also rejects extra arguments in both CLIs, and the `request-semantics-summary` alias now also reports its own extra-argument rejection name so direct alias diagnostics stay exact. The request-surface inventory now also has a dedicated `request-surface-summary` command in both CLIs, the short `request-surface` alias is mirrored in both CLIs too, that inventory now names the chart TT/TDB alias set explicitly instead of only naming the generic offset family, and the request-surface command now also rejects stray arguments through the shared validation path so the compact inventory fails closed like the other summary commands. A dedicated `release-profile-identifiers-summary` command now exposes the release-profile pair directly as well. A dedicated `time-scale-policy-summary` command now exposes the shared time-scale posture directly as well. A dedicated `delta-t-policy-summary` command now exposes the deferred no-built-in-Delta-T posture directly as well. The time-scale, Delta T, observer, apparentness, and frame policy summaries now also have short aliases in both CLIs for faster request-semantics archaeology. The comparison-tolerance and comparison-envelope aliases now also preserve their exact extra-argument labels through the validation wrapper. A dedicated `comparison-tolerance-policy-summary` command now exposes the release comparison-tolerance posture directly in both CLIs as well, and the new short `comparison-tolerance-summary` alias now mirrors it in both front ends. The new `pluto-fallback-summary` command now also gives the Pluto approximate fallback posture a dedicated direct CLI path for quick inspection, and the short `pluto-fallback` alias now mirrors it in both CLIs. That formatter now validates the shared request-policy summary before rendering the observer, apparentness, and request-policy lines so drifted policy prose fails closed in the report layer; the observer and apparentness policy summaries themselves are now typed and validated before the report layer renders them, and the observer/apparentness policy summaries now also have dedicated CLI inspection paths in both CLIs. The Delta T posture is now surfaced separately as its own typed policy summary so the deferred no-built-in-Delta-T decision stays explicit too; the lunar-theory request-policy and frame-treatment summaries now also have direct CLI inspection paths in both CLIs; chart-snapshot diagnostics now reuse the shared backend time-scale, frame, and apparentness summaries so the render path stays aligned with the same contract, and the core frame-transform regression coverage now includes a near-polar mean-obliquity round-trip case plus a negative-right-ascension normalization sample and a reverse-transform wraparound sample with a negative-declination seed so the precision expectations stay visible across the ecliptic/equatorial conversion edge cases; the mean-obliquity validation corpus now also includes matching north- and south-polar wraparound samples so the report-facing frame evidence exercises both hemispheres, and the mean-obliquity frame round-trip summary now also has direct CLI inspection paths in both CLIs; compatibility-profile summaries now also surface house formula families alongside the broader catalog breadth, and the compatibility-profile validation now fails closed if documented custom-definition ayanamsa labels drift away from metadata coverage; routing metadata now canonicalizes mixed-scale nominal ranges so valid composite providers keep validating cleanly; comparison and regression tolerance audits now use body-class-specific release thresholds for the active planetary corpus, and the new comparison-envelope-summary command now also exposes the aggregate comparison envelope and percentile tail directly in both CLIs. The new short `comparison-envelope` alias now mirrors that summary in both CLIs. The house catalog validation summary now also renders the built-in formula-family coverage inline through a shared `pleiades-houses` helper, and the house-validation corpus, house-formula-families, and ayanamsa catalog validation summaries now also have direct CLI inspection paths in both CLIs, matching the rest of the phase-3 audit surfaces; the new short `house-formula-families` alias now mirrors that summary in both CLIs for quicker archaeology. The ayanamsa sidereal metadata coverage summary now has direct CLI inspection paths in both CLIs as well, and it now renders `missing-sidereal-metadata=none` explicitly so the current no-gap posture stays visible in release-facing reports. The latest request-semantics hardening also covers mixed apparent-plus-topocentric batch failures so the unsupported-mode precedence stays explicit in regression coverage. The backend batch validator now also preserves apparentness before sidereal rejection when apparent and sidereal are combined in the same unsupported slice. The same precedence is now pinned when apparent, sidereal, and topocentric flags all appear together in a batch request, so the failure ordering stays explicit across the full unsupported-mode stack.

## Why this frontier comes first

Phase 2 production artifacts require trusted generation inputs and tolerances. Phase 4 release claims require the same evidence. Therefore the next work should close source-backed accuracy gaps before expanding packaged-data or compatibility claims that depend on those outputs.

## Immediate blockers

1. **Reference corpus breadth** — Expand source/reference data enough to support production validation and artifact generation, not only fixture exactness.
2. **Advanced request semantics** — Decide whether Delta T/UTC convenience, apparent corrections, and topocentric body positions are implemented for the first release or intentionally deferred with metadata and structured errors.

## Recommended next slice

Pluto downgrade and corpus cleanup is complete:

- keep Pluto explicitly labeled as approximate in backend metadata and fallback provenance;
- use the Pluto-excluded release-grade corpus for comparison/tolerance evidence;
- preserve the full snapshot corpus for provenance and validation archaeology;
- keep release tolerance audits fail-closed if Pluto is advertised beyond its evidence.

The next slice should focus on broader reference coverage and the remaining request-semantics decisions. As a small step in that direction, the reference major-body boundary evidence at JD 2451917.5 now has a dedicated summary, a dedicated boundary-window summary, and direct CLI paths; the 1749-12-31 major-body boundary anchor now also has a dedicated summary and direct CLI paths; the early major-body boundary anchor at JD 2378498.5 now also has a dedicated summary and direct CLI paths; the new 1800-01-03 major-body boundary sample now also has a dedicated summary and direct CLI path; the 2451915.5 boundary day now also has its own dedicated summary/CLI path; the short aliases `1749-major-body-boundary-summary`, `early-major-body-boundary-summary`, and `1800-major-body-boundary-summary` now mirror those report blocks in both CLIs; the reference snapshot provenance text now also calls out the Mars outer-boundary tail at JD 2600000.0 and JD 2634167.0; the backend-matrix summary now also surfaces the boundary-epoch coverage and boundary-day lines so the now-populated 2451915.5 day is visible from the backend overview too; and the compact validation-report summary now surfaces that block for easier archaeology.

## Parallel safe work

The following can proceed without distracting from accuracy closure:

- house/ayanamsa formula and alias audits; the compatibility-profile display path now validates before rendering so direct profile output fails closed on invalid catalog metadata and unknown house-formula families, and the catalog inventory summary now also surfaces house formula-family coverage alongside alias counts;
- documentation cleanup for already-explicit request policy;
- release-bundle smoke-test maintenance, including the `compare-backends-audit` comparison-audit command now mirrored in `pleiades-cli`, plus the top-level `benchmark` command now mirrored there as well, and the workspace-audit summary now also has a `native-dependency-audit` / `native-dependency-audit-summary` alias in both CLIs; release bundles now also archive `native-dependency-audit-summary.txt` alongside `workspace-audit-summary.txt`, `compatibility-caveats-summary.txt` alongside `request-policy-summary.txt`, `catalog-inventory-summary.txt` alongside the other release-facing compatibility summaries, and `release-profile-identifiers-summary.txt` alongside `release-profile-identifiers.txt` so the pure-Rust audit wording, known-gap posture, catalog totals, and release-profile pair are preserved in staged release artifacts; the smoke tests now also pin the compatibility-caveats and native-dependency audit entries so those archived files stay covered alongside the existing bundle inventory assertions;
- artifact-profile identifier groundwork and production-profile skeleton plumbing that do not claim production fit accuracy yet, including the release-profile identifier now threaded through packaged-artifact target thresholds, plus the new `artifact-profile-coverage-summary`, `packaged-artifact-production-profile-summary`, `packaged-artifact-generation-manifest-summary`, and `packaged-artifact-regeneration-summary` direct CLI paths for the packaged-artifact profile coverage, production-profile skeleton, generation-manifest posture, and regeneration provenance, the validation-CLI `regenerate-packaged-artifact` workflow for mirroring the packaged fixture check/write path, and the new `comparison-snapshot-summary`, `reference-snapshot-summary`, `comparison-corpus-summary`, and `benchmark-corpus-summary` direct CLI paths for the overall snapshot and corpus coverage posture;
- direct ayanamsa reference-offset inspection now also has a dedicated `ayanamsa-reference-offsets-summary` command in both CLIs for release-facing archaeology, and the representative offset set now includes J2000, J1900, B1950, True Citra, Lahiri (ICRC), Lahiri (1940), True Sheoran, Suryasiddhanta (499 CE), and the galactic-center / Dhruva anchors alongside the earlier baseline entries.
- direct compatibility-caveats inspection now also has a dedicated `compatibility-caveats-summary` command in both CLIs so the profile's known-gap posture stays visible apart from the broader compatibility-profile summary.
- VSOP87 source-documentation, source-documentation-health, source-audit, and generated-binary-audit summaries now also have direct CLI paths in both CLIs, so the release-facing provenance layer can be inspected without opening the broader validation report.
- reference snapshot source-window formatting parity now matches the other coverage summaries so report surfaces can rely on `to_string()` consistently, the source-window summary implementation now derives body order directly from the checked-in snapshot entries, the new reference body-class coverage summary now surfaces typed major-body and selected-asteroid window breakdowns in release-facing reports, the compatibility profile now also renders the latitude-sensitive house-system set through the shared formatter used by validation reports, the new reference/hold-out overlap summary guards against silently collapsing the validation slice into the reference corpus, the independent hold-out source-window summary now has a direct CLI path in both CLIs, the selected-asteroid boundary and source-window evidence now have direct CLI paths in both CLIs, the comparison-snapshot-source-summary and reference-snapshot-source-summary commands now also expose the snapshot source metadata directly in both CLIs, the lunar apparent comparison evidence now also has a direct `lunar-apparent-comparison-summary` CLI path, and the lunar reference and lunar equatorial error-envelope evidence now also have dedicated `lunar-reference-error-envelope-summary` and `lunar-equatorial-reference-error-envelope-summary` CLI paths, the reference/comparison snapshot manifest summaries now also have direct CLI paths for provenance inspection, and the packaged lookup epoch policy now also has a direct CLI path in both CLIs, with the short `packaged-lookup-epoch-policy` alias now mirrored in both CLIs as well.

## Constraints

- Preserve pure Rust and first-party crate layering.
- Do not couple domain crates to concrete backends.
- Do not loosen unsupported-mode errors to silently satisfy apparent/topocentric/native-sidereal requests.
- Do not publish broader compatibility or accuracy claims until validation evidence supports them.
