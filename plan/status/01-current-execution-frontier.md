# Status 1 — Current Execution Frontier

## Frontier

The active frontier is **Phase 1: Production Reference/Source Corpus**. Production compressed-data work depends on a broader source and hold-out corpus, so artifact changes must not broaden release claims until Phase 1 is complete. The release bundle now also carries the reference snapshot body-class coverage summary alongside the reference source-window artifact, the reference snapshot equatorial parity summary alongside that coverage artifact, and the independent-holdout equatorial parity summary alongside the hold-out source-window artifact, keeping the staged frame-coverage evidence visible without changing the Phase 1 scope. The release bundle now also carries the reference snapshot major-body boundary window, boundary epoch coverage, and pre-bridge boundary summaries alongside the bridge-day and sparse-boundary evidence, with manifest and semantic verification wired for each slice. The release bundle now also carries the house-code-aliases, house-formula-families, and latitude-sensitive house summaries alongside the release-house-validation summary, and the compatibility-caveats summary now also surfaces the house-formula-families posture directly, keeping the broader house posture visible in staged bundles. The consolidated source-corpus summary now also surfaces reference and independent-holdout body-class coverage directly, so body-count and epoch-count drift are visible in the same release-facing provenance block, and it now explicitly includes the production-generation source-window summary and reference snapshot exact J2000 evidence so the consolidated corpus block carries the source-window and fixture-exactness payloads alongside the boundary-request corpus payload. The source-corpus summary validation now also exercises the comparison-corpus guard and the JPL contract/classification/provenance-only fields directly, so the top-level provenance block fails closed on those remaining corpus posture lines as well. The source-corpus construction now also rejects duplicated JPL label prefixes in nested payloads, keeping those embedded audit labels normalized rather than silently double-prefixed. The independent hold-out slice now also extends the selected-asteroid outer boundary to JD 2634167 for asteroid:433-Eros and asteroid:99942-Apophis, and the 2378498.5 selected-asteroid bridge now also includes asteroid:99942-Apophis in both the reference and hold-out slices, keeping the release-claimed asteroid boundary coverage aligned with the reference corpus without broadening release claims. The independent hold-out slice now also includes Sun, Moon, Mercury, and Venus at JD 2451915.5, bringing the hold-out boundary row count to 84 rows across 16 bodies and 14 epochs while keeping the major-body boundary slice aligned with the reference corpus; the independent-holdout source coverage line now makes that 84-row / 16-body / 14-epoch posture explicit in the release-facing provenance text. The selected-asteroid Apophis bridge now renders consistently across the manifest, source-window, and production-generation summary surfaces after the latest validation pass. Release-bundle verification now also re-checks the reference and independent-holdout equatorial parity summaries against the current renderers, closing the last unchecked staged parity summaries. The CLI now also exposes the reference-snapshot mixed TT/TDB batch parity summary and alias, and it now also exposes the body-date-channel claims summary and alias, plus the latitude-sensitive house-constraints summary and alias, while the body/date/channel claims summary now also surfaces the production-generation coverage posture explicitly, keeping the reference corpus batch shape and release-claim boundary directly inspectable without leaving the front-end.

Recent implementation note: the consolidated source-corpus posture now also has a standalone `source-corpus-summary` / `source-corpus` surface in the validation and CLI front-ends, so the release-grade guard, JPL source-corpus contract, and phase-2 corpus alignment can be inspected directly without going through the larger release summary. The comparison audit gate now also has a dedicated compact `comparison-audit-summary` / `comparison-audit` surface in the validation and CLI front-ends, reusing the release-grade comparison corpus while keeping the full `compare-backends-audit` report available for regression detail. The consolidated source-corpus surface now also carries the shared schema label explicitly, keeping the release-facing corpus block aligned with the JPL fixture schema contract, and now uses a typed, field-validated summary record so drift in any embedded corpus-provenance line fails closed with a field-specific error. The source corpus summary now also surfaces the generation-command fragment and production-generation coverage posture alongside the shared-schema and provenance split, and now also carries the production-generation source provenance line plus the boundary source, reference snapshot sparse boundary, exact J2000 evidence, equatorial parity lines, and explicit production-generation date range, keeping the phase-1 provenance contract explicit. It now also embeds the validated body/date/channel claims line and now carries the production-generation source-revision payload explicitly, so the release-facing corpus block carries the corpus-shape/body-claim pairing and corpus version in one validated surface. The source-corpus summary validation now also exercises the release-grade body claims, body/date/channel claims, and phase-2 corpus alignment fields, so the remaining embedded posture lines fail closed with field-specific errors as well. The body/date/channel claims surface now also carries the frame-policy posture explicitly, so the compact claim boundary now makes frame coverage visible alongside body and corpus-shape claims. The release body-claims summary now also has a dedicated fail-closed bundle-verification check, and the Pluto fallback summary now also has a dedicated current-rendering recheck, so staged release-grade claim artifacts now fail closed on semantic drift as well as checksum drift. The checked-in snapshot schema posture now also derives from validated reference and hold-out manifest columns rather than a hardcoded literal, so the shared schema line now fails closed if either checked-in fixture drifts from the canonical `epoch_jd, body, x_km, y_km, z_km` layout. The independent-holdout manifest and source-window coverage now continue to validate the 2378498.5 Apophis bridge cleanly after the bridge-day alignment update, and the hold-out source revision checksum was resynced after the 84-row boundary expansion so the provenance sidecar matches the checked-in CSV payload. The catalog posture derived from the compatibility profile is now also exposed as a dedicated `catalog-posture-summary` / `catalog-posture` surface, and the compatibility-profile verification summary reuses the shared core helper so the posture line stays consistent across report surfaces; the catalog posture line now also surfaces ayanamsa alias-bearing entry counts, explicit ayanamsa metadata-gap counts, and explicit known-gap entries alongside the metadata-bearing/descriptor-only split, and the canonical known-gaps summary text is now shared across the compact posture and bundle-verification surfaces. Release bundle rendering and verification now also carry the standalone source-corpus summary file and checksum, so the bundle contract stays aligned with the top-level source-corpus surface. The staged bundle now also carries the house-code-aliases, house-formula-families, and house-latitude-sensitive audit summaries, and bundle directory validation counts them in the release checklist contents line. Release bundle verification now also re-checks the comparison-corpus summary against the current renderer, so the release corpus posture fails closed on semantic drift. Release bundle verification now also re-checks the release notes text against the current renderer, so staged release-note artifacts fail closed on semantic drift. Release bundle verification now also re-checks the backend matrix report and summary against the current renderers, so staged backend-matrix artifacts fail closed on semantic drift. Release bundle rendering now also carries the selected-asteroid source request corpus equatorial summary, so the staged selected-asteroid slice exposes both request-frame variants. The packaged-artifact phase-2 corpus alignment summary now also carries that equatorial selected-asteroid request-corpus surface, so the release-facing corpus alignment line explicitly covers both frame variants. Release bundle rendering and verification now also carry the production-generation boundary request corpus equatorial summary, so the boundary overlay corpus exposes both request frames in staged releases and the equatorial checksum/semantic posture is verified alongside the ecliptic boundary request corpus. Release bundle rendering and verification now also carry the catalog posture summary and checksum, so catalog-posture release surfaces stay aligned with the compatibility profile. The compatibility profile's known-gaps line now also has a dedicated `known-gaps-summary` / `known-gaps` inspection surface in the validation and CLI front-ends, so release caveats can be inspected directly instead of only through the larger caveats block. The production-generation boundary request corpus now also has a dedicated equatorial summary surface, matching that frame split for the boundary overlay corpus, and its validation now reports field-specific drift for request count, body count, bodies, epoch count, epochs, time scale, zodiac mode, and apparentness instead of a generic derived-summary mismatch. The request-mode policy surface now also includes a dedicated zodiac-policy summary in the staged release bundle and CLI/validation front-ends, keeping the policy-report set complete without changing the underlying tropical-only posture. The release summary and backend matrix now also keep the embedded JPL source corpus contract label singular instead of repeating it inside the consolidated provenance lines, and the backend matrix and release summary now also surface the dedicated catalog posture line derived from the compatibility profile. The selected-asteroid 2634167 outer-boundary source slice now also has dedicated validation and CLI entrypoints, keeping the release-facing source coverage visible alongside the 2001-01-08, 2003-12-27, 2378498.5, and 2500000 slices. The production-generation source-corpus cadence check now also fails closed if the ecliptic and equatorial boundary-request epoch counts drift apart. The production-generation corpus-shape validation now also checks cross-frame parity for request, body, epoch, time-scale, and apparentness coverage between the ecliptic and equatorial boundary request corpora. The production-generation boundary-request corpus parity validation now also compares the ecliptic and equatorial request corpora field-by-field and fails closed on drift in request count, body count, bodies, epoch count, earliest/latest epoch, time scale, zodiac mode, or apparentness. The lunar reference mixed TT/TDB batch parity summary now also has a direct validation/CLI entrypoint, keeping the phase-1 batch-shape evidence user-facing in both reference corpora.

## Why this frontier comes first

The specification requires compressed 1500-2500 CE data to be reproducible from public inputs and validated against measured error envelopes. The current artifact and reports are useful infrastructure, but current comparison output still shows production tolerance failures. A better generator alone is insufficient until the source corpus is broad, documented, and separated into fitting/reference/hold-out evidence.

Recent implementation note: production-generation source-summary validation now checks the exact rendered source-revision checksum payload, so checksum drift in the corpus provenance block is no longer only implied by nested struct equality. The new production-generation corpus-shape summary now ties the validated source summary to both the ecliptic and equatorial boundary request corpora, so body order, epochs, frame, time scale, columns, apparentness, and checksum posture are surfaced together in release-facing reports; the boundary-request validation coverage now also has explicit regression tests for body-count, body-order, epoch-count, frame, time-scale, and apparentness drift. Source-revision checksums are also exposed through a dedicated `production-generation-source-revision-summary` CLI/report surface, validated against the current fixture checksums, and bundled/verified as a first-class release artifact. The production-generation source summary now also validates the explicit evidence-class fragment (`reference`, `hold-out`, `boundary overlay`, `provenance-only`), spells out the evidence-class split, and now makes the license posture explicit alongside redistribution posture, and its cadence fragment is now derived from the checked-in source-window and boundary-request corpus counts rather than a hardcoded prose pair, so the source-window payload stays partitioned after rendering changes. Reference and hold-out source summaries now also carry explicit frame/time-scale posture fields, and the release-facing body-class coverage summaries now reuse the shared celestial-body class taxonomy. The release-checklist bundle-contents list now carries the current 19-item posture, and the CLI release-checklist-summary test was resynced to that count so the compact release checklist view stays aligned with the validation crate's summary contract. JPL reference and hold-out provenance summaries now also carry explicit evidence-class labels and now record explicit frame-treatment and time-scale posture, the boundary-overlay render path preserves that label in release-facing output, the JPL source corpus contract now has a typed, fail-closed release-facing summary, and that contract now also has a direct CLI/report entrypoint for release-audit checks. The JPL evidence summary now renders a dedicated provenance-only posture line, provenance-only evidence now also has a dedicated CLI/report entrypoint, and release bundle generation/verification now also carry the standalone JPL provenance-only evidence summary so that provenance-only posture is bundled and fail-closed alongside the other release-facing corpus reports. The selected-asteroid 2001-01-08 source slice now also has a dedicated CLI/report entrypoint for direct inspection alongside the 2003-12-27 and 2500000 slices. The selected-asteroid 2378498.5 bridge slice now also has a direct-inspection entrypoint so the bridge-day corpus can be inspected on demand. The comparison snapshot manifest summary now fails closed on redistribution drift, the backend matrix summary now folds in the comparison corpus release-grade guard, reference/hold-out overlap, independent hold-out, release-grade body claims, Pluto fallback posture, and validated production-generation corpus claims, release bundles now carry independent-holdout body-class coverage alongside the existing source-window evidence, release bundles now also carry the production-generation corpus-shape summary alongside the source-window and manifest summaries while directory verification fails closed if it is missing, and the release summary now surfaces validated production-generation body-class coverage and corpus-shape lines alongside the release-grade body-claims line. The independent hold-out fixture was also broadened with selected-asteroid and major-body bridge rows at JD 2451915.5 in addition to the existing JD 2378498.5, JD 2451545, JD 2451917.5, JD 2453000.5, and JD 2500000 anchors, so hold-out validation now spans 84 rows across 16 bodies and 14 epochs while the major-body boundary slice remained unchanged. The compatibility inventory now surfaces ayanamsa alias-bearing entries and representative ayanamsa provenance examples so the alias/provenance audit remains visible in release-facing summaries, and the release-house-validation summary now also surfaces house-code aliases alongside the baseline house corpus. The production-generation source summary now also carries an explicit apparentness posture (`apparentness=Mean`) and schema posture (`schema=epoch_jd, body, x_km, y_km, z_km`) so the corpus provenance line and the boundary-request corpus contract stay aligned on request semantics, and the source-corpus summary now also carries the production-generation boundary-request corpus in both ecliptic and equatorial frames so the corpus provenance block exposes the same boundary overlay contract as the release bundle. Selected-asteroid source evidence and source-window summaries now also expose explicit evidence-class, frame, and time-scale posture, keeping the selected-asteroid corpus slice aligned with the broader provenance contract. The release summary, validation report, and compact corpus views now also surface a consolidated source-corpus line combining the comparison corpus release-grade guard, the JPL source corpus contract, and the phase-2 corpus alignment, and that source-corpus line now also surfaces the JPL evidence-classification and provenance-only split explicitly; the consolidated source-corpus surface now also binds the release-grade body-claims posture directly, and the request-policy and request-semantics summary aliases are now validated independently: request-policy remains lean, while request-semantics carries the explicit unsupported-modes line for the UTC/Delta-T, topocentric, apparent, and native-sidereal deferrals. The compatibility inventory now also distinguishes constrained house systems, descriptor-only ayanamsa entries, and custom-only labels so the catalog posture stays explicit in release-facing output. The consolidated source-corpus summary now also fails closed if any embedded JPL labels lose their required prefixes, and now surfaces the reference snapshot sparse-boundary evidence line explicitly, keeping the release-facing corpus posture strict about summary drift. The backend matrix and release summary now also surface a corpus-derived body/date/channel claims line that binds release-grade body claims to the current corpus-shape posture, and the release summary now also surfaces the compatibility-profile known gaps explicitly so the top-level release posture keeps caveats visible. Validation-report summary bundle verification now treats per-run benchmark timing lines as volatile so staged summaries remain reproducible across bundle generation and verification. Release-bundle manifest checksum sidecar validation now also includes offending-line context in its error messages, making malformed checksum diagnostics easier to audit without relaxing canonical-format checks. Benchmark provenance now also records rustfmt and clippy version strings, tightening the tool-version provenance carried through bundled validation reports.

## Immediate blockers

1. **Production source coverage** — checked-in snapshots do not yet form a broad production corpus for all release bodies, channels, and epochs.
2. **Claim boundaries** — Pluto, full lunar theory/lunar points, and selected asteroids need source-backed validation or explicit constrained/excluded status.
3. **Artifact promotion** — packaged data must remain draft-grade until it passes thresholds against production reference and hold-out corpora.
4. **Compatibility evidence** — broad house and ayanamsa catalogs need formula/provenance audits before stronger compatibility claims.

## Recommended next slice

Broaden the production corpus around the new contract surface:

- add or generate representative source coverage for every release-claimed body/channel/frame;
- separate fitting, reference, boundary, hold-out, and provenance-only evidence in the source corpus itself;
- derive backend matrices and release profiles from validated corpus evidence;
- wire any remaining corpus validation gaps into release-facing reports without manually curated summary prose.

## Parallel safe work

- Improve packaged-artifact fitting and reconstruction behind draft labels.
- Audit body release status for Pluto, lunar channels, and selected asteroids.
- Audit house/ayanamsa entries whose release status is stronger than their evidence.
- Keep unsupported request modes documented and structurally rejected.
- Harden release gates where they check current evidence rather than expanding claims.
