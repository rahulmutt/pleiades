# Status 1 — Current Execution Frontier

## Frontier

The active frontier is **Phase 1: Artifact accuracy and packaged-data production**, with **Phase 2: Reference/source corpus productionization** and **Phase 3: Body-model completion and claim boundaries** as dependencies.

Recent progress: the validation-report interpolation-quality subsection now appends the JPL body-class error-envelope evidence line, so the release-facing backend diagnostics surface the empirical envelopes alongside the existing quality, coverage, posture, and hold-out rows. The checked-in packaged-artifact fixture has now been regenerated from the current code path, which restores the artifact checksum sidecar assertions and the repeat-write regression to green. The interpolation-quality sample request corpus summary now also stages in the release bundle and the bundle verifier now cross-checks it against the live renderer, so the sampled Phase 2 request corpus fails closed on semantic drift. The benchmark corpus summary now also has a validated wrapper and the release bundle verifier now cross-checks it against the live renderer, so the benchmark corpus posture fails closed alongside the other bundle evidence lines; the release-bundle regression now also rejects semantic benchmark-corpus-summary.txt drift even after its checksum sidecar is refreshed. The packaged-artifact phase-2 corpus alignment summary now has its own dedicated validation error type, so the phase-2 posture can fail closed with a more explicit API contract instead of reusing the target-threshold error type. The packaged-artifact regeneration write path now reuses the committed artifact bytes while the `--check` path continues to exercise the regeneration validation path, keeping the release-gate smoke tests tractable without changing the checked-in fixture bytes, and the CLI regeneration path now uses the cached regenerated bytes helper directly so repeated sidecar writes stay aligned with the live rebuild cache; the CLI regression suite now also invokes the sidecar-writing path twice and checks the emitted artifact and sidecars stay stable across repeated writes. The packaged-artifact generation manifest checksum summary and the production-generation manifest summary/checksum helpers now also cache their rendered strings in-process, trimming repeated bundle/report recomputation. The comparison snapshot source, source-window, body-class coverage, and batch-parity CLI commands now also route through validated helpers instead of the fallback-string path, so malformed comparison snapshot provenance fails closed at the command boundary. The reference snapshot source-window summary now also routes through a validated helper, so the checked-in reference source-window surface fails closed alongside the rest of the reference-corpus provenance lines. The chart CLI help clause now validates the shared request-surface inventory before rendering the explicit UTC/UT1 and TT/TDB alias wording, so the user-facing help path fails closed if the request-surface posture drifts. The release-bundle verifier now also cross-checks the packaged-artifact target-threshold summary against the current render helper, so stale threshold prose fails closed alongside the other release-facing summaries, and the release bundle now stages and verifies the packaged-artifact fit sample classes summary inside both the manifest and directory checks. The packaged-artifact target-threshold summary path now also routes through a validated wrapper, so the release-facing target-threshold line itself fails closed before rendering. The release bundle verifier now also cross-checks the staged packaged-artifact output-support, access, fit-sample-classes, speed-policy, and generation-policy summaries against the live render helpers, so those artifact posture lines fail closed when the checked-in bundle drifts away from the current profile. The release bundle now also stages and verifies the lunar-theory source-family summary alongside the lunar-theory limitations and catalog-validation summaries, keeps the lunar source-family release surface aligned with the checked-in bundle payload, and now also cross-checks the lunar-theory catalog validation summary against the live renderer so checksum refreshes cannot hide catalog-validation drift. The lunar-theory source-selection summary is now also exposed as a release-facing CLI surface and stages with the other lunar provenance files in the release bundle, giving the typed selection record its own direct inspection path alongside the existing source and family provenance lines. The packaged-artifact dense-body cadence classification now flows through a shared helper so fit-candidate, residual, and validation sampling stay aligned, the validation fraction lattice now reuses that same helper too, and the long-span subdivision path now uses a curvature-aware split bias on dense bodies to favor off-center windows when the quarter-point probes show asymmetric motion, with quarter-biased splits now reserved for very long dense-body spans that remain strongly asymmetric. The packaged-artifact output-support posture now also distinguishes approximated motion support from deterministically derived outputs, so numerical-difference motion estimates remain explicitly classified. The core façade now also exports matching release-specific house-system and ayanamsa canonical-name report helpers, so the validation surfaces can reuse one public path for those release-facing summaries. The packaged-artifact report and summary renderers now cache their rendered text in-process, trimming repeated compact-report recomputation alongside the already-cached inspection report object. The ayanamsa reference offsets summary now also routes through a validated report helper, so the zero-point evidence line fails closed before the release-facing summary is rendered. The release-bundle verifier now also cross-checks the validation report's packaged-artifact fit envelope line against the live renderer, so stale fit-envelope prose fails closed even when the bundle checksum is refreshed. It now also cross-checks the staged packaged-artifact generation manifest summary against the live renderer, so stale generation-manifest prose fails closed even when the bundle checksum is refreshed. It now also cross-checks the staged reference and comparison snapshot source summaries against the live renderers, so source-corpus provenance drift fails closed even when the manifest checksums still line up. It now also cross-checks the staged production-generation boundary source and request-corpus summaries against the live renderers, so checksum refreshes cannot mask phase-2 generation-provenance drift, and it now also cross-checks the staged lunar-theory source-family summary against the live renderer so the family-posture line fails closed if the prose drifts, and the backend-matrix selected-asteroid source evidence/window lines now fail closed there too.

Pluto's terminal-boundary regression now uses a body-aware tolerance so the packaged-artifact test suite matches the backend's explicitly approximate Pluto posture without weakening the exact-body checks.

The packaged-artifact target-threshold scope envelopes summary now also routes through a validated helper in the CLI, validation-report, and release-summary surfaces, keeping the scope-envelope posture fail closed alongside the target-threshold and source-fit summaries. The aggregate scope-envelope posture now also has a typed wrapper, so the release-facing helper no longer reconstructs the lines manually. The release bundle verifier now also cross-checks the packaged-artifact target-threshold scope envelopes summary against the live renderer, and it also cross-checks the release-profile identifiers summary against the live renderer, closing one more drift path in the release-profile manifest payload.

Completed work such as workspace bootstrap, broad catalog scaffolding, report aliases, release-bundle rehearsal, artifact manifest completeness, body-class cadence summaries, span-cap summaries, and packaged lookup/decode benchmark surfaces is no longer listed as active implementation work.

## Why this frontier comes first

The specification requires a compressed 1500-2500 CE artifact with measured accuracy, deterministic generation, efficient random access, and explicit stored/derived/unsupported output semantics. The current artifact has the structure and reports needed to diagnose it, and the draft path now passes the current calibrated thresholds while the target-threshold policy is recorded as production-ready, but release-grade corpus coverage is still not finalized.

Accuracy work should therefore precede any release claim broadening. Phase 2 source/corpus work still needs to broaden the trusted corpus before broader artifact claims move forward.

Recent progress: the packaged-artifact fit-outlier diagnostics now preserve segment-span and family-sample-count context, and the body/channel outlier summaries now use a denser report lattice while keeping distance-channel entries first; the fit-envelope sample lattice now also follows the same body-specific dense cadence for luminaries, selected asteroids, Pluto, and custom bodies instead of using the flatter mid-span grid, and the regression now keeps that dense-body contract explicit, the lunar-point cadence now follows the same dense sampling path for future body-model expansion, and the common-planet fit-outlier lattice now uses a medium cadence for inner and outer planets so the everyday planet classes are less sparse, the validation report no longer double-prefixes the body-class span-cap summary, the comparison body-class error envelopes now also have a standalone compact CLI summary alongside the existing validation-report section, and the release summary now surfaces that empirical envelope count directly, the packaged-artifact generator now applies measured-fit subdivision with an error-aware span-limited candidate-versus-fallback choice, residual correction channels now get attempted across the bundled body set on high-curvature spans when they improve the measured fit, the residual search now evaluates residual-channel combinations rather than only greedy single-channel additions, and now also explores all remaining channel-order permutations when composing those channels, and now prefers the smaller residual footprint when equal-error candidates tie, the residual correction lattice now uses channel-specific sample fractions so angular channels can stay dense while distance channels remain sparse, the luminary and custom-body residual fit lattices now use denser body-specific cadences, the packaged-artifact generator now uses body-specific validation/outlier lattices for luminaries, lunar points, selected asteroids, Pluto, and custom bodies, and the sensitive-body fit path now also tries a denser candidate lattice before the legacy fallback ladder, with the dense candidate lattice now carrying 10-point, 12-point, 14-point, 16-point, 18-point, and 20-point Chebyshev-Lobatto options for luminaries, Pluto, selected asteroids, and custom bodies, the generator now also tries 8-point and 10-point Chebyshev-Lobatto baseline candidates for every body before that body-specific dense ladder, and inner and outer planets now also use a 12-point medium candidate baseline before fallback, the generation-policy prose now explicitly names the dense body-specific 10/12/14/16/18/20-point ladder and best-dense-candidate-before-fallback rule, and equal-error candidate-versus-fallback decisions now prefer the simpler segment, and the packaged-artifact candidate-vs-fallback path is now accuracy-first so a worse fit no longer wins purely because it is simpler. The fit-candidate scorer now also breaks equal-error, equal-sample-count ties by preferring the simpler segment, and the packaged-artifact regeneration command now writes regenerated bytes instead of re-emitting the embedded fixture, the checked-in fixture and lookup baselines are re-synced with the current packaged posture, artifact-derived fit samples are cached so summary reports stay tractable, the validation/benchmark/artifact smoke-report paths now cache their expensive report objects and use reduced timing subsets so bundle verification stays tractable under test, the packaged artifact now also exposes a bundled-body cadence summary surface to keep future body-specific window tuning explicit without changing the current validation lattice, the packaged-artifact fit margin summary now has a validated structured wrapper for release-facing error-statistics reuse, the normalized-intermediate summary now carries a deterministic checksum, routes through a validated wrapper in release-facing validation surfaces, and is cross-checked against current rendering during release-bundle verification, packaged artifact profiles now fail closed when a built-in output would remain unlisted, the compatibility-profile and release-summary renderers now re-validate the target-house and target-ayanamsa scope prose before emitting release-facing text, and the compatibility-profile summary now also fail-closes on baseline/release split drift, the target-threshold posture is now carried by a typed production-ready state with explicit phase-2 reference/comparison/hold-out corpus alignment evidence while rendering production thresholds recorded, the target-threshold summary now fails closed when any advertised scope exceeds the calibrated fit thresholds, the stale target-threshold drift regression now exercises the Draft-versus-production-ready split, the JPL source posture is now documented as a hybrid fixture corpus, the reference snapshot source summary now carries explicit column schema metadata, redistribution posture, and a deterministic source checksum, the independent hold-out source summary now carries a deterministic source checksum, the reference and hold-out snapshot manifests now carry redistribution posture comments alongside the checksum-bearing source summaries, the comparison snapshot source summary now also carries the redistribution posture directly in the release-facing provenance line, the production-generation source summary now has a typed validated wrapper around the merged provenance block, the boundary overlay source summary now carries an explicit hold-out checksum marker, the selected-asteroid source request corpus now mirrors the checked-in source slice for frame-specific and mixed-frame validation, the new selected-asteroid source request corpus summary now fails closed on frame, epoch, body-order, and apparentness drift, and the selected-asteroid source request corpus summary is now exposed as a release-facing CLI surface, the JPL reference snapshot already carries source evidence for Ceres/Pallas/Juno/Vesta plus the custom asteroid corpus, the release body claims summary now explicitly lists the selected asteroid validation bodies (Ceres, Pallas, Juno, Vesta, asteroid:433-Eros, asteroid:99942-Apophis) and keeps them validation-only, the comparison-corpus release-grade guard summary now validates before rendering in comparison/report surfaces, the compatibility profile summary now fails closed if it stops describing the baseline/release split explicitly, the catalog inventory summary now also validates the release-profile identifiers before rendering and now has a validated core helper for report surfaces, the release-profile identifiers summary now routes through a shared core validated helper, the release-bundle manifest now includes a release-house-validation summary and the production-generation boundary source summary with checksum verification, the release-house-validation summary is now surfaced in the bundle-release/verify-release-bundle directory listings, and the house-validation summary now also has a validated report helper for release-facing reuse, and the release-bundle verifier now also cross-checks that summary against the live renderer, and the release-house-validation summary now routes through a shared helper so the CLI command and bundle writer use the same validated path, the packaged-artifact phase-2 corpus alignment summary now carries source/provenance lines for the reference, comparison, and hold-out corpora alongside the body-class coverage evidence and now also keeps the selected-asteroid source evidence/windows and request corpus plus the production-generation boundary source evidence in the same phase-2 posture, the backend matrix summary now validates the compatibility profile before rendering, the selected-asteroid coverage line now routes through a validated summary helper shared by backend-matrix and reference-asteroid report surfaces, the packaged-artifact fixture was refreshed to the current bytes, the boundary lookup regression now reports the failing body and epoch when compressed distance roundoff drifts at a boundary epoch, and the artifact provenance string now mirrors the current dense-candidate ladder wording, the release-bundle generator now validates the release-grade body-claims and Pluto-fallback summaries before serializing them, the release-notes/release-summary CLI expectations now derive the packaged-artifact generation policy line from the live helper instead of stale hard-coded wording, the ayanamsa catalog validation summary now has a validated wrapper in release-facing report surfaces, the ayanamsa provenance summary now validates before rendering in release-facing report surfaces, the compatibility-caveats and release-notes summary surfaces now route through validated latitude-sensitive house-system, house-code alias, and custom-definition ayanamsa wrappers, the body-class comparison-envelope formatter now validates before rendering, the frame-policy summary surface now validates before rendering in CLI and validation report paths, the release bundle now keeps the native-dependency audit summary bookkeeping explicit instead of reusing workspace-audit byte counts, and the verify-release-bundle path now also cross-checks the staged native-dependency audit summary against the live workspace-audit rendering before accepting the bundle, and the packaged-artifact regeneration command now reuses cached encoded bytes for repeated sidecar writes, the JPL interpolation-quality report now also includes body-class error envelopes for the luminary, major-planet, selected-asteroid, and custom-body slices, and the interpolation-quality sample request corpus now also has a release-facing summary surface for the exact held-out request slice, request-policy/request-semantics report titles now share a guarded validation helper before emitting release-facing text, the zodiac policy summary now also routes through a validated release helper in validation and release-facing report surfaces, and request-policy validation now pins invalid-observer precedence ahead of unsupported-body coverage in both direct and batch metadata checks, the backend matrix and validation-report summary surfaces now route house-code aliases through a validated profile-backed helper, the JPL snapshot evidence classification and source-posture report lines now also route through validated summary helpers before release rendering, the packaged-artifact phase-2 corpus alignment now has a standalone validated CLI surface, the packaged-artifact fit sample classes summary now validates its boundary continuity component before rendering, the packaged-artifact fit threshold violation count and summary surfaces now also have a structured validated wrapper, the regeneration regression now collapses the sidecar writes into a single invocation so bundle verification remains tractable under test, the validation-report regression sections now render each regression finding through a validated summary helper so malformed notes fail closed during report formatting, the compatibility-profile verification summary regression now also rejects stale release house-system canonical names, the packaged-artifact storage/reconstruction and production-profile summary surfaces now route through explicit validated wrappers before rendering, and the packaged-artifact regeneration helper now caches the deterministic rebuilt artifact in-process so repeated validation/report invocations do not rebuild the full artifact every time, the compatibility-profile, release-notes, and release-summary surfaces now route latitude-sensitive house-system and house-formula-family lines through validated helpers, the packaged-artifact body cadence summary now has a validated structured wrapper and fail-closed report rendering, and the packaged-artifact generation residual bodies summary now also routes through a validated helper so the release-notes, release-summary, validation-report, and CLI surfaces fail closed on residual-body drift, and the body-class span-cap summary now validates before rendering in CLI and validation-report paths, so the remaining Phase 3 work is claim promotion rather than fresh corpus discovery, while the release-bundle verifier now cross-checks the Pluto fallback summary against the release-grade body claims posture before accepting a staged bundle, and the staged-bundle display now includes the packaged-artifact phase-2 corpus alignment summary alongside the other release artifacts, with the artifact and validation summaries now surfacing that phase-2 line explicitly beside the target-threshold block, and the release bundle now also stages and verifies the lunar-theory catalog validation summary alongside the lunar-theory limitations summary, and the bundle manifest now records cargo-version provenance alongside source revision, workspace status, and rustc version. The validation-report and packaged-data backend provenance surfaces now also route request-policy and frame-treatment through validated helpers, and the backend layer now exposes validated Pluto fallback and release-grade body-claims summary-line helpers so the claim-boundary posture can be reused by release-facing surfaces without duplicating policy checks; the core chart façade now also renders its frame-policy line through the validated frame-treatment helper. The release summary now also surfaces the comparison body-class tolerance posture line so the empirical backend-path comparison envelope stays visible in the release-facing overview, and the new comparison-body-class-tolerance-posture summary command now exposes that same posture directly for release-facing reuse. The comparison tolerance scope coverage summary command now publishes the per-scope body-class tolerance rows behind the aggregate comparison tolerance policy line, the comparison snapshot source summary is now staged and verified in the release bundle alongside the reference/hold-out provenance files, the comparison snapshot source summary now also fails closed on source, coverage, columns, and checksum drift, the comparison snapshot summary validation coverage now explicitly rejects missing rows, missing bodies, and invalid epoch-range drift, the compatibility-profile verification summary now also exposes a validated compact summary-line helper, the compatibility-profile release summary now must match the canonical checked-in text exactly so overstated catalog prose fails closed, the release bundle verifier now also semantically cross-checks the catalog inventory and custom-definition ayanamsa labels summaries against the current compatibility profile, and the residual-correction search now also explores all remaining channel-order permutations when composing channels so order-sensitive residual candidates are no longer blocked by the old suffix-only traversal.

The release bundle verifier now also cross-checks the request-policy and request-surface summaries against the live render helpers, so stale bundle prose fails closed even when the manifest checksums still line up. It now also cross-checks the native sidereal policy summary against the live renderer, so sidereal-policy prose fails closed alongside the rest of the request-policy block. The reference snapshot mixed TT/TDB batch-parity summary is now exposed as a release-facing CLI surface, so the checked-in batch slice can report its time-scale mix alongside the existing ecliptic batch parity evidence. The packaged-artifact generator now also uses higher-order distance reconstruction from fit samples and cubic four-point control-point reconstruction when available, its generation-policy wording now matches the quadratic-window strategy, the CLI regeneration regression now expects that quadratic wording, and the checked-in fixture remains byte-stable. The phase-2 corpus alignment posture now also includes the production-generation source provenance and body-class coverage so the threshold summary tracks the separate generation-input corpus, not just the checked-in reference/comparison/hold-out fixtures, and the production-generation source summary now also folds in the merged source-window lattice as release-visible provenance and now models the source-revision checksum pair as structured fields instead of a raw string, and the production-generation source-window summary now stages and verifies alongside it in release bundles, while the release bundle verifier now also cross-checks that staged source-window summary against the live renderer. The independent-holdout source-window summary now stages and verifies in the same release-bundle path, so the hold-out source-window posture also fails closed. The release notes / release summary surfaces now route the compatibility profile release note through a validated helper before rendering. The release bundle verifier now also cross-checks the compatibility profile summary and release summary against the live renderer, so stale overview prose fails closed after checksum refreshes as well. The maintainer-facing docs now also name `utc-convenience-policy-summary` alongside the other request-policy and bundle-reproducibility entry points, keeping the release-smoke guidance aligned with the current bundle payload. UTC convenience, native sidereal, time-scale, Delta T, observer, apparentness, request-policy, and API stability summaries now also have validated release helpers, so the advanced request-policy surfaces no longer rely on ad hoc validation formatting. The packaged-artifact profile output-support summary now also has validated wrappers for the full capability line and explicit support-entry rendering, keeping that release-facing capability surface fail-closed too. The comparison tolerance policy, scope coverage, body-class tolerance posture, envelope, and Pluto fallback summary renderers now fail closed instead of panicking if comparison report construction fails, and the comparison tolerance scope coverage summary now validates every scope row before rendering, so malformed per-scope coverage no longer leaks through as partial drift text. The comparison body-class tolerance posture summary now runs through a validated helper before rendering. The comparison body-class tolerance summary renderer now also validates each class row before formatting, so malformed tolerance rows fail closed instead of rendering partial drift text.

The packaged-artifact source-fit and hold-out sync summary now has a validated CLI surface and also appears in artifact, validation, and release-bundle surfaces, so the calibrated fit thresholds can be reviewed alongside the current target-threshold posture without leaving the release-facing report set; the sync line now also carries the explicit phase-2 corpus alignment evidence so the threshold posture and corpus posture stay visible together, the source-fit regression now pins that shared phase-2 payload against the stand-alone summary and the production-ready target-threshold posture, the release-bundle verifier now cross-checks that shared phase-2 payload against the stand-alone phase-2 corpus alignment summary before accepting a staged bundle, and the production-generation source summary now also cross-checks its embedded source-window payload against the standalone source-window summary during release-bundle verification.

The packaged-artifact source/provenance prose now shares the generation-tail wording between the artifact source summary and the generation-policy note, reducing drift in release-facing narrative while preserving the fail-closed validation path. The validation-report packaged-artifact profile block now also routes output support, speed policy, access, and generation policy through dedicated validated wrappers so that surface follows the same fail-closed pattern as the storage and production-profile summaries, and the packaged-artifact source-fit and hold-out sync summary now routes through a validated helper in the same release-facing report family. The release-bundle verifier now also cross-checks the packaged-artifact source-fit and hold-out sync summary against the live renderer, closing one more semantic drift path for the staged bundle. It now also cross-checks the packaged-artifact phase-2 corpus alignment summary against the live renderer, so checksum refreshes cannot mask phase-2 alignment drift, and it now also cross-checks the packaged-artifact target-threshold scope envelopes summary against the live renderer so scope-envelope drift fails closed alongside the target-threshold posture.

## Immediate blockers

1. **Source coverage** — checked-in JPL snapshots and boundary overlays are not yet a broad production corpus for all release bodies and epochs.
2. **Body claim boundaries** — Pluto, fuller lunar theory, lunar points, Ceres/Pallas/Juno/Vesta, and selected custom asteroids need source-backed validation or constrained/excluded release status.
3. **Release fail-closed behavior** — release gates must block stale generated summaries and overbroad compatibility/backend claims.

## Recommended next slice

Align the finalized threshold posture with the Phase 2 corpus and hold-out validation:

- keep the source-fit and hold-out checks synchronized with the recorded production-ready threshold policy;
- keep the artifact labeled draft until the source corpus is aligned;
- continue using the cached fit summaries and measured-fit subdivision path to keep report generation tractable.

## Parallel safe work

- Expand the documented hybrid source corpus with broader coverage and provenance.
- Align the finalized threshold posture with the Phase 2 source corpus.
- Audit body release status for Pluto, lunar channels, and selected asteroids.
- Audit house/ayanamsa entries whose release claims are stronger than their evidence.
- Keep request-policy docs and structured unsupported errors synchronized.
- Harden release-bundle verification without changing feature claims, and keep the lunar-theory source-family summary exposed as a release-facing CLI surface with matching docs.
- The chart help block now reuses the same compact request-policy line for the request-semantics alias instead of applying a dead string rewrite, keeping the CLI help wording aligned with the docs.
