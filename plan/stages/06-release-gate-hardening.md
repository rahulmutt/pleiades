# Phase 6 — Release Gate Hardening

## Goal

Make production release claims reproducible and enforceable from a clean checkout, as required by `spec/validation-and-testing.md`.

## Starting point

CLI and validation tooling can render audits, summaries, artifact reports, benchmark matrices, compatibility checks, API stability reports, release-house-validation summaries, packaged-artifact phase-2 corpus alignment summaries, and release-bundle rehearsals. These gates must become blocking once artifact, reference, body-claim, request-policy, and catalog evidence are production-ready. The backend matrix summary now validates the compatibility profile before rendering so release matrix output fails closed on profile drift, the release bundle generator now validates the release-grade body-claims and Pluto-fallback summaries before serializing them, the comparison-corpus release-grade guard summary now validates before rendering in comparison/report surfaces, the release bundle verifier now cross-checks the Pluto fallback summary against the release-grade body claims posture before accepting a staged bundle, the benchmark provenance block now records cargo version alongside source revision, workspace status, and rustc version for reproducibility, and the release bundle manifest/verification now carry the same cargo-version provenance field, the validation-report and packaged-data backend provenance surfaces now route request-policy and frame-treatment through validated report helpers, the release bundle keeps the native-dependency audit summary bookkeeping explicit instead of reusing workspace-audit byte counts, and the verify-release-bundle path now also cross-checks the staged native-dependency audit summary against the live workspace-audit rendering before accepting the bundle; the release-bundle tamper regression now also rejects semantic native-dependency-audit-summary.txt drift after its checksum sidecar is refreshed. The verify-release-bundle display now includes the packaged-artifact phase-2 corpus alignment summary alongside the other staged release artifacts. The benchmark corpus summary now also has a validated wrapper and the release bundle verifier now cross-checks it against the live renderer, so benchmark corpus drift fails closed alongside the other staged release artifacts; the release-bundle tamper regression now also updates its checksum sidecar and still rejects semantic benchmark-corpus-summary.txt drift. The release bundle now also stages and verifies the lunar-theory source-family summary alongside the lunar-theory limitations and catalog-validation summaries, the house-validation summary now also has a release-facing helper that centralizes the validated release-house-validation summary for CLI and bundle rendering reuse, and the release-bundle verifier now also cross-checks that summary against the live renderer, and the release-bundle tamper regression now also rejects semantic release-house-validation-summary.txt drift after its checksum sidecar is refreshed; the release bundle verifier now also cross-checks that lunar-theory source-family summary against the live renderer so stale family prose fails closed, it now also cross-checks the lunar-theory limitations summary against the live renderer so limitations prose fails closed even after checksum refreshes, and it now also cross-checks the lunar-theory catalog validation summary against the live renderer so checksum refreshes cannot hide catalog-validation drift, the packaged-artifact fit sample classes summary now also stages and verifies alongside the other packaged-artifact release files, and the release bundle verifier now also cross-checks that summary against the live renderer so stale fit-sample prose fails closed, the packaged-artifact body cadence summary now also has a validated wrapper and the release-bundle verifier now cross-checks it against the live renderer, the packaged-artifact fit threshold violation count and summary surfaces now also have a structured validated wrapper, keeping the body-model claim-boundary evidence explicit in the manifest and directory checks, and the release bundle verifier now semantically cross-checks the catalog inventory and custom-definition ayanamsa labels summaries against the current compatibility profile. The release bundle verifier now also cross-checks the request-policy, request-surface, API stability, packaged-artifact output support, packaged-artifact access, packaged-artifact speed policy, packaged-artifact generation policy, packaged-artifact production-profile, native sidereal policy, and validation-report fit-envelope line against their live renderers so stale bundle prose fails closed even when checksums still line up. It now also cross-checks the compatibility profile summary and release summary against the live renderer, so the overview files fail closed on semantic drift even after checksum refreshes. It now also cross-checks the backend matrix's selected-asteroid source evidence and source-window lines against the live renderer, so the selected-asteroid provenance prose fails closed even when the backend-matrix checksum is refreshed. It now also cross-checks the staged reference and comparison snapshot source summaries against their live renderers, so source-corpus provenance drift fails closed even when checksum refreshes would otherwise hide the prose change. It now also cross-checks the staged production-generation boundary source and request-corpus summaries against their live renderers, so checksum refreshes cannot mask drift in the phase-2 generation provenance. It now also cross-checks the packaged-artifact phase-2 corpus alignment summary against the live renderer, so checksum refreshes cannot hide semantic drift in the standalone phase-2 posture, and it now also cross-checks the packaged-artifact target-threshold scope envelopes summary against the live renderer so scope-envelope drift fails closed alongside the target-threshold posture. The release bundle now also stages and verifies the packaged-artifact generation manifest checksum summary alongside the generation-manifest files, and the release bundle verifier cross-checks that summary against the live renderer so checksum-posture drift on the generation-manifest summary itself fails closed. The release bundle verifier now also cross-checks the production-generation source-window and production-generation manifest summaries against the live renderer, closing the last bundle-side drift gap in that provenance block. The comparison tolerance policy, scope coverage, body-class tolerance posture, envelope, and Pluto fallback summary renderers now fail closed instead of panicking if comparison report construction fails, the comparison tolerance scope coverage summary now validates every scope row before rendering, and the comparison body-class tolerance posture summary now runs through a validated helper before rendering.

## Implementation goals

- Ensure formatting, clippy, tests, pure-Rust/native-dependency audits, artifact validation, compatibility-profile verification, benchmarks, and bundle verification run reproducibly locally and in CI.
- Stage release bundles with current compatibility profiles, backend matrices, request-policy summaries, validation reports, benchmark summaries, artifact manifests, checksums, source revisions, tool versions, and release notes.
- Fail on stale generated outputs, artifact threshold violations, unsupported-mode claim drift, profile mismatches, missing release files, checksum drift, or native dependency regressions.
- Archive enough generation parameters and input checksums to reproduce shipped artifacts.
- Keep README, docs, release notes, and compatibility profiles aligned with the exact release evidence.

## Completion criteria

Phase 6 is complete when maintainers can cut a release from a clean checkout and verify that every published claim is backed by current generated evidence.

## Out of scope

- Inventing new feature scope during release hardening.
- Relaxing thresholds or claims to make a release pass without corresponding evidence.
