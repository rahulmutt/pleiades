# Phase 2 — Reproducible Compressed Artifacts

## Purpose

Turn the current compressed-artifact scaffolding into a reproducible packaged-data system for 1500-2500 CE. This phase depends on Phase 1 reference-quality source outputs because artifact fit errors must be measured against trustworthy inputs.

## Spec drivers

- `spec/requirements.md`: FR-8, FR-9, NFR-2, NFR-4
- `spec/data-compression.md`: segmented polynomial/residual format, stored-vs-derived outputs, generation pipeline
- `spec/backends.md`: `pleiades-data` responsibilities
- `spec/validation-and-testing.md`: artifact validation, benchmarks, release gates

## Current baseline

`pleiades-compression` defines an in-memory artifact model, deterministic binary codec, artifact capability profile metadata, lookup path, and explicit body/segment random-access helpers. `pleiades-data` exposes a packaged backend backed by a checked-in deterministic binary artifact fixture with an ecliptic-only/no-motion profile, and validation tooling can inspect, summarize, and regenerate that fixture from the checked-in JPL reference snapshot in tests.

## Remaining implementation goals

1. Define the artifact profile format.
   - Initial header fields, versioning, endian policy, checksums, provenance, and capability/profile sections are implemented in the codec.
   - Initial profile metadata records stored channels, derived outputs, unsupported outputs, and speed derivation policy.
   - Remaining work: refine body-specific profile semantics as generated artifacts become available and surface profile summaries in validation/release reports.
   - Progress note: the packaged-artifact profile summary now also reports the bundled body count that shares the same conservative capability profile, which makes the current single-profile packaged scope explicit without widening the artifact format. The packaged-data request policy summary now also exposes a structured lookup-epoch policy label, and the lookup-epoch policy itself now has a typed summary helper that the request-policy renderer reuses directly, which keeps the TT/TDB re-tagging note machine-readable for the current prototype artifact posture. Latest progress (2026-04-29): that packaged lookup-epoch policy now also has a dedicated validation hook, and the packaged request-policy summary reuses it before rendering, which keeps the TT-grid retag posture fail-closed at the policy-enum boundary instead of only inside the higher-level request summary. The lunar baseline now also exposes a one-entry catalog plus catalog summary helper, which gives future artifact-generation work a stable source-selection shape to mirror when a source-backed lunar theory selection eventually lands. The compressed artifact header now also carries an explicit little-endian byte-order policy, and both the compact artifact summary and release-facing artifact reports now surface that byte-order line alongside the stored-channel profile so the binary layout stays obvious in the packaged-data evidence. The artifact-profile summary now also uses stable `Display` labels for stored channels, derived outputs, unsupported outputs, and speed policy, so release-facing output no longer depends on enum debug names. The artifact profile record itself now also implements `Display` and a `summary_line()` helper in `pleiades-compression`, which lets downstream report code reuse the typed codec metadata directly instead of rebuilding the same string shape in each caller. The packaged-artifact profile summary now also carries the bundled body list in a dedicated helper, and the validation/release-facing artifact summaries now render that body coverage alongside the profile line so the current profile record stays visibly tied to the packaged body set. The packaged-artifact profile summary record now also implements `Display`, which keeps the typed packaged profile line reusable from downstream report code without reconstructing the same summary shape in each caller. The artifact summary now also reuses that shared packaged-artifact coverage helper directly, so the compact artifact report names the bundled body set explicitly instead of only printing the body count. The packaged body coverage itself is now also exposed as a structured backend-owned summary record, which keeps the packaged-body list reusable by regeneration and release-facing reporting code instead of only as a formatted string. The compression crate now also exposes a derived equatorial lookup helper for artifacts that advertise equatorial reconstruction, and the artifact profile itself now has a typed output-support query helper so generated artifacts can distinguish derived, unsupported, and unlisted high-level outputs without parsing the summary string. The packaged-data wrapper now formats that output-support line from a dedicated prefix-free helper instead of trimming the profile summary string, and the packaged body-coverage wording now also comes from a shared `pleiades-compression` list formatter, which keeps the regeneration and profile summaries aligned instead of duplicating the comma-join logic locally. The compression crate now also exposes a typed artifact-profile coverage summary that groups a profile with its bundled body set, which makes the body-specific profile semantics reusable by future generated-artifact variants instead of living only in the packaged-data wrapper. The profile validator now also enforces that Motion matches the declared speed policy, which keeps the first speed-bearing capability aligned with the stored/derived/unsupported profile fields while the body-specific artifact semantics continue to mature. The artifact-profile coverage summary now also exposes a reusable `validate()` helper, and the packaged-data profile summary now delegates to that check so body-count drift or malformed capability profiles can be caught before future generated artifacts are formatted or shipped. The artifact header now also validates its version, generation label, and source provenance before encoding or decoding, which keeps malformed provenance metadata from slipping through the compressed-artifact pipeline. Latest progress (2026-04-27): the artifact header validation now also rejects whitespace-padded generation labels and source provenance, which keeps the compressed-artifact metadata canonical instead of merely non-blank. Latest progress (2026-04-27): the packaged-artifact profile summary wrapper now also rejects empty and duplicate bundled-body lists through the shared coverage validator, so malformed packaged-body coverage fails before the compact artifact posture line is rendered. Latest progress (2026-04-28): `pleiades-compression::Segment` now exposes a public `validate()` helper and `BodyArtifact::validate()` reuses it directly, which gives artifact generators a typed segment-level preflight hook before the body wrapper or codec path is invoked. Latest progress (2026-04-28): artifact profile validation now also requires derived coordinate outputs to be backed by Longitude, Latitude, and DistanceAu stored channels, so the stored-vs-derived contract stays explicit when future generated profiles advertise reconstructed coordinate modes. Latest progress (2026-04-28): `pleiades-data::PackagedArtifactStorageSummary` now also has a typed validation helper plus a fail-closed report wrapper, and the storage summary now also validates its profile-backed stored-channel posture against the bundled artifact before rendering, which keeps the packaged storage/reconstruction line on the same canonical-string guardrail as the packaged frame-treatment summary while the current packaged backend remains fixture-backed. Latest progress (2026-04-28): the packaged-artifact profile views now also surface a typed output-support line for the built-in coordinate and motion outputs, which makes the stored/derived/unsupported breakdown a little more explicit in the validation and release summary paths without changing the bundled artifact format. Latest progress (2026-04-28): that output-support line now also has a typed packaged-artifact summary helper plus fail-closed validation in `pleiades-data`, so the release-facing support breakdown now refuses to render if the embedded profile ever drifts from the checked-in artifact metadata. The core compression lookups now also require the artifact profile to advertise the requested derived output before serving `lookup_ecliptic()` or `lookup_equatorial()`, so unsupported derived-output access now fails closed instead of silently reconstructing an unadvertised coordinate family. Latest progress (2026-04-28): the packaged-data output-support summary now also validates the current built-in equatorial and motion posture directly, which keeps the compact support line fail-closed if the checked-in profile stops advertising equatorial coordinates as derived or motion as unsupported. Latest progress (2026-04-29): the packaged-artifact generation policy now also has a dedicated typed summary/validation helper, and the validation/release summary surfaces now render that policy line directly alongside the storage and lookup-epoch posture so the linear-segment generation rule is visible outside the regeneration provenance block. The packaged-artifact storage/reconstruction summary now also appears in the artifact, validation, and release report surfaces, which keeps the stored-vs-derived posture visible alongside the existing profile and output-support lines.
   - Latest progress (2026-04-29): the compression crate now also exposes a public artifact profile coverage summary from `CompressedArtifact`, and `CompressedArtifact::validate()` now checks the bundled body/profile coverage directly before segment validation so empty packaged artifacts fail closed at the codec boundary instead of only in downstream report code.
   - Latest progress (2026-04-29): the packaged artifact's residual-bearing body set now has a typed `ArtifactResidualBodyCoverageSummary` in `pleiades-compression`, and `pleiades-data` reuses it for the packaged residual-body report path, which keeps the Moon-only residual-correction posture reusable by future generated artifacts without changing the current report text.
   - Latest progress (2026-04-29): the packaged artifact profile summary validation now also fails closed if the bundled body list drifts away from the checked-in packaged body set, which keeps the compact byte-order/profile summary aligned with the actual packaged coverage instead of only checking count and uniqueness. The same validation now also checks the packaged profile metadata against the checked-in artifact profile, which keeps the capability line fail-closed instead of relying on the summary record's construction path alone.
   - Latest progress (2026-04-29): the artifact profile validator now also enforces canonical declaration order for stored channels and declared output lists, which keeps the deterministic capability metadata stable across builders instead of relying only on uniqueness and disjointness checks.
   - Keep decode deterministic and independent of platform-specific binary layout.

2. Build a deterministic generation pipeline.
   - Add pure-Rust tooling to sample a trusted backend over 1500-2500 CE.
   - Fit body/channel-specific polynomial or Chebyshev segments.
   - Progress note: the packaged artifact fixture now uses residual-corrected Moon spans over the checked-in JPL reference epochs, so the high-curvature lunar slice no longer depends on a purely linear fit while the broader generated-artifact pipeline continues to mature.
   - Stamp source versions, generation parameters, and checksums.
   - Progress note: `pleiades-data` now exposes a public pure-Rust regeneration helper for the packaged artifact, and `pleiades-cli` can write the checked-in fixture back to disk from the reference snapshot so maintainers have an explicit regeneration path without ad hoc scripts. The regeneration command now also surfaces backend-owned packaged-body coverage alongside the JPL reference-snapshot coverage line and artifact label/source/checksum, which makes both the packaged output set and the public-input lineage visible in the maintainer-facing regeneration output. The validation artifact summary and release summary now also render the packaged-artifact regeneration provenance line, and that provenance record now implements `Display`, so the maintainer-facing regeneration source stays visible in the compact release reports through the typed summary record instead of a duplicated string literal. The packaged-artifact regeneration summary now also has a typed `validate()` helper plus a report-facing wrapper that preflights the bundled body list against the checked-in reference snapshot before formatting the provenance line, which keeps the regeneration metadata and public-input coverage aligned at the reporting boundary. That validation now also preflights the nested JPL reference-snapshot coverage summary itself, which keeps the reproducibility report honest about malformed coverage metadata before the body-list check runs. The packaged regeneration provenance now also records the artifact format version, checksum, and the current linear-segment generation policy, which makes the deterministic fit rule explicit alongside the reproducibility metadata. The compression codec now also supports optional residual-correction channels on a per-segment basis, which gives future lunar and other high-curvature fits a place to store deterministic correction terms while keeping the current packaged fixture reproducible. The regeneration-summary validation now also requires the bundled body list to match the checked-in packaged body set exactly, so order or membership drift in the packaged artifact coverage now fails before the provenance line is emitted. Latest progress (2026-04-27): the `regenerate-packaged-artifact` CLI now accepts either a positional output path or `--out FILE`, and the help text and README examples document the regeneration path explicitly so maintainers can invoke the fixture rebuild without guessing the argument shape. Latest progress (2026-04-27): the same regeneration command now also supports a `--check` mode that verifies the regenerated bytes against the checked-in packaged artifact fixture without writing to disk, which makes the deterministic regeneration workflow easier to script for release checks. Latest progress (2026-04-27): the packaged-artifact regeneration summary now also validates its label, source, artifact version, and checksum against the checked-in packaged artifact metadata before rendering, so the maintainer-facing provenance line now fails closed if the regenerated header drifts from the committed bytes. Latest progress (2026-04-29): the packaged-artifact generation policy summary now also validates the current residual-bearing body set against the checked-in artifact, so the Moon-only residual posture now fails closed if the encoded artifact ever starts advertising a different residual-body mix. Latest progress (2026-04-29): the packaged artifact regeneration summary now also surfaces that residual-bearing body set directly, and the validation, release-notes, and release-summary surfaces now render that same residual-body line explicitly, so the Moon-only residual posture stays visible outside the regeneration provenance block too.

3. Implement binary encode/decode and random access.
   - Move beyond sample in-memory construction to a stable serialized artifact representation.
   - Provide efficient lookup by body and time segment.
   - Validate boundary behavior around segment edges and unsupported bodies.
   - Keep artifact loading usable for desktop/server applications without mandatory native dependencies.
   - Progress note: the compression crate now prefers the later segment when two adjacent segments both include the same boundary instant, and the random-access regression suite covers that shared-boundary behavior alongside the existing missing-body and out-of-range checks.
   - Progress note: the compression codec now also rejects duplicate body entries, duplicate stored/derived/unsupported profile entries, and malformed segment metadata during encode/decode, which keeps the serialized artifact representation stricter and makes the random-access surface less ambiguous.
   - Progress note (2026-04-28): segment validation now also fails closed on non-finite bounds and overlapping same-scale segments while still allowing shared-boundary adjacency, so the packaged artifact's time-slice ordering is explicit before lookup or encoding. Segment validation now also rejects unsorted stored and residual channel lists, which keeps the deterministic binary layout canonical across builders instead of relying on insertion order to stay stable.

4. Measure compression quality.
   - Define initial per-body-class target error envelopes.
   - Compare decoded positions and speeds against generation sources.
   - Benchmark artifact size, lookup latency, batch throughput, and memory footprint.
   - Include measured limits in artifact metadata and validation reports.
   - Progress note: the artifact inspection report now also renders a packaged-artifact decode benchmark slice alongside the existing comparison envelope and boundary checks, so the compact validation path surfaces a concrete decode-cost measurement without waiting for a separate benchmark command. The same artifact inspection path now also surfaces an aggregate packaged-artifact boundary envelope across all bundled bodies, which makes segment-edge continuity easier to audit in the compact and detailed validation views. The packaged-artifact decode benchmark metadata now also validates its label, source, round, sample-count, and encoded-size fields before the report path formats it, which keeps the release-facing benchmark line from admitting malformed provenance or zero-sized coverage. The packaged-artifact decode benchmark now also exposes a typed compact summary helper, and the artifact inspection report reuses it so the decode-cost provenance stays centralized in the backend-owned report path. The artifact inspection report now also cross-checks the benchmark's decoded label, source, and byte count against the inspected header before it renders, and the compact inspection summary now also carries the encoded-byte count directly, which keeps the artifact size visible in the release-facing provenance line instead of only in the detailed benchmark block. Latest progress (2026-04-29): the artifact inspection report now also validates the residual-bearing body list against the per-body residual segment counts, so the compact artifact summary fails closed if that list ever omits a bundled residual body. Latest progress (2026-04-29): the same artifact inspection validation now also checks the total residual-bearing segment count against the inspected body-level totals, which keeps the compact artifact summary aligned with the decoded residual coverage instead of only the body list.

5. Upgrade `pleiades-data` to consume generated artifacts.
   - Bundle or fixture a small deterministic artifact for tests.
   - Provide feature-gated paths for larger distributable artifacts when appropriate.
   - Report packaged-data capabilities honestly through backend metadata and release profiles.
   - Progress note: the packaged backend now loads a checked-in binary artifact fixture and the tests compare it against a deterministic regeneration helper from the JPL reference snapshot, so the bundled artifact path is no longer a hand-authored segment chain. The compression crate now also exposes explicit body and segment random-access helpers, which makes the lookup path easier to reuse in future artifact validation and generation work. The compression artifact, body, and profile types now also expose public `validate()` helpers that mirror the codec's encode/decode checks, giving artifact generators a fail-fast path before serialization. The packaged-data regeneration path now validates the reconstructed artifact before encoding it, so fixture drift or generator regressions fail fast before the deterministic payload is written back to disk. The packaged body coverage summary now also validates against the bundled body set before report formatting, so packaged-body provenance drift now fails closed at the report boundary as well. The packaged-data backend now also exposes explicit artifact-from-bytes and optional artifact-from-path constructors, which gives the phase-2 loader a typed extension point for larger distributable artifacts without changing the default checked-in fixture path. Latest progress (2026-04-29): the packaged-data backend now also publishes a typed packaged-artifact access summary that states whether external artifact-path loading is enabled, and the backend provenance list reuses it so the feature-gated external-artifact posture stays explicit alongside the checked-in fixture path. The packaged mixed-frame and mixed TT/TDB batch-parity summaries now also validate their derived counts before release-facing formatting, which keeps the artifact transparency lines fail-closed if the frame or time-scale splits ever drift. The compact packaged-data request-policy, frame-treatment, and storage summary accessors now also validate and cache an unavailable fallback before returning, so those user-facing posture lines now fail closed if their canonical literals ever drift. Latest progress (2026-04-28): the packaged mixed-frame and mixed TT/TDB batch-parity summaries now also require both frames and both time scales to be represented, so the compact packaged-data transparency lines now fail closed if a future regression collapses a supposedly mixed batch into a single-frame or single-scale slice. Latest progress (2026-04-29): the packaged-artifact regeneration check now compares the regenerated bytes directly against the checked-in fixture bytes via a public helper, which keeps the maintainer-facing reproducibility check aligned with the committed binary artifact instead of depending on a decode/re-encode round-trip. The packaged-artifact access posture now also appears in the validation, release-notes, and release-summary report surfaces, keeping the feature-gated external-artifact flag explicit alongside the checked-in fixture path instead of only inside the backend provenance list. Latest progress (2026-04-29): the validation, release-notes, and release-summary smoke tests now also pin that packaged-artifact access posture line directly, which keeps the feature-gated external-artifact flag anchored in the compact release-facing audit surfaces as well as the backend provenance helper.

## Done criteria

- A maintainer can regenerate the test artifact from documented public inputs and deterministic parameters.
- `pleiades-compression` can encode, decode, checksum, and query the artifact format.
- `pleiades-data` uses generated artifacts rather than hand-authored sample segments for claimed coverage.
- Validation reports include artifact error summaries, boundary checks, and benchmark numbers.
- Artifact metadata states exactly what is stored, derived, or unsupported.

## Work that belongs in later phases

- Expanding compatibility catalogs belongs to Phase 3.
- Archiving release artifacts and final release bundle signing/checksum policy belongs to Phase 4.
