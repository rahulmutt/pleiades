# Phase 1 — Production Ephemeris Accuracy

## Purpose

Replace preliminary, sample-driven, or simplified backend behavior with source-backed astronomical calculations and explicit accuracy evidence. This phase is active because the workspace structure and user-facing APIs exist, but production release claims require validated ephemeris output.

## Spec drivers

- `spec/requirements.md`: FR-1, FR-2, FR-3, FR-7, FR-8, NFR-2, NFR-3
- `spec/backend-trait.md`: request/result semantics, metadata, errors, composite backends
- `spec/backends.md`: `pleiades-jpl`, `pleiades-vsop87`, `pleiades-elp` responsibilities
- `spec/api-and-ergonomics.md`: deterministic typed APIs, batch queries, failure modes
- `spec/validation-and-testing.md`: reference comparison and golden/regression tests

## Current baseline

Implemented foundations include backend traits, metadata, composite routing, major body identifiers, lunar points, baseline asteroids, a chart façade, CLI commands, a snapshot-style `pleiades-jpl` with exact lookup plus cubic/quadratic/linear interpolation, selected asteroid coverage, and expanded public-input leave-one-out interpolation quality reporting, preliminary `pleiades-vsop87` and `pleiades-elp` crates, deterministic central-difference motion estimates for the current VSOP87 path and compact ELP Moon/lunar-point path, explicit mean-only support for mean lunar apogee and mean lunar perigee, explicit rejection of unsupported topocentric and apparent requests in geocentric/mean-only backends, an initial time-scale/Delta T/observer policy document, caller-supplied time-scale offset helpers for explicit external UT1-to-TT/related policies, validation corpus summaries that now label their unique epochs with explicit time-scale tags and the validation/release summaries now also render an explicit time-scale policy line derived from the caller-supplied TT/TDB surface, with the shared time-scale and request-policy summaries now also exposing validated one-line helpers so the compact policy text fails closed before the report wrappers render it, vendored IMCCE VSOP87B Earth, Mercury, Venus, Mars, Jupiter, Saturn, Uranus, and Neptune source files for geocentric Sun-through-Neptune output, plus generated binary coefficient-table slices for the Sun/Earth, Mercury, Venus, Mars, Jupiter, Saturn, Uranus, and Neptune paths, VSOP87 per-body source profiles rendered in validation backend matrices, explicit VSOP87 frame-treatment notes for the J2000 ecliptic/equinox source frame and mean-obliquity equatorial transform, and validation reports that expose aggregate plus per-body comparison error summaries, explicit interim tolerance status by body, compact source-backed VSOP87 evidence snapshots, and a concise VSOP87 body-evidence outlier note when a measured body falls outside the interim envelope. The VSOP87 batch-parity validation helpers now also re-evaluate the exact/interpolated/approximate/unknown mix against fresh backend requests, so the release-facing batch summaries now fail closed if the quality distribution drifts even when the total request count still matches.

## Remaining implementation goals

1. Maintain and document the production `pleiades-vsop87` planetary calculations.
   - Keep the generated VSOP87 coefficient data path reproducible in pure Rust.
   - Document variant, truncation policy, frames, units, and date range.
   - Preserve geocentric astrology-facing ecliptic positions for Sun and planets.
   - Keep the source-backed major-planet coefficient path reproducible from public inputs and validate error envelopes.
   - Add batch-path tests covering all supported planets at canonical epochs.
   - Progress note: the comparison-audit gate now shares a typed comparison-audit summary helper, and the validation, release, and backend-matrix summaries surface that structured body/count/status line explicitly so the `compare-backends-audit` gate text stays aligned across the compact report views. That summary now also validates its derived body-count totals before formatting, so the compact audit line fails closed if the within-tolerance/outside-tolerance split ever drifts out of coherence. The comparison-tolerance policy summary now also validates its scope rows, per-row body counts, per-row tolerance limits, and aggregate coverage totals before the compact report formatter renders it, which keeps the release-facing tolerance policy line on the same fail-closed path as the audit summary. The validation corpus summary and body-tolerance status rows now also preflight their epoch ordering, zero-request posture, and margin/status coherence before the report wrappers reuse them, so the compact comparison corpus and expected-tolerance lines now fail closed if those derived summaries drift away from the underlying comparison evidence. Latest progress (2026-04-30): `pleiades-validate` now exposes the combined comparison-envelope summary as a public typed helper too, which keeps the compact median and 95th-percentile comparison envelope co-located with the aggregate comparison record for downstream release tooling instead of only inside the private report formatter.
   - Progress note (2026-04-30): the comparison-report smoke coverage now exact-pins the report header, corpus name, corpus description, epoch-label, span, and backend lines, and the request-surface summary test now exact-pins the one-line primary request surface inventory too, which keeps both compact report surfaces fail-closed instead of only checking them with substring or prefix matches.
   - Progress note (2026-04-30): the compact comparison tail envelope is now a public typed summary helper in `pleiades-validate`, and the release-summary smoke coverage exact-pins the full `Comparison tail envelope: ...` line through that helper, which keeps the 95th-percentile error tail aligned with the report formatter instead of only checking it through substring matches.
   - Progress note (2026-04-28): the comparison-envelope helper records now also validate their median and percentile fields directly before the release-facing percentile line reuses them, which keeps the compact error-envelope posture on the same fail-closed path as the body-count audit summary instead of relying only on the enclosing sample checks.
   - Progress note: the crate now ships a maintainer-facing regeneration helper plus binary that rewrites the checked-in generated blobs from the vendored source text. The helper now also reports an explicit unknown-source-file error that lists the supported vendored inputs, which keeps the reproducibility failure mode typed instead of stringly. The helper now also supports a `--check` mode that regenerates the blobs in memory and compares them against the committed artifacts without writing to disk. The checked-in generated-blob lookup path now also has an explicit helper, so the source-file-to-binary mapping used by the reproducibility tests is discoverable without a hardcoded match block. Latest progress (2026-04-28): `pleiades-vsop87` now exposes that body/source pairing as a public source-manifest helper too, and the regeneration CLI threads it through its write/check diagnostics, which makes a missing or mismatched VSOP87 blob easier to audit without changing the pure-Rust reproducibility path. Latest progress (2026-04-28): the library regression suite now also pins that public source-manifest ordering against the source-specification catalog and checked-in blobs directly, so the reproducibility path now fails closed if the manifest ever drifts from the release-order body/file pairing. Latest progress (2026-04-28): the same regeneration helper now also preflights the manifest against the current catalog before write/check modes run, which keeps the failure mode fail-closed even before the regenerated bytes are compared. Latest progress (2026-04-28): the same source manifest now also has a compact typed summary/display helper, and the regeneration CLI prints the release-order body/source summary before write/check work begins so maintainers can audit the checked-in pairing at a glance. Latest progress (2026-04-28): source-manifest length mismatches now also report the expected and actual body/source pairs inline, which makes a truncated or expanded manifest easier to diagnose at the reproducibility boundary.
   - Progress note: the canonical J2000 VSOP87 evidence summary now names the body that drives each maximum delta axis, and now also records the source kind and source file behind each axis peak, which makes the release-facing envelope easier to audit without widening the public request model.
   - Progress note: the VSOP87 source-audit summary now also prints the deterministic fingerprint count directly in release-facing output, so the reproducibility line stays explicit alongside the raw source-size and term-count evidence.
   - Progress note: the VSOP87 generated-binary audit is now also surfaced as a backend-owned summary and reused by the validation and release summaries, so the checked-in `.bin` artifacts now carry an explicit reproducibility line alongside the source-audit and canonical evidence reports. The generated-binary summary now also carries the canonical body and source-file order directly, which makes the checked-in blob provenance a little easier to audit without reconstructing it in the reporting layer. The generated-binary report path now also preflights the checked-in blob lookup before formatting, so a missing binary now fails closed as an unavailable reproducibility line instead of depending on a panic-prone lookup during report construction.
   - Progress note: the VSOP87 source-audit and generated-binary audit summaries now validate their derived fields before the release-facing report wrappers render them, so provenance drift now surfaces as an unavailable report line instead of quietly formatting a stale summary record. The per-body VSOP87 source profiles now also validate their provenance text and source-kind pairing before the canonical evidence builders reuse them, so a drifted catalog entry now fails closed in the release-facing source-backed evidence path instead of only in the final report line.
   - Progress note (2026-04-28): the VSOP87 per-source audit records now also expose typed summary/display and self-validation helpers against the vendored source text, and the aggregate source-audit summary now preflights those records before report formatting, so a drifted single-file audit now fails closed even if the overall totals still look coherent.
   - Progress note: the VSOP87 regression suite now also covers the full supported planetary batch at J2000, including the Pluto fallback path, so batch-path verification now spans the complete supported set rather than only the source-backed subset. The validation summary now also names Pluto explicitly as the mean-element fallback body in the source-documentation line, keeping the remaining non-VSOP87 special case visible in release-facing output.
   - Progress note: the VSOP87 batch-path suite now also exercises the full supported planetary set at the J1900 canonical epoch in equatorial TDB mode, preserving batch/single parity and the mean-obliquity frame transform across a second reference instant instead of only at J2000. The canonical J1900 batch-parity helper is now public and reused by validation/release reporting, which keeps that second-epoch coverage visible outside the test module. The supported-body batch regression now also covers the full planetary set at the canonical J2000 epoch in equatorial TDB mode, which keeps the J2000 batch/single parity visible alongside the existing J1900 supported-body coverage. The same supported-body slice now also covers the canonical J2000 planetary set in the geocentric ecliptic frame, which keeps the astrology-facing batch path pinned in addition to the equatorial release-report frame.
   - Progress note (2026-04-29): the VSOP87 source-backed J1900 ecliptic batch corpus now also exposes matching `source_backed_body_j1900_ecliptic_batch_parity_requests()` / `..._request_corpus()` aliases, which keeps the supported-body ecliptic slice reusable under the same body-order-oriented naming pattern as the other validated VSOP87 batch helpers.
Progress note (2026-04-29): the VSOP87 canonical J1900 batch-parity summary now uses an explicit J1900 TDB reference epoch instead of inferring it from the request corpus, and the summary test suite now pins the reference-epoch drift case directly, which keeps the canonical epoch anchor explicit even if the request constructor changes later.
Progress note (2026-04-30): the validation and release smoke suites now exact-pin the packaged lookup epoch policy line, which keeps the TT-grid retag wording on the same fail-closed path as the other compact packaged-data report lines instead of leaving it as a substring-only check.
   - Progress note (2026-04-30): the VSOP87 canonical J2000 slice now also exposes a `canonical_epoch_request_corpus()` compatibility alias, which keeps the legacy request-corpus name available alongside the newer canonical batch helper for downstream validation and reproducibility tooling.
   - Progress note (2026-04-30): the same canonical J2000 batch slice now also exposes `canonical_epoch_batch_parity_requests()` and `canonical_epoch_batch_parity_request_corpus()` aliases, which keeps the older canonical-epoch batch helper discoverable beside the explicit J2000 name without changing the request shape.
   - Progress note (2026-04-30): the canonical J1900 batch-parity slice now also exposes a `canonical_j1900_batch_parity_request_corpus()` compatibility alias, which keeps the J1900 request-corpus naming aligned with the other batch helper families for downstream validation and reproducibility tooling.
   - Progress note (2026-04-30): the supported-body J2000 and J1900 batch corpora now also expose plain `*_request_corpus` aliases for the equatorial and ecliptic request shapes, which keeps the same discoverability pattern available for the Sun-through-Pluto helper slices too.
   - Progress note (2026-04-30): the supported-body J2000 and J1900 request surfaces now also expose plain `supported_body_j2000_request_corpus()` and `supported_body_j1900_request_corpus()` compatibility aliases for the ecliptic batch slices, which keeps the astrology-facing Sun-through-Pluto corpora discoverable through the same plain request-corpus naming pattern as the other release-facing helpers.
   - Progress note (2026-04-28): the supported-body J2000 ecliptic batch parity summary now also renders in the validation, release, and backend-matrix reports, so the full Sun-through-Pluto batch regression is visible in the compact release-facing report set instead of only in the unit test slice.
   - Progress note (2026-04-29): the supported-body J1900 ecliptic batch parity slice now also renders in the same validation, release, and backend-matrix surfaces, which keeps the chart-facing canonical-epoch batch coverage explicit at both J2000 and J1900 in the ecliptic frame.
   - Progress note (2026-04-29): the supported-body J2000 equatorial batch parity slice now also renders alongside the ecliptic batch evidence, so the full supported planetary set now has an explicit batch/single parity cross-check in the mean-obliquity frame as well as the astrology-facing ecliptic frame. The canonical J2000 parity summary now also has explicit `sample_bodies` and `frame` drift regressions, which keeps the source-backed canonical epoch aligned with the same fail-closed guardrails already exercised on the J1900 summary path. Latest progress (2026-04-30): the source-backed J2000 equatorial request corpus now also has matching `source_backed_body_j2000_equatorial_batch_parity_requests()` / `..._request_corpus()` aliases, which keeps the source-backed mean-obliquity slice discoverable under the same naming pattern as the other validated VSOP87 batch helpers. Latest progress (2026-04-30): the source-backed J2000 ecliptic request corpus now also has matching `source_backed_body_j2000_ecliptic_batch_parity_requests()` / `..._request_corpus()` aliases, which keeps the source-backed geocentric-ecliptic slice aligned with the same naming pattern as the frame-qualified supported-body helpers. Latest progress (2026-04-30): the source-backed J1900 ecliptic request corpus now also exposes a plain `source_backed_body_j1900_request_corpus()` compatibility alias, which keeps the canonical J1900 geocentric-ecliptic slice discoverable through the same shorter naming pattern as the other release-facing batch helpers. Latest progress (2026-04-30): the source-backed J2000 and J1900 frame-specific VSOP87 corpora now also expose plain `request_corpus` aliases, which keeps the reusable source-backed ecliptic/equatorial batch slices discoverable under the same naming pattern as the supported-body helpers.
   - Progress note (2026-04-29): the supported-body J1900 equatorial batch parity slice now also renders alongside the J2000 equatorial and J1900 ecliptic evidence, completing the supported-body canonical-epoch parity matrix in the mean-obliquity frame.
   - Progress note (2026-04-29): the supported-body J2000 ecliptic batch-parity slice now also exposes a reusable request-corpus helper, plus an explicit `supported_body_j2000_ecliptic_batch_parity_requests()` compatibility alias, which keeps the Sun-through-Pluto ecliptic batch shape reusable for downstream validation and reproducibility tooling without reconstructing it from the summary metadata.
   - Progress note (2026-04-28): that supported-body batch parity summary now also has a report-wrapper drift regression for the frame field, so the compact VSOP87 batch-parity evidence now fails closed if the release-facing ecliptic batch line ever drifts out of sync with the validated supported-body slice.
   - Progress note: the canonical VSOP87 body-evidence envelope is now owned by `pleiades-vsop87` as a structured backend summary, and validation reuses it directly instead of recomputing the source-kind and interim-limit breakdown in the tooling layer. The VSOP87 canonical evidence, source documentation, source audit, and body-evidence report strings are now backend-owned too, so the release-facing reporting surface stays co-located with the source metadata instead of being rebuilt privately in validation. The source-documentation summary now also carries the fallback-body list as structured data, which keeps the Pluto fallback name available without recomputing it from the body-profile table in the reporting layer. The same summary now also carries the shared date-range note for the retained VSOP87 slices, so the compact release-facing lineage now shows the documented epoch scope alongside the source-path mix. The canonical J2000 evidence summary now also reports mean absolute deltas and an out-of-limit count, so the compact release-facing envelope now mirrors the richer lunar and JPL summaries a little more closely. The source-backed body evidence summaries now also verify that any outlier bodies still belong to the sampled body set, which keeps the release-facing outlier notes aligned with the canonical sample list instead of only with the aggregate counts. The source-backed body and body-class evidence summaries now also validate their canonical sample order in addition to the count and provenance checks, so a reordered compact report now fails closed before it can drift out of sync with the canonical source-backed evidence. The source-backed body-class evidence summaries now also validate their finite, non-negative, and metric-order invariants before the release-facing formatter reuses them, which keeps the coarse luminary/major-planet envelopes on the same fail-closed path as the per-body and source-documentation summaries. Latest progress (2026-04-30): the VSOP87 frame-treatment line now also travels through a backend-owned report helper, which keeps the current ecliptic/equinox and mean-obliquity wording aligned with the other fail-closed source-backed provenance lines.
   - Progress note (2026-04-30): the supported-body canonical batch matrix now also exposes a reusable request corpus plus compatibility alias, which keeps the combined J2000/J1900 ecliptic and equatorial batch shape available for downstream validation and reproducibility tooling without reconstructing the four slices inline.
   - Progress note (2026-04-30): the canonical VSOP87 body-evidence rows now expose typed summary/validation helpers and the canonical-evidence builder now preflights them before the aggregate summaries render, which keeps the per-row source provenance, source-file pairing, and interim-limit status fail-closed instead of trusting only the aggregate envelope.
   - Progress note: the canonical J2000 source-backed evidence and equatorial companion summaries now validate their sample-body, peak-body, and source-file invariants before release-facing formatting, so malformed VSOP87 evidence now fails closed instead of rendering a stale compact line.
   - Progress note (2026-04-30): those same VSOP87 canonical summaries now also re-derive their full field sets from the current body-evidence slices during validation, which keeps drifted peak bodies, source metadata, and summary statistics from slipping through if the stored rows remain internally coherent.
   - Progress note (2026-04-28): the canonical J2000 evidence and equatorial companion summaries now also reject non-finite or negative compact error-envelope metrics before the release wrappers reuse them, which keeps the derived statistics on the same fail-closed path as the body/source provenance.
   - Progress note (2026-04-28): the comparison-envelope formatter now also fails closed if a sample slice mixes present and absent distance deltas, which keeps partially populated distance channels from being averaged into the release-facing error envelope.
   - Progress note: the VSOP87 source-documentation summary now also enumerates the source-backed body list explicitly, so the compact release-facing provenance line now names the current Sun-through-Neptune source-backed set alongside the fallback Pluto path without requiring the backend matrix to be opened. The same summary now also names the exact public source file set (`VSOP87B.ear` through `VSOP87B.nep`), which makes the reproducibility line a little more concrete without changing the runtime path. The supported source-file catalog is now also exposed as a public helper for regeneration tooling, so downstream reproducibility checks can reuse the backend-owned source-file order without hardcoding the vendored list. The source-documentation breakdown now also names the generated-binary, vendored full-file, and truncated body lists explicitly, so the compact provenance line can point at the exact body coverage behind each source-path category instead of only reporting counts. The source-documentation health summary now mirrors those per-category body lists too, and now also surfaces the exact source-file order in the same structured record, which keeps the compact release-facing health check aligned with the richer source-path breakdown. The source-documentation health check now also verifies the documented variant, coordinate-family, frame, units, reduction, transform-note, truncation-policy, and date-range invariants in addition to the catalog counts, and now explicitly checks that the source-backed body order still matches the current generated-binary/vendored/truncated partition order, so release-facing provenance can flag documentation drift as well as body-count or partition-order drift. The health summary now also carries the source-backed and fallback body lists themselves, so the compact release-facing health line can name the current body partition directly instead of only surfacing the counts, and now also exposes the explicit source-backed partition order in the same record. The health formatter now also emits explicit issue labels when the catalog ever drifts, which keeps the failure mode actionable without changing the current consistent-path wording. The VSOP87 source-documentation, health, and body-evidence summaries now also expose typed `summary_line()`/`Display` rendering, so the validation and release helpers can reuse backend-owned reporting records directly instead of rebuilding those strings in the tooling layer. The VSOP87 source audit summary now also names the source-backed body order and public source-file set explicitly, so the compact reproducibility line can audit the exact public inputs instead of only surfacing aggregate counts. Validation and release summary tests now pin the VSOP87 source-documentation health line exactly, so the source-file and body-order consistency checks stay exercised in the release-facing smoke suite. The source-documentation partition order is now also exposed through a public helper, which lets regeneration tooling reuse the backend-owned generated-binary/vendored/truncated partitioning directly instead of reconstructing it from the summary record. The source-documentation health issue labels are now typed too, which keeps the release-facing issue list aligned with stable enum labels instead of raw string literals. Progress note (2026-04-27): the VSOP87 source-documentation health summary now also exposes a public `validate()` helper with a boxed failure path, giving regeneration and release tooling a typed fail-fast check before formatting the release-facing provenance line. The release-facing report wrapper now also invokes that validation helper before formatting the compact health line, so future catalog drift is caught at the report boundary rather than only in the underlying summary record. Progress note (2026-04-27): that same health validator now also checks the source-backed and fallback list/count coherence directly, and now fails on duplicate or overlapping source-backed/fallback body lists too, so partition-order or profile-count drift now fails closed even if a caller accidentally leaves the top-level booleans optimistic. Progress note (2026-04-28): the source-documentation health report wrapper now returns an unavailable summary instead of asserting when the cached record drifts, which keeps the release-facing provenance line fail-closed even if a caller reaches the wrapper directly with stale data. Progress note (2026-04-27): `pleiades-vsop87::Vsop87SourceSpecification` now also has a reusable `validate()` helper, and the release-facing source-specification report now preflights it before formatting, so blank source metadata fails closed before the compact catalog line renders. Progress note (2026-04-27): the same source-specification validation now also rejects duplicate public source-file labels, so the release-facing reproducibility line fails closed if two catalog entries ever point at the same public input file. Progress note (2026-04-30): `Vsop87SourceSpecification::validate()` now also fails closed on drifted canonical provenance fields such as the documented frame, units, coordinate family, variant, transform note, and date range, which keeps the source-documentation health line anchored to the current public source catalog instead of only checking for non-blank metadata. The regression suite now also pins explicit transform-note and date-range drift cases, which keeps the provenance guard exercised across the full canonical field set instead of only the already-covered frame, reduction, and truncation-policy examples.
   - Progress note (2026-04-28): that same VSOP87 source-specification guard now also trims duplicate file labels before surfacing them in the validation error, and the regression suite now covers both a whitespace-padded duplicate source file and a blank `date_range` field, which keeps the provenance-validation path a little easier to audit when the release-facing catalog is regenerated.
   - Progress note (2026-04-28): the canonical VSOP87 source-backed evidence summaries now also preflight their peak source-kind and source-file provenance against the current catalog before the release-facing report wrappers render them, so stale peak provenance labels now fail closed instead of silently reusing an old source mapping.
   - Progress note: the VSOP87 request-policy and unsupported-body messages now use the production backend label instead of the older MVP phrasing, so request failures stay aligned with the source-backed backend state. The VSOP87 and JPL request-policy summary helpers now also validate their structured posture before they format release-facing report lines, so the compact snapshot policy text fails closed if a future summary drifts away from the current ecliptic/equatorial TT/TDB mean-only posture. `pleiades-backend::FrameTreatmentSummary` now also exposes a validated summary-line helper, and the JPL, VSOP87, ELP, and packaged-data frame-treatment report paths reuse it so the shared mean-obliquity wording fails closed before the compact report surfaces render.
   - Progress note: the Pluto VSOP87 source-profile provenance now explicitly calls the body a mean-element fallback special case, which keeps the non-VSOP87 outlier note visible in the backend matrix and source-profile summaries.
   - Progress note: backend provenance is now surfaced through a typed `BackendProvenance::summary_line()`/`Display` helper, and the validation comparison/backend-matrix renderers reuse that helper directly so source-material provenance stays centralized on the backend metadata type instead of being reconstructed from the raw field everywhere.
   - Progress note: the canonical VSOP87 evidence summary now also reports 95th-percentile longitude, latitude, and distance deltas, giving the backend-owned source-backed envelope a compact upper-tail metric alongside the existing mean/median/RMS view.
   - Progress note: the canonical VSOP87 evidence summary now also re-derives its out-of-limit count from the current body-evidence slice before the compact report formatter reuses it, so the release-facing envelope now fails closed if that count ever drifts out of sync with the measured sample evidence.
   - Progress note: the VSOP87 source-backed body evidence summary now also rejects empty sample sets and duplicate body labels before release-facing formatting, which keeps the compact provenance line fail-closed if the canonical body order or coverage bookkeeping ever drifts. The same summary also prints the canonical source-backed body order, so the compact provenance line now mirrors the Sun-through-Neptune ordering instead of only listing aggregate counts. The formatter now labels that sequence explicitly as the source-backed body order in release-facing output, which keeps the reproducibility cue a little clearer in the compact report text.
   - Progress note: the backend-matrix implementation note for the VSOP87 backend now correctly describes the Sun-through-Neptune generated-binary paths as derived from the vendored full-file inputs, keeping the release-facing implementation status aligned with the runtime path.
   - Progress note: the VSOP87 and ELP backend-matrix status notes now use production-neutral wording for the current Pluto and lunar baselines, and the remaining stale pending phrasing has been removed from the VSOP87 source comments, ELP source-material summary, lunar-theory policy doc, and the corresponding crate metadata strings so the compact release-facing provenance text stays aligned with the current implementation posture.
   - Progress note: the VSOP87 canonical J2000 evidence now also has a backend-owned equatorial companion summary, so the source-backed planetary slice now exposes a mean-obliquity frame cross-check alongside the ecliptic envelope in validation and release-facing summaries.
   - Progress note: the VSOP87 canonical J2000 and equatorial evidence summaries now also implement typed `summary_line()`/`Display` rendering, and the backend report helpers now reuse those typed records directly, which keeps the release-facing formatter ownership aligned with the other backend-owned VSOP87 summaries.
   - Progress note: the validation backend-matrix VSOP87 rows now also render source-kind and accuracy through the typed `Display` labels instead of enum debug spellings, so the release-facing source-profile lines stay aligned with the public enum contracts instead of ad hoc formatting.
   - Progress note: the VSOP87 canonical J2000 evidence now also carries a concise interim-outlier note in the validation, backend-matrix, and release summaries, so any future envelope regression can name the affected bodies without leaving the compact report readers to infer that from the aggregate count alone. Latest progress (2026-04-28): that interim-outlier line is now backed by a typed canonical-outlier summary plus fail-closed validation in `pleiades-vsop87`, which keeps the compact note on the same drift-check path as the rest of the source-backed VSOP87 evidence.

Progress note: the compact validation report summary now also surfaces the VSOP87 source-backed body-class envelopes alongside the existing source documentation and body evidence lines, keeping the coarse class rollup visible in the shorter release-facing validation view too.
Progress note: the VSOP87 source-specification records now have typed `summary_line()`/`Display` helpers plus reusable single-catalog formatting helpers, so the documented variant, frame, unit, and date-range fields can travel through typed report code instead of being rebuilt ad hoc. The per-body source profile records now have the same typed summary/display surface, and the backend-matrix report reuses it directly so the Sun-through-Neptune provenance lines stay centralized on the VSOP87 crate side instead of being hand-formatted only in validation.
Progress note: `pleiades-vsop87` now also exposes a reusable batch-request corpus helper for arbitrary body iterators, instant tags, and coordinate frames, and the canonical J2000/J1900 batch-path regressions reuse that helper directly so validation tools can share the same request-shape construction instead of rebuilding it at each call site. The J1900 helper now also has a direct crate-level regression that checks the release-facing summary line and the supported-body order against the backend metadata. The canonical J2000/J1900 batch-parity summaries now also validate their derived counts before the compact report wrappers render them, so future drift in the sample/body or quality-count totals fails closed at the report boundary instead of formatting stale batch evidence. Those batch-parity validators now also pin the expected reference epoch, frame, and body order for the two canonical slices, which keeps the canonical batch evidence aligned with the checked-in reference metadata as the source-backed path continues to harden. Latest progress (2026-04-28): the VSOP87 canonical batch evidence now also includes a mixed TT/TDB parity slice, and the validation/release/backend-matrix summaries now surface it alongside the J2000 and J1900 batch evidence so the planetary time-scale policy has a compact release-facing regression too. The mixed-slice validator is intentionally exact-count driven, so the canonical alternating TT/TDB split is already enforced by the existing request totals rather than a separate degenerate-mix guard. Latest progress (2026-04-29): the canonical J2000 request corpus now also has an explicit `canonical_j2000_batch_parity_requests()` alias, which keeps the source-backed J2000 batch slice discoverable under the same naming pattern as the summary helper while preserving the older compatibility alias for existing callers. Latest progress (2026-04-29): the supported-body J2000 equatorial and J1900 ecliptic batch-parity slices now also expose matching public request-corpus helpers, which keeps the supported-body batch shapes reusable from downstream validation and reproducibility tooling without rebuilding them from the summary metadata. Latest progress (2026-04-29): the VSOP87 canonical J1900 batch parity slice now also exposes an explicit `canonical_j1900_equatorial_batch_parity_requests()` alias, which keeps the equatorial canonical batch shape discoverable under the same frame-qualified naming pattern as the J2000 helper while preserving the compatibility alias for existing callers. The canonical mixed TT/TDB batch-parity corpus now also has a request-corpus alias, which keeps the alternating ecliptic slice reusable through the same naming pattern as the other validated corpora. Latest progress (2026-04-29): the canonical J2000 and supported-body equatorial slices now also expose matching `*_request_corpus` aliases, which keeps the canonical and supported-body VSOP87 batch shapes consistent with the existing `request_corpus` naming pattern used by the other release-facing batch helpers. Latest progress (2026-04-29): the source-backed J2000 batch slice now also has a dedicated `source_backed_body_j2000_batch_parity_requests()` / `..._request_corpus()` alias pair, which keeps the exact Sun-through-Neptune batch shape available under a body-order-oriented name for downstream validation and reproducibility tooling. Latest progress (2026-04-29): the source-backed J1900 equatorial batch slice now also has matching `source_backed_body_j1900_equatorial_batch_parity_requests()` / `..._request_corpus()` aliases, which keeps the canonical J1900 equatorial slice discoverable under the same body-order-oriented naming pattern as the other source-backed VSOP87 request helpers. Latest progress (2026-04-30): the VSOP87 plain `*_request_corpus` aliases now also cover the canonical J2000, source-backed J2000, canonical J1900, and mixed TT/TDB batch slices, which keeps the most commonly reused source-backed corpora easier to discover for downstream validation and reproducibility tooling without changing the request shapes themselves. The canonical mixed TT/TDB slice now also exposes a direct `canonical_mixed_tt_tdb_batch_parity_requests()` alias, which keeps the mixed-scale batch helper discoverable through the same `*_requests` naming pattern as the other VSOP87 corpora.

- Progress note (2026-04-27): `pleiades-backend::BackendMetadata` now also exposes a shared validation helper that catches blank identifiers, duplicate provenance sources, duplicate coverage entries, and inverted nominal ranges before release-facing metadata is reused, and `pleiades-core::ChartEngine` now forwards that check through `validated_metadata()` for callers that want a preflighted metadata snapshot.
- Progress note (2026-04-27): backend capability metadata now also validates that each backend declares at least one usable position mode and one usable value mode, so malformed capability combinations fail closed before they can leak into the shared backend inventory or release summaries.
- Progress note (2026-04-28): routing backends now defer request-shape and batch gating to the selected provider while still preflighting body coverage, which keeps the route-level consumer API from being blocked by a coarse aggregate capability summary before the routed backend has a chance to accept the request.
- Progress note (2026-04-29): the routed batch metadata preflight now also has a mixed TT/TDB observer/apparentness regression, so the batch deferral path stays aligned with the single-request routing posture while preserving each request's own time-scale label.
- Progress note (2026-04-29): the routing backend batch path now also preserves mixed TT/TDB requests when the first provider rejects a topocentric observer and the request falls back to a later provider, which keeps the routed batch positions path aligned with the same request-shape policy now exercised by the direct backend and chart-facade batch preflights.
- Progress note (2026-04-28): `pleiades-validate::BodyToleranceSummary::validate()` now rejects zero-sample summaries and non-finite tolerance metrics before the compact body-tolerance report formatter reuses them, so the release-facing per-body tolerance rows now fail closed instead of carrying an invalid comparison envelope.
- Progress note (2026-04-27): the validation report's per-body comparison summaries now also have a typed finite/extrema validator and a report preflight path, so malformed body-level error envelopes fail closed before the compact body-comparison lines render.

   - Progress note: the VSOP87 canonical ecliptic and equatorial evidence summaries now also validate their internal mean/median/percentile/RMS ordering before release-facing formatting, so the compact planetary error envelopes fail closed if a future field swap ever makes the derived shape incoherent even when all values remain finite.

- Progress note (2026-04-29): the VSOP87 source-documentation report now routes through a testable formatter helper and has a drift regression for stale catalog counts, which keeps the compact provenance line fail-closed when the cached summary stops matching the current source catalog.
- Progress note (2026-04-30): the release-summary smoke coverage now exact-pins the `Release summary` header and the `Backend matrix summary: backend-matrix-summary` line, which keeps two more user-facing status lines fail-closed instead of relying only on substring matches in the compact release view.
- Progress note (2026-04-30): the release-notes-summary surface now also surfaces the VSOP87 source-documentation health line, which keeps the compact maintainer-facing notes view aligned with the source-backed planetary provenance that already appears in the validation and release-summary views.

2. Implement production `pleiades-elp` lunar calculations.
   - Select and document a pure-Rust lunar theory source.
   - Support Moon longitude, latitude, distance, and useful speed outputs.
   - Implement or explicitly defer mean/true nodes and apogee/perigee where mathematically justified.
   - Progress note: the backend now has an explicit lunar-theory policy document plus a short high-curvature Moon-window regression test, so the current baseline provenance and nearby motion continuity are exercised while source-backed ELP selection remains queued. The same high-curvature slice now also has batch-path regressions in both ecliptic and equatorial frames, which keeps the nearby-motion window covered through `positions()` as well as the single-query continuity envelope.
   - Progress note: the same high-curvature Moon-window regression slice now also has a backend-owned continuity summary rendered in validation/backend-matrix reports, so the nearby-motion check is visible in release-facing output rather than only in the unit test. The same J2000 window now also has a companion equatorial continuity slice in the validation and backend-matrix outputs, which keeps the mean-obliquity frame check visible alongside the ecliptic motion check. The high-curvature continuity envelopes now also validate their stored regression-limit flags against the derived step thresholds, so the compact nearby-motion evidence fails closed if the summary bool ever drifts away from the actual maxima.
   - Progress note: the current lunar-theory selection now exposes a stable source identifier, canonical bibliographic citation, explicit source-family label, and explicit license/provenance posture alongside the supported and unsupported lunar bodies, and the validation/reporting layer renders those provenance details in release-facing summaries. The source-family label is now also surfaced through `Display` so future source-selection variants can reuse the same typed enum in release text. The same baseline now also exposes a structured `source_selection()` helper so the provenance fields can travel as one typed record in backend-owned reporting. The lunar backend now also publishes a compact source-selection summary helper, and validation/release reports now render that shorter provenance line alongside the full specification so the selected lunar baseline is easier to scan without parsing the longer catalog text. The compact source-selection summary now also carries the current validation window, which keeps the short provenance line explicit about the evidence span without requiring the longer specification block. The main lunar theory specification summary now also includes the typed selected catalog key, keeping the longer provenance line aligned with the resolver surface used by the compact source-selection helper. The compact lunar catalog-validation summary now also includes the typed family key alongside the selected catalog key, which keeps the fail-closed resolver surface visible in the short catalog-audit line too. The lunar catalog-validation summary now also has a fail-closed report wrapper, so the compact catalog audit line refuses to render stale selected-count or validation-result fields if the cached summary drifts.
   - Progress note: validation backend-matrix output now also renders the ELP unsupported lunar bodies explicitly, so the release-facing lunar-theory section shows both the supported channels and the deferred true apogee/perigee slots.
   - Progress note: the Moon path now uses a Meeus-style truncated lunar position series instead of the earlier simplified orbital surrogate, and the backend now validates against the published 1992-04-12 geocentric Moon example in addition to the J2000 lunar-point checks.
   - Progress note: the compact lunar backend now also exports a canonical reference evidence slice for the published Moon example plus the J2000 lunar-point samples, and validation reports render that slice in the backend matrix and summary output so the current lunar baseline is easier to audit without changing the API contract.
   - Progress note: the lunar reference evidence summary now also carries the JD 2419914.5..2459278.5 range explicitly, making the current 1913-node-to-2021 coverage window visible in release-facing summaries.
   - Progress note: the lunar backend now also has a batch-path regression over the Moon, nodes, and mean apogee/perigee evidence slice, so the current supported lunar points are exercised through `positions()` in addition to single-request coverage. The same backend now also rejects true apogee and true perigee through batch requests with the same structured `UnsupportedBody` error used for single-request calls, keeping the deferred lunar channels explicit even when they arrive through `positions()`.
   - Progress note: the compact lunar-theory specification now also exposes a structured request-policy field — TT/TDB only, tropical only, mean only, ecliptic/equatorial frames, and no topocentric observer mode — so the current baseline remains visibly scoped while source-backed ELP selection stays queued. The request-policy summary is now also exposed as a backend-owned helper and reused by validation reporting, which keeps the structured lunar policy text centralized for future source-selection variants.
   - Progress note: the compact lunar-theory specification now also carries explicit truncation and output-unit notes, and the validation/reporting layer renders those notes alongside the provenance fields so the current truncated baseline is easier to audit without changing the public API. The same baseline now also exposes its source-family label directly, which keeps the source-selection posture structured without widening the API surface. The current source family is now also available as a dedicated `lunar_theory_source_family_summary()` / report helper, which keeps the Meeus-style family label available as its own fail-closed provenance line alongside the longer source-selection summary.
   - Progress note: the lunar-theory selection data is now centralized into a single static specification, and the canonical evidence slice is checked against that supported-body list so the current Meeus-style baseline stays internally consistent as the source-selection work continues.
   - Progress note: the backend now also exposes dedicated supported/unsupported lunar-body accessors from that centralized selection, and the backend metadata derives its body coverage from the same source-of-truth slice so future source-selection swaps have one less place to drift.
   - Progress note: the compact lunar-theory metadata now also exposes its current validation window as a structured `TimeRange` in addition to the prose date-range note, so release-facing summaries can render the sampled epoch span without inferring it from freeform text.
   - Progress note: the release-facing lunar summary formatter now lives in `pleiades-elp`, which keeps the lunar provenance text owned by the backend crate rather than duplicated in validation/reporting code.
   - Progress note: that lunar summary is now parameterized through `format_lunar_theory_specification(&LunarTheorySpecification)`, so future source-selection variants can reuse the same backend-owned one-liner without rebuilding the template in validation. The `LunarTheorySpecification` record now also exposes a typed `summary_line()`/`Display` helper, and the release-facing lunar summary path reuses it directly so the specification text can travel as a typed record instead of only through the free function formatter.
   - Progress note: the canonical lunar reference evidence summary now also lives in `pleiades-elp`, and validation/reporting reuses that backend-owned summary and formatter instead of keeping a duplicate copy in the tooling layer.
   - Progress note: the new compact lunar capability summary helper now gives validation a structured body/frame/time-scale snapshot without parsing prose, and now also carries the supported and unsupported lunar body lists so the current baseline channels stay visible without opening the full specification. The lunar source-selection, catalog-validation, and capability summary records now also implement `summary_line()`/`Display`, so validation and release callers can render the typed lunar records directly without rebuilding those strings in the reporting layer. The lunar capability summary now also has a typed validation helper plus a report-facing wrapper, so the compact ELP capability line can fail closed if the model/source/body/frame/time-scale metadata drifts. The backend-owned lunar reference summary now has a measured error-envelope companion rendered in the detailed report and backend-matrix summary, keeping the source-selection posture easier to reuse in later release reports.
   - Progress note: the backend-owned lunar reference evidence summary now also has a measured error-envelope companion, and both the detailed report and backend-matrix summary render that envelope line so the current lunar evidence slice exposes a compact residual snapshot alongside the canonical samples. The lunar reference and equatorial evidence summaries now also validate against the checked-in slices before their report wrappers render, so the short release-facing lunar provenance lines fail closed instead of reusing a stale cached summary.
   - Progress note: the lunar high-curvature continuity evidence slices now also have backend-owned typed `summary_line()`/`Display` helpers, and the report formatter reuses those typed records directly so the nearby-motion regression wording stays centralized in `pleiades-elp` instead of being rebuilt only as ad hoc report text. The J2000 continuity window is now sampled more densely with half-day spacing across a six-point slice, which gives the nearby-motion regression a little more resolution without changing the public request model. The high-curvature ecliptic and equatorial continuity slices now also expose public request-corpus helpers plus compatibility aliases, which keeps the fixed Moon regression windows reusable by downstream tooling without reconstructing the request shapes inline. The same high-curvature slices now also expose plain `request_corpus` compatibility aliases, which keeps the fixed Moon regression windows discoverable through the same corpus naming pattern as the other release-facing helpers. The canonical lunar reference and lunar equatorial evidence rows now also expose typed summary/display helpers, and the detailed validation report reuses those helpers directly so the sample-level lunar evidence table stays centralized in the backend crate instead of being rebuilt inline in `pleiades-validate`. The lunar apparent-comparison evidence slice now also exposes public ecliptic and equatorial request-corpus helpers plus compatibility aliases, which keeps the four published mean/apparent comparison rows reusable by downstream tooling without reconstructing the request shapes inline.
   - Progress note: the lunar reference slice now also includes the 1992 Moon example, J2000 lunar-point anchors, published 1913-05-27 true-node and mean-node examples, a published 1959-12-07 mean-node example, and a published 2021-03-05 mean-perigee example, which broadens the current validation evidence without changing the public request model. The compact lunar theory date-range note now also names the reference-only 1968-12-24 apparent geocentric Moon comparison datum, so the broader validation slice remains visible in the release-facing provenance line.
   - Progress note: the `pleiades-elp::LunarTheorySpecification` record now carries the selected source family directly, which keeps the current lunar baseline selection structured for future source-backed swaps and simpler release reporting.
   - Progress note: the current lunar-theory selection is now also exposed as a one-entry catalog plus catalog summary helper, so release-facing lunar reporting has a stable selection surface ready for future source-backed variants without changing the public API shape. The same selection is now also available through a direct `lunar_theory_source_selection()` accessor, which keeps the structured provenance record easy to reuse without reconstructing it from prose. The lunar source-selection surface now also has typed lookup helpers by source identifier, model name, family label, and the documented short alias `Meeus-style truncated lunar baseline`, and now exposes a typed `LunarTheoryCatalogKey` plus `resolve_lunar_theory_by_key(...)`, which makes the current one-entry catalog easier to resolve from future source-backed selection code without changing the public API shape. The typed key now also has an explicit alias variant for the documented short label, keeping alias resolution symmetric with the other lookup families. The source-selection path now also has a structured family-key lookup and resolver, which makes the selected source family itself directly addressable without falling back to the string label. The lunar catalog now also exposes a direct `lunar_theory_catalog_entry_for_selection(...)` helper so the selected source record can round-trip back to its catalog entry without re-deriving the lookup key in reporting code. The compact lunar catalog summary now also reports the selected alias count plus supported and unsupported body counts, which makes the source-selection surface a little more explicit for future source-backed variants. The same catalog surface now also has a backend-owned consistency check that enforces the selected-entry round trip plus case-insensitive uniqueness for identifiers, model names, family labels, and documented aliases, which keeps the one-entry baseline safer to extend when a second lunar-theory source lands. The same helper now also rejects duplicate aliases inside a single entry and alias collisions against the selected entry's core labels, so the future multi-source catalog path has one more explicit guardrail. The catalog validation summary now also exposes the selected source, selected count, and validation result as structured data before formatting the release-facing line, keeping the backend-owned validation snapshot aligned with the selected lunar provenance while the source-selection catalog remains a one-entry baseline. The compact lunar capability summary now also reports whether the catalog validates cleanly, so the release-facing capability snapshot keeps the one-entry catalog health visible alongside the body/frame/time-scale counts without opening the longer validation summary. The lunar catalog summary now also has a typed validation helper and report preflight, so future drift in the catalog counts or selected-source fields now surfaces as an unavailable report line instead of a silently stale compact summary. The lunar source-selection summary now also has a typed validation helper and fail-closed report wrapper, so the compact source-selection provenance line now refuses to render stale source-family fields if the selected record ever drifts. The lunar reference evidence slice now also explicitly includes the J2000 mean apogee anchor in its release-facing summary, so the currently validated lunar-point set is a little more explicit without widening the public API. The lunar catalog validator now also rejects duplicate supported bodies, duplicate unsupported bodies, and supported/unsupported body overlap within a single entry, so future source-backed lunar selections now have body-coverage disjointness checked alongside the existing alias and round-trip guardrails.
   - Progress note (2026-04-29): `LunarTheoryCatalogEntry` now also exposes a compact typed summary/display helper, and the lunar catalog-selection regression now pins the selected/unselected entry wording too, which gives the one-entry source-selection baseline a reusable entry-level audit line for future lunar variants.
   - Progress note (2026-04-29): `LunarTheorySpecification::validate()` now also covers the lunar baseline's model name plus truncation, unit, date-range, frame, and validation-window fields, and the catalog validation path reuses that check before the existing alias/body round-trip audits, so drift in the descriptive lunar provenance now fails closed alongside the selection and coverage guards.
   - Progress note (2026-04-30): `pleiades-core::ObserverSummary` now has explicit validation plus a checked summary-rendering helper, which keeps the observer posture and stored location ready for future fail-closed report surfaces in the same typed-summary style as the other Phase 1 policy records.
   - Progress note (2026-04-30): `ObserverSummary::validate()` now fails closed on house-only summaries without a location and on invalid rendered locations, which gives the observer-summary record an explicit typed integrity check before future report callers reuse it.
   - Progress note (2026-04-30): `pleiades-backend::validate_observer_policy()` now validates any supplied observer location before it checks topocentric support, which keeps malformed observer input fail-closed on the shared backend path even when a provider advertises topocentric support.
   - Progress note: the lunar reference and equatorial error envelopes now also report mean residuals and explicit out-of-limit sample counts alongside the existing maxima, which makes the release-facing lunar evidence a little easier to scan while the current Meeus-style baseline remains the active lunar source-selection placeholder. The lunar error-envelope report strings now also spell out the applied regression limits directly, and now name any outlier bodies explicitly when a threshold is exceeded, so the phase-1 lunar evidence stays explicit about the current threshold policy in release-facing summaries.
   - Progress note: the lunar reference and lunar equatorial envelopes now also include median and 95th-percentile deltas in the backend-owned report helpers and validation smoke tests, which gives the compact lunar release evidence a slightly richer spread/shape view without changing the request contract or the current baseline slice.
   - Progress note: the lunar apparent comparison envelope now also reports mean, median, and 95th-percentile absolute deltas for longitude, latitude, distance, right ascension, and declination, so the apparent Moon reference-only slice now exposes the same compact spread metrics as the other lunar evidence summaries. The same apparent-comparison summary now also validates itself against the checked-in evidence slice before release-facing formatting, so drift in the derived sample count or peak axes now fails closed at the report boundary instead of rendering stale apparent-comparison prose. The same 1992-derived equatorial companion now also feeds the lunar equatorial reference slice through the shared mean-obliquity transform, which broadens the frame-conversion envelope beyond the original direct RA/Dec anchor while keeping the mean/apparent request policy unchanged. The individual apparent Moon sample rows now also expose typed summary/display helpers, which keeps the row-level provenance ready for future table-style report surfaces without relying only on inline note inspection. The lunar evidence row validators now also pin the supported lunar-body sets explicitly, so drifted Moon/node/apogee/perigee bodies now fail closed before the summary records render.
   - Progress note: the compact release summary now also surfaces the ELP lunar capability line plus the lunar reference and lunar equatorial evidence summaries alongside the JPL interpolation evidence, so the one-screen release overview keeps the current lunar theory baseline visible next to the other phase-1 accuracy evidence. The compact release and validation summaries now also render the lunar request-policy line explicitly, so the current TT/TDB, frame, apparentness, and observer posture is visible at a glance in release-facing output. The compact lunar summary views now also surface the lunar frame-treatment line explicitly, which keeps the mean-obliquity ecliptic/equatorial policy visible without opening the longer specification text. The lunar backend now also owns a validated frame-treatment report helper, and the validation report now reuses that backend-owned helper directly so the compact mean-obliquity wording stays co-located with the lunar source metadata instead of being rebuilt in the report layer.
   - Progress note: the compact ELP and VSOP87 implementation notes now describe the current baselines in production-neutral language, which keeps the remaining release-facing fallback wording aligned with the current phase-1 posture while the source-backed upgrades continue.

   - Progress note: the lunar reference and lunar equatorial batch-parity request corpora now also expose parity-specific wrappers plus compatibility aliases, which keeps the reusable request-shape helpers aligned with the JPL and VSOP87 corpus naming pattern for downstream tooling. The same reference and equatorial reference slices now also expose plain `request_corpus` compatibility aliases, which keeps those canonical Moon evidence windows discoverable through the same naming pattern as the planetary corpora without changing the batch shape.

3. Upgrade `pleiades-jpl` from snapshot fixture to reference backend.
   - Parse documented public JPL-style files or a reproducible derivative format in pure Rust.
   - Support multiple epochs through interpolation rather than exact fixture lookup only.
   - Include selected asteroid coverage for Ceres, Pallas, Juno, and Vesta when source data is available.
   - Preserve snapshot fixtures as small regression/golden tests.
   - Progress note: the reference snapshot now has exact J2000 regression coverage for Ceres, Pallas, Juno, Vesta, and the custom 433-Eros asteroid entry, so the baseline asteroid subset is exercised as a golden path while broader corpus work remains queued.
   - Progress note (2026-04-29): the selected asteroid evidence slice now also exposes public exact-frame and mixed-frame request-corpus helpers plus batch-path regressions, which keeps the Ceres/Pallas/Juno/Vesta/433-Eros batch shape reusable for downstream validation and reproducibility tooling without reconstructing it from the sample metadata.
   - Progress note: validation reports, backend matrices, and release notes now render the exact J2000 asteroid evidence rows for that subset instead of listing the bodies alone, which makes the source-backed asteroid fixture path visible in release-facing output.
   - Progress note: the checked-in JPL reference snapshot now prefers cubic interpolation on four-sample windows when the fixture has enough same-body epochs, with quadratic and linear fallbacks for smaller windows, which tightens the current pure-Rust interpolator without changing the public request model.
   - Progress note: the JPL reference backend now also has a batch-path regression over the exact asteroid evidence slice, so the same source-backed rows are verified through `positions()` as well as one-body-at-a-time queries.
   - Progress note: the JPL interpolation-quality summary now also carries the worst-case epoch for each measured envelope, and now reports mean bracket-span and mean error metrics alongside the maxima, so the report can name both the body and the held-out instant that produced the current interpolation peak while also exposing the average leave-one-out envelope.
   - Progress note: the checked-in JPL comparison snapshot now includes an added 2600000.0 Mars hold-out epoch in addition to the 2400000.0 epoch across the Sun-through-Pluto bodies, expanding the fixture to 46 rows across 15 bodies and 6 epochs and broadening the leave-one-out evidence to 21 samples across 10 bodies.
   - Progress note: the JPL interpolation-quality summary now lives in `pleiades-jpl`, and validation reuses the backend-owned summary/formatter instead of recomputing the same envelope privately, so the interpolation evidence now follows the same backend-owned reporting pattern as the lunar reference summary. The backend now also publishes a distinct-body interpolation-kind coverage helper for the cubic/quadratic/linear tiers, which gives the hold-out transparency slice a small extra audit hook without expanding the fixture. The same summary now also reports RMS longitude, latitude, and distance error, which gives the leave-one-out envelope a clearer spread metric without expanding the fixture. The rendered interpolation-quality summary now also identifies the metric as transparency evidence rather than a production tolerance envelope, keeping the release-facing wording explicit about the evidence class. The interpolation-quality and independent hold-out summaries now also preflight their derived counts before the compact report wrappers render them, which keeps the transparency evidence from reformatting stale aggregate metadata if the checked-in fixtures ever drift. The same JPL transparency summaries now also reject blank peak-body provenance labels when those peak metrics are populated, so the release-facing maximum-error wording fails closed if future summary generation ever drops the body names attached to the reported peaks. The interpolation-quality and independent hold-out summaries now also compare against their derived backend-owned evidence records during validation, so summary-field drift now fails closed before the compact report wrappers can reuse stale cached values. The interpolation-quality distinct-body coverage helper now also validates against the derived checked-in sample set before the report wrapper reuses it, so stale cubic/quadratic/linear body counts now fail closed alongside the rest of the hold-out transparency evidence.

   - Latest progress (2026-04-26): the same interpolation-quality summary now also reports 95th-percentile bracket-span and error tails, so the JPL transparency evidence has a compact upper-tail view in addition to the mean, median, and RMS envelope.
   - Latest progress (2026-04-26): the JPL interpolation-quality, distinct-body coverage, and independent hold-out summaries now also expose typed `summary_line()`/`Display` helpers, and the validation/release report paths reuse those displayable records directly, keeping the JPL transparency evidence aligned with the other backend-owned release summaries.
   - Latest progress (2026-04-26): the JPL snapshot backend now also publishes an independent Mars/Jupiter/Saturn hold-out corpus from the same public Horizons source material, and the validation/release evidence now renders that separate hold-out interpolation envelope alongside the existing leave-one-out transparency summary, so the JPL interpolation story now shows both internal and externalized checks without changing the public backend contract.
   - Latest progress (2026-04-26): the JPL interpolation-quality sample corpus now also has a batch-path regression, so the same hold-out epochs that feed the transparency summary are now exercised through `positions()` with preserved order and interpolated-quality results.
   - Latest progress (2026-04-29): the JPL interpolation-quality corpus now also exposes a public exact request-corpus helper plus compatibility alias, which keeps the hold-out sample batch shape reusable for downstream validation and reproducibility tooling without reconstructing the request rows inline.
   - Latest progress (2026-04-26): the independent JPL hold-out summary now also labels itself as transparency evidence rather than a production tolerance envelope, which keeps the release-facing wording aligned with the rest of the JPL validation surface.
   - Latest progress (2026-04-26): the independent JPL hold-out batch path now also preserves mixed TT/TDB request scales, so the externalized Horizons check exercises the same per-request dynamical-time labeling contract as the reference snapshot batch path.
   - Latest progress (2026-04-26): the independent hold-out coverage summary now also lists the Mars/Jupiter/Saturn body names explicitly in first-seen order, so the compact transparency line now shows the exact sampled bodies alongside the row, epoch, and time-scale window without changing the hold-out corpus.
   - Latest progress (2026-04-26): that independent JPL hold-out evidence is now surfaced in the full validation report, compact validation summary, and release summary alongside the leave-one-out interpolation evidence, so the externalized Horizons check is visible in the release-facing report set instead of living only in the backend-owned helper. The same combined evidence line now also surfaces the independent hold-out manifest summary, which keeps the checked-in public-input provenance explicit alongside the source, coverage, and interpolation envelopes.
   - Latest progress (2026-04-30): the JPL snapshot manifest summaries now also travel through a typed wrapper in `pleiades-jpl`, and the comparison/reference/hold-out report helpers reuse that validated record directly so the compact provenance lines fail closed on label drift as well as manifest drift.
   - Latest progress (2026-04-26): the JPL snapshot backend now also has a batch-path regression over the full checked-in reference snapshot in equatorial mode, preserving exact order and exact-coordinate parity across every reference row while exercising the mean-obliquity transform on the complete public-input fixture. The same reference snapshot now also has a full ecliptic batch-path regression, so both coordinate frames are now exercised across the entire checked-in corpus instead of only the asteroid subset. The mixed-frame batch regression is now also surfaced as a backend-owned summary and reused by validation/release reporting, so the alternating ecliptic/equatorial parity evidence is visible alongside the equatorial snapshot line.
   - Latest progress (2026-04-27): the JPL reference and independent hold-out batch-parity summaries now validate their derived request and quality counts before report formatting, and the report wrappers now fail closed if those summary records drift.
   - Latest progress (2026-04-27): the JPL reference and independent hold-out equatorial-parity summaries now also validate their derived coverage counts and epoch ranges before release-facing formatting, so the frame-transform parity lines fail closed instead of rendering stale batch metadata.
   - Latest progress (2026-04-26): the independent JPL hold-out corpus now also has a batch-path parity regression, and now also publishes an equatorial parity summary, so the Mars/Jupiter/Saturn transparency rows are exercised through `positions()` in both ecliptic and mean-obliquity equatorial modes instead of only the existing single-query interpolation check.
   - Latest progress (2026-04-26): the checked-in JPL reference and hold-out CSV comments are now parsed into structured manifest metadata, and the release-facing evidence helper now surfaces the hold-out source line from that metadata so the public-input provenance is less hardcoded. The parsed snapshot manifest now also exposes typed source and coverage fallback helpers plus a default-aware summary builder, so the remaining provenance defaults stay centralized in the backend crate instead of being duplicated across the report helpers. The JPL reference and hold-out source-material summaries are now backend-owned typed records too, which keeps the checked-in Horizons provenance available as structured data for future validation/reporting work instead of only as formatted strings. Latest progress (2026-04-27): the JPL snapshot manifest parser now preserves blank column slots, so malformed provenance headers can now fail through the structured `BlankColumn` validation path instead of being silently normalized away. The same validation path now also rejects duplicate column names after trimming, so the checked-in manifest can no longer advertise the same field twice without failing validation. The same validation path now also rejects blank coverage comments and trims malformed coverage/source/title labels in compact summary output, which closes another small provenance drift gap before the release-facing manifest text is reused elsewhere. Latest progress (2026-04-27): the JPL snapshot loader now also rejects duplicate body/epoch rows with a structured duplicate-entry error before interpolation can reuse the same sample twice, which keeps the checked-in reference fixtures safer to regenerate and audit. The duplicate-entry diagnostic now also carries the first-seen line number, so the malformed-row error points to both the original and repeated sample. The same loader now also treats blank body cells as a distinct structured parse error instead of folding them into the generic unsupported-body path, which keeps malformed snapshot rows a little easier to diagnose during regeneration. Latest progress (2026-04-27): the JPL manifest report helpers now validate the parsed snapshot manifest before rendering the release-facing summary, so malformed provenance headers can no longer leak a formatted report line when the cached fixture is reused. The interpolation-quality and independent hold-out summary validators now also reject duplicate and blank body labels, and the interpolation-quality path now fails closed on non-finite metric values, which keeps the release-facing JPL transparency evidence a little harder to drift into a stale state. Latest progress (2026-04-27): the reference and hold-out provenance summaries now also validate their own blank-field labels, which keeps the backend-owned source-material lines aligned with the other fail-closed JPL report helpers. Latest progress (2026-04-28): the JPL interpolation-quality, reference snapshot, and independent hold-out summaries now also validate against the derived checked-in fixture snapshots, and the reference batch parity summary now also fails closed on derived coverage drift, so the transparency evidence now rejects cached-metric, row-count, and coverage-order drift before the compact report wrappers reuse stale snapshot state.
   - Latest progress (2026-04-28): the parsed JPL snapshot manifests now also reject surrounding whitespace in title, source, and coverage provenance fields, which keeps the checked-in fixture metadata on the same fail-closed normalization path as the existing blank-field and duplicate-column checks.
   - Latest progress (2026-04-28): the cached JPL comparison/reference/hold-out source summaries now also reject surrounding whitespace in their own provenance labels, which keeps the report-facing provenance records aligned with the parsed manifest normalization instead of relying only on blank-field checks.
   - Latest progress (2026-04-28): the comparison snapshot source summary report now also preflights the parsed manifest before formatting, so malformed comparison provenance headers now fail closed instead of reaching the release-facing source line.
   - Progress note: the JPL comparison corpus now also has a backend-owned coverage summary that validation and release reporting surface alongside the interpolation evidence, which makes the checked-in comparison window easier to audit without changing the comparison corpus itself. The comparison snapshot now also surfaces its checked-in J2000 source and manifest metadata in the validation, release, and backend-matrix reports, so the comparison corpus provenance is now explicit alongside the coverage line. The comparison snapshot summary now also carries the bodies covered by the comparison corpus in first-seen order, which keeps the ordered body list available as structured provenance for downstream tooling instead of only as a derived string. The comparison snapshot source metadata is now also exposed through a backend-owned typed summary, and the detailed validation report now reuses that typed line directly too, so the fallback/source/coverage text can travel as structured provenance instead of being assembled only in the reporting layer. The comparison and independent hold-out coverage summaries now also preflight their body-count and epoch-range invariants before report formatting, so provenance drift now fails closed instead of rendering stale coverage text.
   - Progress note: the JPL snapshot evidence summary now consolidates the reference snapshot coverage, exact asteroid evidence, and comparison snapshot coverage into a backend-owned report helper, so the compact release-facing JPL line stays co-located with the snapshot backend instead of being assembled piecemeal in validation. The reference snapshot coverage summary now also carries the first-seen body order, and the equatorial-parity summary reuses that structured body list so the checked-in JPL provenance can name the covered bodies alongside the row and epoch counts. Latest progress (2026-04-27): the same reference snapshot summary now also validates the cached body order against the checked-in first-seen reference body sequence, so the release-facing coverage helper fails closed if the ordered body list drifts even when the counts still match. Latest progress (2026-04-28): the same reference snapshot summary now also revalidates its derived epoch and asteroid-row counts against the checked-in fixture, and the equatorial parity path reuses the same derived asteroid-row check before it formats release-facing text, so drifted coverage metadata now fails closed with a field-specific error instead of a generic summary mismatch. Latest progress (2026-04-30): the validation-report-summary, release-notes-summary, release-summary, and CLI report-summary smoke coverage now exact-pins the comparison snapshot coverage line, which keeps the JPL comparison inventory fail-closed across the compact release-facing summary surfaces instead of only checking it with a substring match.
   - Progress note: the same JPL evidence helper now also includes a backend-owned source-material line for the checked-in DE441 fixture, which keeps the compact validation and release summaries explicit about the vector-table provenance alongside the coverage rows.
   - Progress note: the JPL snapshot backend now also publishes an equatorial asteroid evidence slice derived from the exact J2000 asteroid rows, and the release-facing JPL evidence summaries now carry that mean-obliquity cross-check alongside the exact ecliptic asteroid evidence so the asteroid frame transform remains visible in the report output. Latest progress (2026-04-28): the exact and equatorial asteroid evidence report wrappers now preflight their body order, canonical J2000 epoch, and mean-obliquity transform consistency before formatting, so the compact asteroid provenance line now fails closed if the checked-in slice drifts. Latest progress (2026-04-29): those same exact and equatorial asteroid evidence paths now also expose typed compact summary helpers with summary-line and drift-check support, so the selected-asteroid report records can be reused directly instead of staying trapped inside the report formatter. Latest progress (2026-04-29): the reference snapshot now also exposes reusable request-corpus helpers for uniform-frame and alternating mixed-frame batch shapes, which keeps the checked-in batch inputs reusable by downstream validation and reproducibility tooling without reconstructing them from snapshot metadata. The reference snapshot now also exposes a mixed TT/TDB batch-parity request-corpus helper, which keeps the same checked-in rows reusable by downstream time-scale validation tooling without rebuilding the alternation in each caller. Those batch corpus helpers now also have matching `*_request_corpus` compatibility aliases, which keeps the request-shape naming aligned with the VSOP87 corpus helpers for downstream tooling. Latest progress (2026-04-29): the comparison snapshot now also exposes a request-corpus helper for the current validation corpus, and `pleiades-validate` now reuses it instead of reconstructing the TT-tagged request rows locally. Latest progress (2026-04-29): the comparison snapshot now also exposes a mixed TT/TDB batch-parity request corpus helper, which keeps the same validation rows reusable by downstream tooling without rebuilding the time-scale alternation in each caller. Latest progress (2026-04-29): the independent hold-out batch-parity path now also reuses a public mixed-scale request-corpus helper, which keeps the alternating TT/TDB hold-out slice reusable without rebuilding the request list inline in the summary path. Latest progress (2026-04-29): the exact asteroid request surface now also has a `reference_asteroid_request_corpus(...)` compatibility alias, which keeps the selected-asteroid batch naming aligned with the other JPL request-corpus helpers for downstream tooling.
   - Latest progress (2026-04-30): the independent hold-out corpus now also includes Uranus and Neptune samples at 2451545 and 2500000, broadening the public-input derivative slice to 16 rows across 5 bodies while keeping the same 8-epoch span and mixed-scale batch posture intact.
   - Latest progress (2026-04-30): the independent JPL hold-out corpus now also exposes an equatorial-parity request-corpus alias, which keeps the frame-specific hold-out batch shape discoverable alongside the existing frame-parameterized helper for downstream validation and reproducibility tooling.
   - Latest progress (2026-04-30): the JPL reference and independent hold-out corpora now also expose ecliptic request-corpus aliases, which keeps those checked-in batch slices discoverable through the same plain `*_ecliptic_request_corpus()` naming pattern as the other release-facing helpers.
   - Latest progress (2026-04-30): the JPL reference, comparison, and independent hold-out equatorial parity corpora now also expose `*_equatorial_batch_parity_requests()` compatibility aliases, which keeps the batch-parity naming aligned with the other reusable request-corpus helpers for downstream tooling.
   - Latest progress (2026-04-30): the independent hold-out batch-parity helper and summary tests now reflect that 24-row corpus directly, with the mixed-scale request slice balanced at 12 TT and 12 TDB requests so the release-facing parity posture stays aligned with the expanded fixture. The same hold-out parity validator now also fails closed if the mixed slice ever collapses to a single time scale, keeping the transparency evidence explicit about the TT/TDB mix instead of only the aggregate request total.
   - Latest progress (2026-04-30): the JPL batch taxonomy summary now also surfaces the supported-body control sample in the compact release-facing line, which makes the checked-in Ceres/MeanNode taxonomy a little more explicit in the validation and release smoke coverage. The same control sample now also has a public request-corpus helper, which keeps the compact batch-taxonomy shape reusable by downstream validation and reproducibility tooling instead of only by the summary constructor.
   - Progress note: the JPL snapshot manifest record now also implements `Display`, keeping the parsed provenance summary self-formatting alongside the existing label-specific helpers.
   - Progress note: the compact release summary now also surfaces the JPL interpolation-quality envelope, so the one-screen overview keeps the reference backend's leave-one-out evidence visible alongside the comparison totals. The same release overview now also carries the body-class tolerance posture and expected tolerance status, so release-facing error envelopes stay visible without opening the full validation report. The compact release summary now also surfaces the mean longitude, latitude, and distance deltas alongside the maxima, which gives the one-screen envelope a little more shape without changing the comparison corpus. The same release summary now also surfaces JPL reference snapshot coverage, exact asteroid evidence, VSOP87 source evidence, and the lunar reference/equatorial error envelopes, so the compact release view now carries the missing source-backed evidence tables and frame-conversion envelope alongside the comparison and tolerance data. The interpolation-quality summary now also reports distinct epoch coverage plus the earliest/latest hold-out epoch window, so the transparency evidence shows the sampled span alongside the body/kind breakdown. The full validation report's JPL interpolation section now also renders the backend-owned kind-coverage line alongside the summary and sample rows, so the detailed report mirrors the compact release view more closely. The reference, comparison, equatorial-parity, and independent hold-out coverage summaries now also have backend-owned `summary_line()`/`Display` helpers, keeping the JPL coverage provenance centralized on the typed backend records instead of separate report-only formatters.

4. Strengthen time, apparentness, observer, and coordinate semantics.
   - Expand the initial Delta T policy into implemented conversion support or a release-grade caller-provided conversion contract.
   - Clarify TT/UT/TDB handling in requests and validation data.
   - Keep chart house observers separate from backend topocentric position requests unless an explicit topocentric chart mode is added.
   - Implement equatorial/ecliptic transforms where the release profile claims them.
   - Ensure topocentric observer requests and apparent/mean flags either produce distinct documented behavior or return structured unsupported-feature errors.
   - Progress note: the shared `pleiades-types::EclipticCoordinates::to_equatorial` helper now centralizes the mean-obliquity ecliptic-to-equatorial rotation used by the VSOP87 and ELP paths, so the frame transform is implemented once and covered by shared unit tests instead of being duplicated per backend.
   - Progress note: the shared ecliptic and equatorial coordinate types now also expose fail-closed validation helpers for finite angles, latitude bounds, and non-negative distances, and the round-trip tests now preflight those checks before asserting the mean-obliquity rotation math.
   - Progress note: `pleiades-types::Instant::mean_obliquity` now centralizes the shared cubic mean-obliquity approximation used by the JPL, VSOP87, ELP, and packaged-data equatorial transforms, so the frame-conversion policy is owned by the foundation crate instead of being duplicated in each backend.
   - Progress note: `pleiades-backend::EphemerisRequest::new` now defaults to mean geometric output, which keeps bare geocentric requests aligned with the current mean-only backend policy while still allowing callers to opt into apparent requests explicitly.
   - Progress note: the VSOP87 and compact lunar backend paths now accept both TT and TDB dynamical-time requests while still rejecting UT-based requests explicitly, which makes the current phase-1 time-scale policy easier to exercise in validation without adding a built-in Delta T or relativistic conversion model.
   - Progress note: `pleiades-core::ChartEngine::chart()` now batches body-position dispatch through the backend `positions()` path after metadata validation, so chart assembly can take advantage of batch-aware backends without widening the public request shape or changing the current observer/houses separation.
   - Progress note (2026-04-30): `pleiades-backend::EphemerisResult` now exposes a typed validation helper plus a checked summary-line helper, and `pleiades-core::BodyPlacement` / `ChartSnapshot` now validate backend result rows, house-number consistency, and house-snapshot observer requirements before rendering fail-closed snapshot summaries. That keeps malformed placement data out of the chart-report path instead of only relying on the backend's raw result metadata.
   - Progress note: the backend crates now also share centralized request-policy helpers for time-scale, frame, apparentness, and observer validation, which keeps the existing structured errors consistent while reducing duplicated guardrail code. The shared batch adapter now also has a fail-fast regression that preserves structured error kinds when a request sequence contains an unsupported body, so chart-style batch queries keep the same explicit failure semantics as single-request calls. The same batch adapter now also preserves structured apparentness rejections in mean-only backends, so unsupported `Apparentness::Apparent` requests still fail with the backend's explicit `InvalidRequest` kind when they arrive through `positions()`. The same batch path now also preserves structured observer rejections in geocentric-only backends, so batch-mode topocentric requests fail with the same `InvalidObserver` posture as the single-request path. The shared batch adapter now also has an explicit mixed TT/TDB regression, so the default `positions()` path keeps each request's own dynamical-time label instead of normalizing a mixed batch to a hidden batch-wide scale. The VSOP87 and ELP batch paths now also have explicit UTC time-scale rejection regressions, which keeps the TT/TDB-only request policy exercised through both single-position and batch-position entry points. The shared backend request-policy record now also has a compact one-line renderer, and the release-facing validation summaries now reuse that typed record for the consolidated request-policy line instead of rebuilding the combined wording locally.
   - Progress note (2026-04-27): `pleiades-backend::validate_request_against_metadata()` now combines those shared request-shape checks with zodiac, observer, and body-coverage validation, giving direct backend callers a single preflight path before dispatch without reconstructing the common guardrails.
   - Progress note (2026-04-27): `pleiades-backend::EphemerisRequest` now also exposes caller-supplied time-scale conversion helpers plus the checked offset convenience at the backend-request layer, so direct backend callers can preflight and retag explicit Delta T/TDB policies without reconstructing the request shape by hand.
   - Progress note (2026-04-27): `pleiades-core::ChartEngine::validate_chart_request()` now mirrors that chart-shape preflight at the façade boundary, so callers can separate validation from chart assembly without repeating the backend metadata lookup themselves.
   - Progress note (2026-04-27): `ChartEngine::chart()` now validates backend metadata before request preflight or assembly, so malformed backend inventory fails closed at the façade boundary instead of being reused in chart execution.
   - Progress note (2026-04-27): `pleiades-core` now re-exports `TimeScaleConversion` at the façade level too, so applications can stay on the main crate while still using the explicit caller-supplied conversion policy record from the time-scale contract.
   - Progress note: zodiac-mode validation is now centralized too, so the tropical-only first-party backends reject sidereal requests with the same structured `InvalidRequest` posture instead of repeating the same manual checks in each backend crate. Zodiac-mode rendering in chart snapshots now uses the shared typed display label, and the validation backend-matrix time-scale listing now also uses the shared `Display` labels instead of debug formatting so the release-facing request-policy text stays aligned with the typed enums.
   - Progress note: the raw instant and chart-request time-scale offset helpers now also have checked counterparts, which lets the explicit caller-supplied conversion contract reject non-finite offsets before retagging the request instant when callers want the façade-level or low-level convenience instead of the typed `TimeScaleConversion` policy object.
   - Progress note (2026-04-27): `pleiades-types` now exposes a typed `TimeScaleConversion` policy record plus generic instant/chart helpers, so caller-supplied Delta T and TDB offset rules can travel as one explicit conversion contract instead of only as per-source helper arguments. The shared time-scale policy wording now also travels through a typed `TimeScalePolicySummary` helper in `pleiades-backend`, and the validation/release report paths reuse that typed record directly so the TT/TDB + caller-supplied-conversion posture stays co-located with the backend contract instead of only as a raw string.
   - Progress note (2026-04-27): `pleiades-types::TimeRange::validate()` now centralizes finite-bound, matching-scale, and ordering checks for reusable nominal ranges, `pleiades-backend::BackendMetadata::validate()` now reuses it for backend nominal-range metadata, and the validation comparison-tolerance summary now reuses the same shared range check while also rejecting duplicate coordinate-frame entries before formatting, so the shared time-range guardrail lives at the foundation layer instead of only inside backend-specific code.
   - Progress note (2026-04-27): `BackendProvenance` now also has its own reusable `validate()` helper, and `BackendMetadata::validate()` now reuses it before the shared metadata checks map provenance drift back into the existing backend-metadata error types, so blank summaries, padded source labels, and duplicate provenance entries fail closed at the provenance layer instead of only as a side effect of metadata validation.
   - Progress note (2026-04-27): `pleiades-core::ChartRequest` now also exposes a matching `validate_time_scale_conversion()` preflight, so chart-level callers can validate the same explicit conversion contract against the façade request instant before choosing whether to apply it.
   - Progress note (2026-04-28): `docs/time-observer-policy.md` now includes a short primary-entry-point matrix for `Instant`, `ChartRequest`, `EphemerisRequest`, `HouseRequest`, and the CLI chart command, and the backend crate docs now point at that policy note so the caller-supplied conversion boundary stays easy to find from the public API surface.
   - Progress note (2026-04-28): the same policy note now also spells out the current request-scale contract as a layer-by-layer matrix, so the typed `Instant`/`ChartRequest`/`EphemerisRequest` retagging helpers and the TT/TDB-only backend posture stay visible in one place instead of being inferred from the individual API docs.
   - Progress note (2026-04-28): `pleiades-backend::EphemerisRequest` now also mirrors the chart layer's named TT/TDB/UT1/UTC conversion conveniences, including the non-signed TDB-to-TT helper with the expected `f64` offset signature, so direct backend callers can stage explicit time-scale policies with the same helper vocabulary instead of dropping to the generic retagging helper for every conversion.
   - Progress note (2026-04-28): `pleiades-core::ChartEngine::validate_chart_requests()` now also has a mixed TT/TDB acceptance regression for backends that advertise both scales, which keeps the chart-level batch preflight aligned with the explicit per-request time-scale policy instead of silently suggesting a batch-wide normalization step. The same batch preflight now also has a house-aware mixed TT/TDB acceptance regression, which keeps observer-bearing chart requests in the same per-request contract instead of implying that house calculations force a batch-wide time-scale rewrite.
   - Progress note: the compact validation, release, and backend-matrix summaries now also render an explicit apparentness policy line, so the current mean-only first-party posture is visible alongside the time-scale and observer notes without opening the full report. The same compact validation and release views now also reuse the typed backend request-policy record for their consolidated one-line policy summary, which keeps the combined wording centralized instead of being rebuilt locally.
   - Progress note: the compact validation, release, and backend-matrix summaries now also surface an explicit frame policy line, which makes the mean-obliquity ecliptic/equatorial contract visible beside the time-scale and observer notes without needing to open the backend-specific frame-treatment sections. Mixed-frame batch regressions now also cover the JPL, VSOP87, and ELP source-backed backends, so `positions()` is explicitly exercised with alternating ecliptic and equatorial requests instead of only uniform-frame batches. The packaged-data backend now also publishes a mixed ecliptic/equatorial batch-parity summary and matching regression, which keeps the artifact-backed frame reconstruction path visible in the same release-facing audit surface.
   - Progress note: the JPL snapshot backend now also has batch-path regressions for unsupported time scales, observer-bearing requests, and apparent-place requests, so the shared request-policy posture is covered through `positions()` as well as single-request lookups.
   - Progress note: the packaged-data backend now also accepts TDB-tagged batch requests and returns the same decoded coordinates as the TT-grid lookup path, so the positive-side time-scale policy is covered through `positions()` as well as the existing rejection checks. The VSOP87, ELP, and packaged-data batch paths now also reject apparent-place and topocentric requests explicitly, which keeps the shared mean-only request-policy gate covered through `positions()` for the remaining first-party backends.
   - Progress note: the JPL snapshot backend now also publishes a backend-owned frame-treatment summary, and the validation/release summaries render it alongside the request-policy and interpolation evidence, keeping the checked-in ecliptic-to-equatorial mean-obliquity transform explicit in release-facing output.
   - Progress note: the JPL, ELP, and VSOP87 batch-path regressions now also exercise explicit equatorial-frame requests, so the shared mean-obliquity transform and frame-preserving batch contract are covered in addition to the existing ecliptic batch tests.
   - Progress note: the `pleiades-cli` chart command now accepts an explicit `--tdb` instant tag in addition to its default TT-tagged request, so the user-facing chart report can surface a TDB-tagged instant directly instead of implying a hidden conversion policy. The same chart path now also accepts `--tdb-from-tt-offset-seconds` for explicitly converting a TT-tagged instant to TDB before assembly, and still accepts `--tt-from-tdb-offset-seconds` for re-tagging a TDB instant as TT, which keeps the caller-supplied TT/TDB policy surface symmetric in both directions. The chart snapshot summary now also stays geocentric when an observer is supplied without house calculations, which keeps the compact observer policy aligned with actual house usage instead of raw observer presence. The chart crate now also exposes a typed `ObserverSummary` helper, which keeps the observer posture and rendered location together as reusable report data for request and snapshot summaries.
   - Progress note: `pleiades-types` and `pleiades-core::ChartRequest` now also expose caller-supplied UTC-to-TDB helpers in addition to the UT1/UTC-to-TT and TT-to-TDB conveniences, so civil-time chart inputs can be lifted to TDB with explicit offset policy steps instead of hidden conversion logic. Signed UT1-to-TT and UTC-to-TT helpers now round out the same typed surface, so historical or externally modeled negative Delta T policies can stay explicit too. `ChartRequest::summary_line()` now also renders the same request-shape vocabulary in one compact line for diagnostics and release-facing request summaries, keeping the current TT/TDB, observer, apparentness, and house-system policy visible without rebuilding the wording in each report path. The request summary now also stays geocentric when an observer is supplied without a house system, mirroring the snapshot policy and avoiding an implied house-only request when the observer is not actually used for house calculations. The lower-level `Instant`, `ObserverLocation`, and backend `EphemerisRequest` records now also carry matching compact display helpers, which keeps the same typed request shape available from the foundation layer through the backend contract. The chart façade now also has regression coverage proving the time-scale conversion helpers preserve the rest of the request shape while only retagging the instant, so the signed UTC/TDB conversion path and the signed UT1/TDB conversion path stay visible in both the builder API and the compact request summary, and the generic `TimeScaleConversion` path now also keeps custom house metadata intact while only retagging the instant. The shared request-policy summary is now also validated before the validation and release report renderers format it, so whitespace-padded or blank policy drift fails closed instead of leaking into release-facing output. `TimeScaleConversionError` now also exposes a typed summary helper for its structured failure modes, which keeps the caller-supplied offset diagnostics aligned with the rest of the compact typed-summary pattern. `EphemerisError` now also exposes a compact `summary_line()` helper, and the CLI/validation error renderers reuse that typed record directly instead of reconstructing the `kind: message` string locally, which keeps structured backend failures aligned with the other typed summary records. `BodyPlacement` now also exposes a typed `summary_line()`/`Display` helper, and the detailed chart snapshot table reuses that placement-row formatter directly so the per-body snapshot rows stay centralized instead of being rebuilt inline. Latest progress (2026-04-27): `pleiades-core::ChartRequest` and `ChartSnapshot` now also expose typed `ObserverPolicy` helpers, so the geocentric-versus-house-only distinction is available as a reusable API instead of only as compact report text. Latest progress (2026-04-29): the chart request and chart snapshot summary lines now also render the stored observer location separately from the geocentric/house-only policy label, which keeps the house-observer coordinates explicit in diagnostics without implying a topocentric body-position request. Latest progress (2026-04-30): the release-summary and validation-summary smoke coverage now exact-pins the time-scale, observer, apparentness, and request-policy lines, which keeps the shared time/observer posture fail-closed in the compact release-facing report surfaces instead of only checking for those headings by substring.
   - Progress note: chart assembly now also has a regression proving house observers are used for house calculations only and are not forwarded into geocentric body-position backend requests, which keeps the observer/topocentric separation explicit in the façade layer.
   - Progress note (2026-04-30): chart requests and snapshots now keep the house observer separate from an optional topocentric body observer, and chart dispatch now forwards that body observer into backend body-position requests when present so topocentric-capable backends can receive an explicit position-mode signal instead of reusing the house-observer channel.
   - Progress note: topocentric house calculations now reject non-finite observer elevations with a distinct `InvalidElevation` house error, and the chart façade maps that to `InvalidObserver` before any backend body query runs, so observer validation stays explicit even when the topocentric house path fails early.
   - Progress note: the shared geocentric-only observer-policy error now also names the rejected observer coordinates, which makes the `InvalidObserver` failure a little more actionable while preserving the same structured error kind for chart and backend callers.
   - Progress note: chart snapshot rendering now also has a regression for the no-observer branch, so the geocentric-body-position policy line remains covered even when no house observer is supplied.
   - Progress note: chart snapshots now also render an explicit ecliptic frame policy line, so the release-facing chart report makes the geocentric/ecliptic request shape visible alongside the time-scale, observer, and apparentness notes.
   - Progress note: the shared `pleiades-core::MotionSummary` chart record now also has a typed `summary_line()`/`Display` helper, and the chart snapshot renderer reuses it directly instead of rebuilding the motion-count wording inline, keeping the compact motion audit on the same typed pattern as the other report summaries. The same motion summary helper now also validates its total placement count before rendering, which keeps the chart snapshot motion line on the same fail-closed path as the other report helpers. The shared `pleiades-types::Motion` sample now also has the same compact summary/display surface, which keeps the raw speed tuple ready for future diagnostics and release-facing provenance without reconstructing the tuple ad hoc.
   - Progress note: UT1-tagged callers now also have a direct caller-supplied UT1-to-TDB helper, so explicit dynamical-time staging can stay typed even when a caller already has TT-UT1 and TDB-TT offsets available.
   - Progress note: TDB-tagged callers now also have a caller-supplied TT-from-TDB helper in `pleiades-types` and `pleiades-core::ChartRequest`, and the same path now has an explicit `*_signed` alias for the signed-offset naming convention used by the other helpers, which keeps the explicit conversion policy symmetric for callers that already begin from a TDB-tagged instant.
   - Progress note: the VSOP87 and compact ELP batch-path regressions now also cover explicit TDB-tagged requests for their supported body slices, so the current time-scale policy is exercised through `positions()` as well as single-request lookups without adding a built-in Delta T or relativistic conversion model.
   - Progress note: the VSOP87, ELP, and JPL batch-path regressions now also cover mixed TT/TDB request sets, so batch queries preserve each request's own time-scale label even when a single call mixes dynamical-time scales instead of normalizing them to a hidden batch-wide policy. The VSOP87 source-backed planetary batch suite now also has an explicit mixed TT/TDB canonical-epoch regression, which keeps the planetary request-policy surface exercised through both same-scale and mixed-scale batch queries. The lunar validation/release reports now also surface a backend-owned mixed TT/TDB lunar batch-parity summary, so the current lunar time-scale coverage is visible in the release-facing reports instead of only in the backend test slice. The canonical mixed-scale request corpus is now factored into a reusable `lunar_reference_batch_requests()` helper as well, which keeps the batch-parity slice shared between validation and future tooling without duplicating the request construction. Latest progress (2026-04-28): that same lunar batch-parity summary now also validates its derived request counts and preserves the compact report line only while the mixed-scale slice still matches the current evidence, so the lunar batch-parity evidence now fails closed on count or parity drift instead of formatting stale mixed-scale metadata. Latest progress (2026-04-29): `pleiades-vsop87::canonical_mixed_time_scale_batch_parity_requests()` is now public and regression-covered, so the VSOP87 mixed TT/TDB batch corpus can be reused by future tooling instead of only by the summary constructor.
   - Progress note: the `pleiades-cli` chart command now exposes explicit `--utc` / `--ut1` instant tags plus caller-supplied `--tt-offset-seconds` and signed `--tdb-offset-seconds` flags, so command-line chart runs can exercise the same conversion policy without implying a built-in Delta T or relativistic model. The top-level `pleiades-cli help` summary now also advertises both `--tdb-from-tt-offset-seconds` and `--tt-from-tdb-offset-seconds`, so the explicit TDB/TT retagging options are discoverable from the main help text as well as the chart subcommand synopsis. The CLI regression suite now also pins that top-level help line, so the discoverability claim stays exercised in tests instead of living only in the string literal.
   - Progress note: the chart CLI now rejects time-scale offset flags that do not match the tagged instant, so caller-supplied TT/TT-TDB retagging helpers stay explicit instead of being silently ignored on UTC/UT1/TDB requests. TDB-tagged requests now also reject a direct `--tdb-offset-seconds` retagging flag, and the CLI regression suite covers that explicit failure path alongside the existing TT/UTC/UT1 mismatch checks.
   - Progress note: the chart façade's signed TT/TDB retagging helpers now also reject non-finite offsets with the same structured `TimeScaleConversionError` used by the lower-level `Instant` policy, so caller-supplied Delta T and TDB-TT conversions fail explicitly instead of propagating `NaN` or infinite offsets through the chart builder API.

- Progress note (2026-04-28): the batch request-policy preflight now also preserves tropical-only sidereal failures with the same batch index prefix, which keeps the direct-backend sidereal guardrail explicit when callers validate slice-shaped requests instead of single requests.

5. Expand validation evidence.
   - Add golden positions for major bodies, lunar points, and baseline asteroids.
   - Generate cross-backend comparison reports with body/date/error summaries. Aggregate and per-body summary sections are now implemented; future source-backed backend increments should populate them with tighter measured errors.
   - Progress note (2026-04-30): the CLI release-notes-summary path now also exact-pins the JPL request-policy and batch-error-taxonomy lines, which keeps the reference-backend policy and failure taxonomy visible in the compact maintainer-facing notes surface instead of only in the fuller validation reports.
   - Progress note: the validation and artifact reports now name the body driving each max longitude, latitude, and distance delta in the body-comparison and body-class envelope sections, which makes the release-facing error envelopes easier to audit without changing the measured corpus.
   - Progress note (2026-04-29): the comparison-tolerance limit rows now also travel through a typed `ComparisonToleranceEntry::summary_line()`/`Display` helper and validation preflight, which keeps the compact scope-limit wording centralized on the backend-owned tolerance entry record instead of rebuilding it ad hoc in the report formatter.
   - Progress note (2026-04-28): the lunar reference and equatorial error envelopes now also have explicit validation helpers and fail-closed report wrappers, so drift in their computed metric finiteness, outlier bookkeeping, or epoch ordering now surfaces as unavailable instead of formatting stale lunar-envelope text.
   - Progress note: the comparison-audit failure path now mirrors the body-class error envelopes, body-class tolerance posture, and tolerance policy sections, so release-failure output carries the same coarse envelope detail as the full validation report.
   - Progress note: the expected-tolerance status lines now also report signed margins to each class limit alongside the measured maxima, and the compact validation report summary now mirrors those margins in its expected-tolerance rows, which makes the remaining headroom or overshoot explicit in release-facing validation output without changing the comparison corpus.
   - Progress note: the compact release summary now also carries a short validation evidence line with comparison sample and tolerance counts, and now names the comparison-audit status explicitly, so the release-facing overview exposes a little more of the current error envelope without expanding into the full validation report.
   - Progress note (2026-04-29): the compact release-notes summary now also surfaces the comparison-tolerance policy line, which keeps the current backend-family scope table visible in the maintainer-facing notes surface instead of only in the validation and release-summary reports.
   - Progress note: the compact comparison-envelope formatter now also reuses a typed median helper before appending it to the aggregate summary, which keeps the release-facing median wording centralized alongside the existing aggregate and 95th-percentile helpers instead of rebuilding the same string inline.
   - Progress note: the aggregate comparison summary now also has a backend-owned `summary_line()`/`Display` helper, and the compact validation/release comparison envelope now reuses that typed aggregate line before appending the median values, keeping the maxima/means/RMS wording centralized alongside the existing per-body summary helper. The matching 95th-percentile tail line is now also backed by a typed `ComparisonPercentileEnvelope` helper, and the validation/release report paths reuse that record directly instead of rebuilding the tail wording inline. The median comparison envelope is now also public and reusable on its own, which keeps the compact median wording available to downstream tooling as a typed helper instead of only through the combined comparison summary.
   - Progress note: the backend matrix summary now also mirrors the current comparison-audit status and counts, so the compact backend capability view exposes the active regression gate alongside the backend inventory and source-backed evidence lines.
   - Progress note: backend-family and accuracy labels now have stable `Display`-backed renderings in `pleiades-backend`, and the validation backend-matrix formatter now uses those public labels directly so the release-facing metadata lines stay consistent with the shared backend contract. The backend matrix now also adds a typed family-posture line (`algorithmic`, `data-backed`, `routing`, or `other`) so the compact release-facing inventory makes the data-backed-versus-formula-driven split explicit.
   - Progress note: backend capability flags now have a shared backend-owned `summary_line()`/`Display` helper in `pleiades-backend`, and the validation backend-matrix capability row now reuses that typed record directly so the compact capability posture stays centralized instead of being rebuilt locally.
   - Progress note: the same compact release summary now also surfaces the body-named comparison envelope, so the one-screen overview now mirrors the detailed comparison report a little more closely while the broader release-grade threshold work remains queued. The compact packaged-artifact summary now also includes the expected-tolerance rows and comparison tolerance audit counts, so the artifact release view now carries the current regression posture without forcing maintainers to open the full validation report. The packaged-artifact regeneration summary now also reports the artifact checksum and deterministic generation policy, so the compact artifact provenance line carries both integrity evidence and the current fit rule. The body-class tolerance posture now also prints the candidate backend family and class-level tolerance profile directly, which keeps the coarse class audit aligned with the backend-scoped expectation table. The same posture now also reports out-of-tolerance sample counts alongside the body counts, so sample-level regressions stay visible in the coarse release audit.
   - Progress note: comparison-tolerance scope coverage rows now also have backend-owned `summary_line()`/`Display` helpers, and the validation/release tolerance-policy renderers reuse those typed rows directly instead of rebuilding the per-scope wording inline, which keeps the compact release-facing tolerance catalog a little more consistently typed.
   - Progress note: the comparison envelope now also carries RMS longitude, latitude, and distance deltas in the validation report, compact validation summary, and body comparison summaries, so the current release-facing error envelope now includes a spread metric alongside the maxima and means. The body-class error envelopes now also carry the same RMS metrics, which keeps the coarse class audit aligned with the per-body spread statistics. The body-class envelopes now also surface 95th-percentile longitude, latitude, and distance tails, adding a compact tail metric to the coarse release audit alongside the existing max/mean/median/RMS rows.
   - Progress note (2026-04-28): the comparison-envelope validation path now also re-derives and compares its stored median and 95th-percentile envelopes against the sampled comparison slice before release-facing formatting, so stale compact error-envelope metrics now fail closed instead of only proving their internal finiteness.
   - Progress note: notable regression findings now also have a typed `summary_line()`/`Display` helper, and the validation/artifact regression sections reuse that record directly instead of rebuilding the body, delta, and note wording inline, keeping the compact release-facing regression text aligned with the rest of the typed summary pattern.
   - Progress note: the aggregate comparison envelope now also carries median longitude, latitude, and distance deltas in the full report, compact validation summary, and release summary, which adds one more robust central-tendency view to the release-facing error envelope without widening the comparison corpus. The same comparison-report family now also surfaces a 95th-percentile tail envelope for absolute comparison deltas, giving the release-facing audit one more shape metric without changing the comparison corpus.
   - Progress note: the backend-matrix summary now also distinguishes bounded versus open-ended nominal ranges, which keeps the compact catalog view honest about which backends are still open-ended versus constrained to a published artifact window.
   - Progress note: validation benchmarking now also reports a full-chart assembly slice in addition to backend request throughput, using representative house scenarios so chart latency stays visible alongside the existing backend throughput evidence. The backend and chart benchmark validators now also reject zero estimated corpus heap footprints, so the benchmark memory-footprint lines fail closed instead of implying an empty corpus size.
   - Progress note: the compact validation-report summary now also includes the shared benchmark provenance block and benchmark summaries, so the shorter release-facing validation view now surfaces the reference/candidate/packaged/chart timing rows and packaged-artifact decode size without forcing maintainers to open the full report.
   - Progress note: house calculations now reject non-finite obliquity overrides explicitly instead of letting NaN or infinite inputs flow into the quadrant formulas, and the remaining house-system docs were retitled to drop the stale MVP wording so the public-facing house module language matches the current production-ready behavior. The invalid-obliquity path now has a dedicated structured house error kind, which keeps the user-facing failure classification closer to invalid input than generic numerical failure. `HouseSnapshot` now also exposes a `house_for_longitude()` convenience wrapper over the shared wraparound helper, and the house-placement regression now covers exact cusp boundaries plus the wraparound lookup path, which keeps the snapshot/query pairing explicit for callers while tightening the house-placement helper contract. `HouseRequest` now also exposes a `validate()` preflight for observer latitude, obliquity overrides, and topocentric elevation, and `calculate_houses()` reuses that shared request check so the house-observer contract stays centralized before the quadrant formulas run. That preflight now also rejects non-finite observer longitudes, and the chart façade reuses the house-layer validation before house derivation so malformed observer locations fail fast through the same structured observer path. The chart-facade batch preflight now also has an explicit topocentric-house regression, which keeps the indexed observer failure path covered at the façade boundary instead of only in the lower-level backend request helper. Latest progress (2026-04-30): `pleiades-types::ObserverLocation` now has a shared validation helper and `pleiades-houses` reuses it for observer preflight, so invalid latitude, longitude, and elevation are now fail-closed at the shared observer-location boundary instead of only inside the house crate.
   - Progress note (2026-04-28): the house-validation corpus summary now also preflights a typed validation helper before the compact validation and release summary paths reuse it, so a drifted scenario label or sample shape now fails closed with an unavailable house-validation line instead of rendering stale release-facing house evidence.
   - Progress note: custom house-system definitions now render their name, aliases, and notes in unsupported-house errors, which makes the release-facing extensibility boundary more explicit without changing the public house API.
   - Progress note (2026-04-28): custom house-system and ayanamsa request validation now also checks user-defined labels against the built-in compatibility catalogs, so chart preflight rejects colliding custom names before they can blur the shipped release-profile vocabulary.
   - Progress note: backend quality annotations now have stable display labels in `pleiades-backend`, and chart snapshots render that typed label directly instead of Debug formatting, which keeps the body-quality column aligned with the shared backend contract.
   - Progress note: the packaged-artifact boundary continuity summary now also reports RMS longitude, latitude, and distance deltas at both the body and aggregate levels, which keeps the validation and release artifact audit aligned with the mean/RMS envelope shape used elsewhere in phase 1.
   - Progress note: the packaged-data backend metadata now also routes its storage/reconstruction provenance through a typed backend-owned summary record, so the final packaged-artifact data-source line no longer relies on a crate-local string literal.
   - Store expected tolerances by backend and body class.
   - Progress note (2026-04-29): `pleiades-validate` now exposes the expected comparison-tolerance catalog as a reusable backend-family/body-class helper, and the validation/report paths reuse that catalog directly, which keeps the stored thresholds centralized instead of reconstructing them only inside the report formatter.
   - Progress note (2026-04-29): the body-class tolerance summary now also has a typed validation helper and fail-closed report wrapper, so the coarse comparison-class audit now rejects drifted counts or stale distance metrics before it reaches the compact release-facing envelope.
   - Progress note (2026-04-29): comparison tolerance profile labels now validate their blank and surrounding-whitespace cases before the validation and release summaries reuse them, which keeps the tolerance catalog aligned with the rest of the report-label guardrails instead of carrying drifted profile text into the compact audit lines.
   - Progress note: the comparison tolerance policy summaries now include the current comparison corpus size alongside the backend-family scope table, and the compact release summary now also carries per-scope body-and-sample coverage counts, so the release-facing tolerance catalog now ties the policy table a little more directly to the current corpus. The per-body tolerance status record now also renders as a typed `summary_line()`/`Display` helper, and the validation formatter reuses that record directly instead of rebuilding the compact expected-tolerance wording inline. The body-class comparison envelope now follows the same typed-summary pattern, so the coarse class report path reuses the backend-owned summary helper instead of reconstructing the release-facing wording inline. The VSOP87 source-backed body evidence and body-class envelopes now also validate their derived counts before the report wrappers render them, which keeps the release-facing source-evidence summaries on the same fail-fast pattern as the existing source-documentation and generated-audit helpers.
   - Progress note (2026-04-28): the release-notes-summary and release-summary smoke coverage now also pin the workspace-audit summary line, which keeps the pure-Rust workspace gate visible in the compact release-facing audit path alongside the other bundled status summaries.
   - Latest progress (2026-04-29): the shared frame-treatment summary validation now also rejects embedded line breaks in addition to blank and padded text, which keeps backend-owned provenance notes on the same fail-closed compact-line path as the other release-facing policy summaries.
   - Latest progress (2026-04-29): the validation-layer mean-obliquity frame round-trip evidence now also travels through a typed summary record with `summary_line()`/`Display` and a fail-closed report wrapper, and the summary helper is now publicly reusable for downstream tooling, which keeps the compact frame-conversion check centralized instead of rebuilding the same wording ad hoc in the report path.
   - Keep validation reproducible and pure Rust.

## Done criteria

- `pleiades-vsop87`, `pleiades-elp`, and `pleiades-jpl` metadata describe real source material and accuracy class.
- Major planets, Sun, Moon, and baseline asteroid support are tested against reference data.
- Unsupported bodies, frames, time scales, topocentric modes, and apparentness modes fail explicitly.
- Validation reports include measured errors and are consumable by release tooling.
- `cargo test --workspace` passes after the backend upgrades.

- Progress note (2026-04-28): the compatibility-profile verification smoke coverage now also pins the custom-definition label names line directly, which keeps the explicit `Custom-definition label names verified:` release posture visible in both the CLI and validation audit paths alongside the house-formula-family and release-posture checks.
- Progress note (2026-04-29): the compact compatibility-profile, release-notes summary, and release-summary views now also surface the target house and ayanamsa scope lines, which keeps the long-term Swiss-Ephemeris-class catalog framing visible in the compact release-facing summaries as well as the verification helper.
- Progress note (2026-04-30): the release-notes-summary smoke coverage now also pins the target house and ayanamsa scope lines plus the release-profile identifiers line, which keeps the compact maintainer-facing release notes artifact tied to the long-term compatibility catalog and release provenance as well as the public-facing summary text.
- Progress note (2026-04-28): the descriptor-local house and ayanamsa validators now also reject duplicate aliases within a single entry while preserving the intentional case-only canonical-name variants already used by the built-in interoperability spellings, which keeps the lower-level profile-ingest guardrail aligned with the release-facing catalog vocabulary.

## Work that belongs in later phases

- Generating compressed 1500-2500 artifacts from these outputs belongs to Phase 2.
- Broad house/ayanamsa compatibility audits belong to Phase 3.
- Public release bundle publication and archival policies belong to Phase 4.
