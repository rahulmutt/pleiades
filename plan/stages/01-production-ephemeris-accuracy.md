# Phase 1 — Production Ephemeris Accuracy

## Purpose

Replace preliminary, sample-driven, or simplified backend behavior with source-backed astronomical calculations and explicit accuracy evidence. This phase is active because the workspace structure and user-facing APIs exist, but production release claims require validated ephemeris output.

## Spec drivers

- `spec/requirements.md`: FR-1, FR-2, FR-3, FR-7, FR-8, NFR-2, NFR-3
- `spec/backend-trait.md`: request/result semantics, metadata, errors, composite backends
- `spec/backends.md`: `pleiades-jpl`, `pleiades-vsop87`, `pleiades-elp` responsibilities
- `spec/api-and-ergonomics.md`: deterministic typed APIs, batch queries, failure modes
- `spec/validation-and-testing.md`: reference comparison and golden/regression tests

## Current baseline

Implemented foundations include backend traits, metadata, composite routing, major body identifiers, lunar points, baseline asteroids, a chart façade, CLI commands, a snapshot-style `pleiades-jpl` with exact lookup plus cubic/quadratic/linear interpolation, selected asteroid coverage, and expanded public-input leave-one-out interpolation quality reporting, preliminary `pleiades-vsop87` and `pleiades-elp` crates, deterministic central-difference motion estimates for the current VSOP87 path and compact ELP Moon/lunar-point path, explicit mean-only support for mean lunar apogee and mean lunar perigee, explicit rejection of unsupported topocentric and apparent requests in geocentric/mean-only backends, an initial time-scale/Delta T/observer policy document, caller-supplied time-scale offset helpers for explicit external UT1-to-TT/related policies, validation corpus summaries that now label their unique epochs with explicit time-scale tags and the validation/release summaries now also render an explicit time-scale policy line derived from the caller-supplied TT/TDB surface, vendored IMCCE VSOP87B Earth, Mercury, Venus, Mars, Jupiter, Saturn, Uranus, and Neptune source files for geocentric Sun-through-Neptune output, plus generated binary coefficient-table slices for the Sun/Earth, Mercury, Venus, Mars, Jupiter, Saturn, Uranus, and Neptune paths, VSOP87 per-body source profiles rendered in validation backend matrices, explicit VSOP87 frame-treatment notes for the J2000 ecliptic/equinox source frame and mean-obliquity equatorial transform, and validation reports that expose aggregate plus per-body comparison error summaries, explicit interim tolerance status by body, compact source-backed VSOP87 evidence snapshots, and a concise VSOP87 body-evidence outlier note when a measured body falls outside the interim envelope.

## Remaining implementation goals

1. Maintain and document the production `pleiades-vsop87` planetary calculations.
   - Keep the generated VSOP87 coefficient data path reproducible in pure Rust.
   - Document variant, truncation policy, frames, units, and date range.
   - Preserve geocentric astrology-facing ecliptic positions for Sun and planets.
   - Keep the source-backed major-planet coefficient path reproducible from public inputs and validate error envelopes.
   - Add batch-path tests covering all supported planets at canonical epochs.
   - Progress note: the crate now ships a maintainer-facing regeneration helper plus binary that rewrites the checked-in generated blobs from the vendored source text. The helper now also reports an explicit unknown-source-file error that lists the supported vendored inputs, which keeps the reproducibility failure mode typed instead of stringly. The checked-in generated-blob lookup path now also has an explicit helper, so the source-file-to-binary mapping used by the reproducibility tests is discoverable without a hardcoded match block.
   - Progress note: the canonical J2000 VSOP87 evidence summary now names the body that drives each maximum delta axis, and now also records the source kind and source file behind each axis peak, which makes the release-facing envelope easier to audit without widening the public request model.
   - Progress note: the VSOP87 source-audit summary now also prints the deterministic fingerprint count directly in release-facing output, so the reproducibility line stays explicit alongside the raw source-size and term-count evidence.
   - Progress note: the VSOP87 generated-binary audit is now also surfaced as a backend-owned summary and reused by the validation and release summaries, so the checked-in `.bin` artifacts now carry an explicit reproducibility line alongside the source-audit and canonical evidence reports. The generated-binary summary now also carries the canonical body and source-file order directly, which makes the checked-in blob provenance a little easier to audit without reconstructing it in the reporting layer.
   - Progress note: the VSOP87 regression suite now also covers the full supported planetary batch at J2000, including the Pluto fallback path, so batch-path verification now spans the complete supported set rather than only the source-backed subset. The validation summary now also names Pluto explicitly as the mean-element fallback body in the source-documentation line, keeping the remaining non-VSOP87 special case visible in release-facing output.
   - Progress note: the VSOP87 batch-path suite now also exercises the full supported planetary set at the J1900 canonical epoch in equatorial TDB mode, preserving batch/single parity and the mean-obliquity frame transform across a second reference instant instead of only at J2000. The canonical J1900 batch-parity helper is now public and reused by validation/release reporting, which keeps that second-epoch coverage visible outside the test module.
   - Progress note: the canonical VSOP87 body-evidence envelope is now owned by `pleiades-vsop87` as a structured backend summary, and validation reuses it directly instead of recomputing the source-kind and interim-limit breakdown in the tooling layer. The VSOP87 canonical evidence, source documentation, source audit, and body-evidence report strings are now backend-owned too, so the release-facing reporting surface stays co-located with the source metadata instead of being rebuilt privately in validation. The source-documentation summary now also carries the fallback-body list as structured data, which keeps the Pluto fallback name available without recomputing it from the body-profile table in the reporting layer. The same summary now also carries the shared date-range note for the retained VSOP87 slices, so the compact release-facing lineage now shows the documented epoch scope alongside the source-path mix. The canonical J2000 evidence summary now also reports mean absolute deltas and an out-of-limit count, so the compact release-facing envelope now mirrors the richer lunar and JPL summaries a little more closely.
   - Progress note: the VSOP87 source-documentation summary now also enumerates the source-backed body list explicitly, so the compact release-facing provenance line now names the current Sun-through-Neptune source-backed set alongside the fallback Pluto path without requiring the backend matrix to be opened. The same summary now also names the exact public source file set (`VSOP87B.ear` through `VSOP87B.nep`), which makes the reproducibility line a little more concrete without changing the runtime path. The supported source-file catalog is now also exposed as a public helper for regeneration tooling, so downstream reproducibility checks can reuse the backend-owned source-file order without hardcoding the vendored list. The source-documentation breakdown now also names the generated-binary, vendored full-file, and truncated body lists explicitly, so the compact provenance line can point at the exact body coverage behind each source-path category instead of only reporting counts. The source-documentation health summary now mirrors those per-category body lists too, and now also surfaces the exact source-file order in the same structured record, which keeps the compact release-facing health check aligned with the richer source-path breakdown. The source-documentation health check now also verifies the documented variant, coordinate-family, frame, units, reduction, transform-note, truncation-policy, and date-range invariants in addition to the catalog counts, and now explicitly checks that the source-backed body order still matches the current generated-binary/vendored/truncated partition order, so release-facing provenance can flag documentation drift as well as body-count or partition-order drift. The health summary now also carries the source-backed and fallback body lists themselves, so the compact release-facing health line can name the current body partition directly instead of only surfacing the counts, and now also exposes the explicit source-backed partition order in the same record. The health formatter now also emits explicit issue labels when the catalog ever drifts, which keeps the failure mode actionable without changing the current consistent-path wording. The VSOP87 source-documentation, health, and body-evidence summaries now also expose typed `summary_line()`/`Display` rendering, so the validation and release helpers can reuse backend-owned reporting records directly instead of rebuilding those strings in the tooling layer. The VSOP87 source audit summary now also names the source-backed body order and public source-file set explicitly, so the compact reproducibility line can audit the exact public inputs instead of only surfacing aggregate counts. Validation and release summary tests now pin the VSOP87 source-documentation health line exactly, so the source-file and body-order consistency checks stay exercised in the release-facing smoke suite. The source-documentation partition order is now also exposed through a public helper, which lets regeneration tooling reuse the backend-owned generated-binary/vendored/truncated partitioning directly instead of reconstructing it from the summary record.
   - Progress note: the VSOP87 request-policy and unsupported-body messages now use the production backend label instead of the older MVP phrasing, so request failures stay aligned with the source-backed backend state.
   - Progress note: backend provenance is now surfaced through a typed `BackendProvenance::summary_line()`/`Display` helper, and the validation comparison/backend-matrix renderers reuse that helper directly so source-material provenance stays centralized on the backend metadata type instead of being reconstructed from the raw field everywhere.
   - Progress note: the canonical VSOP87 evidence summary now also reports 95th-percentile longitude, latitude, and distance deltas, giving the backend-owned source-backed envelope a compact upper-tail metric alongside the existing mean/median/RMS view.
   - Progress note: the VSOP87 source-backed body evidence summary now also prints the canonical source-backed body order, so the compact provenance line now mirrors the Sun-through-Neptune ordering instead of only listing aggregate counts. The formatter now labels that sequence explicitly as the source-backed body order in release-facing output, which keeps the reproducibility cue a little clearer in the compact report text.
   - Progress note: the backend-matrix implementation note for the VSOP87 backend now correctly describes the Sun-through-Neptune generated-binary paths as derived from the vendored full-file inputs, keeping the release-facing implementation status aligned with the runtime path.
   - Progress note: the VSOP87 and ELP backend-matrix status notes now use production-neutral wording for the current Pluto and lunar baselines, and the remaining stale pending phrasing has been removed from the VSOP87 source comments, ELP source-material summary, lunar-theory policy doc, and the corresponding crate metadata strings so the compact release-facing provenance text stays aligned with the current implementation posture.
   - Progress note: the VSOP87 canonical J2000 evidence now also has a backend-owned equatorial companion summary, so the source-backed planetary slice now exposes a mean-obliquity frame cross-check alongside the ecliptic envelope in validation and release-facing summaries.
   - Progress note: the VSOP87 canonical J2000 and equatorial evidence summaries now also implement typed `summary_line()`/`Display` rendering, and the backend report helpers now reuse those typed records directly, which keeps the release-facing formatter ownership aligned with the other backend-owned VSOP87 summaries.
   - Progress note: the validation backend-matrix VSOP87 rows now also render source-kind and accuracy through the typed `Display` labels instead of enum debug spellings, so the release-facing source-profile lines stay aligned with the public enum contracts instead of ad hoc formatting.
   - Progress note: the VSOP87 canonical J2000 evidence now also carries a concise interim-outlier note in the validation, backend-matrix, and release summaries, so any future envelope regression can name the affected bodies without leaving the compact report readers to infer that from the aggregate count alone.

Progress note: the compact validation report summary now also surfaces the VSOP87 source-backed body-class envelopes alongside the existing source documentation and body evidence lines, keeping the coarse class rollup visible in the shorter release-facing validation view too.

2. Implement production `pleiades-elp` lunar calculations.
   - Select and document a pure-Rust lunar theory source.
   - Support Moon longitude, latitude, distance, and useful speed outputs.
   - Implement or explicitly defer mean/true nodes and apogee/perigee where mathematically justified.
   - Add regression tests around high-curvature lunar intervals.
   - Progress note: the backend now has an explicit lunar-theory policy document plus a short high-curvature Moon-window regression test, so the current baseline provenance and nearby motion continuity are exercised while source-backed ELP selection remains queued.
   - Progress note: the same high-curvature Moon-window regression slice now also has a backend-owned continuity summary rendered in validation/backend-matrix reports, so the nearby-motion check is visible in release-facing output rather than only in the unit test. The same J2000 window now also has a companion equatorial continuity slice in the validation and backend-matrix outputs, which keeps the mean-obliquity frame check visible alongside the ecliptic motion check.
   - Progress note: the current lunar-theory selection now exposes a stable source identifier, canonical bibliographic citation, explicit source-family label, and explicit license/provenance posture alongside the supported and unsupported lunar bodies, and the validation/reporting layer renders those provenance details in release-facing summaries. The source-family label is now also surfaced through `Display` so future source-selection variants can reuse the same typed enum in release text. The same baseline now also exposes a structured `source_selection()` helper so the provenance fields can travel as one typed record in backend-owned reporting. The lunar backend now also publishes a compact source-selection summary helper, and validation/release reports now render that shorter provenance line alongside the full specification so the selected lunar baseline is easier to scan without parsing the longer catalog text. The compact source-selection summary now also carries the current validation window, which keeps the short provenance line explicit about the evidence span without requiring the longer specification block. The main lunar theory specification summary now also includes the typed selected catalog key, keeping the longer provenance line aligned with the resolver surface used by the compact source-selection helper.
   - Progress note: validation backend-matrix output now also renders the ELP unsupported lunar bodies explicitly, so the release-facing lunar-theory section shows both the supported channels and the deferred true apogee/perigee slots.
   - Progress note: the Moon path now uses a Meeus-style truncated lunar position series instead of the earlier simplified orbital surrogate, and the backend now validates against the published 1992-04-12 geocentric Moon example in addition to the J2000 lunar-point checks.
   - Progress note: the compact lunar backend now also exports a canonical reference evidence slice for the published Moon example plus the J2000 lunar-point samples, and validation reports render that slice in the backend matrix and summary output so the current lunar baseline is easier to audit without changing the API contract.
   - Progress note: the lunar reference evidence summary now also carries the JD 2419914.5..2459278.5 range explicitly, making the current 1913-node-to-2021 coverage window visible in release-facing summaries.
   - Progress note: the lunar backend now also has a batch-path regression over the Moon, nodes, and mean apogee/perigee evidence slice, so the current supported lunar points are exercised through `positions()` in addition to single-request coverage. The same backend now also rejects true apogee and true perigee through batch requests with the same structured `UnsupportedBody` error used for single-request calls, keeping the deferred lunar channels explicit even when they arrive through `positions()`.
   - Progress note: the compact lunar-theory specification now also exposes a structured request-policy field — TT/TDB only, tropical only, mean only, ecliptic/equatorial frames, and no topocentric observer mode — so the current baseline remains visibly scoped while source-backed ELP selection stays queued. The request-policy summary is now also exposed as a backend-owned helper and reused by validation reporting, which keeps the structured lunar policy text centralized for future source-selection variants.
   - Progress note: the compact lunar-theory specification now also carries explicit truncation and output-unit notes, and the validation/reporting layer renders those notes alongside the provenance fields so the current truncated baseline is easier to audit without changing the public API. The same baseline now also exposes its source-family label directly, which keeps the source-selection posture structured without widening the API surface.
   - Progress note: the lunar-theory selection data is now centralized into a single static specification, and the canonical evidence slice is checked against that supported-body list so the current Meeus-style baseline stays internally consistent as the source-selection work continues.
   - Progress note: the backend now also exposes dedicated supported/unsupported lunar-body accessors from that centralized selection, and the backend metadata derives its body coverage from the same source-of-truth slice so future source-selection swaps have one less place to drift.
   - Progress note: the compact lunar-theory metadata now also exposes its current validation window as a structured `TimeRange` in addition to the prose date-range note, so release-facing summaries can render the sampled epoch span without inferring it from freeform text.
   - Progress note: the release-facing lunar summary formatter now lives in `pleiades-elp`, which keeps the lunar provenance text owned by the backend crate rather than duplicated in validation/reporting code.
   - Progress note: that lunar summary is now parameterized through `format_lunar_theory_specification(&LunarTheorySpecification)`, so future source-selection variants can reuse the same backend-owned one-liner without rebuilding the template in validation.
   - Progress note: the canonical lunar reference evidence summary now also lives in `pleiades-elp`, and validation/reporting reuses that backend-owned summary and formatter instead of keeping a duplicate copy in the tooling layer.
   - Progress note: the new compact lunar capability summary helper now gives validation a structured body/frame/time-scale snapshot without parsing prose, and now also carries the supported and unsupported lunar body lists so the current baseline channels stay visible without opening the full specification. The lunar source-selection, catalog-validation, and capability summary records now also implement `summary_line()`/`Display`, so validation and release callers can render the typed lunar records directly without rebuilding those strings in the reporting layer. The backend-owned lunar reference summary now has a measured error-envelope companion rendered in the detailed report and backend-matrix summary, keeping the source-selection posture easier to reuse in later release reports.
   - Progress note: the backend-owned lunar reference evidence summary now also has a measured error-envelope companion, and both the detailed report and backend-matrix summary render that envelope line so the current lunar evidence slice exposes a compact residual snapshot alongside the canonical samples.
   - Progress note: the lunar reference slice now also includes the 1992 Moon example, J2000 lunar-point anchors, published 1913-05-27 true-node and mean-node examples, a published 1959-12-07 mean-node example, and a published 2021-03-05 mean-perigee example, which broadens the current validation evidence without changing the public request model. The compact lunar theory date-range note now also names the reference-only 1968-12-24 apparent geocentric Moon comparison datum, so the broader validation slice remains visible in the release-facing provenance line.
   - Progress note: the `pleiades-elp::LunarTheorySpecification` record now carries the selected source family directly, which keeps the current lunar baseline selection structured for future source-backed swaps and simpler release reporting.
   - Progress note: the current lunar-theory selection is now also exposed as a one-entry catalog plus catalog summary helper, so release-facing lunar reporting has a stable selection surface ready for future source-backed variants without changing the public API shape. The same selection is now also available through a direct `lunar_theory_source_selection()` accessor, which keeps the structured provenance record easy to reuse without reconstructing it from prose. The lunar source-selection surface now also has typed lookup helpers by source identifier, model name, family label, and the documented short alias `Meeus-style truncated lunar baseline`, and now exposes a typed `LunarTheoryCatalogKey` plus `resolve_lunar_theory_by_key(...)`, which makes the current one-entry catalog easier to resolve from future source-backed selection code without changing the public API shape. The typed key now also has an explicit alias variant for the documented short label, keeping alias resolution symmetric with the other lookup families. The source-selection path now also has a structured family-key lookup and resolver, which makes the selected source family itself directly addressable without falling back to the string label. The lunar catalog now also exposes a direct `lunar_theory_catalog_entry_for_selection(...)` helper so the selected source record can round-trip back to its catalog entry without re-deriving the lookup key in reporting code. The compact lunar catalog summary now also reports the selected alias count plus supported and unsupported body counts, which makes the source-selection surface a little more explicit for future source-backed variants. The same catalog surface now also has a backend-owned consistency check that enforces the selected-entry round trip plus case-insensitive uniqueness for identifiers, model names, family labels, and documented aliases, which keeps the one-entry baseline safer to extend when a second lunar-theory source lands. The same helper now also rejects duplicate aliases inside a single entry and alias collisions against the selected entry's core labels, so the future multi-source catalog path has one more explicit guardrail. The catalog validation summary now also exposes the selected source, selected count, and validation result as structured data before formatting the release-facing line, keeping the backend-owned validation snapshot aligned with the selected lunar provenance while the source-selection catalog remains a one-entry baseline. The compact lunar capability summary now also reports whether the catalog validates cleanly, so the release-facing capability snapshot keeps the one-entry catalog health visible alongside the body/frame/time-scale counts without opening the longer validation summary. The lunar reference evidence slice now also explicitly includes the J2000 mean apogee anchor in its release-facing summary, so the currently validated lunar-point set is a little more explicit without widening the public API.
   - Progress note: the lunar reference and equatorial error envelopes now also report mean residuals and explicit out-of-limit sample counts alongside the existing maxima, which makes the release-facing lunar evidence a little easier to scan while the current Meeus-style baseline remains the active lunar source-selection placeholder. The lunar error-envelope report strings now also spell out the applied regression limits directly, and now name any outlier bodies explicitly when a threshold is exceeded, so the phase-1 lunar evidence stays explicit about the current threshold policy in release-facing summaries.
   - Progress note: the lunar reference and lunar equatorial envelopes now also include median and 95th-percentile deltas in the backend-owned report helpers and validation smoke tests, which gives the compact lunar release evidence a slightly richer spread/shape view without changing the request contract or the current baseline slice.
   - Progress note: the compact release summary now also surfaces the ELP lunar capability line plus the lunar reference and lunar equatorial evidence summaries alongside the JPL interpolation evidence, so the one-screen release overview keeps the current lunar theory baseline visible next to the other phase-1 accuracy evidence. The compact release and validation summaries now also render the lunar request-policy line explicitly, so the current TT/TDB, frame, apparentness, and observer posture is visible at a glance in release-facing output. The compact lunar summary views now also surface the lunar frame-treatment line explicitly, which keeps the mean-obliquity ecliptic/equatorial policy visible without opening the longer specification text.
   - Progress note: the compact ELP and VSOP87 implementation notes now describe the current baselines in production-neutral language, which keeps the remaining release-facing fallback wording aligned with the current phase-1 posture while the source-backed upgrades continue.

3. Upgrade `pleiades-jpl` from snapshot fixture to reference backend.
   - Parse documented public JPL-style files or a reproducible derivative format in pure Rust.
   - Support multiple epochs through interpolation rather than exact fixture lookup only.
   - Include selected asteroid coverage for Ceres, Pallas, Juno, and Vesta when source data is available.
   - Preserve snapshot fixtures as small regression/golden tests.
   - Progress note: the reference snapshot now has exact J2000 regression coverage for Ceres, Pallas, Juno, Vesta, and the custom 433-Eros asteroid entry, so the baseline asteroid subset is exercised as a golden path while broader corpus work remains queued.
   - Progress note: validation reports, backend matrices, and release notes now render the exact J2000 asteroid evidence rows for that subset instead of listing the bodies alone, which makes the source-backed asteroid fixture path visible in release-facing output.
   - Progress note: the checked-in JPL reference snapshot now prefers cubic interpolation on four-sample windows when the fixture has enough same-body epochs, with quadratic and linear fallbacks for smaller windows, which tightens the current pure-Rust interpolator without changing the public request model.
   - Progress note: the JPL reference backend now also has a batch-path regression over the exact asteroid evidence slice, so the same source-backed rows are verified through `positions()` as well as one-body-at-a-time queries.
   - Progress note: the JPL interpolation-quality summary now also carries the worst-case epoch for each measured envelope, and now reports mean bracket-span and mean error metrics alongside the maxima, so the report can name both the body and the held-out instant that produced the current interpolation peak while also exposing the average leave-one-out envelope.
   - Progress note: the checked-in JPL comparison snapshot now includes an added 2600000.0 Mars hold-out epoch in addition to the 2400000.0 epoch across the Sun-through-Pluto bodies, expanding the fixture to 46 rows across 15 bodies and 6 epochs and broadening the leave-one-out evidence to 21 samples across 10 bodies.
   - Progress note: the JPL interpolation-quality summary now lives in `pleiades-jpl`, and validation reuses the backend-owned summary/formatter instead of recomputing the same envelope privately, so the interpolation evidence now follows the same backend-owned reporting pattern as the lunar reference summary. The backend now also publishes a distinct-body interpolation-kind coverage helper for the cubic/quadratic/linear tiers, which gives the hold-out transparency slice a small extra audit hook without expanding the fixture. The same summary now also reports RMS longitude, latitude, and distance error, which gives the leave-one-out envelope a clearer spread metric without expanding the fixture. The rendered interpolation-quality summary now also identifies the metric as transparency evidence rather than a production tolerance envelope, keeping the release-facing wording explicit about the evidence class.

   - Latest progress (2026-04-26): the same interpolation-quality summary now also reports 95th-percentile bracket-span and error tails, so the JPL transparency evidence has a compact upper-tail view in addition to the mean, median, and RMS envelope.
   - Latest progress (2026-04-26): the JPL interpolation-quality, distinct-body coverage, and independent hold-out summaries now also expose typed `summary_line()`/`Display` helpers, and the validation/release report paths reuse those displayable records directly, keeping the JPL transparency evidence aligned with the other backend-owned release summaries.
   - Latest progress (2026-04-26): the JPL snapshot backend now also publishes an independent Mars/Jupiter/Saturn hold-out corpus from the same public Horizons source material, and the validation/release evidence now renders that separate hold-out interpolation envelope alongside the existing leave-one-out transparency summary, so the JPL interpolation story now shows both internal and externalized checks without changing the public backend contract.
   - Latest progress (2026-04-26): the JPL interpolation-quality sample corpus now also has a batch-path regression, so the same hold-out epochs that feed the transparency summary are now exercised through `positions()` with preserved order and interpolated-quality results.
   - Latest progress (2026-04-26): the independent JPL hold-out summary now also labels itself as transparency evidence rather than a production tolerance envelope, which keeps the release-facing wording aligned with the rest of the JPL validation surface.
   - Latest progress (2026-04-26): the independent JPL hold-out batch path now also preserves mixed TT/TDB request scales, so the externalized Horizons check exercises the same per-request dynamical-time labeling contract as the reference snapshot batch path.
   - Latest progress (2026-04-26): the independent hold-out coverage summary now also lists the Mars/Jupiter/Saturn body names explicitly in first-seen order, so the compact transparency line now shows the exact sampled bodies alongside the row, epoch, and time-scale window without changing the hold-out corpus.
   - Latest progress (2026-04-26): that independent JPL hold-out evidence is now surfaced in the full validation report, compact validation summary, and release summary alongside the leave-one-out interpolation evidence, so the externalized Horizons check is visible in the release-facing report set instead of living only in the backend-owned helper. The same combined evidence line now also surfaces the independent hold-out manifest summary, which keeps the checked-in public-input provenance explicit alongside the source, coverage, and interpolation envelopes.
   - Latest progress (2026-04-26): the JPL snapshot backend now also has a batch-path regression over the full checked-in reference snapshot in equatorial mode, preserving exact order and exact-coordinate parity across every reference row while exercising the mean-obliquity transform on the complete public-input fixture. The same reference snapshot now also has a full ecliptic batch-path regression, so both coordinate frames are now exercised across the entire checked-in corpus instead of only the asteroid subset. The mixed-frame batch regression is now also surfaced as a backend-owned summary and reused by validation/release reporting, so the alternating ecliptic/equatorial parity evidence is visible alongside the equatorial snapshot line.
   - Latest progress (2026-04-26): the independent JPL hold-out corpus now also has a batch-path parity regression, and now also publishes an equatorial parity summary, so the Mars/Jupiter/Saturn transparency rows are exercised through `positions()` in both ecliptic and mean-obliquity equatorial modes instead of only the existing single-query interpolation check.
   - Latest progress (2026-04-26): the checked-in JPL reference and hold-out CSV comments are now parsed into structured manifest metadata, and the release-facing evidence helper now surfaces the hold-out source line from that metadata so the public-input provenance is less hardcoded. The parsed snapshot manifest now also exposes typed source and coverage fallback helpers plus a default-aware summary builder, so the remaining provenance defaults stay centralized in the backend crate instead of being duplicated across the report helpers. The JPL reference and hold-out source-material summaries are now backend-owned typed records too, which keeps the checked-in Horizons provenance available as structured data for future validation/reporting work instead of only as formatted strings.
   - Progress note: the JPL comparison corpus now also has a backend-owned coverage summary that validation and release reporting surface alongside the interpolation evidence, which makes the checked-in comparison window easier to audit without changing the comparison corpus itself. The comparison snapshot now also surfaces its checked-in J2000 source and manifest metadata in the validation, release, and backend-matrix reports, so the comparison corpus provenance is now explicit alongside the coverage line. The comparison snapshot summary now also carries the bodies covered by the comparison corpus in first-seen order, which keeps the ordered body list available as structured provenance for downstream tooling instead of only as a derived string. The comparison snapshot source metadata is now also exposed through a backend-owned typed summary, and the detailed validation report now reuses that typed line directly too, so the fallback/source/coverage text can travel as structured provenance instead of being assembled only in the reporting layer.
   - Progress note: the JPL snapshot evidence summary now consolidates the reference snapshot coverage, exact asteroid evidence, and comparison snapshot coverage into a backend-owned report helper, so the compact release-facing JPL line stays co-located with the snapshot backend instead of being assembled piecemeal in validation. The reference snapshot coverage summary now also carries the first-seen body order, and the equatorial-parity summary reuses that structured body list so the checked-in JPL provenance can name the covered bodies alongside the row and epoch counts.
   - Progress note: the same JPL evidence helper now also includes a backend-owned source-material line for the checked-in DE441 fixture, which keeps the compact validation and release summaries explicit about the vector-table provenance alongside the coverage rows.
   - Progress note: the JPL snapshot backend now also publishes an equatorial asteroid evidence slice derived from the exact J2000 asteroid rows, and the release-facing JPL evidence summaries now carry that mean-obliquity cross-check alongside the exact ecliptic asteroid evidence so the asteroid frame transform remains visible in the report output.
   - Progress note: the JPL snapshot manifest record now also implements `Display`, keeping the parsed provenance summary self-formatting alongside the existing label-specific helpers.
   - Progress note: the compact release summary now also surfaces the JPL interpolation-quality envelope, so the one-screen overview keeps the reference backend's leave-one-out evidence visible alongside the comparison totals. The same release overview now also carries the body-class tolerance posture and expected tolerance status, so release-facing error envelopes stay visible without opening the full validation report. The compact release summary now also surfaces the mean longitude, latitude, and distance deltas alongside the maxima, which gives the one-screen envelope a little more shape without changing the comparison corpus. The same release summary now also surfaces JPL reference snapshot coverage, exact asteroid evidence, VSOP87 source evidence, and the lunar reference/equatorial error envelopes, so the compact release view now carries the missing source-backed evidence tables and frame-conversion envelope alongside the comparison and tolerance data. The interpolation-quality summary now also reports distinct epoch coverage plus the earliest/latest hold-out epoch window, so the transparency evidence shows the sampled span alongside the body/kind breakdown. The full validation report's JPL interpolation section now also renders the backend-owned kind-coverage line alongside the summary and sample rows, so the detailed report mirrors the compact release view more closely. The reference, comparison, equatorial-parity, and independent hold-out coverage summaries now also have backend-owned `summary_line()`/`Display` helpers, keeping the JPL coverage provenance centralized on the typed backend records instead of separate report-only formatters.

4. Strengthen time, apparentness, observer, and coordinate semantics.
   - Expand the initial Delta T policy into implemented conversion support or a release-grade caller-provided conversion contract.
   - Clarify TT/UT/TDB handling in requests and validation data.
   - Keep chart house observers separate from backend topocentric position requests unless an explicit topocentric chart mode is added.
   - Implement equatorial/ecliptic transforms where the release profile claims them.
   - Ensure topocentric observer requests and apparent/mean flags either produce distinct documented behavior or return structured unsupported-feature errors.
   - Progress note: the shared `pleiades-types::EclipticCoordinates::to_equatorial` helper now centralizes the mean-obliquity ecliptic-to-equatorial rotation used by the VSOP87 and ELP paths, so the frame transform is implemented once and covered by shared unit tests instead of being duplicated per backend.
   - Progress note: `pleiades-types::Instant::mean_obliquity` now centralizes the shared cubic mean-obliquity approximation used by the JPL, VSOP87, ELP, and packaged-data equatorial transforms, so the frame-conversion policy is owned by the foundation crate instead of being duplicated in each backend.
   - Progress note: `pleiades-backend::EphemerisRequest::new` now defaults to mean geometric output, which keeps bare geocentric requests aligned with the current mean-only backend policy while still allowing callers to opt into apparent requests explicitly.
   - Progress note: the VSOP87 and compact lunar backend paths now accept both TT and TDB dynamical-time requests while still rejecting UT-based requests explicitly, which makes the current phase-1 time-scale policy easier to exercise in validation without adding a built-in Delta T or relativistic conversion model.
   - Progress note: the backend crates now also share centralized request-policy helpers for time-scale, frame, apparentness, and observer validation, which keeps the existing structured errors consistent while reducing duplicated guardrail code. The shared batch adapter now also has a fail-fast regression that preserves structured error kinds when a request sequence contains an unsupported body, so chart-style batch queries keep the same explicit failure semantics as single-request calls. The same batch adapter now also preserves structured apparentness rejections in mean-only backends, so unsupported `Apparentness::Apparent` requests still fail with the backend's explicit `InvalidRequest` kind when they arrive through `positions()`. The shared backend request-policy record now also has a compact one-line renderer, and the release-facing validation summaries now reuse that typed record for the consolidated request-policy line instead of rebuilding the combined wording locally.
   - Progress note: zodiac-mode validation is now centralized too, so the tropical-only first-party backends reject sidereal requests with the same structured `InvalidRequest` posture instead of repeating the same manual checks in each backend crate. Zodiac-mode rendering in chart snapshots now uses the shared typed display label, and the validation backend-matrix time-scale listing now also uses the shared `Display` labels instead of debug formatting so the release-facing request-policy text stays aligned with the typed enums.
   - Progress note: the compact validation, release, and backend-matrix summaries now also render an explicit apparentness policy line, so the current mean-only first-party posture is visible alongside the time-scale and observer notes without opening the full report. The same compact validation and release views now also reuse the typed backend request-policy record for their consolidated one-line policy summary, which keeps the combined wording centralized instead of being rebuilt locally.
   - Progress note: the compact validation, release, and backend-matrix summaries now also surface an explicit frame policy line, which makes the mean-obliquity ecliptic/equatorial contract visible beside the time-scale and observer notes without needing to open the backend-specific frame-treatment sections. Mixed-frame batch regressions now also cover the JPL, VSOP87, and ELP source-backed backends, so `positions()` is explicitly exercised with alternating ecliptic and equatorial requests instead of only uniform-frame batches.
   - Progress note: the JPL snapshot backend now also has batch-path regressions for unsupported time scales, observer-bearing requests, and apparent-place requests, so the shared request-policy posture is covered through `positions()` as well as single-request lookups.
   - Progress note: the packaged-data backend now also accepts TDB-tagged batch requests and returns the same decoded coordinates as the TT-grid lookup path, so the positive-side time-scale policy is covered through `positions()` as well as the existing rejection checks. The VSOP87, ELP, and packaged-data batch paths now also reject apparent-place and topocentric requests explicitly, which keeps the shared mean-only request-policy gate covered through `positions()` for the remaining first-party backends.
   - Progress note: the JPL snapshot backend now also publishes a backend-owned frame-treatment summary, and the validation/release summaries render it alongside the request-policy and interpolation evidence, keeping the checked-in ecliptic-to-equatorial mean-obliquity transform explicit in release-facing output.
   - Progress note: the JPL, ELP, and VSOP87 batch-path regressions now also exercise explicit equatorial-frame requests, so the shared mean-obliquity transform and frame-preserving batch contract are covered in addition to the existing ecliptic batch tests.
   - Progress note: the `pleiades-cli` chart command now accepts an explicit `--tdb` instant tag in addition to its default TT-tagged request, so the user-facing chart report can surface a TDB-tagged instant directly instead of implying a hidden conversion policy. The same chart path now also accepts `--tt-from-tdb-offset-seconds` for explicitly re-tagging a TDB instant as TT before assembly, which keeps the caller-supplied TDB-to-TT policy surface symmetric with the existing TT/TDB helpers.
   - Progress note: `pleiades-types` and `pleiades-core::ChartRequest` now also expose caller-supplied UTC-to-TDB helpers in addition to the UT1/UTC-to-TT and TT-to-TDB conveniences, so civil-time chart inputs can be lifted to TDB with explicit offset policy steps instead of hidden conversion logic. Signed UT1-to-TT and UTC-to-TT helpers now round out the same typed surface, so historical or externally modeled negative Delta T policies can stay explicit too.
   - Progress note: chart assembly now also has a regression proving house observers are used for house calculations only and are not forwarded into geocentric body-position backend requests, which keeps the observer/topocentric separation explicit in the façade layer.
   - Progress note: topocentric house calculations now reject non-finite observer elevations with a distinct `InvalidElevation` house error, and the chart façade maps that to `InvalidObserver` before any backend body query runs, so observer validation stays explicit even when the topocentric house path fails early.
   - Progress note: chart snapshot rendering now also has a regression for the no-observer branch, so the geocentric-body-position policy line remains covered even when no house observer is supplied.
   - Progress note: chart snapshots now also render an explicit ecliptic frame policy line, so the release-facing chart report makes the geocentric/ecliptic request shape visible alongside the time-scale, observer, and apparentness notes.
   - Progress note: the shared `pleiades-core::MotionSummary` chart record now also has a typed `summary_line()`/`Display` helper, and the chart snapshot renderer reuses it directly instead of rebuilding the motion-count wording inline, keeping the compact motion audit on the same typed pattern as the other report summaries.
   - Progress note: UT1-tagged callers now also have a direct caller-supplied UT1-to-TDB helper, so explicit dynamical-time staging can stay typed even when a caller already has TT-UT1 and TDB-TT offsets available.
   - Progress note: TDB-tagged callers now also have a caller-supplied TT-from-TDB helper in `pleiades-types` and `pleiades-core::ChartRequest`, and the same path now has an explicit `*_signed` alias for the signed-offset naming convention used by the other helpers, which keeps the explicit conversion policy symmetric for callers that already begin from a TDB-tagged instant.
   - Progress note: the VSOP87 and compact ELP batch-path regressions now also cover explicit TDB-tagged requests for their supported body slices, so the current time-scale policy is exercised through `positions()` as well as single-request lookups without adding a built-in Delta T or relativistic conversion model.
   - Progress note: the VSOP87, ELP, and JPL batch-path regressions now also cover mixed TT/TDB request sets, so batch queries preserve each request's own time-scale label even when a single call mixes dynamical-time scales instead of normalizing them to a hidden batch-wide policy. The VSOP87 source-backed planetary batch suite now also has an explicit mixed TT/TDB canonical-epoch regression, which keeps the planetary request-policy surface exercised through both same-scale and mixed-scale batch queries. The lunar validation/release reports now also surface a backend-owned mixed TT/TDB lunar batch-parity summary, so the current lunar time-scale coverage is visible in the release-facing reports instead of only in the backend test slice.
   - Progress note: the `pleiades-cli` chart command now exposes explicit `--utc` / `--ut1` instant tags plus caller-supplied `--tt-offset-seconds` and signed `--tdb-offset-seconds` flags, so command-line chart runs can exercise the same conversion policy without implying a built-in Delta T or relativistic model.
   - Progress note: the chart CLI now rejects time-scale offset flags that do not match the tagged instant, so caller-supplied TT/TT-TDB retagging helpers stay explicit instead of being silently ignored on UTC/UT1/TDB requests. TDB-tagged requests now also reject a direct `--tdb-offset-seconds` retagging flag, and the CLI regression suite covers that explicit failure path alongside the existing TT/UTC/UT1 mismatch checks.

5. Expand validation evidence.
   - Add golden positions for major bodies, lunar points, and baseline asteroids.
   - Generate cross-backend comparison reports with body/date/error summaries. Aggregate and per-body summary sections are now implemented; future source-backed backend increments should populate them with tighter measured errors.
   - Progress note: the validation and artifact reports now name the body driving each max longitude, latitude, and distance delta in the body-comparison and body-class envelope sections, which makes the release-facing error envelopes easier to audit without changing the measured corpus.
   - Progress note: the comparison-audit failure path now mirrors the body-class error envelopes, body-class tolerance posture, and tolerance policy sections, so release-failure output carries the same coarse envelope detail as the full validation report.
   - Progress note: the expected-tolerance status lines now also report signed margins to each class limit alongside the measured maxima, and the compact validation report summary now mirrors those margins in its expected-tolerance rows, which makes the remaining headroom or overshoot explicit in release-facing validation output without changing the comparison corpus.
   - Progress note: the compact release summary now also carries a short validation evidence line with comparison sample and tolerance counts, and now names the comparison-audit status explicitly, so the release-facing overview exposes a little more of the current error envelope without expanding into the full validation report.
   - Progress note: the compact comparison-envelope formatter now also reuses a typed median helper before appending it to the aggregate summary, which keeps the release-facing median wording centralized alongside the existing aggregate and 95th-percentile helpers instead of rebuilding the same string inline.
   - Progress note: the aggregate comparison summary now also has a backend-owned `summary_line()`/`Display` helper, and the compact validation/release comparison envelope now reuses that typed aggregate line before appending the median values, keeping the maxima/means/RMS wording centralized alongside the existing per-body summary helper. The matching 95th-percentile tail line is now also backed by a typed `ComparisonPercentileEnvelope` helper, and the validation/release report paths reuse that record directly instead of rebuilding the tail wording inline.
   - Progress note: the backend matrix summary now also mirrors the current comparison-audit status and counts, so the compact backend capability view exposes the active regression gate alongside the backend inventory and source-backed evidence lines.
   - Progress note: backend-family and accuracy labels now have stable `Display`-backed renderings in `pleiades-backend`, and the validation backend-matrix formatter now uses those public labels directly so the release-facing metadata lines stay consistent with the shared backend contract. The backend matrix now also adds a typed family-posture line (`algorithmic`, `data-backed`, `routing`, or `other`) so the compact release-facing inventory makes the data-backed-versus-formula-driven split explicit.
   - Progress note: backend capability flags now have a shared backend-owned `summary_line()`/`Display` helper in `pleiades-backend`, and the validation backend-matrix capability row now reuses that typed record directly so the compact capability posture stays centralized instead of being rebuilt locally.
   - Progress note: the same compact release summary now also surfaces the body-named comparison envelope, so the one-screen overview now mirrors the detailed comparison report a little more closely while the broader release-grade threshold work remains queued. The compact packaged-artifact summary now also includes the expected-tolerance rows and comparison tolerance audit counts, so the artifact release view now carries the current regression posture without forcing maintainers to open the full validation report. The packaged-artifact regeneration summary now also reports the artifact checksum and deterministic generation policy, so the compact artifact provenance line carries both integrity evidence and the current fit rule. The body-class tolerance posture now also prints the candidate backend family and class-level tolerance profile directly, which keeps the coarse class audit aligned with the backend-scoped expectation table. The same posture now also reports out-of-tolerance sample counts alongside the body counts, so sample-level regressions stay visible in the coarse release audit.
   - Progress note: comparison-tolerance scope coverage rows now also have backend-owned `summary_line()`/`Display` helpers, and the validation/release tolerance-policy renderers reuse those typed rows directly instead of rebuilding the per-scope wording inline, which keeps the compact release-facing tolerance catalog a little more consistently typed.
   - Progress note: the comparison envelope now also carries RMS longitude, latitude, and distance deltas in the validation report, compact validation summary, and body comparison summaries, so the current release-facing error envelope now includes a spread metric alongside the maxima and means. The body-class error envelopes now also carry the same RMS metrics, which keeps the coarse class audit aligned with the per-body spread statistics. The body-class envelopes now also surface 95th-percentile longitude, latitude, and distance tails, adding a compact tail metric to the coarse release audit alongside the existing max/mean/median/RMS rows.
   - Progress note: the aggregate comparison envelope now also carries median longitude, latitude, and distance deltas in the full report, compact validation summary, and release summary, which adds one more robust central-tendency view to the release-facing error envelope without widening the comparison corpus. The same comparison-report family now also surfaces a 95th-percentile tail envelope for absolute comparison deltas, giving the release-facing audit one more shape metric without changing the comparison corpus.
   - Progress note: the backend-matrix summary now also distinguishes bounded versus open-ended nominal ranges, which keeps the compact catalog view honest about which backends are still open-ended versus constrained to a published artifact window.
   - Progress note: validation benchmarking now also reports a full-chart assembly slice in addition to backend request throughput, using representative house scenarios so chart latency stays visible alongside the existing backend throughput evidence.
   - Progress note: the compact validation-report summary now also includes the shared benchmark provenance block and benchmark summaries, so the shorter release-facing validation view now surfaces the reference/candidate/packaged/chart timing rows and packaged-artifact decode size without forcing maintainers to open the full report.
   - Progress note: house calculations now reject non-finite obliquity overrides explicitly instead of letting NaN or infinite inputs flow into the quadrant formulas, and the remaining house-system docs were retitled to drop the stale MVP wording so the public-facing house module language matches the current production-ready behavior. The invalid-obliquity path now has a dedicated structured house error kind, which keeps the user-facing failure classification closer to invalid input than generic numerical failure. `HouseSnapshot` now also exposes a `house_for_longitude()` convenience wrapper over the shared wraparound helper, and the house-placement regression now covers exact cusp boundaries plus the wraparound lookup path, which keeps the snapshot/query pairing explicit for callers while tightening the house-placement helper contract.
   - Progress note: custom house-system definitions now render their name, aliases, and notes in unsupported-house errors, which makes the release-facing extensibility boundary more explicit without changing the public house API.
   - Progress note: backend quality annotations now have stable display labels in `pleiades-backend`, and chart snapshots render that typed label directly instead of Debug formatting, which keeps the body-quality column aligned with the shared backend contract.
   - Store expected tolerances by backend and body class.
   - Progress note: the comparison tolerance policy summaries now include the current comparison corpus size alongside the backend-family scope table, and the compact release summary now also carries per-scope body-and-sample coverage counts, so the release-facing tolerance catalog now ties the policy table a little more directly to the current corpus. The per-body tolerance status record now also renders as a typed `summary_line()`/`Display` helper, and the validation formatter reuses that record directly instead of rebuilding the compact expected-tolerance wording inline. The body-class comparison envelope now follows the same typed-summary pattern, so the coarse class report path reuses the backend-owned summary helper instead of reconstructing the release-facing wording inline.
   - Keep validation reproducible and pure Rust.

## Done criteria

- `pleiades-vsop87`, `pleiades-elp`, and `pleiades-jpl` metadata describe real source material and accuracy class.
- Major planets, Sun, Moon, and baseline asteroid support are tested against reference data.
- Unsupported bodies, frames, time scales, topocentric modes, and apparentness modes fail explicitly.
- Validation reports include measured errors and are consumable by release tooling.
- `cargo test --workspace` passes after the backend upgrades.

## Work that belongs in later phases

- Generating compressed 1500-2500 artifacts from these outputs belongs to Phase 2.
- Broad house/ayanamsa compatibility audits belong to Phase 3.
- Public release bundle publication and archival policies belong to Phase 4.
